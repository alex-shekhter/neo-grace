<Registry>
  <Row name="C-CRITERION-CLOSE-EVIDENCE" status="retired" kind="chartered">
    <Number>1</Number>
    <Charter>A close/verdict-bound acceptance-criterion state, so post-archive lint 0/0 is authorable rather than reinvented as an unsatisfiable `AC-*`. Named here 2026-08-15. No existing name covers it: no `C-CRITERION*` / `C-CLOSE-EVIDENCE` in this directory; [`C-DRIFT-HONESTY`](../../../../.ngrace/changes/archive/C-DRIFT-HONESTY/) archived the workaround, not a third `AC-*` state.</Charter>
    <Pays>[F82](#f82), [F83](#f83), [F83.1](#f83.1), [F100](#f100), [F101](#f101), [F102](#f102), [F103](#f103), [F104](#f104), [F105](#f105). F82 is already discharged as *practice* by `C-DRIFT-HONESTY`; this bundle is the product state that would make that practice authorable. F83's P2.6 / P2.4 halves were paid by the same archive; the live remainder is F83.1.</Pays>
    <StatusText>**Delivered** 2026-08-30, archived as [`C-CRITERION-CLOSE-EVIDENCE`](../../../../.ngrace/changes/archive/C-CRITERION-CLOSE-EVIDENCE/). Closed with [F100](#f100)–[F105](#f105). The residual hole is unpaid by design: after the archive move no gate is required to run, so the close-evidence verdict is skippable — `review` detects a skip, nothing refuses one. `C-ARCHIVE-CURSOR` / P3.1 own the mandatory post-archive act.</StatusText>
  </Row>
  <Row name="C-APPROVAL-FINGERPRINT" status="retired" kind="chartered">
    <Number>2</Number>
    <Charter>[D18](#d18) entire: `gate approve` becomes the sanctioned `draft` to `approved` writer, records a per-`Decision` fingerprint of the bytes it wrote plus the artifact it targeted, ships the forced-permit escape hatch, **and** `review` reports an `approved` spec or plan whose newest per-artifact fingerprint is absent or no longer matches. Briefly split into a writer half and a detector half on 2026-08-30 and re-merged the same day; see [F108](#f108) and [F108.1](#f108.1).</Charter>
    <Pays>[F95](#f95) / [F95.1](#f95.1), [F112](#f112), [F113](#f113), [F81](#f81), [F109](#f109), and retires [F19](#f19)'s accepted transient by removing its premise — F19 ruled its subject **not** a defect, so this is a premise change, not a defect fix ([F109.3](#f109.3)).</Pays>
    <StatusText>**Delivered** 2026-08-31, archived as [`C-APPROVAL-FINGERPRINT`](../../../../.ngrace/changes/archive/C-APPROVAL-FINGERPRINT/). Closed with [F112](#f112) and [F113](#f113), both defects in its own approved spec found at execution. Detection uses the three-way trigger of [F108.2](#f108.2), so no bootstrap stamp was needed: the bundle carries exactly one unfingerprinted approve `Decision` and its own artifacts sit in the silent third row. It re-sources `approved-contract-drift` through one shared classifier with the git reading demoted to a fallback ([F109.1](#f109.1)), and retires [F19](#f19)'s accepted transient. **First bundle to author a `CloseEvidence` criterion and have it evaluated at close** — `AC-CLOSE-LINT` recorded `Exit 0 / Result pass` on the applied archive. Residual limits recorded, not closed: row three is permanent, the git fallback misses committed edits, and this bundle is never itself stamped. Prerequisite of position 5.</StatusText>
  </Row>
  <Row name="C-SUPERSEDE-VERB" status="retired" kind="chartered">
    <Number>3</Number>
    <Charter>A verb that performs the four writes plus the move that superseding currently is, atomically with the replacement. Named here 2026-08-15. No existing name.</Charter>
    <Pays>[F86.1](#f86.1), [F86.2](#f86.2).</Pays>
    <StatusText>**Superseded** 2026-08-31, archived as [`C-SUPERSEDE-VERB`](../../../../.ngrace/changes/archive/C-SUPERSEDE-VERB/), replaced by `C-SUPERSEDE-COMMAND` (row 3a). Executed to completion, then rejected at the scope audit: its approved plan's `ObservedWriteScope` omitted `scripts/release-check.ts`, and its spec carried a false completeness claim about the pins the deliverable moves ([F120](#f120), [F122](#f122)). Production reverted; nothing durable was folded, so the archived ledger holds two approve Decisions and no execution record.</StatusText>
    <Successor>C-SUPERSEDE-COMMAND</Successor>
  </Row>
  <Row name="C-SUPERSEDE-COMMAND" status="retired" kind="chartered">
    <Number>3a</Number>
    <Charter>The `ngrace supersede` verb, carrying forward `C-SUPERSEDE-VERB`'s deliverable and both maintainer rulings, with the `package.json#files` → `PACK_ALLOWED_EXACT` coupling declared rather than discovered.</Charter>
    <Pays>[F86.1](#f86.1), [F86.2](#f86.2), [F120](#f120), [F122](#f122); closes [F121](#f121)'s second half, plus [F125](#f125).</Pays>
    <StatusText>**Delivered** 2026-08-31, archived as [`C-SUPERSEDE-COMMAND`](../../../../.ngrace/changes/archive/C-SUPERSEDE-COMMAND/). Six tasks, reds 2/3/1/3/3/0, no write outside `ObservedWriteScope`. Closed with [F125](#f125): three defects a green 1562-test suite did not surface, found by exercising the CLI and running the validators the report omitted. `AC-CLOSE-LINT` CloseEvidence evaluated to `Exit 0 / Result pass`; post-archive lint 0/0.</StatusText>
  </Row>
  <Row name="C-PHASE-RULE-PIN" status="retired" kind="chartered">
    <Number>7</Number>
    <Charter>Reconcile the plan skill's `MustPassCommand` restriction with the contract the linter actually enforces, and close the detector's implicit-`current` gap. Prose: `skills/ngrace/ngrace-plan/SKILL.md:102` forbids what 22 archived plans do and what lint never checks. Code: `src/artifact/assertions.ts:262` matches only an explicit `--assertions current`, though current is the default (`src/grace-lint.ts:88`). Both sides plus the packaged mirror. Named here 2026-09-01.</Charter>
    <Pays>[F130](#f130).</Pays>
    <StatusText>**Ordered, not deferred** — after `C-BOUND-VERDICT`. The skill file and its mirror are outside that bundle's `ObservedWriteScope`, so grafting the fix would require amending an approved spec; that conflict is the [D11](#d11) condition. Safe by measurement: archived plans are exempt (`src/lint/core.ts:535-536`) and no active plan in either tree carries a lifecycle command in `TargetAssertions`. Must **not** resolve package scripts transitively — that is the rejected reading B.</StatusText>
  </Row>
  <Row name="C-EVIDENCE-DISCRIMINATION" status="retired" kind="chartered">
    <Number></Number>
    <Charter>A lint rule refusing a `CloseEvidence` command with no failing path: allowlist by command **shape** (not a denylist of binaries), configurable in `.ngrace-lint.json`, polyglot defaults, warning severity with a documented escape. Named here 2026-09-02 by [D26](#d26). Searched before minting: `C-CRITERION-CLOSE-EVIDENCE` created the state, `C-CALIBRATION-COMMAND-EVIDENCE` / `C-COMMAND-EVIDENCE-RECORDED` / `C-FOLD-USES-RECORDED-COMMAND-EVIDENCE` / `C-VERDICT-EVIDENCE` are adjacent; none asks whether a recorded command can fail.</Charter>
    <Pays>[F147](#f147). `change.close-evidence-incomplete` checks a `Command` exists and `change.close-evidence-and-satisfied` checks task mapping; neither checks power.</Pays>
    <StatusText>**Named 2026-09-02; not in the order.** Must judge command text only ([`C-PHASE-RULE-PIN`](#named-bundle-registry)'s rejected reading B forbids resolving package scripts transitively). Its `--explain` must say the rule catches only the cheap class: `pytest --collect-only`, `cargo test --no-run` and `git diff --exit-code` over the wrong paths all exit 0.</StatusText>
  </Row>
  <Row name="C-APPROVE-TIME-REVIEW" status="retired" kind="chartered">
    <Number></Number>
    <Charter>Teach `ngrace review --change` as a step **before** `ngrace gate approve`, at both the spec and the plan stage: `ngrace-spec` approve step, `ngrace-plan` rule 15, and `ngrace-reviewer` broadened from one moment (&quot;before judgment&quot;) to three. **Must ship the partial-review caveat, corrected by [F150.1](#f150.1)**: at **spec** approve the scope and WriteEvidence audits report `not-run` (no plan on disk) while the attempt-pair audit *runs* and reports `ran over 0 fail→pass pair(s)` — a clean line over an empty denominator, which is a **weaker** signal than `not-run` and reads as substantiation. Quote each audit's actual status string per stage; a grouping phrase is how [F147](#f147)'s manufactured-pass shape gets taught. Named here 2026-09-03 by [F149](#f149), **amended by [F150](#f150)**: the bundle must also ship what a clean approve-time review looks like, because at **plan** approve the scope audit reports the bundle's own `spec.xml` and `plan.xml` as out-of-scope writes on every bundle (`decisions.md` only when it changed *after* the recorded base — [F150.2](#f150.2)), and the `WriteEvidence` audit reports a third status there, `unable-to-determine`, distinct from its spec-stage `not-run` (at **spec** approve it does not run at all, and naming those findings as expected there teaches a not-run audit as if it had spoken — [F150.1](#f150.1)) — an instruction that produces unexplained errors on first use is a trap, not guidance. **Also carries [F152](#f152)**: show the closed approval lexicon whenever approval is requested, one approval request per message naming one artifact, and record the ratifying phrase verbatim — three concerns in this bundle now (approve-time review, F150's expected-findings guidance, F152's lexicon rules), plus the carried XML-escaping item, and its Intent must say so rather than let scope accrete. Searched before minting: `C-APPROVAL-FINGERPRINT` made the gate the status writer, `C-APPROVAL-SCOPE` covers the phrase and the close acts ([D19](#d19)/[D20](#d20)), `C-REVIEW-SURFACE` created the detector surface; **none says when review runs.**</Charter>
    <Pays>[F149](#f149), and retroactively [F136](#f136) and [F148](#f148) — both were catchable by a review the artifact was still editable for.</Pays>
    <StatusText>**Next after `C-EVIDENCE-DISCRIMINATION`.** Candidate to carry the carried item *&quot;the XML-escaping rule's skill homes&quot;* — same two skill files, same rule shape; the spec author decides, and says which. Collides with `C-EVIDENCE-DISCRIMINATION` on `skills/ngrace/ngrace-spec/SKILL.md` and the `skillTextLines` pins, so **sequential, never concurrent**.</StatusText>
  </Row>
  <Row name="C-REVIEW-ARCHIVE-SCOPE" status="retired" kind="chartered">
    <Number></Number>
    <Charter>Restrict `detectZeroOrMoreSwallow` to plans under `changes/active/`, and rule on `detectSelfReferential`'s plan loop, which carries the same missing guard and fires on nothing today. `detectConfidentlyWrong` **keeps** its archive scan — its `MustExist` targets assert present-tree state — so the change must state why the two differ rather than applying one rule to all three. Named here 2026-09-03 by [F149.1](#f149.1). Searched before minting: `C-REVIEW-LANGUAGE-SCOPE` is the closest precedent (it fixed three review false positives) but by marker-scan scoping, not archive scoping; not a synonym.</Charter>
    <Pays>[F149.1](#f149.1).</Pays>
    <StatusText>**After `C-APPROVE-TIME-REVIEW`.** Ordered, not deferred: the cost is one ack per close, recurring forever, and the fix is measured at one line with the ratchet intact.</StatusText>
  </Row>
  <Row name="C-SCOPE-AUDIT-ATTRIBUTION" status="retired" kind="chartered">
    <Number></Number>
    <Charter>Stop `auditScopeOutsideWriteScope` attributing the approve gate's own writes to the executor: exempt the bundle's `spec.xml` and `plan.xml` in the **changed-files** audit (`src/review/core.ts:1033-1060`) through a **sibling predicate keyed on the reviewed change id, never an extension of `isCliLifecyclePath`** — that helper is shared with the WriteEvidence audit (`:1045` and `:1108`), so widening it would silence the breach signal ([F155.1](#f155.1)), and scope the base-diffing audits to bundles under `changes/active/` so a closed or superseded bundle is not re-reported present-tense. **`auditWriteEvidenceOutsideScope` (`:1093-…`) keeps raising on `spec.xml`/`plan.xml`** — there the writer is the executor's own ledger and the claim is a real breach signal. **Overturns a shipped criterion**: `AC-SCOPE-LIFECYCLE-EXCLUSION` (`.ngrace/changes/archive/C-REPORT-HONESTY/plan.xml:469,481`, red at `src/review/core.test.ts:1273-1289`) asserts the current behaviour, recorded as intentional in `brownfield-run4-findings.md:300` for approved-artifact immutability; the spec must say so and answer it with `auditApprovedArtifact` (`:1736-1773`), which enforces immutability byte-exactly via `review.approved-fingerprint-mismatch`. Named here 2026-09-04. Searched before minting: `C-REVIEW-ARCHIVE-SCOPE` scoped the plan-walking detectors and not these audits, `C-PLAN-SCOPE-PATHS` compares the spec bound to `ObservedWriteScope` (a different comparison), `C-DECLARED-WRITES` created the declared-write surface; none touches attribution. `AC-SCOPE-LIFECYCLE-EXCLUSION` is an acceptance criterion, not a bundle name.</Charter>
    <Pays>[F137](#f137), [F150](#f150), [F155](#f155).</Pays>
    <StatusText>**Delivered** 2026-09-05 as `C-SCOPE-AUDIT-ATTRIBUTION-2` and merged in PR #71; the original was superseded ([D27](#d27), [F157](#f157)) after its approved plan proved unsatisfiable, and both are archived under `.ngrace/changes/archive/`. Ratified at **position 1 of five** 2026-09-04. [F137](#f137)'s pair at every close and again at plan-approve is paid and no longer recurs. Archive-residue counts are **conditional on tree state and expire** — see [F155.1](#f155.1) rather than quoting a number from this row.</StatusText>
    <Successor>C-SCOPE-AUDIT-ATTRIBUTION-2</Successor>
  </Row>
  <Row name="C-CITATION-ANCHOR-REPAIR" status="retired" kind="chartered">
    <Number></Number>
    <Charter>Inject explicit `a id`/`name` anchors and resolve ambiguous strip-dot citations while `decisions.md` is still one document. Nothing moves. Carries the delimiter-preserving slug grammar (`F9.1 → f9.1`). Named here 2026-09-05 by [D31](#d31). Searched before minting: no prior name covered citation identity on this file; `C-RECORD-RETIREMENT` is the engine that follows.</Charter>
    <Pays>[F164](#f164); identity half of [D31](#d31).</Pays>
    <StatusText>**Delivered** 2026-09-05 as `C-CITATION-ANCHOR-REPAIR-2` and merged in PR #72; the original was superseded after its approved plan put current-mode lint of this root in task Verification. Both archived under `.ngrace/changes/archive/`. The board is defined for names that do not exist on disk; this row stays so the D31 insertion at **position 2 of nine** is not silently dropped. The identity work is done.</StatusText>
    <Successor>C-CITATION-ANCHOR-REPAIR-2</Successor>
  </Row>
  <Row name="C-RECORD-RETIREMENT" status="retired" kind="chartered">
    <Number></Number>
    <Charter>Retire `decisions.md` into three genres — findings, decisions, registry — each with an observable retirement trigger and a line ratchet that refuses growth. `decisions.md` itself **survives as the citation index**: 45 archived immutable artifacts cite its path and 528 internal anchors depend on its headings, so bodies move and anchors stay. The registry becomes machine-readable and its rule is the enforcement core: a live row naming an archived bundle is an error. Content preservation is a verified criterion, not a claim. The four `scripts/skill-contracts.test.ts` teaching sites move with it, canonical plus packaged mirror. `docs/plans/archive/RM-AGENT-RELIABILITY/decisions.md` is out of scope permanently. Named here 2026-09-05 by [D28](#d28). Searched before minting: `C-RECORDED-DEBT` records debt inside a bundle, `C-RECORD-FALSE` is about a false record, `C-TEACHING-SURFACE` covers the docs surface, `C-DRIFT-HONESTY` covered a lint workaround; none addresses record lifecycle.</Charter>
    <Pays>[F161](#f161), and it pays the cost measured in [F155.1](#f155.1) / [F157](#f157) / [F160](#f160), where expired counts and stale citations in a 9940-line file produced three corrections in two sessions.</Pays>
    <StatusText>**Position 3 of nine** — ratified 2026-09-05 at position 2, renumbered by [D31](#d31). Enforcement ships with the structure, never after.</StatusText>
  </Row>
  <Row name="C-AUDIT-001" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Brownfield run 1 bundle in another repository ([F88](#f88)).</StatusText>
  </Row>
  <Row name="C-IDENT-COVERAGE" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Brownfield run 4 bundle in another repository ([F86.1](#f86.1)).</StatusText>
  </Row>
  <Row name="C-ESTABLISH-MODULE-GRAPH-4" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Prior-attempt backup cited in the brownfield notes, not a charter here.</StatusText>
  </Row>
  <Row name="C-001" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Brownfield / generator example id (`spec new C-001`).</StatusText>
  </Row>
  <Row name="C-P0-CALIBRATION" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Rejected name; `C-CALIBRATION-COMMAND-EVIDENCE` shipped (`p0-calibration-derivation.md:394–396`).</StatusText>
  </Row>
  <Row name="C-LEDGER-MEMBERSHIP" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Rejected split; `C-REPORT-HONESTY` absorbed F14+F15 (`p0-report-honesty-derivation.md:511–512`).</StatusText>
  </Row>
  <Row name="C-BASE" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Temp-fixture id in the P0 derivation.</StatusText>
  </Row>
  <Row name="C-TEST" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Temp-fixture id in the cursor derivation.</StatusText>
  </Row>
  <Row name="C-PROBE" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Example change id in a command line ([F25](#f25)).</StatusText>
  </Row>
  <Row name="C-ID" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Placeholder in skill / command prose.</StatusText>
  </Row>
  <Row name="C-SLUG" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Placeholder in [plan.md](./plan.md).</StatusText>
  </Row>
  <Row name="C-X" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Example id in [review.md](./review.md).</StatusText>
  </Row>
  <Row name="C-CURSOR" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Abbreviation of `C-CURSOR-INTEGRITY` in the cursor derivation.</StatusText>
  </Row>
  <Row name="C-TOKEN" status="retired" kind="sweep-remainder">
    <Number></Number>
    <Charter></Charter>
    <Pays></Pays>
    <StatusText>Abbreviation of `C-TOKEN-INTEGRITY` in the cursor derivation.</StatusText>
  </Row>
  <Row name="C-CURSOR-TASK-RESOLVER" status="retired" kind="historical">
    <Number></Number>
    <Charter>Stop guessed task identity on a run event: inherit a plan-declared in-scope task for command-run, or refuse before write; never invent T-000. This row exists so the derivation can see the payment.</Charter>
    <Pays>F90, F90.1</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: resolveDeclaredCommandRunTask at src/grace-cursor.ts:3401 (&quot;Never invent T-000&quot;) and the invalid-arguments refusal at :3422-3427; tests &quot;C-CURSOR-TASK-RESOLVER T-001: command-run inherits a declared in-scope task&quot; (src/grace-cursor.test.ts:3846) and &quot;C-CURSOR-TASK-RESOLVER T-001: command-run refuses when no declared task is in scope&quot; (:3894). Does not claim the range-hole residual or regenerate re-derivation of an undeclared last event.</StatusText>
  </Row>
  <Row name="C-SUBSTANCE-OVER-NAME" status="retired" kind="historical">
    <Number></Number>
    <Charter>One structural-clone definition in src/, verified by body rather than identifier: cloneXmlNode is the single home. This row exists so the derivation can see the payment.</Charter>
    <Pays>F35</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: export function cloneXmlNode at src/artifact/xml.ts:239; src/gates/ledger.ts:65 imports it and :252 calls it; no function cloneNode in src/.</StatusText>
  </Row>
  <Row name="C-DECLARED-WRITES" status="retired" kind="historical">
    <Number></Number>
    <Charter>Compare each change's durable WriteEvidence paths to its plan ObservedWriteScope at review time; teach ngrace-plan that ObservedWriteScope covers what the deliverable forces. This row exists so the derivation can see the payment.</Charter>
    <Pays>F27, F27.1, F27.2</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: auditWriteEvidenceOutsideScope (src/review/core.ts:1112, C-DECLARED-WRITES / F27 comment at :1104-1111) raises review.write-evidence-outside-scope when a WriteEvidence path is outside ObservedWriteScope; ngrace-plan SKILL.md rule 6 (skills/ngrace/ngrace-plan/SKILL.md:61) teaches that ObservedWriteScope covers what the deliverable forces.</StatusText>
  </Row>
  <Row name="C-GRAMMAR-SEAM" status="retired" kind="historical">
    <Number></Number>
    <Charter>Make Clarification authorable by moving its target to a self-closing IC-*, INV-*, or AC-* child, never a target attribute. This row exists so the derivation can see the payment.</Charter>
    <Pays>F38</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: Clarification takes exactly one self-closing IC-*, INV-*, or AC-* child (src/artifact/grammar.ts:1730-1731); a target attribute is rejected (:1768-1769). The element is authorable.</StatusText>
  </Row>
  <Row name="C-PAYMENT-RECORD" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Charter.** Make already-discharged findings retirable by recording their payments in a form the derivation can read. A finding retires when its `PaidBy` names an archived bundle, and `PaidBy` is derived from registry rows and a phrase in archived specs — so a payment nobody recorded is a payment that never happened, as far as the ratchet is concerned. - **Direct historical rows in `registry.xml`**, ruled by the maintainer. Archived specs are never edited, so the row is the only route. - **The row must be visible to the derivation.** `kind` is a closed union (`scripts/validate-record-retirement.ts:69`) and the payer derivation filters to `chartered` (`:731`); `collectPaidBy` additionally requires `StatusText` matching `/delivered|superseded/i`. A new `kind` is inert unless the union and filter widen. - **Every payment is verified, never asserted.** Each row carries the symbol, test name, or `file:line` that discharges the finding. Recording a payment that did not happen would convert eligibility — the enforcement itself — into a formality, which is [D28.1](#d28.1)'s defect in a worse form than a config key. - **Scope for margin, not the minimum.** The window after a move is `252 + Δ_retired` against a staged wrap that grows every session.</Charter>
    <Pays>[F187](#f187) in part, and whichever live findings the audit verifies as discharged.</Pays>
    <StatusText>**Position 6**, [D36](#d36). Charter rulings: [D28](#d28), [D28.1](#d28.1), [D30](#d30), [D36](#d36).</StatusText>
  </Row>
  <Row name="C-RETIREMENT-WINDOW" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Charter.** Make retirement create room instead of erasing its own. The ceiling is re-derived as `base + headroom` at every move, so retiring content removes an equal amount of ceiling and the staged entries can never be flushed ([F183](#f183)). - **The window formula.** `Base_k = Base_live − Δ_retired` and `Ceiling_k = min(Ceiling_(k−1), Base_live + H)` at one `--retire` event, where `Base_live` is the **pre-move** footprint. Retirement then opens a window of `Δ_retired + H` that the next pass closes. The `min` is required, not optional: without it the ceiling rises whenever growth outpaced the previous retirement. - **Findings genre only.** Rulings, registry and index keep their current timing, each with a stated reason. The rulings `CodifiedIn` tagging pass is a separate bundle. - **`H` stays a constant derived in tracked repository space** — never a function of the staged buffer or any untracked state. A ceiling read from `.ngrace/scratch/` would be unreproducible from a fresh clone and greenable by appending to a `.gitignore`d file, which is [D28.1](#d28.1)'s rejected shape in a worse form than a config key. - **Remove the spent one-time exception**, whose clearing premise was false ([F186](#f186)), and move the provenance of the 1883 ceiling to a constant a validator asserts rather than a comment that drifts from the number.</Charter>
    <Pays>[F183](#f183), [F186](#f186).</Pays>
    <StatusText>**Position 5**, [D35](#d35). Charter rulings: [D28](#d28), [D28.1](#d28.1), [D30](#d30), [D35](#d35).</StatusText>
  </Row>
  <Row name="C-RETIRE-AND-CODIFY" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Charter.** Ship the **ongoing** retirement operation and the first codification tagging, so the record can retire as fast as it grows. - **A `PaidBy` feed.** Something must write `&amp;lt;PaidBy&amp;gt;` onto a live finding when the bundle that paid it archives. Today `collectPaidBy` (`scripts/validate-record-retirement.ts:372-410`) derives the map correctly but runs **only** in the one-shot `--split` path. - **Detection that reads the derivation, not just the stored element**, *and* a derivation wide enough to fire. The validator exits 0, and so would a re-derivation: the shipped `collectPaidBy` returns **0** live-eligible ([F180](#f180)). Widening it is a spec decision to argue against `D28.1`, not a refinement to assume. - **A retirement move.** A command that moves an eligible entry to its retired sibling and re-derives the live root's `base` / `ceiling`. There is no such command; `--split` refuses once `decisions.md` is a stub. - **First `CodifiedIn` / `TaughtIn` tagging**, with both resolution checks already shipped. [D27.1](#d27.1) is the first candidate by its own text. [D24](#d24) against `C-REWORK-CIRCUIT` remains *a candidate to verify, not a fact to restate* ([D28.1](#d28.1), [F123](#f123)). - **Headroom sizing is an open question, not a given.** [F181](#f181) measured headroom derived from median **entry size** while the pressure comes from **arrival rate**. Re-deriving a ceiling after legitimate retirement is not the same act as raising a number to pass, but the spec must say which it is doing and answer [D28.1](#d28.1)'s *&quot;a budget that can be raised to pass is not a budget&quot;*.</Charter>
    <Pays>[F180](#f180), [F181](#f181), [F185](#f185).</Pays>
    <StatusText>**Position 4**, [D34](#d34). Charter rulings: [D28](#d28), [D28.1](#d28.1), [D30](#d30), [D34](#d34).</StatusText>
  </Row>
  <Row name="C-CODIFY-DECISIONS" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Chartered 2026-09-07 by the maintainer**, verbatim: *&quot;option C&quot;*, on four itemized variants. The position is deferred **deliberately, and the deferral is the point**: a position decision costs the decisions genre roughly forty lines, while a charter row costs it nothing — staged charter rows are not `[FD]\d` entries and so are not counted in the decisions wrap at all. Ratify a position once this bundle has made room. Priority needs no stating in the meantime: this is the only chartered work that can open the rulings window, so nothing else can sensibly precede it. **Charter.** Give the decisions genre a working retirement trigger. **It is not, as this row first read, the thing blocking the flush** — that claim was a measurement artifact, corrected the same day in [F191](#f191): both genres fit their windows today. What remains true is that **a live resolving `CodifiedIn` is `decision-eligible-still-live`**. That is the validator invariant, not a census of the live set. A resolving `TaughtIn` annotates and does not retire. The retired sibling is the precedent that a move is possible; it is not a population figure. - **Codification alone was never the whole delivery, and the spec proved which.** [`C-RETIREMENT-WINDOW`](#named-bundle-registry) applied its window formula to the **findings genre only**; the rulings call site clamps against the **pre-move** base — `retireRecord` captures `rulingsBaseRead` before the move and passes it to `persistSnapshot` as the ceiling base, so a decisions-genre retirement leaves the rulings ceiling unchanged and the freed lines become headroom. That behaviour was derived from the code and confirmed on a fixture, not assumed from the findings behaviour. - **The evidence standard is the load-bearing question.** [D28.1](#d28.1) retires a decision when its prose invariant is *permanently replaced by an automated machine check* that enforces it *unconditionally*. A tag on a decision merely **related** to a shipped test retires live governance text on a false premise, and retirement is one-way in practice. [D24](#d24)'s tag was gated on two **named tests passing at execution**, which is the pattern. - **`TaughtIn` versus `CodifiedIn` is settled.** The move condition is `allResolveAndSomeOk` on `CodifiedIn` children only; a resolving `TaughtIn` annotates and does not retire, and teaching a rule in skill prose is not enforcement. - **The bundle must pay for itself.** Whatever chartering and closing costs the decisions genre, the scope must retire more, with margin — every staged figure on this work has aged in the same direction.</Charter>
    <Pays>the verified tags and the working decisions-genre retirement trigger this bundle delivered; [F187](#f187) is retired with PaidBy `C-PAYMENT-RECORD` and is not live to pay.</Pays>
    <StatusText>**Position pending the maintainer's word.** Charter rulings: [D28](#d28), [D28.1](#d28.1), [D30](#d30), [D36](#d36).</StatusText>
  </Row>
  <Row name="C-RECORD-TEST-ISOLATION-2" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Name.** `C-RECORD-TEST-ISOLATION-2`. **Searched before minting**, 2026-09-09 at `99b5266ed432eb5cc1930c30a408069418d5e80e`, over `.ngrace/changes/archive/` (74 directories matching `^C-[A-Z0-9-]+$`) and the record's structural `Row name=` attributes (12 live, 33 retired): **zero hits** for the exact name across the archive directories and both registry files; 2 textual mentions of it inside the superseded bundle's archived spec.xml and plan.xml (its Replacement pointer and its supersede prose — not a directory or Row name). Per-token: `ISOLATION` 1 archive directory (the superseded bundle; a different name), 0 rows; `MUTAT` 0 and 0; `RECORD` 8 archive directories and 2 rows (record-lifecycle bundles, none covering isolating tests from mutating the production record); `TEST` 1 archive directory and 1 retired row (`C-TEST`, kind sweep-remainder, not this territory); `RETIRE` 4 archive directories and 3 rows. No colliding or better-fitting prior name. **Charter.** Isolate this repository's record tests from the production record: no test invokes a mutating record operation against the real tree; the nothing-eligible and second-`--retire` probes run on isolated fixtures with the production observation staying read-only; the shipped live-pair assertion is reshaped to the move-invariant pair form; and the bundle flushes its pinned staged findings while correcting the retired row the current code falsifies.</Charter>
    <Pays>[F192](#f192), [F194](#f194), [F195](#f195).</Pays>
    <StatusText>**Ratified 2026-09-09** — the maintainer approved this bundle's plan; the row mints its payment only at this bundle's close, when its name equals an archive directory.</StatusText>
  </Row>
  <Row name="C-RECORD-RETIREMENT-2" status="retired" kind="historical">
    <Number></Number>
    <Charter>Remove the citation validator's qualifier-suffix slug grammar — the mechanism [F172](#f172) describes — removed by this bundle's own merged work, commit `67bc55c` (PR #74). This row exists so the derivation can see the payment.</Charter>
    <Pays>F172</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD-2 because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: `expectedSlugFor` and `duplicateCount` are absent from `src/` and `scripts/` (0 hits at this commit); the citation validator's only duplicate rule is `duplicate-id` over index ids at `scripts/validate-citation-anchors.ts:122`; the file carries 0 occurrences of `slug`; the removal commit is `67bc55c` (PR #74).</StatusText>
  </Row>
  <Row name="C-ARTIFACT-VALIDITY" status="retired" kind="historical">
    <Number></Number>
    <Charter>Artifact parsing and well-formedness validation: every artifact the lint reads is parsed, and a malformed active artifact refuses. This row exists so the derivation can see the payment.</Charter>
    <Pays>F39</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD-2 because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: `xml.comment-not-well-formed` at `src/lint/catalog.ts:529` with its named test at `src/lint/catalog.test.ts:903`; `readGraceXmlArtifact` at `src/artifact/xml.ts:201`; a malformed active artifact refuses with `xml.parse` and exit 1; the archived plans the original count measured sit outside the scan set.</StatusText>
  </Row>
  <Row name="C-CONTRACT-DEBT" status="retired" kind="historical">
    <Number></Number>
    <Charter>Closing the contract-debt items: the single exported cloneXmlNode and the additive UTF-8 byte skill footprint beside the frozen line total. This row exists so the derivation can see the payment.</Charter>
    <Pays>F20, F29, F29.1</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD-3 because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: exactly one definition of cloneXmlNode survives, exported at src/artifact/xml.ts:239, and the sites that held private copies import it (src/grace-cursor.ts:126, src/artifact/run-membership.ts:22, src/gates/ledger.ts:65); the token-accounting helper ships UTF-8 byte accounting beside the frozen line total (countUtf8Bytes, perSkillBytes, totalBytes at src/test-support/token-accounting.ts:70-115, documented as matching wc -c).</StatusText>
  </Row>
  <Row name="C-RECORDED-DEBT" status="retired" kind="historical">
    <Number></Number>
    <Charter>Paying four recorded P1 debts: undeclared-flag refusal with usage, getModulePath reduced to the graph path, and the as-state class taxonomy. This row exists so the derivation can see the payment.</Charter>
    <Pays>F76, F71, F71.1, F77</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD-3 because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: the real CLI refuses an undeclared flag — bun ./src/grace.ts lint --path . --totallyBogusFlag hello exits 1 with invalid-arguments naming the flag and printing usage, on a throwaway project at this commit; getModulePath returns moduleRecord.graph.path and nothing else (src/query/core.ts:111-113), covering all four display strings F71.1 counted; the as-state class taxonomy is a single exported const with an exhaustiveness guard that throws when a class is unassigned (AS_STATE_PURITY_CLASSES at src/lint/types.ts:36, iterated and enforced at src/lint/core.ts:206-212).</StatusText>
  </Row>
  <Row name="C-SUBSTANTIATION-HONESTY" status="retired" kind="historical">
    <Number></Number>
    <Charter>Closing the attempt-pair review surface: the identical-tree raise rule and the active/-or-archive plan resolution. This row exists so the derivation can see the payment.</Charter>
    <Pays>F9.10, F31, F32</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD-3 because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: the raise condition is the identical-tree rule — raise only when the non-.ngrace/ trees are identical across a fail→pass pair (src/review/core.ts:1405-1411, with the comment at :1589 naming this bundle and F31); the attempt-pair audit resolves the plan under active/ or archive/ (the "no plan found for … under active/ or archive/" resolution and planLocation "archive" in src/review/core.ts's pair loader).</StatusText>
  </Row>
  <Row name="C-REPORT-HONESTY" status="retired" kind="historical">
    <Number></Number>
    <Charter>Making the tool's own report surfaces agree about facts on disk: one shared loose-event membership for status and the archive gate, the archive-identity reading for MustExist, and lifecycle-path exclusion from the scope audit. This row exists so the derivation can see the payment.</Charter>
    <Pays>F11, F11.2, F14, F15, F16</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD-3 because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: isCliLifecyclePath (src/review/core.ts:1013) skips the CLI's own lifecycle writes in the scope audit at :1063 and :1127, covering run.xml, run-ledger.xml and run/**; both status and the archive gate take loose-event membership from the one shared module (src/gates/core.ts:33 and :480, src/grace-status.ts:29 and :267, whose comment says "membership is listLooseEvents (shared with archive gate)"); the MustExist check reuses expandScopePathsForArchiveIdentity for the archive-identity reading (src/review/core.ts:504 and :523).</StatusText>
  </Row>
  <Row name="C-FLAG-HONESTY" status="retired" kind="historical">
    <Number></Number>
    <Charter>Refusing the space-separated true/false form for boolean flags with a loud error naming the two working forms, fixed as a class across the CLI. This row exists so the derivation can see the payment.</Charter>
    <Pays>F18</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD-3 because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: the real CLI refuses the space form — bun ./src/grace.ts gate archive --change C-X --record false exits 1 with "Boolean flag `--record` does not accept a space-separated value" naming both working forms, before any ledger write, on a throwaway project at this commit; the check lives at src/query/command.ts:84-116 and the gate-side comment at src/gates/command.ts:214 names F18.</StatusText>
  </Row>
  <Row name="C-ESCALATION-HONESTY" status="retired" kind="historical">
    <Number></Number>
    <Charter>The fix-budget / resume seam: the signature-budget thrash rule, the distinct-signature backstop, and the reason-required escalation-clearing resume. This row exists so the derivation can see the payment.</Charter>
    <Pays>F22</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD-3 because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: an escalation-clearing resume requires a recorded --reason and refuses before any write (src/grace-cursor.ts:1048-1068).</StatusText>
  </Row>
  <Row name="C-RECOVER-FOLDABLE" status="retired" kind="historical">
    <Number></Number>
    <Charter>Making ngrace cursor recover --fix produce a ledger that can actually fold. This row exists so the derivation can see the payment.</Charter>
    <Pays>F13</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD-3 because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: writeCoveringOpened allocates headroom past the covering event's own id — OPEN_EPOCH_DEFAULT_HEADROOM = 98 at src/grace-cursor.ts:742, applied at :756 as to = Math.max(coveringRequirementTo, openedId + OPEN_EPOCH_DEFAULT_HEADROOM).</StatusText>
  </Row>
  <Row name="C-EXECUTION-CONTRACT" status="retired" kind="historical">
    <Number></Number>
    <Charter>Documenting the cursor event protocol the CLI already enforces inside skills/ngrace/ngrace-execute/SKILL.md and its packaged mirror. This row exists so the derivation can see the payment.</Charter>
    <Pays>F12</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-RECORD-3 because the archived spec never used the payer phrase and no registry row existed at close. Present-tree witness: the terminal kind entry at skills/ngrace/ngrace-execute/SKILL.md:84-87 says to emit terminal before ngrace cursor fold and that fold requires a terminal event inside the covering allocation range, and the packaged mirror carries the same text (grep -c terminal identical in both copies).</StatusText>
  </Row>
  <Row name="C-INDEX-METRIC-2" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Name.** `C-INDEX-METRIC-2`. **Searched before minting**, 2026-09-10 at `65c99ec`, over `.ngrace/changes/archive/` and both registry layers for any prior row carrying the name: **zero rows in either layer**. No prior name covers this ground. **Charter.** The index-ratchet remedy bundle: replaced the index budget with the live-`Entry` metric denominated in live `Entry` children (`INDEX_HEADROOM_ENTRIES = 40`), performed the one-time root rewrite the metric requires, and shipped the genre-aware breach diagnostic, so the index ceiling can no longer be consumed by retired entries.</Charter>
    <Pays>[F205](#f205).</Pays>
    <StatusText>**Delivered.** Archived applied 2026-09-09 as `C-INDEX-METRIC-2` (PR #84). This row was minted post-archive by `C-FLUSH-AND-PAY` so the derivation can see a payment the archived bundle's own spec names — the prose-mint pass is gone since C-RECORD-PARSE (PR #82), so the row is the only route at HEAD.</StatusText>
  </Row>
  <Row name="C-ROOT-WINDOW" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Name.** `C-ROOT-WINDOW`. **Searched before minting**, 2026-09-11 at `c1f4dae`, over `.ngrace/changes/archive/` and both registry layers for any prior row carrying the name: **zero rows in either layer**. No prior name covers this ground. **Charter.** The record engine's root-window bundle: charters the `--rewrite-roots` mode (holds every ceiling byte-for-byte, re-derives base from the shipped metric read back, refuses an exceeded or missing ceiling before any write), floors the headroom derivation at all four sites, persists the unmoved genre's opened window under the maintainer's resolution, and normalises the move writer's inter-element whitespace to the canonical house shape.</Charter>
    <Pays>F216 F182 F193 F190 F225 F226</Pays>
    <StatusText>**Ordered.** Ratified 2026-09-11 by the maintainer. The row is minted live by the plan and paid by the close's move.</StatusText>
  </Row>
  <Row name="C-FLUSH-AND-UNPIN" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Name.** `C-FLUSH-AND-UNPIN`. **Searched before minting**, 2026-09-11 at `8a1a3cd`, over `.ngrace/changes/archive/` and both registry layers for any prior row carrying the name: **zero rows in either layer**. No prior name covers this ground. **Charter.** The staged-record flush bundle: two waves land the staged findings the record's own rules force into the live record, the first wave's move retires the two already-minted entries under the shipped writer's stamp, the expired zero-carriers pin in the payment invariant is rewritten to the at-most-once retired-carrier relation the record's own payment cycle keeps true, five more entries land inside the findings window, and this row is minted live so the close's own move can pay the suite-repair finding the rewritten invariant names.</Charter>
    <Pays>[F232](#f232).</Pays>
    <StatusText>**Delivered.** Minted live by the bundle's own plan so the close's own move pays the bundle's repair finding under the archive event this row names; the `Pays` cell above is the row's whole derived payment set.</StatusText>
  </Row>
  <Row name="C-TAUGHT-RULES" status="retired" kind="chartered">
    <Number></Number>
    <Charter>Searched before minting, 2026-09-11 at 3f8a5ca: zero prior rows in either registry layer and zero prior archive directories carrying the name — searched over `.ngrace/changes/archive/` (89 directories at the search) and both registry layers by listing and parse, not by spot-check.</Charter>
    <Pays>F210 F196 F199 F188 F233 F234</Pays>
    <StatusText>Ordered; the row is minted live by the plan and paid by the close move.</StatusText>
  </Row>
  <Row name="C-APPLY-VERB" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Name.** `C-APPLY-VERB`. **Searched before minting**, 2026-09-12, over `.ngrace/changes/archive/` (91 directories at the search) and both registry layers for any prior row carrying the name: **zero rows in either layer and zero prior archive directories**. No prior name covers this ground. **Charter.** The top-level `ngrace apply` verb: the sanctioned `approved` to `applied` close and the archive move, with surgical status writes on the active spec and plan, per-artifact SHA-256 fingerprints of the applied bytes, two gate=applied permitting Decisions in document order spec then plan, and a same-filesystem rename with rollback. The gate commands keep their evaluate-only contract; the verb replaces the two hand acts that closed a bundle.</Charter>
    <Pays>F224</Pays>
    <StatusText>Ordered. The row is minted live by the plan and paid by the close's move.</StatusText>
  </Row>
  <Row name="C-FLUSH-AND-TEACH" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Name.** `C-FLUSH-AND-TEACH`. **Searched before minting**, 2026-09-12 at `bd284cec05e8c18f6b24584e28018a7a9e4458da`, over `.ngrace/changes/archive/` (92 directories matching the canonical C-* pattern) and the record structural Row name attributes (12 live, 49 retired): zero hits for the exact name across the archive directories and both registry files. Per-token: FLUSH 6 archive directories and 1 retired row; TEACH 1 archive directory and 0 rows; AND 8 archive directories and 3 rows. No colliding or better-fitting prior name. **Charter.** Flush the three staged findings into the record and teach the five rules in six skills, canonical and mirror, minting this row payment for the teaching increment.</Charter>
    <Pays>F236 F237 F238 F239 F241</Pays>
    <StatusText>Ordered; the row is minted live by the plan and paid by the close move.</StatusText>
  </Row>
  <Row name="C-PAYMENT-RECORD-2" status="retired" kind="historical">
    <Number></Number>
    <Charter>Populate the record's payments from the rowless-archive survey: the historical row for C-RECORD-RETIREMENT-2 paying F172, and the measured population of archived bundles carrying no row in either layer. This row exists so the derivation can see the payment.</Charter>
    <Pays>F198</Pays>
    <StatusText>**Delivered.** Historical payment record written by C-PAYMENT-INTEGRITY because the bundle that did the work had no row. Present-tree witness: the C-RECORD-RETIREMENT-2 historical row in registry-retired.xml carries &lt;Pays&gt;F172&lt;/Pays&gt; and F172 is retired with &lt;PaidBy&gt;C-RECORD-RETIREMENT-2&lt;/PaidBy&gt;; C-PAYMENT-RECORD-2's own Problem measured 55 archived bundles with no row in either layer.</StatusText>
  </Row>
  <Row name="C-PAYMENT-INTEGRITY" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Name.** `C-PAYMENT-INTEGRITY`. **Searched before minting**, 2026-09-12 at `af0f721`, over `.ngrace/changes/archive/` (93 directories matching `^C-[A-Z0-9-]+$`) and the record structural Row name attributes (12 live, 50 retired): zero hits for the exact name across the archive directories and both registry files. Per token: `PAYMENT` 4 archive directories (`C-PAYMENT-RECORD`, `-2`, `-3`, `-4`) and 1 row; `INTEGRITY` 2 archive directories (`C-CURSOR-INTEGRITY`, `C-TOKEN-INTEGRITY`) and 0 rows. No colliding or better-fitting prior name. **Charter.** Delete the StatusText prose payment path and declare its nine payments in Pays; record F185's and F198's payments; mint this row to pay F197 at the close.</Charter>
    <Pays>F197 F244</Pays>
    <StatusText>Ordered; the row is minted live by the plan and paid by the close's move.</StatusText>
  </Row>
  <Row name="C-GUARD-RATCHET" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Name.** `C-GUARD-RATCHET`. **Searched before minting**, 2026-09-13 at `fa1773d7b853050d2330e4aa21f0514c3e8d6b05`, over `.ngrace/changes/archive/` (94 directories matching `^C-[A-Z0-9-]+$`) and the record structural Row name attributes (12 live, 52 retired): zero hits for the exact name across the archive directories and both registry files. Per token: `GUARD` 0 archive directories and 0 rows; `RATCHET` 0 archive directories and 0 rows. No colliding or better-fitting prior name. **Charter.** Flush the six staged entries, record `F244`'s payment, and state the anti-occupancy guard's threat model and boundary per `D39`; mint this row to pay `F222` and `F223` at the close.</Charter>
    <Pays>F222 F223</Pays>
    <StatusText>Ordered; the row is minted live by the plan and paid by the close's move.</StatusText>
  </Row>
  <Row name="C-REVIEW-SELF-SCOPE-2" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Name.** `C-REVIEW-SELF-SCOPE-2`. **Searched before minting**, 2026-09-13 at `370ed79`, over `.ngrace/changes/archive/` (96 directories matching `^C-[A-Z0-9-]+$`, the superseded predecessor `C-REVIEW-SELF-SCOPE` among them) and the record's structural Row name attributes across both registry layers (65 rows: 12 live + 53 retired): zero exact hits for `C-REVIEW-SELF-SCOPE-2`; per token `REVIEW` 5 directories and 2 rows, `SELF` 1 and 0, `SCOPE` 5 and 4. No colliding or better-fitting prior name. **Charter.** Close `F240` and `F245` in the review surface under the lineage id — the narrowed `MustExist` check and the three-artifact identity skip — and pay both findings at the close.</Charter>
    <Pays>F240 F245</Pays>
    <StatusText>Ordered; the row is minted live by the plan and paid by the close's move.</StatusText>
  </Row>
  <Row name="C-TEACH-DRIVE-BEFORE-APPROVE-2" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Name.** `C-TEACH-DRIVE-BEFORE-APPROVE-2`. **Searched before minting**, 2026-09-14 at `2aed6ac55d048664b02e2fadcb01db0a266a1f14`, over `.ngrace/changes/archive/` (98 directories matching `^C-[A-Z0-9-]+$`; the superseded predecessor is one of them) and the record structural Row name attributes across both registry layers (12 live, 54 retired): zero exact hits for `C-TEACH-DRIVE-BEFORE-APPROVE-2`. Per token: `TEACH` 3 directories and 1 row, `DRIVE` 1 and 0, `BEFORE` 1 and 0, `APPROVE` 2 directories and 2 rows. No colliding or better-fitting prior name. **Charter.** Flush the four staged findings into the record and teach one rule per finding plus the verification-state rule as pinned sentences in four skills, canonical and mirror, minting this row payment for the teaching increment.</Charter>
    <Pays>F243 F246 F247 F248 F249</Pays>
    <StatusText>Ordered; the row is minted live by the plan and paid by the close's move.</StatusText>
  </Row>
  <Row name="C-HASHED-BUNDLE-IDS-2" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Name.** `C-HASHED-BUNDLE-IDS-2`. **Searched before minting**, 2026-09-14 at `852b150`, over `.ngrace/changes/archive/` (100 directories matching `^C-[A-Z0-9-]+$`; the predecessor `C-HASHED-BUNDLE-IDS` is one of them) and the record's structural `Row name=` attributes across both registry layers (67 rows: 12 live + 55 retired): zero exact hits for `C-HASHED-BUNDLE-IDS-2`. Per token: `HASHED` 1 archive directory and 0 rows; `BUNDLE` 2 archive directories (`C-BUNDLE-BASE-REF`, `C-HASHED-BUNDLE-IDS`) and 0 rows; `IDS` 1 and 0. No colliding or better-fitting prior name. This `-2` is the last legacy-form mint by D38's third edge. **Charter.** Build D38: `spec new` mints `C-&lt;SLUG&gt;-&lt;N&gt;-&lt;HASH&gt;`, `supersede` derives and accepts only the lineage successor, the permissive anchor pattern and the legacy corpus stay unchanged, the skeleton seeds a docs-and-examples NonGoal, the teaching and README carry the form, `src/gates/core.test.ts` joins the scope, and D38 is codified by two CodifiedIn test suites at the close.</Charter>
    <Pays></Pays>
    <StatusText>Ordered; the row is minted live by the plan and paid by the close move.</StatusText>
  </Row>
  <Row name="C-PAIR-AUDIT-MINT-CWD-1-ED6B7D22" status="retired" kind="chartered">
    <Number></Number>
    <Charter>Collision half: the mint printed `mint-search: 0 active, 0 archive prior bundle(s) share slug PAIR-AUDIT-MINT-CWD`. Scope half: over the 101 archive directories and both registry layers, the surfaces this bundle opens are owned by C-SUBSTANTIATION-HONESTY (the live identical-tree audit and the attempt-pair pairing) and C-HASHED-BUNDLE-IDS-2 (the hashed mint and resolveBranch), with review-surface precedents C-DECLARED-WRITES, C-CRITERION-CLOSE-EVIDENCE and C-REVIEW-SELF-SCOPE-2; 43 archived bundles name src/review/core.ts, 23 name src/review/catalog.ts, 10 name src/grace-generate.ts; no colliding or better-fitting prior name.</Charter>
    <Pays>F250 F251</Pays>
    <StatusText>Paid at the close of C-PAIR-AUDIT-MINT-CWD-1-ED6B7D22.</StatusText>
  </Row>
  <Row name="C-TEACH-PLAN-DRIVES-CORRECTIONS-2-1E59AEAA" status="retired" kind="chartered">
    <Number></Number>
    <Charter>Fourth teaching increment of the `RM-GOVERNED-PATH` record: it flushes the two staged findings it pays, teaches the four `ngrace-plan` drives and the three `ngrace-execute` correction rules, and pins every taught sentence with a `scripts/check-teaching-surface.ts` needle in both trees. Searched before minting — tool half: `mint-search: 1 active, 0 archive prior bundle(s) share slug TEACH-PLAN-DRIVES-CORRECTIONS`. Author half: over the 103 archive directories and both registry layers, the only name hit for `C-TEACH-PLAN-DRIVES-CORRECTIONS` is the lineage predecessor `C-TEACH-PLAN-DRIVES-CORRECTIONS-1-14457871`, which is not a collision; the teaching-surface increments that touched these skills are `C-TAUGHT-RULES`, `C-TEACHING-SURFACE`, `C-FLUSH-AND-TEACH`, `C-TEACH-DRIVE-BEFORE-APPROVE`, `C-TEACH-DRIVE-BEFORE-APPROVE-2` and `C-PAIR-AUDIT-MINT-CWD-1-ED6B7D22`; 23 archived bundles name `ngrace-plan/SKILL.md` and 34 name `ngrace-execute/SKILL.md`; none is a colliding or better-fitting prior name for an increment that adds exactly these drives.</Charter>
    <Pays>F253 F254</Pays>
    <StatusText>Fourth teaching increment of the `RM-GOVERNED-PATH` record; live while the bundle is active and retired by the close's own move.</StatusText>
  </Row>
  <Row name="C-EXPLAIN-ANCHOR-COMMANDS-1-FC0ED3DA" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Searched before minting.** **Tool half (verbatim):** `mint-search: 0 active, 0 archive prior bundle(s) share slug EXPLAIN-ANCHOR-COMMANDS`. **Author half (scope search), measured 2026-09-15 at `3844741`:** over the 104 archive directories and both registry layers, the last bundle to open each of the three `src/` files this bundle opens is, for `src/grace-lint.ts` and `src/lint/core.ts`, `C-LINT-PHASE-HONESTY-2`, and, for `src/review/core.ts`, `C-PAIR-AUDIT-MINT-CWD-1-ED6B7D22`; 8 archived bundles name `src/grace-lint.ts`, 23 name `src/review/core.ts`, 13 name `src/lint/core.ts`; the last bundle to open `skills/ngrace/ngrace-spec/SKILL.md` is `C-HASHED-BUNDLE-IDS-2` and 14 archived bundles name it. None is a colliding or better-fitting prior name for a bundle that repairs the `--explain` fallback, the review anchor keys and the run-commands report line.</Charter>
    <Pays>F255 F256 F257</Pays>
    <StatusText>Pays the three staged findings this bundle names; the record-schema work stays with its own owner.</StatusText>
  </Row>
  <Row name="C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F" status="retired" kind="chartered">
    <Number></Number>
    <Charter>Fifth teaching increment of RM-GOVERNED-PATH: flush the five staged findings and teach the five sentences. mint-search: 0 active, 0 archive prior bundle(s) share slug TEACH-COPY-DRIVES-LINEAGE. Author half: the last bundle to open ngrace-plan/SKILL.md and ngrace-execute/SKILL.md is C-TEACH-PLAN-DRIVES-CORRECTIONS-2-1E59AEAA (24 archived bundles name ngrace-plan/SKILL.md, 35 name ngrace-execute/SKILL.md); the last to open ngrace-spec/SKILL.md is C-EXPLAIN-ANCHOR-COMMANDS-1-FC0ED3DA (15 name it); over the 105 archive directories and both registry layers no name collides with or better fits a bundle teaching the copy-commit order, the pass-signature discard, the archived-draft re-homing, the supersede lineage path and the close-evidence verdict. This Charter names no F token.</Charter>
    <Pays>F258 F259 F260 F261 F262</Pays>
    <StatusText>Live while the bundle is open; the close archives it and the retirement move stamps the five carriers.</StatusText>
  </Row>
  <Row name="C-STATUS-TEST-TIMEOUT-1-E7DA46E8" status="retired" kind="chartered">
    <Number></Number>
    <Charter>T0 hotfix on RM-GOVERNED-PATH: give src/grace-status.test.ts's live-repo reRatificationCount test an explicit generous timeout. mint-search: 0 active, 0 archive prior bundle(s) share slug STATUS-TEST-TIMEOUT. Author half: no archived bundle name collides with a test-timeout hotfix; the nearest are the teaching increments, none of which touches src/grace-status.test.ts's timeouts. This Charter names no F token.</Charter>
    <Pays>F263</Pays>
    <StatusText>Live while the bundle is open; the close archives it and the retirement move stamps the one carrier.</StatusText>
  </Row>
  <Row name="C-SCRIPTS-ADOPTION-2-36DEB1BD" status="retired" kind="chartered">
    <Number></Number>
    <Charter>Searched before minting. Tool half (verbatim): mint-search: 1 active, 0 archive prior bundle(s) share slug SCRIPTS-ADOPTION. Author half, measured 2026-09-16 at 7d7efaf98a34e9eaf3775b91379f33207aa0ddc6 over the 108 archive directories and both registry layers: 9 archived bundles name tsconfig.json and 45 name ignoredDirs, but none ever wrote tsconfig.json (its include has been [&quot;src/**/*.ts&quot;] in every commit but the predecessors' own) and .ngrace-lint.json was written only by C-EVIDENCE-DISCRIMINATION and the now-archived C-SCRIPTS-ADOPTION-1-F621B7C0; 13 archived bundles name scripts/release-check.ts, and F125's payer C-SUPERSEDE-COMMAND is the precedent for forcing a scripts/ file into scope from a src/-class repair; over the 108 archive directories and both registry layers no name collides with or better fits a bundle that adopts scripts/ under the root typecheck and lint — the lineage name C-SCRIPTS-ADOPTION-* carries it.</Charter>
    <Pays>F242 F264</Pays>
    <StatusText>Live while the bundle is open; the close archives it and the retirement move stamps both carriers and moves the row.</StatusText>
  </Row>
  <Row name="C-RECORD-SCHEMA-2-0785BD5E" status="retired" kind="chartered">
    <Number></Number>
    <Charter>Shape schema and canonical serializer for the record; the record engine lives under scripts/. Searched before minting 2026-09-16 over .ngrace/changes/archive/ and both registry layers: the predecessor C-RECORD-SCHEMA-1-1D8D868F is the one prior bundle (mint-search: 1 active, 0 archive), and no other row or directory carries the name.</Charter>
    <Pays>F207 F221 F235 F252 F265</Pays>
    <StatusText>Step (1) of D40; the flush/mint verb is the chartered successor.</StatusText>
  </Row>
  <Row name="C-TEST-TIMEOUT-CEILING-2-7AD2A006" status="retired" kind="chartered">
    <Number></Number>
    <Charter>T0 hotfix on RM-GOVERNED-PATH: no per-test timeout, one job ceiling; the successor names the skill-footprint pin the predecessor missed. mint-search: 1 active, 0 archive prior bundle(s) share slug TEST-TIMEOUT-CEILING. Author half: no archived bundle name collides with a test-timeout hotfix; the bundle flips the test-runner ceiling, the taught ngrace-spec rule and the skill-footprint pin. This Charter names no F token.</Charter>
    <Pays>F266 F267</Pays>
    <StatusText>**Ordered, not deferred** — the T0 hotfix; the close retires both findings and this row.</StatusText>
  </Row>
  <Row name="C-TEACH-CLOSE-DRIVES-PINS-1-B0BC7FBD" status="retired" kind="chartered">
    <Number></Number>
    <Charter>**Name.** `C-TEACH-CLOSE-DRIVES-PINS-1-B0BC7FBD`. Tool half (verbatim): `mint-search: 0 active, 0 archive prior bundle(s) share slug TEACH-CLOSE-DRIVES-PINS`. Author half (the scope search, measured at the base commit): the last bundle to open `ngrace-spec/SKILL.md` is `C-TEST-TIMEOUT-CEILING-2-7AD2A006` and the last to open `ngrace-plan/SKILL.md` and `ngrace-execute/SKILL.md` is `C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F`; over the 113 archive directories and both registry layers no name collides with or better fits a bundle that teaches the second terminal and fold, the copy-drive verdict exception, one guard per pair, the successor-authoring bounded diff, the relation-not-number criterion, the per-effect pin grep, the forced-file tier invariant and the re-homed plan assertion.</Charter>
    <Pays>F268 F269 F271 F272 F273 F274 F275 F276</Pays>
    <StatusText>Teaching increment 6 of the governed-path record: the eight taught rules and their instruments, minted live.</StatusText>
  </Row>
  <Row name="C-RECORD-FLUSH-VERB-2-6BB9DF5A" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 1 active, 0 archive prior bundle(s) share slug RECORD-FLUSH-VERB. Author half: the flush/mint verb, D40 step (2), successor; searched before minting over the 115 archive directories and both registry layers, no colliding name.</Charter>
    <Pays>F228 F277 F278</Pays>
    <StatusText>Step (2) of D40; the flush/mint verb, successor dogfood flush.</StatusText>
  </Row>
  <Row name="C-CLONE-FAITHFUL-TESTS-1-D68520A2" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 0 active, 0 archive prior bundle(s) share slug CLONE-FAITHFUL-TESTS. Searched before minting over the archive and both registry layers for CLONE-FAITHFUL-TESTS; none.</Charter>
    <Pays>F279</Pays>
    <StatusText>Clone-faithful fixtures and the read-audit guard; one finding paid</StatusText>
  </Row>
  <Row name="C-TEST-TIME-BUDGET-4-F173437E" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 3 active, 0 archive prior bundle(s) share slug TEST-TIME-BUDGET. Author half: searched before minting over the archive directories and both registry layers; the fourth of a lineage, minted by the supersede verb, which prints no mint-search line, so the value half is the mint-time count measured by the authority rather than pasted tool output.</Charter>
    <Pays>F281 F282 F286</Pays>
    <StatusText>Test-time budget: the suite halved, a non-gating selective runner, per-test metrics, in-process validator calls, the close-time only rule, and the attempt-pair exemption for abandoned archived governance.</StatusText>
  </Row>
  <Row name="C-RECORD-DECISION-FLUSH-1-368E01F2" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 0 active, 0 archive prior bundle(s) share slug RECORD-DECISION-FLUSH. Author half: searched before minting over the 121 archive directories and both registry layers; no prior bundle carries the slug RECORD-DECISION-FLUSH.</Charter>
    <Pays>F289</Pays>
    <StatusText>The flush verb's decision path: the element and its index entry written together, a genre refusal before any write, and the index-versus-genre presence relation enforced at rest and before the write.</StatusText>
  </Row>
  <Row name="C-CURSOR-EVENT-ID-INTEGRITY-2-E18DFE92" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 0 active, 0 archive prior bundle(s) share slug CURSOR-EVENT-ID-INTEGRITY. Author half: searched before minting over the archive directories and both registry layers; the successor of a supersede, no prior bundle carries the slug CURSOR-EVENT-ID-INTEGRITY.</Charter>
    <Pays>F288</Pays>
    <StatusText>Cursor event-id integrity: allocation serialized behind a per-bundle write lock, a post-write uniqueness renumber of the writer's own file, and a duplicate-id fold blocker named by recover.</StatusText>
  </Row>
  <Row name="C-INSPECTION-SURFACE-1-2628AA2B" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 0 active, 0 archive prior bundle(s) share slug INSPECTION-SURFACE. Author half: searched the archive directories and both registry layers before minting; no prior bundle carries the slug INSPECTION-SURFACE.</Charter>
    <Pays>F270 F291</Pays>
    <StatusText>A read-only inspection surface for governed artifacts: a shipped ngrace change find|show and a repo-tooling record:query, both reading through the engine's own readers, with the Verdict reader carrying the close-evidence tally it already parsed past.</StatusText>
  </Row>
  <Row name="C-TEACHING-INCREMENT-2-4E28E16C" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 1 active, 0 archive prior bundle(s) share slug TEACHING-INCREMENT. Author half: the teaching increment — the Atomic Mechanism Exception, correction ordering and the run stream's inviolability in ngrace-execute, the close-writes rule in ngrace-plan and ngrace-spec, read-the-shape-first in ngrace-reviewer; and on the flush verb a TaughtIn writer and function replacements so a buffer body is written as data.</Charter>
    <Pays>F296 F298 F283 F285 F290 F293 F302 F303</Pays>
    <StatusText>Eight findings paid by one bundle: six taught rules across four skills, a TaughtIn writer, and the flush verb writing bodies as data.</StatusText>
  </Row>
  <Row name="C-EVENT-LOCK-ATOMIC-1-9711E83D" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 0 active, 0 archive prior bundle(s) share slug EVENT-LOCK-ATOMIC. Author half: the event write lock treats an empty or unparsable holder as unknown and waits, guards the stale unlink, and the concurrency guard names its failing process.</Charter>
    <Pays>F307</Pays>
    <StatusText>One finding paid: the cursor event write lock no longer steals a live holder it read before the holder's content landed.</StatusText>
  </Row>
  <Row name="C-EPOCH-OPEN-DEFAULT-1-CE010A4C" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 0 active, 0 archive prior bundle(s) share slug EPOCH-OPEN-DEFAULT. Author half: an unbounded epoch open allocates from the next event id after the folded history, and recover --fix re-homes a leading hole while declining a mid-range one.</Charter>
    <Pays>F308</Pays>
    <StatusText>One finding paid: a bundle with a folded history can open an epoch without bounds and fold it.</StatusText>
  </Row>
  <Row name="C-PER-BUNDLE-READERS-1-58BB7AB1" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 0 active, 0 archive prior bundle(s) share slug PER-BUNDLE-READERS. Author half: the applied change scope carries its durable anchors, and a per-bundle derived-state reader factored from the whole-project derivation answers change show without a project-wide status walk.</Charter>
    <Pays>F300 F301</Pays>
    <StatusText>Two findings paid: an archived bundle's anchors print, and one bundle's states cost one bundle's work.</StatusText>
  </Row>
  <Row name="C-SECTION-REGISTRY-AND-FLUSH-1-766B2DF9" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 0 active, 0 archive prior bundle(s) share slug SECTION-REGISTRY-AND-FLUSH. Author half: every direct child of a spec or plan wrapper is a registered section, with the optional sets enumerated from the archive population; the flush verb demands --mint-search only when it mints and names every stamp it writes.</Charter>
    <Pays>F304 F305</Pays>
    <StatusText>Two findings paid: a stray or misspelled section is named at lint time, and the flush verb's codify-only path stops demanding a value it never uses.</StatusText>
  </Row>
  <Row name="C-TEACHING-CLOSE-PATH-1-3E91B69F" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 0 active, 0 archive prior bundle(s) share slug TEACHING-CLOSE-PATH. Author half: the supersede path taught in ngrace-spec and ngrace-plan, the close path corrected in ngrace-execute and ngrace-plan, and the reviewer's shipped-parser census; every sentence driven before it was written.</Charter>
    <Pays>F306 F311</Pays>
    <StatusText>Two findings paid: the supersede and close paths are taught as measured, and the instrument no longer pins a false precondition.</StatusText>
  </Row>
  <Row name="C-CURSOR-STATE-HONESTY-1-53C5EC0B" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 0 active, 0 archive prior bundle(s) share slug CURSOR-STATE-HONESTY. Author half: the cursor's state projection reads the current range's closer so a folded stream reads complete, and recover --fix declines to fabricate a covering over a lost event.</Charter>
    <Pays>F309 F310</Pays>
    <StatusText>Two findings paid: a folded stream reads what it is, and a repair verb no longer says yes beside a fold it left blocked.</StatusText>
  </Row>
  <Row name="C-CI-LINT-AND-PLAN-SHAPE-1-3526D6F0" status="retired" kind="chartered">
    <Number></Number>
    <Charter>mint-search: 0 active, 0 archive prior bundle(s) share slug CI-LINT-AND-PLAN-SHAPE. Author half: an assertions-off lint mode wired into the CI chain, an existence branch on the anchor rule with its taught transition, and a metrics runner that regenerates only when stale.</Charter>
    <Pays>F284 F297 F299</Pays>
    <StatusText>Three findings paid: CI now lints the governance surfaces honestly, a declared-but-absent file warns instead of erring, and the suite runs once at the close.</StatusText>
  </Row>
</Registry>
