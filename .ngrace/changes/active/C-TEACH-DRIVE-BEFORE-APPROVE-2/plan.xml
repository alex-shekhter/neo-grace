<NgraceChangePlan graceVersion="1.0" status="approved">
  <C-TEACH-DRIVE-BEFORE-APPROVE-2>
    <IntentSummary>
      This plan is the predecessor's driven draft (`C-TEACH-DRIVE-BEFORE-APPROVE`, archived at
      `.ngrace/changes/archive/C-TEACH-DRIVE-BEFORE-APPROVE/plan.xml`) re-homed under the lineage id
      `C-TEACH-DRIVE-BEFORE-APPROVE-2`: only the id data, the spec commit `2aed6ac`, the re-measured
      mint search, and the predecessor's archive-arrival exclusion in the archive-walking criteria
      changed; every driven command and figure below stands as driven for the predecessor. It
      converts the approved `C-TEACH-DRIVE-BEFORE-APPROVE-2` spec into three sequential tasks: the
      record hand writes (flush `F246` `F247` `F248` `F249`, mint the chartered row live, hold every
      ceiling with `--rewrite-roots`, and add the flush-invariant and row walks red-first); the
      teaching (add the whole-tree stale scan and the section-scoped needles to
      `scripts/check-teaching-surface.ts` red-first, then land the six pinned sentences in
      `ngrace-plan`, `ngrace-spec`, `ngrace-execute` and `ngrace-reviewer` plus their packaged
      mirrors, remove the eight stale occurrences, repair `ngrace-plan` row 15's second sentence, and
      re-measure the skill footprint); and the leaf suites, `validate:ci`, the predicted plan-approve
      review, and the faithful post-close applied-archive drive over every `CloseEvidence` command.
      Ceremony tier **T2** per the spec's `Constraints`. The plan creates no `src/` production file;
      the only `src/` write is the `src/test-support/token-accounting.test.ts` footprint pin, which
      `isModuleOwnableWritePath` does not own, so `change.graph-anchors-miss-write-scope` cannot fire.
      `DurableScope` is exactly the spec's two `AffectedAreas` anchors, `M-SKILLS` and
      `M-TEST-SUPPORT`. The plan's baseline needles assert only what the bundle leaves unchanged
      (shipped files and shipped-green commands at `2aed6ac`); the stale `F247` phrase is flipped by
      design, so it is a target `MustNotContain`, never a baseline needle. The plan's last task ends
      at the live row, `--rewrite-roots`, green validators and the taught sentences in both trees;
      `--retire` (`moved 6`), every gate, the `apply` verb, the archive move and the close are the
      authority's.
    </IntentSummary>
    <BaselineAssertions>
      <MustExist><Value>M-SKILLS</Value></MustExist>
      <MustExist><Value>M-TEST-SUPPORT</Value></MustExist>
      <MustExist><Value>skills/ngrace/ngrace-plan/SKILL.md</Value></MustExist>
      <MustExist><Value>skills/ngrace/ngrace-spec/SKILL.md</Value></MustExist>
      <MustExist><Value>skills/ngrace/ngrace-execute/SKILL.md</Value></MustExist>
      <MustExist><Value>skills/ngrace/ngrace-reviewer/SKILL.md</Value></MustExist>
      <MustExist><Value>plugins/ngrace/skills/ngrace/ngrace-plan/SKILL.md</Value></MustExist>
      <MustExist><Value>plugins/ngrace/skills/ngrace/ngrace-spec/SKILL.md</Value></MustExist>
      <MustExist><Value>plugins/ngrace/skills/ngrace/ngrace-execute/SKILL.md</Value></MustExist>
      <MustExist><Value>plugins/ngrace/skills/ngrace/ngrace-reviewer/SKILL.md</Value></MustExist>
      <MustExist><Value>scripts/check-teaching-surface.ts</Value></MustExist>
      <MustExist><Value>scripts/check-teaching-surface.test.ts</Value></MustExist>
      <MustExist><Value>scripts/validate-record-retirement.test.ts</Value></MustExist>
      <MustExist><Value>scripts/skill-contracts.test.ts</Value></MustExist>
      <MustExist><Value>src/test-support/token-accounting.test.ts</Value></MustExist>
      <MustExist><Value>README.md</Value></MustExist>
      <MustExist><Value>docs/plans/active/RM-GOVERNED-PATH/findings.xml</Value></MustExist>
      <MustExist><Value>docs/plans/active/RM-GOVERNED-PATH/findings-retired.xml</Value></MustExist>
      <MustExist><Value>docs/plans/active/RM-GOVERNED-PATH/decisions.xml</Value></MustExist>
      <MustExist><Value>docs/plans/active/RM-GOVERNED-PATH/registry.xml</Value></MustExist>
      <MustExist><Value>docs/plans/active/RM-GOVERNED-PATH/registry-retired.xml</Value></MustExist>
      <MustExist><Value>docs/plans/active/RM-GOVERNED-PATH/rulings.xml</Value></MustExist>
      <MustExist><Value>docs/plans/active/RM-GOVERNED-PATH/rulings-retired.xml</Value></MustExist>
      <MustExist><Value>.ngrace/changes/active/C-TEACH-DRIVE-BEFORE-APPROVE-2/spec.xml</Value></MustExist>
      <MustPassCommand><Command>bun ./scripts/check-teaching-surface.ts check</Command></MustPassCommand>
      <MustPassCommand><Command>bun test --timeout=30000 scripts/check-teaching-surface.test.ts</Command></MustPassCommand>
      <MustPassCommand><Command>bun test --timeout=30000 scripts/validate-record-retirement.test.ts</Command></MustPassCommand>
      <MustPassCommand><Command>bun test --timeout=30000 src/test-support/token-accounting.test.ts</Command></MustPassCommand>
      <MustPassCommand><Command>bun run ./scripts/validate-marketplace.ts</Command></MustPassCommand>
      <MustPassCommand><Command>bun ./scripts/validate-record-retirement.ts docs/plans/active/RM-GOVERNED-PATH</Command></MustPassCommand>
      <MustPassCommand><Command>bun ./scripts/validate-citation-anchors.ts</Command></MustPassCommand>
      <MustPassCommand><Command>bun ./scripts/prove-record-preservation.ts</Command></MustPassCommand>
      <MustVerify><Module>M-SKILLS</Module></MustVerify>
      <MustVerify><Module>M-TEST-SUPPORT</Module></MustVerify>
    </BaselineAssertions>
    <TargetAssertions>
      <MustExist><Value>M-SKILLS</Value></MustExist>
      <MustExist><Value>M-TEST-SUPPORT</Value></MustExist>
      <MustExist><Value>skills/ngrace/ngrace-plan/SKILL.md</Value></MustExist>
      <MustExist><Value>skills/ngrace/ngrace-spec/SKILL.md</Value></MustExist>
      <MustExist><Value>skills/ngrace/ngrace-execute/SKILL.md</Value></MustExist>
      <MustExist><Value>skills/ngrace/ngrace-reviewer/SKILL.md</Value></MustExist>
      <MustExist><Value>plugins/ngrace/skills/ngrace/ngrace-plan/SKILL.md</Value></MustExist>
      <MustExist><Value>plugins/ngrace/skills/ngrace/ngrace-spec/SKILL.md</Value></MustExist>
      <MustExist><Value>plugins/ngrace/skills/ngrace/ngrace-execute/SKILL.md</Value></MustExist>
      <MustExist><Value>plugins/ngrace/skills/ngrace/ngrace-reviewer/SKILL.md</Value></MustExist>
      <MustExist><Value>scripts/check-teaching-surface.ts</Value></MustExist>
      <MustExist><Value>scripts/check-teaching-surface.test.ts</Value></MustExist>
      <MustExist><Value>scripts/validate-record-retirement.test.ts</Value></MustExist>
      <MustExist><Value>scripts/skill-contracts.test.ts</Value></MustExist>
      <MustExist><Value>src/test-support/token-accounting.test.ts</Value></MustExist>
      <MustExist><Value>README.md</Value></MustExist>
      <MustExist><Value>docs/plans/active/RM-GOVERNED-PATH/findings.xml</Value></MustExist>
      <MustExist><Value>docs/plans/active/RM-GOVERNED-PATH/findings-retired.xml</Value></MustExist>
      <MustExist><Value>docs/plans/active/RM-GOVERNED-PATH/decisions.xml</Value></MustExist>
      <MustExist><Value>docs/plans/active/RM-GOVERNED-PATH/registry.xml</Value></MustExist>
      <MustExist><Value>docs/plans/active/RM-GOVERNED-PATH/registry-retired.xml</Value></MustExist>
      <MustExist><Value>.ngrace/changes/active/C-TEACH-DRIVE-BEFORE-APPROVE-2/spec.xml</Value></MustExist>
      <MustExist><Value>.ngrace/changes/active/C-TEACH-DRIVE-BEFORE-APPROVE-2/plan.xml</Value></MustExist>
      <MustContain><File>skills/ngrace/ngrace-plan/SKILL.md</File><Text>tree state its task leaves</Text></MustContain>
      <MustContain><File>skills/ngrace/ngrace-plan/SKILL.md</File><Text>excludes every earlier task's files</Text></MustContain>
      <MustContain><File>skills/ngrace/ngrace-plan/SKILL.md</File><Text>evaluated by nothing before the plan is approved</Text></MustContain>
      <MustContain><File>skills/ngrace/ngrace-plan/SKILL.md</File><Text>to a test slice, never a whole file</Text></MustContain>
      <MustContain><File>skills/ngrace/ngrace-plan/SKILL.md</File><Text>skips the bundle's own</Text></MustContain>
      <MustContain><File>skills/ngrace/ngrace-plan/SKILL.md</File><Text>gate approve --artifact plan</Text></MustContain>
      <MustContain><File>plugins/ngrace/skills/ngrace/ngrace-plan/SKILL.md</File><Text>tree state its task leaves</Text></MustContain>
      <MustContain><File>plugins/ngrace/skills/ngrace/ngrace-plan/SKILL.md</File><Text>excludes every earlier task's files</Text></MustContain>
      <MustContain><File>plugins/ngrace/skills/ngrace/ngrace-plan/SKILL.md</File><Text>skips the bundle's own</Text></MustContain>
      <MustContain><File>skills/ngrace/ngrace-execute/SKILL.md</File><Text>needs an open epoch on a declared task</Text></MustContain>
      <MustContain><File>skills/ngrace/ngrace-execute/SKILL.md</File><Text>approved plan's own baseline assertion is false at HEAD</Text></MustContain>
      <MustContain><File>plugins/ngrace/skills/ngrace/ngrace-execute/SKILL.md</File><Text>needs an open epoch on a declared task</Text></MustContain>
      <MustContain><File>plugins/ngrace/skills/ngrace/ngrace-execute/SKILL.md</File><Text>approved plan's own baseline assertion is false at HEAD</Text></MustContain>
      <MustContain><File>skills/ngrace/ngrace-spec/SKILL.md</File><Text>skips the bundle's own</Text></MustContain>
      <MustContain><File>skills/ngrace/ngrace-spec/SKILL.md</File><Text>bare mint is never committed</Text></MustContain>
      <MustContain><File>plugins/ngrace/skills/ngrace/ngrace-spec/SKILL.md</File><Text>skips the bundle's own</Text></MustContain>
      <MustContain><File>plugins/ngrace/skills/ngrace/ngrace-spec/SKILL.md</File><Text>bare mint is never committed</Text></MustContain>
      <MustContain><File>skills/ngrace/ngrace-reviewer/SKILL.md</File><Text>skips the bundle's own</Text></MustContain>
      <MustContain><File>plugins/ngrace/skills/ngrace/ngrace-reviewer/SKILL.md</File><Text>skips the bundle's own</Text></MustContain>
      <MustContain><File>scripts/check-teaching-surface.ts</File><Text>checkStaleReviewClaims</Text></MustContain>
      <MustContain><File>scripts/check-teaching-surface.ts</File><Text>design-context.xml</Text></MustContain>
      <MustContain><File>scripts/validate-record-retirement.test.ts</File><Text>F243: "C-TEACH-DRIVE-BEFORE-APPROVE-2"</Text></MustContain>
      <MustContain><File>scripts/validate-record-retirement.test.ts</File><Text>validateStubAndIndex</Text></MustContain>
      <MustContain><File>scripts/validate-record-retirement.test.ts</File><Text>proveRecordPreservation</Text></MustContain>
      <MustContain><File>docs/plans/active/RM-GOVERNED-PATH/findings.xml</File><Text>&lt;Finding id="f246"</Text></MustContain>
      <MustContain><File>docs/plans/active/RM-GOVERNED-PATH/findings.xml</File><Text>&lt;Finding id="f247"</Text></MustContain>
      <MustContain><File>docs/plans/active/RM-GOVERNED-PATH/findings.xml</File><Text>&lt;Finding id="f248"</Text></MustContain>
      <MustContain><File>docs/plans/active/RM-GOVERNED-PATH/findings.xml</File><Text>&lt;Finding id="f249"</Text></MustContain>
      <MustContain><File>docs/plans/active/RM-GOVERNED-PATH/decisions.xml</File><Text>&lt;Entry id="f246"</Text></MustContain>
      <MustContain><File>docs/plans/active/RM-GOVERNED-PATH/decisions.xml</File><Text>&lt;Entry id="f247"</Text></MustContain>
      <MustContain><File>docs/plans/active/RM-GOVERNED-PATH/decisions.xml</File><Text>&lt;Entry id="f248"</Text></MustContain>
      <MustContain><File>docs/plans/active/RM-GOVERNED-PATH/decisions.xml</File><Text>&lt;Entry id="f249"</Text></MustContain>
      <MustContain><File>docs/plans/active/RM-GOVERNED-PATH/registry.xml</File><Text>&lt;Row name="C-TEACH-DRIVE-BEFORE-APPROVE-2"</Text></MustContain>
      <MustContain><File>docs/plans/active/RM-GOVERNED-PATH/registry.xml</File><Text>&lt;Pays&gt;F243 F246 F247 F248 F249&lt;/Pays&gt;</Text></MustContain>
      <MustNotContain><File>skills/ngrace/ngrace-spec/SKILL.md</File><Text>and (when that file changed) decisions.md is expected</Text></MustNotContain>
      <MustNotContain><File>skills/ngrace/ngrace-plan/SKILL.md</File><Text>and (when that file changed) decisions.md is expected</Text></MustNotContain>
      <MustNotContain><File>skills/ngrace/ngrace-reviewer/SKILL.md</File><Text>and (when that file changed) decisions.md is expected</Text></MustNotContain>
      <MustNotContain><File>plugins/ngrace/skills/ngrace/ngrace-spec/SKILL.md</File><Text>and (when that file changed) decisions.md is expected</Text></MustNotContain>
      <MustNotContain><File>plugins/ngrace/skills/ngrace/ngrace-plan/SKILL.md</File><Text>and (when that file changed) decisions.md is expected</Text></MustNotContain>
      <MustNotContain><File>plugins/ngrace/skills/ngrace/ngrace-reviewer/SKILL.md</File><Text>and (when that file changed) decisions.md is expected</Text></MustNotContain>
      <MustNotContain><File>skills/ngrace/ngrace-plan/SKILL.md</File><Text>isReviewedChangeSpecOrPlanPath</Text></MustNotContain>
      <MustNotContain><File>plugins/ngrace/skills/ngrace/ngrace-plan/SKILL.md</File><Text>isReviewedChangeSpecOrPlanPath</Text></MustNotContain>
      <MustPassCommand><Command>bun ./scripts/check-teaching-surface.ts check</Command></MustPassCommand>
      <MustPassCommand><Command>bun test --timeout=30000 scripts/check-teaching-surface.test.ts</Command></MustPassCommand>
      <MustPassCommand><Command>bun test --timeout=30000 scripts/validate-record-retirement.test.ts</Command></MustPassCommand>
      <MustPassCommand><Command>bun test --timeout=30000 src/test-support/token-accounting.test.ts</Command></MustPassCommand>
      <MustPassCommand><Command>bun run ./scripts/validate-marketplace.ts</Command></MustPassCommand>
      <MustPassCommand><Command>bun ./scripts/validate-record-retirement.ts docs/plans/active/RM-GOVERNED-PATH</Command></MustPassCommand>
      <MustPassCommand><Command>bun ./scripts/validate-citation-anchors.ts</Command></MustPassCommand>
      <MustPassCommand><Command>bun ./scripts/prove-record-preservation.ts</Command></MustPassCommand>
      <MustPassCommand><Command>bun run validate:ci</Command></MustPassCommand>
      <MustVerify><Module>M-SKILLS</Module></MustVerify>
      <MustVerify><Module>M-TEST-SUPPORT</Module></MustVerify>
    </TargetAssertions>
    <DurableScope>
      <GraphAnchors>
        <M-SKILLS />
        <M-TEST-SUPPORT />
      </GraphAnchors>
      <VerificationAnchors>
        <V-M-SKILLS />
        <V-M-TEST-SUPPORT />
      </VerificationAnchors>
    </DurableScope>
    <ObservedWriteScope>
      <File>skills/ngrace/ngrace-plan/SKILL.md</File>
      <File>skills/ngrace/ngrace-spec/SKILL.md</File>
      <File>skills/ngrace/ngrace-execute/SKILL.md</File>
      <File>skills/ngrace/ngrace-reviewer/SKILL.md</File>
      <File>plugins/ngrace/skills/ngrace/ngrace-plan/SKILL.md</File>
      <File>plugins/ngrace/skills/ngrace/ngrace-spec/SKILL.md</File>
      <File>plugins/ngrace/skills/ngrace/ngrace-execute/SKILL.md</File>
      <File>plugins/ngrace/skills/ngrace/ngrace-reviewer/SKILL.md</File>
      <File>scripts/check-teaching-surface.ts</File>
      <File>scripts/check-teaching-surface.test.ts</File>
      <File>scripts/validate-record-retirement.test.ts</File>
      <File>scripts/skill-contracts.test.ts</File>
      <File>src/test-support/token-accounting.test.ts</File>
      <File>README.md</File>
      <File>docs/plans/active/RM-GOVERNED-PATH/findings.xml</File>
      <File>docs/plans/active/RM-GOVERNED-PATH/findings-retired.xml</File>
      <File>docs/plans/active/RM-GOVERNED-PATH/decisions.xml</File>
      <File>docs/plans/active/RM-GOVERNED-PATH/registry.xml</File>
      <File>docs/plans/active/RM-GOVERNED-PATH/registry-retired.xml</File>
    </ObservedWriteScope>
    <ImplementationPlan>
      <T-001>
        <Title>
          Flush F246 F247 F248 F249, mint the C-TEACH-DRIVE-BEFORE-APPROVE-2 row, hold every ceiling,
          and assert the flush-invariant and row relations red-first
        </Title>
        <DependsOn></DependsOn>
        <AcceptanceCriteria>
          <Criterion>
            AC-HEAD-RED (flush-invariant): in its own tool round trip, before any record write,
            append the flush-invariant walk for `f246`, `f247`, `f248` and `f249` to
            `scripts/validate-record-retirement.test.ts` in the C-FLUSH-AND-UNPIN house form: for
            each id, exactly one `Finding` across `findings.xml` and `findings-retired.xml` with no
            fixed expectation of which file holds it, its `status` agreeing with the holding file,
            its `token` agreeing with the id, its single index `Entry` carrying the agreeing `genre`
            and a `layer` agreeing with the holding file, plus the shipped read-back arithmetic
            (`base` equals the file's `newlineCount`, `base + headroom = ceiling`); with an explicit
            generous timeout. The four elements are absent at this state, so the walk reds. Record
            the cursor attempt with outcome fail, signature-kind `test`, signature-key
            `flush-invariant`. Run only this case. Do not flush in this round trip. Failure
            conditions: this round trip also flushes; the walk is bound to a count a duplicate would
            satisfy; the red is recorded against a mutated copy instead of the production record the
            elements are absent from.
          </Criterion>
          <Criterion>
            AC-HEAD-RED (record-row): in its own tool round trip, before the row is minted, append
            the `C-TEACH-DRIVE-BEFORE-APPROVE-2` row walk to
            `scripts/validate-record-retirement.test.ts` in the payment-invariant house form: the
            row exists exactly once across `registry.xml` and `registry-retired.xml` with no fixed
            expectation of which file holds it, its `status` agreeing with the holding file,
            `kind="chartered"`, its `Pays` **contains** `F243 F246 F247 F248 F249` (containment,
            never an exact set), and its `StatusText` names no `F` token outside `Pays`; parsed with
            the shipped parser over both registry layers, with an explicit generous timeout. Add the
            read-only production assertions that `validateStubAndIndex` and
            `proveRecordPreservation`, imported from `scripts/validate-citation-anchors.ts` and
            `scripts/prove-record-preservation.ts`, return zero findings on the post-flush record
            with a generous timeout. The row is absent at this state, so the walk reds. Record the
            cursor attempt with outcome fail, signature-kind `test`, signature-key `record-row`. Do
            not mint in this round trip. Failure conditions: this round trip also mints; the walk
            hardcodes `registry.xml`; a red fixture mutates only `registry.xml`; the `Pays` clause
            pins an exact set.
          </Criterion>
          <Criterion>
            Production then pins then pass, in one wave: read
            `.ngrace/scratch/staged-findings.md` (read, never write), select `f246`, `f247`, `f248`
            and `f249` by anchor id matched as `&lt;a id="…" name="…"&gt;&lt;/a&gt;` forms (never by
            position), and render four `&lt;Finding&gt;` elements through the shipped `renderFinding`
            shape — the function is not exported, so render the same bytes: open tag, `Title`
            carrying the heading line, `Body` carrying the shipped `xmlEscape` of the staged body,
            close tag (88 element lines total; `F246` 32, `F247` 20, `F248` 20, `F249` 16) — appended
            to `docs/plans/active/RM-GOVERNED-PATH/findings.xml` after the last live element and
            before the root close tag; append four `Entry id token genre="finding" layer="live"`
            lines to `docs/plans/active/RM-GOVERNED-PATH/decisions.xml` after the last existing
            entry; mint the live `C-TEACH-DRIVE-BEFORE-APPROVE-2` row into
            `docs/plans/active/RM-GOVERNED-PATH/registry.xml`, appended after the last live row in
            the house shape — an empty `Number`, a `Charter` carrying the name
            `C-TEACH-DRIVE-BEFORE-APPROVE-2` and the searched-before-minting statement over
            `.ngrace/changes/archive/` (98 directories matching `^C-[A-Z0-9-]+$`; the superseded
            predecessor is one of them) and both registry layers (12 live, 54 retired) dated at this
            plan's authoring commit `2aed6ac`, zero exact hits for the name and the per-token counts
            (TEACH 3 directories and 1 row, DRIVE 1 and 0, BEFORE 1 and 0, APPROVE 2 directories and
            2 rows), and no `F` token anywhere, a `Pays` naming exactly
            `F243 F246 F247 F248 F249`, and a `StatusText` naming no `F` token outside `Pays` and
            carrying no `Closed with` sentence; then run
            `bun ./scripts/validate-record-retirement.ts --rewrite-roots
            docs/plans/active/RM-GOVERNED-PATH` — never hand-write a root attribute. Apply no
            transform beyond the shipped escape: the decode round trip returns the staged bytes for
            all four, and no citation is repaired (the buffer already writes the keep-dot form).
            Then the flush-invariant walk greens and the findings root reads
            `&lt;Findings base="7638" headroom="195" ceiling="7833"&gt;`, the index root
            `&lt;RecordIndex base="274" headroom="22" ceiling="296"&gt;`, the registry root
            `&lt;Registry base="13" headroom="13" ceiling="26"&gt;`, rulings unchanged at
            `1733 / 150 / 1883`; the shipped
            `bun ./scripts/validate-record-retirement.ts docs/plans/active/RM-GOVERNED-PATH`,
            `bun ./scripts/validate-citation-anchors.ts` and
            `bun ./scripts/prove-record-preservation.ts` return zero; the row walk greens; and a
            mid-bundle `bun ./scripts/validate-record-retirement.ts --retire
            docs/plans/active/RM-GOVERNED-PATH` on a throwaway whole-repository copy reports
            `moved 0` (nothing is eligible while the row's name is not an archive directory). Extend
            the shipped payer-derivation baseline `bundleMinted` in the same task with the five
            tokens (`F243`, `F246`, `F247`, `F248`, `F249`) to `C-TEACH-DRIVE-BEFORE-APPROVE-2`,
            consulted only for tokens the derivation actually mints. Then record the cursor attempt
            with outcome pass for signature-key `flush-invariant` and pass for signature-key
            `record-row`. Do not run `--retire` against the production record, do not set
            `applied`, do not archive the bundle, and do not touch
            `.ngrace/scratch/staged-findings.md`. Failure conditions: a body normalized beyond the
            shipped escape; a fifth entry flushed; the root written by hand; the row minted without
            the post-mint `--rewrite-roots`; a `Pays` or `StatusText` naming an extra token; the
            `bundleMinted` extension absent so the post-close suite would red; the buffer written or
            pruned.
          </Criterion>
        </AcceptanceCriteria>
        <Verification>
          <Command>bun ./scripts/validate-record-retirement.ts docs/plans/active/RM-GOVERNED-PATH</Command>
          <Command>bun ./scripts/validate-citation-anchors.ts</Command>
          <Command>bun ./scripts/prove-record-preservation.ts</Command>
          <Command>bun test --timeout=30000 scripts/validate-record-retirement.test.ts</Command>
          <Command>bun run ./scripts/validate-marketplace.ts</Command>
          <Command>git diff --exit-code 15ccf9885fbd768cdc467b50c3629cbf7e64173f -- docs/plans/active/RM-GOVERNED-PATH/rulings.xml docs/plans/active/RM-GOVERNED-PATH/rulings-retired.xml docs/plans/active/RM-GOVERNED-PATH/decisions.md docs/plans/active/RM-GOVERNED-PATH/record-inventory.json</Command>
        </Verification>
      </T-001>
      <T-002>
        <Title>
          Add the whole-tree stale scan and the section-scoped needles red-first, then teach the six
          sentences in the four skills and the mirror and re-measure the footprint
        </Title>
        <DependsOn>
          <Task>T-001</Task>
        </DependsOn>
        <AcceptanceCriteria>
          <Criterion>
            AC-HEAD-RED (teaching-needles): in its own tool round trip, add the section-scoped
            needles for the six sentences to the `TAUGHT_RULES` table in
            `scripts/check-teaching-surface.ts` and mirror every entry in
            `scripts/check-teaching-surface.test.ts`'s local table. The sentences are absent at this
            state, so `bun ./scripts/check-teaching-surface.ts check` exits 1, refusing with the
            file, the section and the first missing token. Record the cursor attempt with outcome
            fail, signature-kind `test`, signature-key `teaching-needles`. Do not land any sentence
            in this round trip. Failure conditions: this round trip also edits a skill; a needle
            added to the check but not the suite's local table (the synthetic-root case would then
            red for a different reason); the red recorded against an isolated fixture instead of the
            production trees.
          </Criterion>
          <Criterion>
            AC-HEAD-RED (stale-scan): in its own tool round trip, add the exported
            `checkStaleReviewClaims(root)` to `scripts/check-teaching-surface.ts` — it walks every
            `SKILL.md` under `skills/ngrace/` and `plugins/ngrace/skills/ngrace/` with the
            `skillFilesUnder` walk `checkCliTokenForms` already uses and refuses with a line naming
            the file and the stale phrase when any contains
            `and (when that file changed) decisions.md is expected` — compose it into
            `runTeachingSurfaceCheck` ahead of `checkTaughtRules`, and add its suite cases. The
            eight stale occurrences are present at this state, so
            `bun ./scripts/check-teaching-surface.ts check` exits 1 naming a stale file. Record the
            cursor attempt with outcome fail, signature-kind `test`, signature-key `stale-scan`. Do
            not remove any stale sentence in this round trip. Failure conditions: this round trip
            also edits a skill; the scan bound to the four governed skills rather than the whole
            tree; the scan never reached from `runTeachingSurfaceCheck`.
          </Criterion>
          <Criterion>
            Production then pins then pass: land the six pinned sentences verbatim in the canonical
            skills and copy each edited file onto its packaged mirror so the trees stay
            byte-identical. `ngrace-plan` `must_do` row 19 gains the `F243` sentence ("Drive each
            planned `Verification` command in the tree state its task leaves, never at HEAD, where a
            per-task cumulative `git diff` is trivially green; and scope a per-task byte-invariance
            command so it excludes every earlier task's files, or to the paths the task itself must
            not touch rather than everything-but-mine."), the `F246` sentence ("A baseline
            `MustContain` / `MustNotContain` needle is evaluated by nothing before the plan is
            approved: drive every one with `--assertions baseline` on an approved-shaped copy, and
            bound a `MustNotContain` on a spelling other functions share to a test slice, never a
            whole file."), and the `F248` how-to-drive clause ("Drive `--assertions target` /
            `--assertions final` and `--run-commands` on an approved-shaped copy (`ngrace gate
            approve --artifact plan` on the copy, then `ngrace cursor advance --task T-NNN
            --open-epoch`), never against the production bundle."); row 15's stale first sentence is
            replaced with the `F247` sentence ("At plan-approve the review skips the bundle's own
            `spec.xml`, `plan.xml` and `design-context.xml` by identity; `decisions.md` is reported
            and is expected only when that file changed. Do not add those paths to
            `ObservedWriteScope`.") and row 15's following "structurally redundant" sentence is
            repaired to name `design-context.xml`; `ngrace-plan` `validation` gains the `F248`
            precondition sentence ("`--assertions target` and `--assertions final` require an
            approved plan, and `--run-commands` requires an open epoch on a declared task.").
            `ngrace-spec` `status_rules` and `workflow` step 6 each replace the stale sentence with
            the `F247` sentence, and `docs_and_examples` gains the `F249` sentence ("The mint alone
            is CI-red until the rewrite decides `README.md` and `examples/`: a `ngrace spec new`
            skeleton carries no such decision, so a bare mint is never committed.").
            `ngrace-execute` `assertion_commands` gains the `F248` precondition sentence ("Every
            selected command carries a precondition: `--assertions target` and `--assertions final`
            evaluate only an approved plan (`assertion.change-not-approved` otherwise), and
            `--run-commands` needs an open epoch on a declared task (`cursor advance --change C-ID
            --task T-NNN --open-epoch`) before it can record a command-run."), and its
            `recovery_decision_table` gains the `F246` row, state `approved-baseline-false-at-head`,
            required action "Hard stop before any write: the approved plan's own baseline assertion
            is false at HEAD, a defect no draft-state check could see; run ngrace supersede rather
            than edit the plan.". `ngrace-reviewer` `mechanized_first` replaces the stale sentence
            with the `F247` sentence. **Home already corrected in the -2 spec:** this paragraph in
            the predecessor's draft carried a correction narrative — the old spec labelled the
            reviewer copy `review_judgment` while the measured tree holds it in `mechanized_first`
            (`skills/ngrace/ngrace-reviewer/SKILL.md` line 37, inside the `&lt;mechanized_first&gt;`
            block) — so the draft named `review_judgment` twice; the `-2` spec names
            `mechanized_first` at every site, and those two mentions are gone. The sentence
            therefore lands with no deviation and the `checkTaughtRules` needle for `ngrace-reviewer`
            targets `mechanized_first`. The whole-tree `checkStaleReviewClaims` guard makes the
            removal section-independent, so the stale phrase is gone wherever it sat. Repair
            `scripts/skill-contracts.test.ts`'s four `C-APPROVE-TIME-REVIEW` pins, which the
            teaching falsifies (the `-2` spec's write-scope Constraint names this file a forced
            write whose trigger fired): in each of the four
            `expect(&lt;section&gt;).toContain("review.scope-outside-write-scope")` assertions, replace
            that token with `skips the bundle's own` and add
            `expect(&lt;section&gt;).toContain("design-context.xml")`, and replace each
            `toContain("do not add those paths to ObservedWriteScope")` with the corrected
            sentence's bytes, "Do not add those paths to `ObservedWriteScope`".
            Remove all eight stale occurrences (four per tree). Re-measure
            the skill-footprint pair with `skillTextLines()` and update
            `src/test-support/token-accounting.test.ts` and the `README.md` measurement cell to the
            new true values together, never written toward; the pre-remeasure token-accounting fail
            is not a planned signature. Then `bun ./scripts/check-teaching-surface.ts check` exits
            0, `bun ./scripts/check-teaching-surface.ts check` finds no stale phrase,
            `bun run ./scripts/validate-marketplace.ts` exits 0, and both suites exit 0. Record the
            cursor attempt with outcome pass for signature-key `teaching-needles` and pass for
            signature-key `stale-scan`. Do not add `checkCliTokenForms` content, do not edit the
            other twelve skills, and do not edit any section not named above. Failure conditions: a
            sentence paraphrased rather than landed verbatim; a mirror left stale; a stale
            occurrence left in either tree or in a skill the spec did not name; the footprint
            written toward rather than re-measured.
          </Criterion>
        </AcceptanceCriteria>
        <Verification>
          <Command>bun ./scripts/check-teaching-surface.ts check</Command>
          <Command>bun test --timeout=30000 scripts/check-teaching-surface.test.ts</Command>
          <Command>bun test --timeout=30000 scripts/validate-record-retirement.test.ts</Command>
          <Command>bun test --timeout=30000 src/test-support/token-accounting.test.ts</Command>
          <Command>bun run ./scripts/validate-marketplace.ts</Command>
          <Command>git diff --exit-code 15ccf9885fbd768cdc467b50c3629cbf7e64173f -- src/ scripts/ skills/ plugins/ README.md docs/plans/ ':(exclude)src/test-support/token-accounting.test.ts' ':(exclude)scripts/check-teaching-surface.ts' ':(exclude)scripts/check-teaching-surface.test.ts' ':(exclude)scripts/validate-record-retirement.test.ts' ':(exclude)scripts/skill-contracts.test.ts' ':(exclude)skills/ngrace/ngrace-plan/SKILL.md' ':(exclude)skills/ngrace/ngrace-spec/SKILL.md' ':(exclude)skills/ngrace/ngrace-execute/SKILL.md' ':(exclude)skills/ngrace/ngrace-reviewer/SKILL.md' ':(exclude)plugins/ngrace/skills/ngrace/ngrace-plan/SKILL.md' ':(exclude)plugins/ngrace/skills/ngrace/ngrace-spec/SKILL.md' ':(exclude)plugins/ngrace/skills/ngrace/ngrace-execute/SKILL.md' ':(exclude)plugins/ngrace/skills/ngrace/ngrace-reviewer/SKILL.md' ':(exclude)README.md' ':(exclude)docs/plans/active/RM-GOVERNED-PATH/findings.xml' ':(exclude)docs/plans/active/RM-GOVERNED-PATH/decisions.xml' ':(exclude)docs/plans/active/RM-GOVERNED-PATH/registry.xml'</Command>
        </Verification>
      </T-002>
      <T-003>
        <Title>
          Leaf suites, validate:ci, the predicted plan-approve review, and the faithful post-close
          applied-archive drive over every CloseEvidence command
        </Title>
        <DependsOn>
          <Task>T-002</Task>
        </DependsOn>
        <Satisfies>
          <AC-NO-CURRENT-MODE />
        </Satisfies>
        <AcceptanceCriteria>
          <Criterion>
            Task-observable suites, not manufactured reds. `bun run validate:ci` exits 0;
            `bun run validate:packed` exits 0; `bun run ./scripts/validate-marketplace.ts` exits 0;
            `bun ./scripts/check-teaching-surface.ts check` exits 0; and
            `bun test --timeout=30000 scripts/check-teaching-surface.test.ts`,
            `bun test --timeout=30000 scripts/validate-record-retirement.test.ts` and
            `bun test --timeout=30000 src/test-support/token-accounting.test.ts` exit 0. Do not
            manufacture a red for those commands being green at the prior task's state. This task
            carries the plan's single `Satisfies` (`AC-NO-CURRENT-MODE`): no task `Verification`
            command or `TargetAssertions` entry contains a current-mode lint of this project root,
            checked by hand. Do not claim post-archive lint 0/0 at this task pass.
          </Criterion>
          <Criterion>
            The predicted plan-approve review and the anchors window, on a copy. This bundle creates
            no file under `src/`, so `change.graph-anchors-miss-write-scope` has no window: on a
            copy with this `spec.xml` and `plan.xml` set to `approved`,
            `bun ./src/grace.ts lint --path . --assertions target` and
            `bun ./src/grace.ts review --path . --change C-TEACH-DRIVE-BEFORE-APPROVE-2` report no
            miss-write-scope error, because the only module-ownable path in `ObservedWriteScope` is
            `src/test-support/token-accounting.test.ts`, a `.test.ts` file
            `isModuleOwnableWritePath` does not own. Report exactly what `ngrace review --change`
            prints at plan-approve on the copy — the bundle's own `spec.xml`, `plan.xml` and
            `design-context.xml` are identity-skipped by the shipped predicate, so a
            `review.scope-outside-write-scope` naming any of them is **not** expected and would be a
            finding, and a finding naming any file in `ObservedWriteScope` is a defect to report;
            the expected result is 0 findings, and the approval commit names it.
          </Criterion>
          <Criterion>
            The post-close applied-archive drive, on a faithful whole-repository copy (`F235`),
            before recording pass — `ngrace-plan` row 19 applied to this bundle, and the rule this
            bundle teaches in row 19. Copy the working tree **including `.git`** and untracked files
            (rsync without `--exclude .git`), with `node_modules` copied as a real directory — a
            symlink is untracked to git and yields a spurious
            `review.scope-outside-write-scope node_modules` (measured) — and from inside the copy
            reach the state the ceremony produces: approve the copy's `plan.xml` through
            `bun ./src/grace.ts gate approve --change C-TEACH-DRIVE-BEFORE-APPROVE-2 --artifact plan
            --path .`, then run
            `bun ./src/grace.ts apply --change C-TEACH-DRIVE-BEFORE-APPROVE-2 --force --reason
            "post-close drive"` — the verb archives the bundle and writes `applied` on both
            artifacts (a hand `applied` write is the fallback only if the verb refuses, and the
            refusal reason is named in the report). Then run the shipped `bun
            ./scripts/validate-record-retirement.ts --retire docs/plans/active/RM-GOVERNED-PATH`
            from inside the copy: it reports **`moved 6`** (the five findings `F243`, `F246`,
            `F247`, `F248`, `F249` and the row), the row sits in `registry-retired.xml` with
            `status="retired"`, the five findings sit in `findings-retired.xml` stamped
            `&lt;PaidBy&gt;C-TEACH-DRIVE-BEFORE-APPROVE-2&lt;/PaidBy&gt;`, the five index layers are
            retired in place with the `Entry` lines still present, and the roots re-derive to
            findings `7522 / 311 / 7833`, index `269 / 27 / 296`, registry `12 / 14 / 26`, rulings
            unchanged `1733 / 150 / 1883`; a second `--retire` reports `moved 0`. Then run
            **every** `CloseEvidence` `Command` the spec carries, from inside the copy, exit codes
            read directly:
            `bun test scripts/check-teaching-surface.test.ts` (AC-TAUGHT-SENTENCES,
            AC-STALE-REVIEW-CLAIM-GONE);
            `bun test scripts/validate-record-retirement.test.ts` (AC-FLUSH-LANDED, AC-ROW-PAYS,
            AC-RECORD-VALID, AC-ROOTS-HELD);
            `bun run ./scripts/validate-marketplace.ts` (AC-MIRROR-SYNC);
            `bun test src/test-support/token-accounting.test.ts` (AC-FOOTPRINT-PIN); the four
            `git diff --exit-code 15ccf9885fbd768cdc467b50c3629cbf7e64173f -- …` byte-invariance
            criteria exactly as the spec writes them (AC-NO-SRC, AC-RECORD-PRESERVED,
            AC-NO-ARCHIVE-EDIT, AC-DOCS-EXAMPLES), which is why the copy carries `.git` —
            AC-NO-ARCHIVE-EDIT carries both excludes, this bundle's own `-2` arrival and the
            superseded predecessor's arrival under
            `.ngrace/changes/archive/C-TEACH-DRIVE-BEFORE-APPROVE/` that the base commit predates; and
            `bun ./src/grace.ts lint --path . --fail-on warnings` (AC-CLOSE-LINT). A warning from
            `--fail-on warnings` is a finding for the report, never a reason to drop the command.
            Then run `bun run validate:ci` inside the copy, exit code read directly. Finally drive
            the red directions in the post-close state: the row walk reds on a second row with the
            same name, a `Pays` token outside the five, or a `StatusText` naming an outside token,
            mutating whichever layer holds the row; the flush-invariant walk reds on a duplicated
            carrier or a flipped index layer. The production record and the production bundle are
            never mutated by this drive. Leave the copy's path in place and name it in the report.
            Record the cursor attempt with outcome pass, signature-kind `test`, signature-key
            `post-close-drive`. Failure conditions: the drive copies only the record directory or
            omits `.git` (the baseline commit cannot resolve and the byte-invariance criteria cannot
            run); the drive stops at the in-tree suites and skips the `git diff` criteria and
            `--fail-on warnings`; a warning is dropped rather than reported; the production record
            is mutated.
          </Criterion>
          <Criterion>
            The close boundary. This task ends at the live row, `--rewrite-roots` applied, the
            shipped validators at zero, and the taught sentences in both trees. The `--retire`
            (`moved 6`), the two gate-applied Decisions, the `apply` verb, the archive move, and
            every `CloseEvidence` evaluation on the production record are the authority's close
            acts; the executor performs no gate, no status write, no archive move and no production
            `--retire`. Findings raised while this bundle is open go to
            `.ngrace/scratch/staged-findings.md` under the next staged number (`D30`, scoped to the
            record-writing window).
          </Criterion>
        </AcceptanceCriteria>
        <Verification>
          <Command>bun ./scripts/check-teaching-surface.ts check</Command>
          <Command>bun test --timeout=30000 scripts/check-teaching-surface.test.ts</Command>
          <Command>bun test --timeout=30000 scripts/validate-record-retirement.test.ts</Command>
          <Command>bun test --timeout=30000 src/test-support/token-accounting.test.ts</Command>
          <Command>bun run ./scripts/validate-marketplace.ts</Command>
          <Command>bun ./scripts/validate-record-retirement.ts docs/plans/active/RM-GOVERNED-PATH</Command>
          <Command>bun ./scripts/validate-citation-anchors.ts</Command>
          <Command>bun ./scripts/prove-record-preservation.ts</Command>
          <Command>bun run validate:packed</Command>
          <Command>bun run validate:ci</Command>
          <Command>git diff --exit-code 15ccf9885fbd768cdc467b50c3629cbf7e64173f -- examples/ CLAUDE.md CONTRIBUTING.md AGENTS.md docs/plans/README.md docs/plans/archive/ package.json bun.lock CHANGELOG.md .ngrace-lint.json</Command>
        </Verification>
      </T-003>
    </ImplementationPlan>
  </C-TEACH-DRIVE-BEFORE-APPROVE-2>
</NgraceChangePlan>
