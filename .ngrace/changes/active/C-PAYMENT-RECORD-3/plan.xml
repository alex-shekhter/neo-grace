<NgraceChangePlan graceVersion="1.0" status="approved">
  <C-PAYMENT-RECORD-3>
    <IntentSummary>
      Record the eight proven historical payments and open the findings window, then
      flush the one staged entry the window cannot otherwise hold. Eight post-hoc
      historical rows land in live registry.xml — C-CONTRACT-DEBT paying F20, F29,
      F29.1; C-RECORDED-DEBT paying F76, F71, F71.1, F77; C-SUBSTANTIATION-HONESTY
      paying F9.10, F31, F32; C-REPORT-HONESTY paying F11, F11.2, F14, F15, F16;
      C-FLAG-HONESTY paying F18; C-ESCALATION-HONESTY paying F22;
      C-RECOVER-FOLDABLE paying F13; C-EXECUTION-CONTRACT paying F12 — 19 live
      findings, 699 live element lines under the shipped counting rule
      (newlineCount of the element span plus one per element). Each payment is
      witnessed by a present-tree symbol, refuse path, or file:line checkable
      without judgement, and each witness was re-verified against the tree while
      this plan was written. Then a task runs one
      bun ./scripts/validate-record-retirement.ts --retire against the production
      record in the same execution wave, moving the eight rows and 19 findings to
      their retired siblings, stamping PaidBy, and flipping the 19 index Entry
      layer attributes. The move is this bundle's act, not a follow-on, because the
      flush deliverable needs the room the move opens and the close ceremony's
      post-archive move is structurally too late. The intermediate state between
      insert and move is deliberately red — validation reports
      registry-eligible-still-live once per row (8 at plan time) and
      finding-eligible-still-live once per paid token (19), the D28.1 enforcement
      saying the move is due — so the inserts and the move belong to one task and
      no task boundary and no commit may fall inside that window: it is a declared
      working-tree state, never a commit validate:ci can see. Then the bundle
      flushes exactly the one staged entry F204 into findings.xml and
      decisions.xml. The flush set is selected by anchor id, never by position, and
      is closed by id: an entry is flushed if and only if its anchor id is f204,
      and nothing else in the buffer is flushed — including entries that do not
      exist yet. The buffer holds two entries at plan time (f204, which this bundle
      flushes, and f205, staged during the spec review, which stays staged); any
      entry staged during this bundle's window also stays staged, and no criterion
      depends on the buffer's total entry count. The written findings root base
      comes from reading newlineCount back after the write, never from a projection
      (the rule F204 itself records); headroom is recomputed as ceiling minus the
      read-back base; a read-back base above the persisted ceiling stops the task
      and reports and never raises the ceiling. Do not prune or write the staged
      buffer: pruning the flushed entry is post-merge hygiene owned by the
      authority. DurableScope is &lt;None /&gt;: no graph module is written; do not
      mint a synthetic module. ObservedWriteScope is the seven RM-GOVERNED-PATH
      record files:
      docs/plans/active/RM-GOVERNED-PATH/registry.xml, registry-retired.xml,
      findings.xml, findings-retired.xml, decisions.xml, rulings.xml, and
      rulings-retired.xml. Do not add spec.xml, plan.xml, run.xml, run-ledger.xml,
      decisions.md, src/, scripts/, CLAUDE.md, docs/plans/README.md,
      skills/ngrace/*, package.json, or anything under docs/plans/archive/ or
      .ngrace/changes/archive/. D30 is scoped, not skirted: the record is this
      bundle's deliverable; the freeze holds for the execution window of every
      record-writing task — no other dispatch touches the record while one is in
      flight — and findings raised while this bundle is open go to
      .ngrace/scratch/staged-findings.md (F205 onward), never into the record under
      edit. Every figure below was measured 2026-09-08 at b62698eb75aeafd08325c3f08fe0f0896820a694
      (branch c-payment-record-3; the tree is byte-identical to the spec's base
      49b4ebb7c6627601ca77ca384f7b6e205d1f65ed outside
      .ngrace/changes/active/C-PAYMENT-RECORD-3/), with the eligibility and window
      arithmetic re-probed against a throwaway copy of the record outside the
      working tree: the intermediate state exits 1 with 8 + 19 eligibility
      findings; --retire reports moved = 27; the post-move roots are findings
      7109/737/7846, registry 12/14/26, rulings 1697/186/1883 byte-identical,
      index 339/2/341; the flush lands a 30-line element (27 buffer prose lines +
      3 wrapper lines, a projection used only to confirm the ceiling is not
      breached) with the written base read back at 7139 and headroom 707, and
      decisions base 340 headroom 1; a second --retire reports moved = 0 and
      leaves all seven record files byte-identical; and a fixture row whose name
      matches no archive directory pays nothing, which is what makes each
      historical row the only route. Sequence is two tasks in one execution wave:
      T-001 inserts the eight historical rows into live registry.xml and invokes
      one bun ./scripts/validate-record-retirement.ts --retire against the
      production record in the same task; T-002 flushes F204. This bundle mints no
      charter row for itself, so at close the bundle's name has no registry row
      and the close's own --retire is a byte-level no-op (moved = 0, early return
      before any write); every criterion here is invariant under that close, and
      the plan relies on that only by saying so here. Ceremony tier T1. Current
      -mode lint of this project root is not a task Verification command and is
      not a TargetAssertions MustPassCommand; the post-archive close criterion
      AC-CLOSE-LINT is the only place it appears. The executor emits its own
      execution-time cursor events for its tasks (opened, progress, attempt,
      command-run) and does not omit them. Do not hand-write any status. Do not
      perform any close act: the terminal event and the cursor fold are close acts
      owned by the authority, together with the --assertions final --run-commands
      gate, both gate verdicts, gate apply, the status="applied" write, gate
      archive, the git mv to .ngrace/changes/archive/, and the close's own record
      retirement move; no task performs any of them and no sentence in this plan
      implies execution does.
    </IntentSummary>
    <BaselineAssertions>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/registry.xml</Value>
      </MustExist>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/registry-retired.xml</Value>
      </MustExist>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/findings.xml</Value>
      </MustExist>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/findings-retired.xml</Value>
      </MustExist>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/rulings.xml</Value>
      </MustExist>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/rulings-retired.xml</Value>
      </MustExist>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/decisions.xml</Value>
      </MustExist>
      <MustPassCommand>
        <Command>bun test scripts/validate-record-retirement.test.ts</Command>
      </MustPassCommand>
      <MustPassCommand>
        <Command>bun ./scripts/validate-record-retirement.ts</Command>
      </MustPassCommand>
      <MustPassCommand>
        <Command>bun ./scripts/prove-record-preservation.ts</Command>
      </MustPassCommand>
      <MustPassCommand>
        <Command>bun ./scripts/validate-citation-anchors.ts</Command>
      </MustPassCommand>
    </BaselineAssertions>
    <TargetAssertions>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/registry.xml</Value>
      </MustExist>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/registry-retired.xml</Value>
      </MustExist>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/findings.xml</Value>
      </MustExist>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/findings-retired.xml</Value>
      </MustExist>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/rulings.xml</Value>
      </MustExist>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/rulings-retired.xml</Value>
      </MustExist>
      <MustExist>
        <Value>docs/plans/active/RM-GOVERNED-PATH/decisions.xml</Value>
      </MustExist>
      <MustPassCommand>
        <Command>bun test scripts/validate-record-retirement.test.ts</Command>
      </MustPassCommand>
      <MustPassCommand>
        <Command>bun ./scripts/validate-record-retirement.ts</Command>
      </MustPassCommand>
      <MustPassCommand>
        <Command>bun ./scripts/prove-record-preservation.ts</Command>
      </MustPassCommand>
      <MustPassCommand>
        <Command>bun ./scripts/validate-citation-anchors.ts</Command>
      </MustPassCommand>
    </TargetAssertions>
    <DurableScope>
      <None />
    </DurableScope>
    <ObservedWriteScope>
      <File>docs/plans/active/RM-GOVERNED-PATH/registry.xml</File>
      <File>docs/plans/active/RM-GOVERNED-PATH/registry-retired.xml</File>
      <File>docs/plans/active/RM-GOVERNED-PATH/findings.xml</File>
      <File>docs/plans/active/RM-GOVERNED-PATH/findings-retired.xml</File>
      <File>docs/plans/active/RM-GOVERNED-PATH/decisions.xml</File>
      <File>docs/plans/active/RM-GOVERNED-PATH/rulings.xml</File>
      <File>docs/plans/active/RM-GOVERNED-PATH/rulings-retired.xml</File>
    </ObservedWriteScope>
    <ImplementationPlan>
      <T-001>
        <Title>
          Insert eight historical rows and --retire in the same wave to open the findings window
        </Title>
        <DependsOn></DependsOn>
        <AcceptanceCriteria>
          <Criterion>
            Insert eight historical rows into live production registry.xml, then invoke exactly
            one bun ./scripts/validate-record-retirement.ts --retire against the production record
            in this same task, before any commit validate:ci can see. Do not separate the insert
            and the --retire into two tasks or two commits: after the insert each row's name
            equals an archive directory, so all eight rows are immediately registry-eligible, and
            their Pays fields make all 19 findings immediately finding-eligible; a commit or a
            task boundary holding that tree fails registry-eligible-still-live once per row (8 at
            plan time) and finding-eligible-still-live once per paid token (19), and validation
            exits 1. That intermediate state is the D28.1 enforcement saying the move is due: it
            is a declared, verified working-tree state, never a commit validate:ci can see, and
            no task ends inside it. The intermediate occupancy is the pre-insert live row count
            plus eight, within the persisted registry ceiling 26 (12 + 8 = 20 at plan time;
            re-read the pre-insert count from the file at execution rather than trusting the plan
            -time figure). Before inserting, confirm each payment's witness named below still
            holds against the tree; if a witness no longer holds, stop and report — never edit a
            finding to fit the tree (D27), and never select a payment because it frees lines. Do
            not hand-edit &lt;PaidBy&gt;. Do not write the rows directly into
            registry-retired.xml. Do not raise any persisted ceiling. Do not flush
            .ngrace/scratch/staged-findings.md. Do not mint a chartered row for
            C-PAYMENT-RECORD-3.
          </Criterion>
          <Criterion>
            Row contents, eight rows, kind="historical", status="live" on insert (--retire
            rewrites status to "retired"). Number is empty. Charter describes the archived
            bundle's own subject. StatusText begins with **Delivered.**, says the row is a
            historical payment record written by C-PAYMENT-RECORD-3 because the archived spec
            never used the payer phrase and no registry row existed at close, then names the
            present-tree witness. Row 1: name="C-CONTRACT-DEBT", Pays carries F20, F29, F29.1;
            Charter describes closing the contract-debt items: the single exported cloneXmlNode
            and the additive UTF-8 byte skill footprint beside the frozen line total. Witness:
            exactly one definition of cloneXmlNode survives, exported at src/artifact/xml.ts:239,
            and the sites that held private copies import it (src/grace-cursor.ts:126,
            src/artifact/run-membership.ts:22, src/gates/ledger.ts:65); the token-accounting
            helper ships UTF-8 byte accounting beside the frozen line total (countUtf8Bytes,
            perSkillBytes, totalBytes at src/test-support/token-accounting.ts:70-115, documented
            as matching wc -c). Row 2: name="C-RECORDED-DEBT", Pays carries F76, F71, F71.1, F77;
            Charter describes paying four recorded P1 debts: undeclared-flag refusal with usage,
            getModulePath reduced to the graph path, and the as-state class taxonomy. Witness:
            the real CLI refuses an undeclared flag — bun ./src/grace.ts lint --path .
            --totallyBogusFlag hello exits 1 with invalid-arguments naming the flag and printing
            usage, on a throwaway project at this commit; getModulePath returns
            moduleRecord.graph.path and nothing else (src/query/core.ts:111-113), covering all
            four display strings F71.1 counted; the as-state class taxonomy is a single exported
            const with an exhaustiveness guard that throws when a class is unassigned
            (AS_STATE_PURITY_CLASSES at src/lint/types.ts:36, iterated and enforced at
            src/lint/core.ts:206-212). Row 3: name="C-SUBSTANTIATION-HONESTY", Pays carries F9.10,
            F31, F32; Charter describes closing the attempt-pair review surface: the
            identical-tree raise rule and the active/-or-archive plan resolution. Witness: the
            raise condition is the identical-tree rule — raise only when the non-.ngrace/ trees
            are identical across a fail→pass pair (src/review/core.ts:1405-1411, with the comment
            at :1589 naming this bundle and F31); the attempt-pair audit resolves the plan under
            active/ or archive/ (the "no plan found for … under active/ or archive/" resolution
            and planLocation "archive" in src/review/core.ts's pair loader). Row 4:
            name="C-REPORT-HONESTY", Pays carries F11, F11.2, F14, F15, F16; Charter describes
            making the tool's own report surfaces agree about facts on disk: one shared
            loose-event membership for status and the archive gate, the archive-identity reading
            for MustExist, and lifecycle-path exclusion from the scope audit. Witness:
            isCliLifecyclePath (src/review/core.ts:1013) skips the CLI's own lifecycle writes in
            the scope audit at :1063 and :1127, covering run.xml, run-ledger.xml and run/**;
            both status and the archive gate take loose-event membership from the one shared
            module (src/gates/core.ts:33 and :480, src/grace-status.ts:29 and :267, whose comment
            says "membership is listLooseEvents (shared with archive gate)"); the MustExist
            check reuses expandScopePathsForArchiveIdentity for the archive-identity reading
            (src/review/core.ts:504 and :523). Row 5: name="C-FLAG-HONESTY", Pays carries F18;
            Charter describes refusing the space-separated true/false form for boolean flags with
            a loud error naming the two working forms, fixed as a class across the CLI. Witness:
            the real CLI refuses the space form — bun ./src/grace.ts gate archive --change C-X
            --record false exits 1 with "Boolean flag `--record` does not accept a
            space-separated value" naming both working forms, before any ledger write, on a
            throwaway project at this commit; the check lives at src/query/command.ts:84-116 and
            the gate-side comment at src/gates/command.ts:214 names F18. Row 6:
            name="C-ESCALATION-HONESTY", Pays carries F22; Charter describes the fix-budget /
            resume seam: the signature-budget thrash rule, the distinct-signature backstop, and
            the reason-required escalation-clearing resume. Witness: an escalation-clearing
            resume requires a recorded --reason and refuses before any write
            (src/grace-cursor.ts:1048-1068). Row 7: name="C-RECOVER-FOLDABLE", Pays carries F13;
            Charter describes making ngrace cursor recover --fix produce a ledger that can
            actually fold. Witness: writeCoveringOpened allocates headroom past the covering
            event's own id — OPEN_EPOCH_DEFAULT_HEADROOM = 98 at src/grace-cursor.ts:742,
            applied at :756 as to = Math.max(coveringRequirementTo, openedId +
            OPEN_EPOCH_DEFAULT_HEADROOM). Row 8: name="C-EXECUTION-CONTRACT", Pays carries F12;
            Charter describes documenting the cursor event protocol the CLI already enforces
            inside skills/ngrace/ngrace-execute/SKILL.md and its packaged mirror. Witness: the
            terminal kind entry at skills/ngrace/ngrace-execute/SKILL.md:84-87 says to emit
            terminal before ngrace cursor fold and that fold requires a terminal event inside
            the covering allocation range, and the packaged mirror carries the same text (grep
            -c terminal identical in both copies).
          </Criterion>
          <Criterion>
            Registry root after the insert, and the move. Write the rows, then read the live
            row count back from the written file — the registry's base unit is live rows (the
            shipped liveRowCount), not newlineCount — and set the root: base becomes the
            pre-insert live row count plus eight (12 + 8 = 20 at plan time), headroom becomes
            ceiling minus base (26 - 20 = 6), ceiling byte-identical at 26 and never raised.
            Then one --retire pass: retireRecord stamps &lt;PaidBy&gt; onto the 19 eligible
            live findings, moves those findings to findings-retired.xml with
            status="retired", moves the eight archive-named rows to registry-retired.xml, and
            flips the 19 matching Entry layer attributes in decisions.xml from live to retired,
            in one invocation, reporting moved = 27 (8 rows + 19 findings + 0 decisions). After
            --retire: the eight names exist as kind="historical" rows in
            registry-retired.xml and not in live registry.xml; live findings.xml has no Finding
            whose token is any of the 19 paid tokens F20, F29, F29.1, F76, F71, F71.1, F77,
            F9.10, F31, F32, F11, F11.2, F14, F15, F16, F18, F22, F13, F12; those tokens exist
            as status="retired" in findings-retired.xml with a &lt;PaidBy&gt; child naming the
            corresponding archive event; the decisions.xml Entry elements for the 19 tokens
            carry layer="retired"; live findings.xml root has base="7109" headroom="737"
            ceiling="7846" (699 lines freed under the shipped element line-count rule — an
            element's line count is the newlineCount of its span plus one: 127 from
            C-CONTRACT-DEBT (27 + 44 + 56), 141 from C-RECORDED-DEBT (44 + 30 + 26 + 41), 136
            from C-SUBSTANTIATION-HONESTY (71 + 36 + 29), 156 from C-REPORT-HONESTY (23 + 31 +
            24 + 37 + 41), 52 from C-FLAG-HONESTY, 31 from C-ESCALATION-HONESTY, 28 from
            C-RECOVER-FOLDABLE, 28 from C-EXECUTION-CONTRACT; ceiling untouched under
            persistSnapshot's pre-move base clamp min(7846, 7808 + 245) = 7846); registry.xml
            base returns to the pre-insert live row count (12 at plan time), headroom 14,
            ceiling 26; rulings.xml and rulings-retired.xml are rewritten byte-identically at
            1697/186/1883 (no decision is eligible); decisions.xml root stays 339/2/341 (the 19
            layer flips rewrite attributes in place and remove no line);
            bun ./scripts/validate-record-retirement.ts exits 0. If any written root would
            breach a persisted ceiling, or --retire reports a moved count other than 27, stop
            and report.
          </Criterion>
          <Criterion>
            This task's Verification commands and the plan's TargetAssertions contain no
            current-mode lint of this project root, checked by the rule in Constraints, by hand
            (F171): no command text contains --assertions current, and none invokes ngrace lint
            or bun ./src/grace.ts lint of this root while omitting --assertions. The executor
            emits its own execution-time cursor events for this task (opened, progress,
            attempt, command-run) and does not omit them; the terminal event and cursor fold
            are close acts owned by the authority, not this task's.
          </Criterion>
        </AcceptanceCriteria>
        <Verification>
          <Command>bun test scripts/validate-record-retirement.test.ts</Command>
          <Command>bun ./scripts/validate-record-retirement.ts</Command>
          <Command>bun ./scripts/prove-record-preservation.ts</Command>
          <Command>bun ./scripts/validate-citation-anchors.ts</Command>
          <Command>git diff --exit-code 49b4ebb7c6627601ca77ca384f7b6e205d1f65ed -- src/ scripts/</Command>
          <Command>git diff --exit-code 49b4ebb7c6627601ca77ca384f7b6e205d1f65ed -- README.md examples/ CLAUDE.md docs/plans/README.md skills/ngrace/ plugins/ngrace/skills/ngrace/ package.json bun.lock</Command>
          <Command>git diff --exit-code 49b4ebb7c6627601ca77ca384f7b6e205d1f65ed -- docs/plans/archive/ .ngrace/changes/archive/ ':(exclude).ngrace/changes/archive/C-PAYMENT-RECORD-3'</Command>
          <Command>git diff --exit-code 49b4ebb7c6627601ca77ca384f7b6e205d1f65ed -- docs/plans/active/RM-GOVERNED-PATH/rulings.xml docs/plans/active/RM-GOVERNED-PATH/rulings-retired.xml</Command>
        </Verification>
        <Satisfies>
          <AC-NO-CURRENT-MODE />
        </Satisfies>
      </T-001>
      <T-002>
        <Title>
          Flush the staged entry F204 into findings.xml and decisions.xml
        </Title>
        <DependsOn>
          <Task>T-001</Task>
        </DependsOn>
        <AcceptanceCriteria>
          <Criterion>
            Flush mechanics. Flush exactly the one staged entry F204, selected by anchor id
            (f204, matched as &lt;a id="f204" name="f204"&gt;&lt;/a&gt;) and never by position.
            The flush set is closed by anchor id: an entry is flushed if and only if its anchor
            id is f204, and nothing else in the buffer may be flushed — including entries that
            do not exist yet. The buffer holds two entries at plan time (f204 and f205, staged
            during this bundle's spec review) and may grow again while this bundle is open
            (findings raised under D30 are staged there, F205 onward); f205 and every later
            entry stays staged. No criterion in this plan depends on the buffer's total entry
            count. Do not edit or prune the staged buffer: pruning the flushed entry is
            post-merge hygiene owned by the authority. Cut the entry at the anchor that starts
            the next entry, dropping all trailing blank lines; the heading line becomes the
            Title (the `### ` prefix rides along, as the landed F196-F203 elements show); the
            body is copied verbatim; the element adds three wrapper lines — the &lt;Finding&gt;
            open tag, &lt;Body&gt; on its own line, and the close tag — with &lt;/Body&gt;
            inline on the last prose line. The projected landed element at plan time is 30
            lines (27 buffer prose lines + 3 wrapper lines): a projection used only to confirm
            the findings ceiling is not breached, never the written base. Append the
            &lt;Finding id="f204" token="F204" status="live"&gt; element after the last live
            Finding in findings.xml (f203 at plan time) and a matching
            &lt;Entry id="f204" token="F204" genre="finding" layer="live" /&gt; to
            decisions.xml after the last finding Entry. After the write, no duplicate f204 id
            exists in either file.
          </Criterion>
          <Criterion>
            Root attribute derivation, read-back, and the no-op probe. Write findings.xml
            first, then read newlineCount back for the root base attribute (a written root
            attribute is never derived from a projection; the projection is used only to check
            the ceiling is not breached): the projected base is 7139 (7109 + 30) and projected
            headroom 707, but the written base is whatever newlineCount reads back and
            headroom is recomputed as ceiling (7846) minus the read-back base; the ceiling
            remains byte-identical at 7846 and is never raised. A read-back base above the
            persisted ceiling stops the task and reports; it does not raise the ceiling. Then
            append the Entry to decisions.xml, reading newlineCount back after the write for
            its root base the same way: projected base 340 (339 + 1), projected headroom 1
            against ceiling 341, ceiling untouched. After the flush, a second
            bun ./scripts/validate-record-retirement.ts --retire is a byte-level no-op (moved
            = 0, early return before any write), leaving all seven record files
            byte-identical. Validation exits 0; preservation and citation validators exit 0;
            tests pass.
          </Criterion>
          <Criterion>
            This task's Verification commands and the plan's TargetAssertions contain no
            current-mode lint of this project root, checked by the rule in Constraints, by
            hand (F171): no command text contains --assertions current, and none invokes
            ngrace lint or bun ./src/grace.ts lint of this root while omitting --assertions.
            The selected --assertions target --run-commands command is the post-write evidence
            command and is meant to run during execution with an open cursor on a declared
            task; its exit 1 today (pre-approval, no open cursor — it reports
            assertion.change-not-approved) is not a defect (F184). Do not put bun
            ./src/grace.ts lint --path . --fail-on warnings here (AC-CLOSE-LINT is
            post-archive). The executor emits its own execution-time cursor events for this
            task (opened, progress, attempt, command-run) and does not omit them; the terminal
            event and cursor fold are close acts owned by the authority, together with the
            --assertions final --run-commands gate, both gate verdicts, gate apply, the
            status="applied" write, gate archive, the git mv, and the close's own record
            retirement move (a byte-level no-op here — this bundle mints no charter row, so
            its name has no row for the close to move); no task performs any of them.
          </Criterion>
        </AcceptanceCriteria>
        <Verification>
          <Command>bun test scripts/validate-record-retirement.test.ts</Command>
          <Command>bun ./scripts/validate-record-retirement.ts</Command>
          <Command>bun ./scripts/prove-record-preservation.ts</Command>
          <Command>bun ./scripts/validate-citation-anchors.ts</Command>
          <Command>bun ./src/grace.ts lint --path . --change C-PAYMENT-RECORD-3 --assertions target --run-commands</Command>
          <Command>git diff --exit-code 49b4ebb7c6627601ca77ca384f7b6e205d1f65ed -- src/ scripts/</Command>
          <Command>git diff --exit-code 49b4ebb7c6627601ca77ca384f7b6e205d1f65ed -- README.md examples/ CLAUDE.md docs/plans/README.md skills/ngrace/ plugins/ngrace/skills/ngrace/ package.json bun.lock</Command>
          <Command>git diff --exit-code 49b4ebb7c6627601ca77ca384f7b6e205d1f65ed -- docs/plans/archive/ .ngrace/changes/archive/ ':(exclude).ngrace/changes/archive/C-PAYMENT-RECORD-3'</Command>
          <Command>git diff --exit-code 49b4ebb7c6627601ca77ca384f7b6e205d1f65ed -- docs/plans/active/RM-GOVERNED-PATH/rulings.xml docs/plans/active/RM-GOVERNED-PATH/rulings-retired.xml</Command>
        </Verification>
        <Satisfies>
          <AC-NO-CURRENT-MODE />
        </Satisfies>
      </T-002>
    </ImplementationPlan>
  </C-PAYMENT-RECORD-3>
</NgraceChangePlan>