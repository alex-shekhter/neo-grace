<NgraceChangeSpec graceVersion="1.0" status="approved">
  <C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5>
    <Summary>
      **Bundle 1 of five ordered successors of the approved `C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1`, minted replacement-first by `ngrace spec new --supersedes C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1`.** The predecessor's plan stage exhausted its three attempts; its approved behavior contract is not weakened but partitioned into the smallest coherent ordered bundles, each intended to finish a plan/rehearsal within three attempts. This bundle — **membership disappearance and consequences** — owns exactly the four ordinary criteria `AC-MEMBER-EARLY-VANISH`, `AC-MEMBER-LATE-VANISH`, `AC-MEMBER-ORPHANS-TOLERANT`, and `AC-MEMBER-FOLD-CONSEQUENCES`, and pays exactly `F295 F295.1`. Its production surface is `src/artifact/run-membership.ts` (`LINKS: M-ARTIFACT-TYPES`) alone: the existing `ngrace cursor show` / `status` / `gate archive` / `cursor fold` consumers exercise the membership repair without a `src/grace-cursor.ts` production edit, and that was confirmed empirically before this draft was written. Ceremony tier **T1** (module change in one package). The affected module is exactly `M-ARTIFACT-TYPES`; the later plan's `DurableScope` carries `V-M-ARTIFACT-TYPES`. The four later bundles are reserved as fresh semantic slugs without hash suffixes: **`C-LOOSE-MALFORMED-FOLD`** (`AC-MEMBER-MALFORMED-AND-UNREADABLE`, pays `F315`), **`C-FOLD-RETRY-CORE`** (`AC-FOLD-RETRY-IDEMPOTENT`, `AC-FOLD-CONFLICT-REFUSAL`, `AC-POST-LEDGER-BOUNDARY-PRESERVED`, pays `F313`), **`C-FOLD-MIXED-RECOVERY`** (`AC-FOLD-MIXED-RECOVERY`, `AC-MIXED-RECOVERY-VALIDATE-FIRST`, `AC-RECOVERY-HOOKS-CORRECT`, pays no finding and therefore invents no payer/staged/record write), and **`C-DISCARD-PREFLIGHT`** (`AC-DISCARD-NOT-WRITTEN-ON-PREWRITE-REFUSE`, `AC-F158-DEFECT-CLOSED`, `AC-DISCARD-PREFLIGHT-NO-ALLOC`, pays `F292 F292.1 F158`). Each is authored only after its predecessor is closed and committed; the already-reserved B3 slug `C-SUPERSEDE-INTEGRATION-CLOSE` follows all five. The payment multiset across bundles 1–5 is exactly `F292 F295 F292.1 F295.1 F313 F315 F158`, each token once. Base commit `e0a9868916412977fd3b7aa7615b67615fa2a93b`. The `mint-search` line printed at mint is carried verbatim as governance data: `mint-search: 1 active, 0 archive prior bundle(s) share slug FOLD-MEMBERSHIP-RECOVERY`.
    </Summary>
    <Goals>
      <Goal>
        **Disappearing loose-event membership is tolerated in both measured windows, and the orphan inventory shares the tolerance.** In `src/artifact/run-membership.ts`, `listLooseEvents` omits an entry whose backing file vanished after `readdirSync` enumeration — both before the callee existence check (the typed `xml.missing-file` state) and between that check and the read (a thrown `ENOENT`) — without fabricating a filename-derived event and without letting an uncaught `ENOENT` escape. `listRunOrphans` tolerates the late window on a well-named file and keeps the two orphan classes (`event-filename`, `invalid-id`) with their reasons and ordering. Only disappearance is tolerated: `EISDIR` and any read failure other than `ENOENT`/`xml.missing-file` still propagate, and the shared reader `src/artifact/xml.ts` is not widened.
      </Goal>
      <Goal>
        **The real consuming CLI paths keep their exact present and removed diagnostics.** The `ngrace cursor show`, `status`, and `gate archive` consumers are driven for a bundle whose `run/` holds an `invalid-id` orphan (`2-T-001-progress.xml` containing `id="x"`): present — `status` reports `orphans=1` and the gate reports `1 orphan(s): 2-T-001-progress.xml (invalid-id)`; removed before enumeration — `status` prints no `orphans=` for the change and the gate reports `no-open-epoch … run/ empty`; intercepted in the true late window — both exit 0.
      </Goal>
      <Goal>
        **The three fold consequences remain distinct and are driven through the real CLI.** (a) an interior hole refuses; (b) a trailing event that disappears in the true late window folds the surviving events with exit 0 and no fabricated ghost; (c) a vanished `opened` folds with exit 0 and only the legitimate synthesized covering `opened`, preserving `RM-GOVERNED-PATH D8.2`'s single-controller auto-open. The criterion forbids treating every vanished entry as an automatic refusal and forbids manufacturing a ghost event to make the loss observable.
      </Goal>
      <Goal>
        **The payment and the write guard are executable.** The bundle pays exactly `F295 F295.1`; its retirement-payer fixture is forced scope and the close includes the narrow two-token payer assertion. An executable close-time discriminator covers both tracked and untracked changes against the named base commit, with a closed allowed-write set, and is proved by a planted forbidden write on a disposable fixture. The ignored `.ngrace/scratch/staged-findings.md` input is a **transient pre-flush mutation** declared by its exact two owner lines outside the guard's observable population.
      </Goal>
    </Goals>
    <Constraints>
      <Constraint>
        **Tier and base.** Ceremony tier **T1** (single-package module change). Roadmap `RM-GOVERNED-PATH`. Base commit `e0a9868916412977fd3b7aa7615b67615fa2a93b` — the committed approval of the predecessor spec `C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1`. The full hash is the baseline for every byte-invariance relation. Every measurement in this artifact was re-driven against that base on disposable clones; counts expire when a bundle archives, so the close re-measures.
      </Constraint>
      <Constraint>
        **The predecessor supersede transition, measured at this base.** The later governed `ngrace supersede` restamps and moves the predecessor's physical artifacts. **Physical/tool write inventory:** three active identities become three archive identities — `.ngrace/changes/active/C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1/spec.xml`, `.ngrace/changes/active/C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1/plan.xml`, `.ngrace/changes/active/C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1/run-ledger.xml` → `.ngrace/changes/archive/C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1/{spec.xml,plan.xml,run-ledger.xml}`; the verb restamps `spec.xml` and the draft `plan.xml` with root `status="superseded"` and a `&lt;Replacement&gt;` child naming this successor and moves `run-ledger.xml` byte-identically. **Git-observable population relative to base `e0a9868`:** the base commit tracks exactly two predecessor files (`.ngrace/changes/active/C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1/spec.xml` and `.ngrace/changes/active/C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1/run-ledger.xml`) and **no** predecessor plan, so the git-observable set is exactly **two active deletions** plus **three archive arrivals**; the active `plan.xml` is an untracked worktree overlay and its disappearance is **not** a git deletion. There is no predecessor `run/` and no `run.xml`. The later plan's `ObservedWriteScope` lists all six exact transition paths because the transition is a deliverable act, and the activated guard must permit exactly those six plus this successor's own active/archive lifecycle identity.
      </Constraint>
      <Constraint>
        **Modules, kept in agreement.** `AffectedAreas` names exactly `M-ARTIFACT-TYPES`. The `Ownership of forced files` constraint pairs each forced path with its owner from that same set; the two are kept in agreement (F136.1), and the later plan's `DurableScope` carries the exact verification anchor `V-M-ARTIFACT-TYPES`. While this artifact is **draft**, either listing may be corrected in place (`RM-GOVERNED-PATH D27.1`). Once approved the artifact is immutable: a change that requires a new contract is made by `ngrace supersede` and a replacement bundle, never by editing the approved bytes (`RM-GOVERNED-PATH D27`).
      </Constraint>
      <Constraint>
        **Ownership of forced files.** The one forced module-ownable source file is `src/artifact/run-membership.ts` (`LINKS: M-ARTIFACT-TYPES`; this bundle owns `listLooseEvents`, `listRunOrphans`, the vanished-entry tolerance, and the test-only read-boundary seam `setLooseEventReadProbeForTests`). Behavior tests (`src/artifact/run-membership.test.ts`, `src/grace-cursor.test.ts`), the close-time scope guard (`src/artifact/scope.test.ts`), and the retirement ratchet (`scripts/validate-record-retirement.test.ts`) are not module-ownable and carry no module anchor; they are in the write scope because the deliverable forces them. `src/grace-cursor.ts` is **not** forced and must not be edited: the existing consumers already exercise the membership repair. Do not require a file while excluding its owner, and do not invent a module or a `LINKS:` header to place one.
      </Constraint>
      <Constraint>
        **The shared XML reader is not widened.** The membership tolerance lives in `src/artifact/run-membership.ts`. `src/artifact/xml.ts` is **out of scope** — its owner module is not in this bundle's `AffectedAreas`: `readGraceXmlArtifact` already distinguishes a typed missing-file artifact from an `xml.parse` artifact, and the module that enumerates is the module that tolerates the disappearance. If the tolerance cannot fit there, supersede and widen deliberately rather than edit the shared reader.
      </Constraint>
      <Constraint>
        **Payment set, staged handoff, and close writes, enumerated — no blanket permission.** This bundle pays exactly `F295` and `F295.1`, and no other finding. The plan grammar has no `&lt;Pays&gt;` element, so the payment contract is stated exactly three ways: the plan `IntentSummary` semantically declares exactly `F295 F295.1`; the successor-named registry charter row is the structural `&lt;Pays&gt;` carrier; and the retirement payer fixture carries the two successor `bundleMinted` pins after the close. **The clean base `e0a9868916412977fd3b7aa7615b67615fa2a93b` carries no `F295` or `F295.1` `bundleMinted` entry** in `scripts/validate-record-retirement.test.ts`, so this bundle introduces **two** entries naming `C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5`, not a re-home of existing pins. **Staged-buffer owner handoff:** `.ngrace/scratch/staged-findings.md` is ignored by `.gitignore`, read verbatim by `flushRecord`, and invisible to the git-based scope guard; no `git diff` or `git ls-files --others --exclude-standard` can observe it. Its two paid-finding `Owner:` lines are exactly `F295` at `:121` and `F295.1` at `:158`, and this bundle rewrites exactly those two and no other staged owner line. This is a **transient pre-flush input mutation**, outside the guard's observable population; it does not broaden to `.ngrace/scratch/**`. **`PaidBy` is never hand-marked in the staged markdown:** the `--retire` writer derives and stamps XML `&lt;PaidBy&gt;` from the registry payer; the staged correction is the owner-line rewrite only. **The finite non-lifecycle write contract** consists of the one production file, the four test/ratchet files, the exact `.ngrace/scratch/staged-findings.md` path, the six exact predecessor-transition paths, and the seven record XML files under `docs/plans/active/RM-GOVERNED-PATH/` (`decisions.xml`, `findings.xml`, `findings-retired.xml`, `rulings.xml`, `rulings-retired.xml`, `registry.xml`, `registry-retired.xml`), with `decisions.md` **excluded** unconditionally. **Writer mechanics, accurate and re-measured at close:** while this bundle is **active**, `--flush` serializes the four live XML roots (`decisions.xml`, `findings.xml`, `rulings.xml`, `registry.xml`); after **archive**, `--retire` moves the two paid findings plus this bundle's charter row and, when anything moves, serializes **all seven** XML files. The exact byte-differing subset is re-measured by a drive at the close; every writer-touched path is declared, and no catch-all clause licenses repairing whatever a later check flags.
      </Constraint>
      <Constraint>
        **Ordered remainder, no invented ids.** The four later bundles are reserved as fresh semantic slugs (`C-LOOSE-MALFORMED-FOLD`, `C-FOLD-RETRY-CORE`, `C-FOLD-MIXED-RECOVERY`, `C-DISCARD-PREFLIGHT`) and B3 as `C-SUPERSEDE-INTEGRATION-CLOSE`; each is authored only after its predecessor is closed and committed, and each hash suffix is minted when it is authored. The collision/name search is re-run at each actual mint; the search recorded here is evidence for this draft only.
      </Constraint>
      <Constraint>
        **No new dependency, production file family, or permanent artifact.** No new production `src/*.ts` file, no new package dependency, and no permanent file family under `run/`. `listLooseEvents`, `listRunOrphans`, `listAccountingEvents`, and `listLedgerEvents` keep their current public contracts and ordering; only the vanished-entry tolerance and the test-only read-boundary seam are added. The close-time write guard lives in the existing forced test file `src/artifact/scope.test.ts`.
      </Constraint>
      <Constraint>
        **Discriminating evidence for every guard, from an isolated baseline where needed.** Every guard is driven in both directions: a passing control proves it can pass, a planted or intercepted violation proves it can redden. Controls that must observe pre-repair behavior (the fabricated membership entry, the folding of a vanished trailing event) run against an **isolated baseline copy** of the unmodified tree, never against the repaired binary, which cannot both omit and fabricate the same entry. The true late window is driven by isolated-process interception of `readFileSync` after the real `existsSync` check; a pre-call thrown `ENOENT` is not that window. Exit codes are read directly, never through a pipe. No failed import, compilation error, timeout, or failed instrument counts as the intended red. Any `bun test` close command carries `--timeout=0`; no per-test number is pinned.
      </Constraint>
      <Constraint>
        **Escape markup as character data.** Where this artifact must name a tag, attribute form, or angle-bracketed token, the entity form is used (`&lt;`, `&gt;`, `&amp;`); brackets are never paraphrased away, and markup-byte assertions belong in a TypeScript test (F43).
      </Constraint>
      <Constraint>
        **The full CI-equivalent verification is close-time only.** `bun run validate:ci` is the later plan's single final `MustPassCommand`, and the plan must rehearse the applied/archive close on a faithful clone before the production close: the payer test must red while the two `bundleMinted` entries and the durable routes are absent/stale and green only after they name `C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5`. The narrow `CloseEvidence` commands stay tied to their own subjects; a whole-suite command is not attached to a criterion that is not about the whole suite.
      </Constraint>
    </Constraints>
    <NonGoals>
      <NonGoal>**Malformed loose-event parsing and fold refusal are bundle 2's contract.** The parse-state exposure of `listLooseEvents` and the authoritative fold-boundary refusal of an existing-but-unparseable loose file are owned by the reserved bundle `C-LOOSE-MALFORMED-FOLD` (`AC-MEMBER-MALFORMED-AND-UNREADABLE`, pays `F315`). `AC-MEMBER-MALFORMED-AND-UNREADABLE` does not appear as an `AcceptanceCriteria` child in this spec.</NonGoal>
      <NonGoal>**Interrupted-fold retry, conflict refusal, and the post-ledger boundary are bundle 3's contract.** `AC-FOLD-RETRY-IDEMPOTENT`, `AC-FOLD-CONFLICT-REFUSAL`, and `AC-POST-LEDGER-BOUNDARY-PRESERVED` are owned by the reserved bundle `C-FOLD-RETRY-CORE` (pays `F313`) and do not appear as `AcceptanceCriteria` children in this spec.</NonGoal>
      <NonGoal>**Mixed recovery and the recovery hooks are bundle 4's contract.** `AC-FOLD-MIXED-RECOVERY`, `AC-MIXED-RECOVERY-VALIDATE-FIRST`, and `AC-RECOVERY-HOOKS-CORRECT` are owned by the reserved bundle `C-FOLD-MIXED-RECOVERY`, which pays no finding and therefore must not invent a payer, staged owner line, or record write. They do not appear as `AcceptanceCriteria` children in this spec.</NonGoal>
      <NonGoal>**The discarded preflight and F158 are bundle 5's contract.** `AC-DISCARD-NOT-WRITTEN-ON-PREWRITE-REFUSE`, `AC-F158-DEFECT-CLOSED`, and `AC-DISCARD-PREFLIGHT-NO-ALLOC` are owned by the reserved bundle `C-DISCARD-PREFLIGHT` (pays `F292 F292.1 F158`) and do not appear as `AcceptanceCriteria` children in this spec. `F158` is not paid here.</NonGoal>
      <NonGoal>**B3's integrated supersede matrix and governed close are not carried here.** The integrated end-to-end supersede success/refusal/post-fold matrices, the twelve-writer allocator control, B3's integrated finite close-time scope-guard fixtures, and B3's own empty-`&lt;Pays&gt;` charter-row close are owned by the reserved `C-SUPERSEDE-INTEGRATION-CLOSE`, authored only after the five bundle-1..5 successors close and commit.</NonGoal>
      <NonGoal>**`src/artifact/xml.ts` is not changed.** Owner: unchanged; the shared reader already distinguishes `xml.missing-file` from `xml.parse` and the tolerance lives in the enumerating module.</NonGoal>
      <NonGoal>**`src/grace-cursor.ts` is not changed.** Owner: unchanged; the existing `cursor show` / `status` / `gate archive` / `cursor fold` consumers exercise the membership repair, confirmed empirically before this draft.</NonGoal>
      <NonGoal>**`README.md` is unchanged.** Owner: unchanged; no user-visible surface. The vanished-entry tolerance is operator-visible only through recovery outcomes that already existed, and no documented refuse/recovery outcome changes; no stale sentence was identified.</NonGoal>
      <NonGoal>**`examples/` is unchanged.** Owner: unchanged. Examples invoke `status`, `cursor fold`, and `gate archive`, but none exercises the changed disappearance, orphan, or fold-consequence outcomes, so no expected output changes.</NonGoal>
      <NonGoal>**`CHANGELOG.md` is not hand-edited; no version bump or release act.** Owner: release automation (`scripts/release-*.ts`); this dispatch authorizes neither.</NonGoal>
      <NonGoal>**No new production file family, dependency, or permanent artifact.** No new production `src/*.ts` file and no new package dependency are introduced. Owner: this bundle's contract.</NonGoal>
    </NonGoals>
    <AcceptanceCriteria>
      <AC-MEMBER-EARLY-VANISH>With a `run/` event removed after `readdirSync` enumeration and before the callee existence check, driven through the named test-only read-boundary seam `setLooseEventReadProbeForTests` (which fires immediately before `readGraceXmlArtifact` and therefore before the callee existence check) and reporting that it fired; a dangling backing file is an equivalent portable control: a direct `listLooseEvents(bundle)` call returns **no** entry for the vanished file (no filename-derived fabricated event), and `bun run ngrace cursor show --change C --path &lt;root&gt;` exits 0 with the surviving events and unchanged id order. The discriminating control runs the same fixture against an **isolated baseline copy** of the unmodified base `e0a9868916412977fd3b7aa7615b67615fa2a93b`, where the actual returned fabricated entry is observed, proving the guard discriminates rather than merely passing. The measured baseline shape, pinned from the actual returned event rather than a constructed constant, is `{id:2, task:"T-001", kind:"progress", exists:false, attributes:{id:"2", task:"T-001", kind:"progress"}, children:[]}` — filename-derived `{id, task, kind}` attributes with no `graceVersion`, and empty children. The interception exit code is read directly; a run in which the injection did not fire is not the intended red.</AC-MEMBER-EARLY-VANISH>
      <AC-MEMBER-LATE-VANISH>With the event removed **after the real callee existence check and before the read**, driven by isolated-process interception of `readFileSync` after `existsSync` has already returned true (a pre-call thrown `ENOENT` is not this window): `bun run ngrace cursor show --change C --path &lt;root&gt;` exits **0** with no uncaught `ENOENT`, and a direct `listLooseEvents(bundle)` call omits the vanished entry while returning the surviving events with unchanged payload and id order. The interception must report it fired. On the isolated baseline copy the same fixture exits **1** with the uncaught `ENOENT` observed through `src/artifact/xml.ts:219` and `src/artifact/run-membership.ts:70`, so both directions are observed and the late window is proven distinct from the early window. A separate `EISDIR` control (the event path replaced by a directory) still exits non-zero naming the path and is not folded into the disappearance tolerance.</AC-MEMBER-LATE-VANISH>
      <AC-MEMBER-ORPHANS-TOLERANT>`listRunOrphans` tolerates a well-named `run/` file that vanishes after the callee existence check in the true late window (interception of `readFileSync` after `existsSync`): a direct call does not throw `ENOENT`, and the two existing orphan classes (`event-filename`, `invalid-id`) keep their reasons and ordering. A **real consuming CLI path** is driven with a valid prerequisite and deterministic outcomes, using a bundle whose `run/` holds an `invalid-id` orphan (`2-T-001-progress.xml` containing `id="x"`): (a) present — `bun run ngrace status --path &lt;root&gt;` exits 0 with a line containing `orphans=1`, and `bun run ngrace gate archive --change C-ORPH --path &lt;root&gt;` exits 0 with `1 orphan(s): 2-T-001-progress.xml (invalid-id)`; (b) file removed before enumeration — both exit 0, `status` prints no `orphans=` for the change, and the gate prints `no-open-epoch … run/ empty` with no orphan mention; (c) file removed after the existence check before the read (intercepted, reported fired) — both still exit 0. The criterion does not claim every consumer succeeds merely because enumeration tolerates `ENOENT`; the present and removed diagnostics are pinned exactly. The current base is measured red on row (c): the late window exits 1 with an uncaught `ENOENT`.</AC-MEMBER-ORPHANS-TOLERANT>
      <AC-MEMBER-FOLD-CONSEQUENCES>The three measured fold consequences are asserted as distinct cases, each with its own fixture: (a) an interior progress event removed from `opened`/`progress`/`terminal` makes `bun run ngrace cursor fold` exit 1 naming `range hole at N for &lt;worker&gt;`; (b) a trailing progress event materialized with bytes and then removed in the **true late window** — isolated-process interception of `readFileSync` after the real existence check — while `opened` and `terminal` remain, folds with exit 0 and exactly the surviving events and no fabricated ghost; the drive asserts the after-existence-check interceptor fired; (c) a vanished `opened` event with `terminal` and `progress` remaining folds with exit 0 and a synthesized covering `opened`, preserving `RM-GOVERNED-PATH D8.2`'s single-controller auto-open. The criterion forbids treating every vanished entry as an automatic refusal and forbids manufacturing a ghost event to make the loss observable. The three rows are driven through the real CLI on a fixture whose recursive snapshot includes **directory entries** as well as file bytes; on **(a)** the `run/` listing after the refusal is byte-identical; on **(b)** the ledger holds the surviving events and no ghost and `run/` is empty; on **(c)** the synthesized covering `opened` carries a fresh covering id and no event for the vanished id is fabricated.</AC-MEMBER-FOLD-CONSEQUENCES>
      <AC-NO-UNFORCED-WRITES>Activated only by the explicit switch `NGRACE_SCOPE_GUARD_CHANGE=C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5`, used for this bundle's target and final verification and present in this criterion's CloseEvidence. Measured against base `e0a9868916412977fd3b7aa7615b67615fa2a93b` in the applied-archive state, this bundle's complete tracked-and-untracked change set is within the closed allowed set: the forced source file {`src/artifact/run-membership.ts`}; the forced tests {`src/artifact/run-membership.test.ts`, `src/grace-cursor.test.ts`, `src/artifact/scope.test.ts`, `scripts/validate-record-retirement.test.ts`}; this bundle's active directory and its archive arrival (`.ngrace/changes/active/C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5/`, `.ngrace/changes/archive/C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5/`), never an archive glob; the **exact predecessor supersede transition** — its git-derived population relative to base `e0a9868` is two observable active deletions (`.ngrace/changes/active/C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1/spec.xml`, `.ngrace/changes/active/C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1/run-ledger.xml`) plus three archive arrivals (`.ngrace/changes/archive/C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1/spec.xml`, `.ngrace/changes/archive/C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1/plan.xml`, `.ngrace/changes/archive/C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1/run-ledger.xml`), never an archive glob; the active `.ngrace/changes/active/C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1/plan.xml` is absent from the base commit, so its disappearance is a worktree-overlay effect and is **not** counted as a git deletion; and the close's enumerated record/charter writes: **exactly the seven named XML files** under `docs/plans/active/RM-GOVERNED-PATH/`, with `decisions.md` **excluded** — neither `--flush` nor an eligible `--retire` writes the stub, and the guard admits no conditional path. The bundle-lifecycle recognition is **guard-only and finite**: the guard may recognize only this bundle's own lifecycle/archive identity under its exact id `C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5`, and that recognition is not permission for an arbitrary extra file inside the bundle directory — a planted file under it still reddens. The later plan must **not** add this successor's own `spec.xml`, `plan.xml`, `design-context.xml`, `run/`, `run.xml`, or `run-ledger.xml` to `ObservedWriteScope`, because those are governed by identity/lifecycle rules; the non-lifecycle plan scope remains exactly the one forced source, the four forced tests, the `.ngrace/scratch/staged-findings.md` input, the six predecessor-transition paths, and the seven record XML files, with zero globs. Declared **separately, outside the guard's allowlist**, is one transient ignored input mutation: the exact `.ngrace/scratch/staged-findings.md` two owner-line substitutions. The guard's git-derived observable population cannot include the ignored staged buffer, so the staged substitution is audited as a pre-flush input and is **never** a git-observable allowed write. The guard covers untracked files as well as tracked diffs, so `git diff --stat HEAD` alone is insufficient. Fixtures prove (a) a clean applied archive passes; (b) a forbidden ordinary path reddens; (c) a planted file inside the governed directory that is not one of the seven named XML files reddens; (d) unavailable git evidence refuses rather than reporting clean; (e) the exact predecessor transition is allowed; (f) without activation a later unrelated change of the same kind does not fail; and (g) a planted non-lifecycle file inside this bundle's own directory reddens, proving the bundle-directory recognition is identity, not a blanket prefix permission. Deactivation is by the switch, never by editing the archive later. No catch-all path and no glob may be added later. The guard's activation is finite: identity plus `baseCommit` never deactivates it on its own.<CloseEvidence><Command>NGRACE_SCOPE_GUARD_CHANGE=C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5 bun test --timeout=0 src/artifact/scope.test.ts</Command></CloseEvidence></AC-NO-UNFORCED-WRITES>
      <AC-CLOSE-PAYER>This bundle pays exactly `F295 F295.1`, and no other finding. The clean base `e0a9868916412977fd3b7aa7615b67615fa2a93b` carries **no** `F295` or `F295.1` `bundleMinted` pin in `scripts/validate-record-retirement.test.ts`, so this bundle introduces **two** entries naming `C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5`; it is not described as a re-home of existing clean-base pins. The successor-named charter row's `&lt;Pays&gt;` child in `registry.xml` (and `registry-retired.xml` once the row retires) carries exactly `F295 F295.1`; the derived `&lt;PaidBy&gt;` stamped on the retired Finding carriers in `findings-retired.xml` names this bundle; and the flushed durable Finding bodies carry the successor ownership wording. The staged handoff is exactly the **two** `Owner:` lines in `.ngrace/scratch/staged-findings.md` at `:121` (`F295`) and `:158` (`F295.1`); this bundle changes exactly those two and no other staged owner line. `F158` is a durable live Finding paid by the later bundle `C-DISCARD-PREFLIGHT`, not here, and `F292`/`F292.1`/`F313`/`F315` belong to the later bundles; no such token is written by this bundle. The test never reads the ignored staged buffer; it ratchets the durable XML result the flush produces. The CloseEvidence command is red in the applied-archive fixture until both durable routes name this bundle and green only then; a staged-buffer edit alone cannot green the test because the test ratchets the durable XML result, not the ignored buffer.<CloseEvidence><Command>bun test --timeout=0 scripts/validate-record-retirement.test.ts</Command></CloseEvidence></AC-CLOSE-PAYER>
      <AC-CLOSE-LINT>Post-close lint of the repository root reports zero errors and zero warnings, evaluated in the applied-archive state.<CloseEvidence><Command>bun run ngrace lint --path . --fail-on warnings</Command></CloseEvidence></AC-CLOSE-LINT>
      <AC-CLOSE-TESTS>The behavior-pinning suites for the changed surfaces pass with no wall-clock timeout, evaluated in the applied-archive state.<CloseEvidence><Command>bun test --timeout=0 src/artifact/run-membership.test.ts src/grace-cursor.test.ts</Command></CloseEvidence></AC-CLOSE-TESTS>
    </AcceptanceCriteria>
    <AffectedAreas>
      <M-ARTIFACT-TYPES />
    </AffectedAreas>
    <VerificationIntent>
      <ExpectedCommand>bun test --timeout=0 src/artifact/run-membership.test.ts src/grace-cursor.test.ts</ExpectedCommand>
      <ExpectedEvidence>Bundle-1 drives on complete disposable projects with direct exit codes and recursive snapshots that record directory entries as well as per-file bytes: (1) the early-vanish window for both `listLooseEvents` and a real `cursor show`, with the isolated baseline copy reproducing the measured fabricated entry; (2) the true late window through isolated-process interception of `readFileSync` after the real `existsSync` check, for a direct `listLooseEvents` call and a real `cursor show`, plus the `EISDIR` control; (3) `listRunOrphans` tolerance in the true late window with its classes preserved, and the real `status`/`gate archive` consumers in the present, removed-before-enumeration, and intercepted-late states with the exact diagnostics; (4) the three fold-consequence rows through the real CLI — interior-hole refusal with a byte-identical `run/` listing, trailing true-late-window disappearance folding the survivors with no ghost and empty `run/`, and vanished-`opened` auto-open with a fresh covering id. Read-only snapshots precede `cursor show` and `gate archive`, and recursive snapshots include directory entries so an empty-directory mutation cannot pass. Direct production-boundary probes for readers stay distinct from real CLI drives for the named consumer outcomes. The full CI-equivalent verification (`bun run validate:ci`) is the later plan's single final `MustPassCommand`, not a per-task command, and no close criterion depends on `/private/tmp` surviving.</ExpectedEvidence>
    </VerificationIntent>
    <Problem>
      **This is the replacement of the approved `C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1`, forced by its plan stage exhausting its three attempts, not by any defect in its approved behavior contract.** The predecessor carried fourteen ordinary criteria spanning membership tolerance, malformed-fold refusal, interrupted/mixed recovery, and the discarded preflight. Rather than weaken that contract, the maintainer partitioned it into the smallest coherent ordered bundles so each plan/rehearsal can finish within three attempts. This successor is **bundle 1**, membership disappearance and consequences; the four later bundles and B3 are named as reserved semantic slugs in the `Summary` and `NonGoals`, and the five-bundle payment multiset is `F292 F295 F292.1 F295.1 F313 F315 F158`, each token once. The approved predecessor stays `status="approved"` and is moved to the archive only by the later governed `ngrace supersede`; this draft does not supersede it.

      **The four criterion bodies are preserved from the approved predecessor with two measured facts corrected.** (1) The late window is the real `readFileSync`-after-`existsSync` window driven by isolated-process interception; a pre-call thrown `ENOENT` is not that window, and the predecessor's late-window text was corrected accordingly. (2) The early fabricated baseline shape is pinned from the **actual returned entry**, not a constructed constant: measured against base `e0a9868916412977fd3b7aa7615b67615fa2a93b`, a dangling backing file yields `{id:2, task:"T-001", kind:"progress", exists:false, attributes:{id:"2", task:"T-001", kind:"progress"}, children:[]}` — filename-derived `{id,task,kind}` attributes with no `graceVersion` and empty children — while the repaired reader omits it.

      **Measured base directions, re-driven against `e0a9868` on disposable clones.** Early window: the baseline fabricates the entry above; the repaired reader returns only the survivor. True late window: the baseline `listLooseEvents` throws `ENOENT` (direct exit 1; real `cursor show` exit 1) through `src/artifact/xml.ts:219` and `src/artifact/run-membership.ts:70`; the repaired reader omits the entry and `cursor show` exits 0. Orphan late window: the baseline `listRunOrphans`/`status`/`gate archive` exit 1 with the uncaught `ENOENT`; the repaired path exits 0, and the present diagnostics are `orphans=1` and `1 orphan(s): 2-T-001-progress.xml (invalid-id)` while the removed-before state is `run/ empty`. Fold consequences: the interior hole refuses with an exit 1 and a byte-identical `run/` listing and no ledger; the trailing event in the true late window is red at base (exit 1, uncaught `ENOENT`, no ledger) and folds with exit 0 and survivors `1:opened, 2:progress, 3:terminal` after the repair; the vanished `opened` folds at exit 0 with a synthesized covering `opened` both before and after (the membership omission is what keeps it honest).

      **No `src/grace-cursor.ts` production edit is needed, and the `EISDIR` control is satisfied by amending the rethrow.** A strict prototype that changed **only** `src/artifact/run-membership.ts` (`git diff --stat` = one file, 44 insertions / 3 deletions) carries the reader tolerance plus the non-`ENOENT` rethrow and amends the rethrown diagnostic to name the loose-event path; driven against base `e0a9868916412977fd3b7aa7615b67615fa2a93b` on disposable clones, it drives all four criteria green through the existing `cursor show` / `status` / `gate archive` / `cursor fold` consumers. The path-naming amendment is load-bearing for `AC-MEMBER-LATE-VANISH`: an earlier 41-insertion/3-deletion form of the same prototype merely rethrew the original `EISDIR`, and the real `cursor show` printed `Error: EISDIR: illegal operation on a directory, read` with no path, so that form satisfied three of the four criteria but **not** the `EISDIR` control. The corrected form exits non-zero, retains `EISDIR`, and prints the full event path (`…/run/2-T-001-progress.xml`), measured against the base, which exits non-zero and does not name the path. The predecessor's ten other ordinary criteria are removed from this spec and assigned to the four later bundles; the predecessor's `run-membership.ts` parse-state exposure (`parseIssue`) belongs to bundle 2, so this bundle's production change is the tolerance, the path-bearing rethrow, plus the test-only read-boundary seam only.
    </Problem>
    <Assumptions>
      <Assumption>Base commit `e0a9868916412977fd3b7aa7615b67615fa2a93b` is the committed approval of the predecessor `C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1`; its spec and ledger are tracked under `active/`, its draft `plan.xml` is untracked, and the archive holds no directory for the slug `FOLD-MEMBERSHIP-RECOVERY`. All were confirmed by listing the commit's tree and the worktree before this draft was written, and the predecessor's `spec.xml`, `run-ledger.xml`, and draft `plan.xml` are left byte-identical by this delivery.</Assumption>
      <Assumption>The four later bundles are reserved as semantic slugs only: `C-LOOSE-MALFORMED-FOLD` (bundle 2), `C-FOLD-RETRY-CORE` (bundle 3), `C-FOLD-MIXED-RECOVERY` (bundle 4), `C-DISCARD-PREFLIGHT` (bundle 5), followed by `C-SUPERSEDE-INTEGRATION-CLOSE` (B3). Their hash suffixes are minted when authored, each after its predecessor closes and commits.</Assumption>
      <Assumption>Projected relations, not pinned figures: a successful membership repair omits a vanished entry and folds the survivors; a refused consequence leaves the `run/` listing byte-identical; the exact record write set for the two-token payment state is re-measured by a `--flush`/`--retire` drive at the close. The criteria assert the relations; the figures are the executor's to measure.</Assumption>
      <Assumption>The `bundleMinted` map will need two entries (`F295`, `F295.1`) naming `C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5`, because the real payer invariant loop walks every derived payer token against `baseline[token] ?? bundleMinted[token]`. Measured 2026-09-21: the map holds neither at this base. Whether the fixture's exact extension lands in one edit or several is a plan measurement.</Assumption>
    </Assumptions>
  </C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5>
</NgraceChangeSpec>
