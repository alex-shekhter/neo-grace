<NgraceChangeSpec graceVersion="1.0" status="approved">
  <C-SUPERSEDE-INTEGRATION-CLOSE-1-82073AAC>
    <Summary>
      **Bundle B3 of the three-bundle split of the superseded `C-SUPERSEDE-MEMBERSHIP-2-C459A20C` (split design v2), minted fresh from the reserved semantic slug `SUPERSEDE-INTEGRATION-CLOSE`.** B1 (`C-SUPERSEDE-MEMBERSHIP-4-20941257`, candidate ownership and supersede transaction safety) and B2 (`C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5`, `C-LOOSE-MALFORMED-FOLD-1-4C18E876`, `C-FOLD-RETRY-CORE-1-000C564C`, `C-FOLD-MIXED-RECOVERY-1-62A1B852`, `C-DISCARD-PREFLIGHT-1-5087B21A`) are applied, archived, and committed at this bundle's base. This bundle — **the integrated supersede matrix and governed close** — owns exactly the eight ordinary criteria `AC-MEMBER-MATRIX-COMPLETE`, `AC-REFUSED-NO-SUCCESSOR`, `AC-SUCCESS-PATH-PRESERVED`, `AC-POST-FOLD-FAILURES`, `AC-SUPERSEDE-MATRIX-COMPLETE`, `AC-LOCK-AND-ALLOCATOR-KEPT`, `AC-SCOPE-GUARD-FINITE-FILES`, and `AC-SCOPE-GUARD-GIT-STATUS`, plus the four re-derived per-bundle close criteria `AC-NO-UNFORCED-WRITES`, `AC-CLOSE-PAYER`, `AC-CLOSE-LINT`, and `AC-CLOSE-TESTS`. **This bundle pays no finding.** Its successor-named charter row carries an **empty `&lt;Pays&gt;`**. Ceremony tier **T2** (multi-module integration slice). Its write population is test-only plus the close's record XML: the integrated behavior is already implemented by B1/B2, and no production source edit is invented to fill the work set. There is **no predecessor supersede transition** for this fresh slug; B1 owned the only lineage supersede. The `mint-search` line printed at mint is carried verbatim as governance data: `mint-search: 0 active, 0 archive prior bundle(s) share slug SUPERSEDE-INTEGRATION-CLOSE`.
    </Summary>
    <Goals>
      <Goal>
        **The complete supersede matrix (item 11) is driven end to end.** On disposable real-`.git` projects, with direct exit codes and recursive path/type/SHA-256 snapshots whose comparison is over the **union** of before/after keys (so additions, deletions, directory entries, and symlink state are all detected): live and stale predecessor and successor; replacement existence and location races under the fired under-lock seam; branch/timestamp drift proving the locked successor id equals the minted leaf id; explicit replacement never cleaned; post-fold governance-write, rename/EXDEV, and cleanup-removal failures; and the five-step success chain. `AC-SUPERSEDE-MATRIX-COMPLETE`.
      </Goal>
      <Goal>
        **The full membership/fold/discard consumer matrix (item 17, T-004 through T-008) is integrated and driven.** Rows whose subject is a reader or a test-only hook — direct `listLooseEvents`/`listRunOrphans` interception and the fold hooks — are driven at the **direct production boundary**; rows whose subject is a named consumer outcome are driven through the **real CLI**. The B2-shipped rows stay green and are re-driven on the B2-closed baseline; the genuinely new integrated row is T-008's real-CLI fold consequences followed by the twelve-writer allocator exercise. `AC-MEMBER-MATRIX-COMPLETE`.
      </Goal>
      <Goal>
        **The refusal and success post-states are preserved under integration.** A successful supersede retains every enumerated post-state; every refusal class leaves `.ngrace/changes/active/` with no directory the invocation created, except a handled-cleanup refusal under the bounded-cleanup policy, which leaves exactly one named residual directory and its reason. Both implicit (`--replacement` omitted) and explicit `--replacement` forms are driven, and a spec-only predecessor supersedes with exit 0 and no invented `plan.xml`. `AC-REFUSED-NO-SUCCESSOR`, `AC-SUCCESS-PATH-PRESERVED`.
      </Goal>
      <Goal>
        **The lock and allocator contract is pinned structurally and behaviorally.** The per-bundle `.run-write.lock` serialization, the post-write uniqueness scan, and the own-file unlink/retry remain in `src/grace-cursor.ts`; twelve concurrent real-CLI `cursor advance` writers against one opened epoch produce thirteen loose events with thirteen unique ids, every writer exits 0, and an explicit-replacement supersede of that bundle then exits 0 with the discard preflight refusing nothing this path accepts. `AC-LOCK-AND-ALLOCATOR-KEPT`.
      </Goal>
      <Goal>
        **The close is finite, identity-exact, and empty-paid.** `AC-SCOPE-GUARD-FINITE-FILES` and `AC-SCOPE-GUARD-GIT-STATUS` re-derive the activated scope guard for **this** bundle id and base commit: a finite allowlist of named literal paths with zero globs, dormant without its exact switch, red on a planted ordinary path, red on an unlisted file inside the record directory, red on a non-lifecycle file inside its own bundle directory, and fail-closed when git evidence is unavailable. `AC-NO-UNFORCED-WRITES`, `AC-CLOSE-PAYER`, `AC-CLOSE-LINT`, `AC-CLOSE-TESTS` evaluate in the applied-archive state.
      </Goal>
    </Goals>
    <Constraints>
      <Constraint>
        **Tier and base.** Ceremony tier **T2** (the integration spans four modules). Roadmap `RM-GOVERNED-PATH`. Base commit `73044f83c3f4eaaaa5c5b7648a8a31c115337c39` (the committed applied/archived close of `C-DISCARD-PREFLIGHT-1-5087B21A`, which retired its paid findings); the predecessor supersede lineage lives in the archived splits, not here. Every byte-invariance and scope-guard relation is measured against this full hash. Counts expire when a bundle archives, so the close re-measures. There is **no predecessor supersede transition** for this fresh bundle.
      </Constraint>
      <Constraint>
        **Modules, kept in agreement.** `AffectedAreas` names exactly `M-CLI`, `M-GATES`, `M-CURSOR`, and `M-ARTIFACT-TYPES` — the accepted split's broad integration set. The five candidate module-ownable source paths' `LINKS:` headers were rechecked and are **contextual integration anchors, not forced edits** — none is in the write scope: `src/grace-supersede.ts` and `src/grace-generate.ts` carry `LINKS: M-CLI`; `src/gates/ledger.ts` carries `LINKS: M-GATES`; `src/grace-cursor.ts` carries `LINKS: M-CURSOR`; `src/artifact/run-membership.ts` carries `LINKS: M-ARTIFACT-TYPES`. **No production source file is forced by this bundle**: the integrated behavior above is already implemented by B1/B2, and this bundle adds the repository tests and the close rather than editing a source module to fill the work set. Test files and `scripts/validate-record-retirement.test.ts` carry no module anchor and sit in the write scope because the deliverable forces them. Every module list is kept in agreement with this table (F136.1).
      </Constraint>
      <Constraint>
        **Ownership of forced files.** The forced non-lifecycle write population is test-only plus the close record XML, exactly these twelve literal paths and zero globs: `src/grace-supersede.test.ts` (success-path, success payload comparator, refusal matrix, supersede-matrix drives), `src/grace-cursor.test.ts` (the twelve-writer allocator and explicit-replacement rows), `src/gates/core.test.ts` (the integrated-matrix additions), `src/artifact/scope.test.ts` (the re-authored identity-exact guard), `scripts/validate-record-retirement.test.ts` (the re-authored empty-`Pays` row ratchet), and the seven RM-GOVERNED-PATH record XML files the shipped close writers touch (`decisions.xml`, `findings.xml`, `findings-retired.xml`, `rulings.xml`, `rulings-retired.xml`, `registry.xml`, `registry-retired.xml`). `docs/plans/active/RM-GOVERNED-PATH/decisions.md` is **excluded**: a current-writer inspection proves no shipped record writer writes it. The ignored `.ngrace/scratch/staged-findings.md` is **read** by `--flush` but is **not a B3 write** and is excluded from the allowlist and from the declared write population. Do not require a file while excluding its owner, and do not invent a module or a `LINKS:` header to place one.
      </Constraint>
      <Constraint>
        **The record writers, from the shipped code path.** While this bundle is active, `--flush` (`bun ./scripts/validate-record-retirement.ts --flush`) serializes the four live roots (`findings.xml`, `rulings.xml`, `decisions.xml`, `registry.xml`) and adds only this bundle's charter row to `registry.xml`; registry rows receive no index entry in `decisions.xml`. After archive, an eligible `--retire` moves the row — its `name` equals an archive directory — into `registry-retired.xml` and serializes all seven XML files whenever it moves at least one eligible element. With an empty `--pays`, `--flush` and `--retire` were rehearsed on a disposable clone of the current record: `--flush` exited 0 and byte-changed only `registry.xml`; `--retire` exited 0 (`moved 1`) and byte-changed only `registry.xml` and `registry-retired.xml`. Every writer-touched path is declared above; the exact byte-differing subset is a plan/close measurement, and no catch-all clause licenses repairing whatever a later check flags.
      </Constraint>
      <Constraint>
        **The close-time write guard is exact-id and finite.** The guard lives in the forced file `src/artifact/scope.test.ts`, activated only by `NGRACE_SCOPE_GUARD_CHANGE=C-SUPERSEDE-INTEGRATION-CLOSE-1-82073AAC` and measured against base `73044f83c3f4eaaaa5c5b7648a8a31c115337c39`. Its allowed non-lifecycle set is exactly the twelve literal paths above, with zero globs. It recognizes only this bundle's own lifecycle identities under its exact id (`spec.xml`, `plan.xml`, `run-ledger.xml`, `run.xml`, and XML under its own `run/`) in active and archive locations, and that recognition is not permission for an arbitrary extra file inside the bundle directory. It covers tracked diffs and untracked files, is dormant without its exact switch, and refuses when git evidence is unavailable. Availability and parsed path lines come from **one** checked git invocation. `decisions.md` is outside the allowed set, and no `docs/**` catch-all or archive glob exists. **Dormancy versus activated scope, measured at this mint.** The committed predecessor guard (`src/artifact/scope.test.ts:500-514`, `SCOPE_GUARD_CHANGE="C-DISCARD-PREFLIGHT-1-5087B21A"`) carries a mint-time assertion that runs **unconditionally** (with no exact switch) and requires the git-derived population to hold no foreign `C-*` bundle path; it fails the moment this draft exists, because this bundle's untracked `spec.xml` is a foreign path (`bun run validate:ci` = 2341 pass / 1 fail at this mint; the narrow test exits 1 at `:512`, receiving only `.ngrace/changes/active/C-SUPERSEDE-INTEGRATION-CLOSE-1-82073AAC/spec.xml`). That assertion conflates dormancy with an activated scope check. The re-authored guard separates them: **dormant** (no exact switch) it collects no evidence, computes no offenders, and does **not** inspect or fail on a future sibling bundle; **activated** against the recorded base it recognizes only this bundle's own lifecycle identities and still rejects a newly changed foreign sibling path as outside its allowed set. No blanket sibling or `C-*`-lifecycle exemption exists, and the activated finite guard is not weakened. Fixtures prove (a) a clean applied-archive pass; (b) a planted forbidden ordinary path reddens; (c) a non-record file inside the record directory reddens; (d) a planted non-lifecycle file inside this bundle's own directory reddens; (e) unavailable git evidence refuses rather than reporting clean; (f) without activation a later unrelated change of the same kind does not fail; and (g) dormant with a foreign sibling draft present passes, while activated with a foreign non-allowed sibling write refuses and this bundle's own lifecycle files pass.
      </Constraint>
      <Constraint>
        **Payment is empty, and the row ratchet proves the empty shape.** The bundle pays no finding. The plan grammar has no `&lt;Pays&gt;` element; the payment contract is carried by the successor-named registry charter row's empty `&lt;Pays&gt;` child, written by the close's `--flush` while the bundle is active and moved to the retired sibling by the eligible `--retire`. The retirement ratchet in `scripts/validate-record-retirement.test.ts` re-derives, for this bundle's exact id, the precedent-shaped relation proved by `C-HASHED-BUNDLE-IDS-2`: the row exists **exactly once** across `registry.xml` + `registry-retired.xml`; its `status` agrees with the holding layer (`live` in `registry.xml`, `retired` in `registry-retired.xml`); `kind="chartered"`; `Pays` is empty; and no `F&lt;n&gt;` token in `StatusText` lies outside `Pays`. No token is invented to avoid emptiness. The ratchet must red on a duplicate row and on a `StatusText` naming a token outside `Pays`, and the shipped `--retire` writer proves the move that greens it. Staged-buffer-only cannot green the durable assertion; the test never reads the ignored scratch buffer.
      </Constraint>
      <Constraint>
        **The old monolith's claims are provenance, never current fact.** Every criterion body inherited from `C-SUPERSEDE-MEMBERSHIP-2-C459A20C` is re-derived against base `73044f83c3f4eaaaa5c5b7648a8a31c115337c39`; no stale base claim or expired count is imported. The historical `AC-NO-UNFORCED-WRITES` allowance of an exact predecessor archive arrival does **not** apply here: this fresh bundle has no predecessor transition. The historical conditional `decisions.md` allowance is **not** a current entitlement and is dropped, because no shipped writer touches that path.
      </Constraint>
      <Constraint>
        **No new dependency, production file, or permanent artifact.** No new production `src/*.ts` file, no new package dependency, no new permanent file family under `run/`, and no `docs/plans/README.md` or roadmap edit. `listLooseEvents`, `listRunOrphans`, `listLedgerEvents`, and `listAccountingEvents` keep their current public contracts and ordering. The four close criteria are evaluated by the close, not mapped by a task.
      </Constraint>
      <Constraint>
        **Discriminating evidence for every guard, from an isolated baseline where needed.** Every guard is driven in both directions: a passing control proves it can pass, and a planted or injected violation proves it can redden. Refusal byte-invariance is asserted with a recursive snapshot over the union of before/after keys that records directory entries, symlinks, relative paths, types, and per-file bytes, so a deletion cannot escape by key iteration. Exit codes are read directly, never through a pipe. No failed import, compilation error, timeout, or failed instrument counts as the intended red. Any `bun test` close command carries `--timeout=0`; no per-test number is pinned.
      </Constraint>
      <Constraint>
        **The full CI-equivalent verification is close-time only.** `bun run validate:ci` is the later plan's single final `MustPassCommand`. The narrow `CloseEvidence` commands stay tied to their own subjects; a whole-suite command is not attached to a criterion that is not about the whole suite.
      </Constraint>
      <Constraint>
        **Escape markup as character data.** Where this artifact must name a tag, attribute form, or angle-bracketed token, the entity form is used (`&lt;`, `&gt;`, `&amp;`); brackets are never paraphrased away, and markup-byte assertions belong in a TypeScript test (F43).
      </Constraint>
    </Constraints>
    <NonGoals>
      <NonGoal>**B1/B2 behavior is not re-implemented.** Candidate ownership, bounded cleanup, exclusive mint, the candidate lock, attempt snapshot placement, the evidence filter, the plan-new marker refusal, the regenerate entry lock, validate-before-fold, rollback/cleanup, the review-verdict three-phase write, membership tolerance, malformed-input refusal, interrupted-fold idempotence, mixed recovery, and the deterministic discard preflight are already shipped and closed. Owner: B1 and B2 (closed). This bundle integrates, re-drives, and closes; it does not re-code them, and no criterion here re-pays `F314`, `F295`, `F295.1`, `F315`, `F313`, `F292`, `F292.1`, or `F158`.</NonGoal>
      <NonGoal>**`README.md` is unchanged.** Owner: unchanged; no user-visible surface. The integrated matrix and the empty-payment close change no documented refuse, recovery, or supersede outcome, and no stale sentence was identified; a later reader who finds one has a README edit, not a policy question.</NonGoal>
      <NonGoal>**`examples/` is unchanged.** Owner: unchanged. Examples invoke `status`, `cursor fold`, and `gate archive`, but none exercises the refusal matrix, post-fold failures, the twelve-writer allocator, or the empty-payment close, so no expected output changes.</NonGoal>
      <NonGoal>**`CHANGELOG.md` is not hand-edited, and no version bump or release act is performed.** Owner: release automation (`scripts/release-*.ts`); this dispatch authorizes neither.</NonGoal>
      <NonGoal>**`decisions.md` is not a B3 write.** Owner: no shipped record writer touches it; the close's `--flush`/`--retire` write the seven XML files only. The guard excludes it, and a planted modification of it reddens.</NonGoal>
      <NonGoal>**The ignored scratch buffer is not a B3 write.** Owner: unchanged. `--flush` reads `.ngrace/scratch/staged-findings.md`; because this bundle pays nothing, flush selects no staged finding and mutates no owner line. The path is excluded from the declared write population and from the guard allowlist.</NonGoal>
      <NonGoal>**`F312` (`--retire --help` / `--flush --help` run the verb) is out of scope.** Owner: the next actual change to `scripts/validate-record-retirement.ts`; invoking the flush/retire writers at this bundle's close is not itself such a code touch, and this bundle never calls their `--help`.</NonGoal>
      <NonGoal>**No production source edit is invented to fill the work set.** Owner: this bundle's contract. If, during execution, an integrated obligation proves unsatisfiable without a source change, stop and report the contradiction and its measured reproduction rather than widening scope silently.</NonGoal>
    </NonGoals>
    <AcceptanceCriteria>
      <AC-MEMBER-MATRIX-COMPLETE>The full T-004 through T-008 matrix (item 17) is integrated and driven on the B2-closed base. Rows whose subject is a reader or a test-only hook — direct `listLooseEvents`/`listRunOrphans` interception and the fold hooks — are driven at the **direct production boundary**; rows whose subject is a named consumer outcome are driven through the **real CLI**. The rows: T-004 isolated early/late `ENOENT` interception with a fired seam, an isolated-baseline discriminator, `listRunOrphans`, and real `cursor show`/`status`/`gate archive`; T-005 malformed closer and malformed progress through the real `cursor`/`lint`/`fold`/`supersede` surfaces plus valid, absent, omitted-attribute, and `EISDIR` controls, each with byte snapshots; T-006 both original hooks, both new state hooks, repeated interruption, exact single-epoch bytes, first-loop-order partial deletion, mixed valid and bad fresh sets, a matching subset, an older-epoch collision, and a payload conflict; T-007 a duplicate id, an event outside every allocation, and a pre-existing range hole, the repaired positive control, the actual competing-writer seam with recursive snapshots, and the competing-writer stage-(ii-a) analogue retry; T-008 real-CLI fold consequences and the twelve-writer allocator exercise. **Named instruments:** the direct membership rows (the `listLooseEvents`/`listRunOrphans` interception and tolerance cases) are re-driven by `src/artifact/run-membership.test.ts`, an **unchanged read-only input** named in the final command but **not** in the write scope; the fold/discard hook rows and the twelve-writer row are carried by the write-scoped `src/grace-cursor.test.ts`; CLI-consumer rows run through the real CLI from those suites. The criterion does not claim every row is reachable through the CLI.</AC-MEMBER-MATRIX-COMPLETE>
      <AC-REFUSED-NO-SUCCESSOR>Every refusal class in the taxonomy (stages (i), (ii-a)–(ii-c), (iii)–(vii)) leaves `.ngrace/changes/active/` with no directory this invocation created, **except** a handled-cleanup refusal under the bounded-cleanup policy, which instead leaves that directory plus an explicit residual-state diagnostic naming it and the reason. The demonstration is a recursive path/type/SHA-256 snapshot of the whole disposable project before and after, compared over the **union** of keys so deletions and directory changes are detected, not the absence of one successor directory. Both implicit and explicit `--replacement` forms are driven. Positive control: a successful chain of five sequential supersedes leaves exactly one active successor after each step. The **cleaned** refusal fixture is repeated against the **same still-active** predecessor with the **same resolved id** and leaves the active-set count unchanged after every attempt; the **retained-residue** fixture leaves exactly one named invocation-created directory, and a same-id repeat adds none (growth only on a new resolved id is asserted explicitly). Post-fold failures are driven as real fixtures (`AC-POST-FOLD-FAILURES`), not only preflight cases.</AC-REFUSED-NO-SUCCESSOR>
      <AC-SUCCESS-PATH-PRESERVED>A successful supersede of a predecessor with an unterminated open epoch retains all of: the successor id is the lineage successor; the predecessor's `spec.xml` and `plan.xml` (when present) root `status` becomes `superseded` with a `&lt;Replacement&gt;` child naming the successor; the loose events appear in exactly one new ledger epoch including exactly one `discarded` event and no engine-authored `terminal`; the verified loose files are deleted; the `run/` directory still exists and is empty; the predecessor is absent from `active/` and present under `archive/`; each ledger event matches its loose event under `expectedLedgerEventAttributes` plus its complete child subtrees; `src/example.ts` is byte-identical; stdout is exactly the archive-relative path; and `supersedeChainDepth` increments on the replacement only. The integrated version additionally drives an **explicit `--replacement`** successor and a predecessor carrying the thirteen loose events produced by the twelve-writer exercise. A **spec-only** predecessor (no `plan.xml`, no `run/`) supersedes with exit 0 and no invented `plan.xml`, so no criterion requires a plan on a path that legitimately lacks one.</AC-SUCCESS-PATH-PRESERVED>
      <AC-POST-FOLD-FAILURES>Governance-write and rename failures are driven through real CLI or production-hook fixtures, not only preflight cases: (a) a post-fold governance-status write failure, (b) a rename failure (the shipped test-only `io.renameSync` `EXDEV` hook, or a real rename refusal), and (c) a cleanup failure. Each case names the direct exit, the exact allowed snapshot difference, and the fold evidence that legitimately remains; already-folded loose files are not required to reappear; and rollback is composed per the failure taxonomy: status bytes are restored on a successful rollback write, and a persistent rollback failure leaves the last-successfully-written predecessor state named, with successor cleanup attempted once regardless. A successful fold's evidence is recorded separately from the subsequent failure.</AC-POST-FOLD-FAILURES>
      <AC-SUPERSEDE-MATRIX-COMPLETE>The complete supersede matrix (item 11) is driven on disposable real-`.git` projects with recursive snapshots over the union of before/after keys: live and stale predecessor and successor; replacement existence and location races (the replacement moves between `active/` and `archive/`, or appears/disappears, between the preflight check and the write, refused under the revalidated lock with the revalidation firing); branch/timestamp drift proving the locked successor id equals the minted leaf id; explicit replacement never cleaned; governance-write and rename/`EXDEV` rollback failures; cleanup-removal failure; and the five-step success chain. The explicit-replacement rows are revalidated under the sorted candidate locks before any fold or write.</AC-SUPERSEDE-MATRIX-COMPLETE>
      <AC-LOCK-AND-ALLOCATOR-KEPT>The structural pin and the behavioral drive are both required. Structurally, the per-bundle `.run-write.lock` serialization, the post-write uniqueness scan, and the own-file unlink/retry remain in `src/grace-cursor.ts`, and allocation/auto-open behavior is unchanged. Behaviorally, twelve concurrent real-CLI `cursor advance` writers against one opened epoch produce thirteen loose events with thirteen unique ids 1–13, every writer exits 0, and an explicit-replacement supersede of that bundle then exits 0; the discard preflight must not refuse the streams this path accepts.</AC-LOCK-AND-ALLOCATOR-KEPT>
      <AC-SCOPE-GUARD-FINITE-FILES>The guard is identity-exact to `C-SUPERSEDE-INTEGRATION-CLOSE-1-82073AAC`, dormant without its exact switch, and finite over the twelve named literal paths above with zero globs. Activated, it reddens on a planted forbidden ordinary path, on a planted file inside `docs/plans/active/RM-GOVERNED-PATH/` that is not one of the seven named XML files, and on a planted non-lifecycle file inside this bundle's own directory; the seven named XML files pass. `decisions.md` is excluded from the allowed set, so a planted modification of it reddens. Dormant without its exact switch, the guard collects no evidence, computes no offenders, and does not inspect or fail on a future sibling bundle; the mint-time historical/no-predecessor assertion is restricted accordingly. Activated against the recorded base, it recognizes only this bundle's own lifecycle identities and rejects a newly changed foreign sibling path as outside the allowed set — no blanket sibling or `C-*` exemption, and no weakening of the activated finite guard.</AC-SCOPE-GUARD-FINITE-FILES>
      <AC-SCOPE-GUARD-GIT-STATUS>When git evidence is unavailable, the guard refuses rather than reporting clean, and availability and parsed path lines come from **one** checked git invocation — never two spawns whose first success could be paired with a second failure to report `{available: true, lines: []}`. A focused fixture drives the disagreement direction (a first read that names a forbidden path, a second that fails) and the stable clean direction without mutating the production repository's `.git`, and shows the false-clean direction refuses.</AC-SCOPE-GUARD-GIT-STATUS>
      <AC-NO-UNFORCED-WRITES>Activated only by the explicit switch `NGRACE_SCOPE_GUARD_CHANGE=C-SUPERSEDE-INTEGRATION-CLOSE-1-82073AAC`, used for this bundle's target and final verification and present in this criterion's CloseEvidence. Measured against base `73044f83c3f4eaaaa5c5b7648a8a31c115337c39` in the applied-archive state, this bundle's complete tracked-and-untracked change set is within the closed allowed set of exactly twelve observable literal paths: the five forced tests/ratchet (`src/grace-supersede.test.ts`, `src/grace-cursor.test.ts`, `src/gates/core.test.ts`, `src/artifact/scope.test.ts`, `scripts/validate-record-retirement.test.ts`) and the seven named record XML files under `docs/plans/active/RM-GOVERNED-PATH/` (`decisions.xml`, `findings.xml`, `findings-retired.xml`, `rulings.xml`, `rulings-retired.xml`, `registry.xml`, `registry-retired.xml`), with `decisions.md` **excluded**. There are zero globs, no archive glob, no blanket bundle-directory prefix, and no catch-all path. The ignored `.ngrace/scratch/staged-findings.md` is read by `--flush` but never written by this bundle, so it is outside the git-derived allowlist. The bundle-lifecycle recognition is guard-only and identity-exact: the guard may recognize only this bundle's own lifecycle/archive identity under its exact id, and a planted non-lifecycle file under that directory still reddens. The later plan must **not** add this bundle's own `spec.xml`, `plan.xml`, `design-context.xml`, `run/`, `run.xml`, or `run-ledger.xml` to `ObservedWriteScope`. Deactivation is by the switch, never by editing the archive later.<CloseEvidence><Command>NGRACE_SCOPE_GUARD_CHANGE=C-SUPERSEDE-INTEGRATION-CLOSE-1-82073AAC bun test --timeout=0 src/artifact/scope.test.ts</Command></CloseEvidence></AC-NO-UNFORCED-WRITES>
      <AC-CLOSE-PAYER>This bundle pays no finding; the payment set is empty. The retirement ratchet re-derives, for `C-SUPERSEDE-INTEGRATION-CLOSE-1-82073AAC`, the exactly-once empty-`Pays` row relation: the row exists exactly once across `registry.xml` + `registry-retired.xml`; its `status` agrees with the holding layer; `kind="chartered"`; `Pays` is empty; and no `F&lt;n&gt;` token in `StatusText` lies outside `Pays`. The same durable assertion is exercised against a disposable fixture (green on the valid retired row; red on a duplicate matching row and on a `StatusText` naming a token outside `Pays`; staged-only insufficient), and the shipped `--retire` writer proves the move that greens it. The test never reads the ignored `.ngrace/scratch/staged-findings.md`; the durable route is the XML the flush and retire produce. The CloseEvidence command is red in the applied-archive fixture until the row is present exactly once with an empty `Pays`, and green only then.<CloseEvidence><Command>bun test --timeout=0 scripts/validate-record-retirement.test.ts</Command></CloseEvidence></AC-CLOSE-PAYER>
      <AC-CLOSE-LINT>Post-close lint of the repository root reports zero errors and zero warnings, evaluated in the applied-archive state.<CloseEvidence><Command>bun run ngrace lint --path . --fail-on warnings</Command></CloseEvidence></AC-CLOSE-LINT>
      <AC-CLOSE-TESTS>The behavior-pinning suites for the changed surfaces pass with no wall-clock timeout, evaluated in the applied-archive state.<CloseEvidence><Command>bun test --timeout=0 src/grace-supersede.test.ts src/grace-cursor.test.ts src/gates/core.test.ts src/artifact/run-membership.test.ts</Command></CloseEvidence></AC-CLOSE-TESTS>
    </AcceptanceCriteria>
    <AffectedAreas>
      <M-CLI />
      <M-GATES />
      <M-CURSOR />
      <M-ARTIFACT-TYPES />
    </AffectedAreas>
    <VerificationIntent>
      <ExpectedCommand>bun test --timeout=0 src/grace-supersede.test.ts src/grace-cursor.test.ts src/gates/core.test.ts src/artifact/run-membership.test.ts src/artifact/scope.test.ts</ExpectedCommand>
      <ExpectedEvidence>Normative integration evidence is the **repository tests** the plan adds to the named files, evaluated in the applied-archive state; the spec-stage drives recorded in `Problem` are **prototypes**, not the normative verification and not a claim that the criteria already pass. Those prototypes are, by class: product-hook prototypes in disposable Git repositories (A), a disposable version of the shipped ratchet plus the shipped writers (B), a synthetic predicate (C), and a disposable clone prototype of the re-authored guard run through the real `bun test` (D). Every drive reads direct exit codes and compares recursive path/type/symlink/file-byte snapshots over the **union** of before/after keys, so a deletion cannot escape by key iteration. The population: (1) the supersede matrix (item 11) — live/stale predecessor and successor, replacement existence/location races under the fired under-lock seam, branch/timestamp drift proving the locked id equals the minted leaf id, explicit replacement never cleaned, governance-write and rename/`EXDEV` rollback failures, cleanup-removal failure, and the five-step success chain; (2) the refusal matrix — implicit and explicit forms, same-id repeat-count and name invariance, retained-residue growth only on a new resolved id, and the cleaned-refusal no-successor direction; (3) the success path — one open epoch folds with exactly one `discarded` and no engine-authored `terminal`, `run/` survives empty, `src/example.ts` is byte-identical, stdout is the archive-relative path, and `supersedeChainDepth` increments on the replacement only, plus an explicit-replacement successor and a spec-only predecessor; (4) post-fold failures — governance-write, rename/`EXDEV`, and cleanup failures with the exact allowed snapshot difference and composed rollback; (5) the membership/fold/discard matrix (item 17) — readers and hooks at their direct production boundary, named consumers through the real CLI, re-driven on the B2-closed baseline; (6) the lock and allocator — the structural pin plus twelve concurrent real-CLI writers producing thirteen unique ids and a following explicit-replacement supersede exiting 0; (7) the scope guard — finite literal files, `decisions.md` excluded, dormant without the exact switch and not inspecting a future sibling, a planted forbidden ordinary path red, an unlisted record-directory file red, a non-lifecycle file inside the bundle red, a foreign sibling non-allowed write red when activated, and unavailable git evidence refusing from one checked read; (8) the empty-`Pays` close — the row-exists-exactly-once relation against a disposable fixture and the shipped `--flush`/`--retire` move. Payload and ledger assertions parse complete event attributes and children via `expectedLedgerEventAttributes` plus child subtrees, never substring-only checks. The measured `validate:ci`, `tsc --noEmit`, and `bun test` figures are dated snapshots that expire when a bundle archives, so the close re-measures. The four `CloseEvidence` criteria (`AC-NO-UNFORCED-WRITES`, `AC-CLOSE-PAYER`, `AC-CLOSE-LINT`, `AC-CLOSE-TESTS`) are evaluated by the close in the applied-archive state and are deliberately absent from every task's `Satisfies`; the eight ordinary criteria are the task-mapped population.</ExpectedEvidence>
    </VerificationIntent>
    <Problem>
      **Bundle B3 of the maintainer-directed three-bundle split, minted fresh from the reserved slug `SUPERSEDE-INTEGRATION-CLOSE`.** B1 and B2 are applied, archived, and committed at base `73044f83c3f4eaaaa5c5b7648a8a31c115337c39`; the active directory held no bundle before this mint, and this fresh slug has no predecessor supersede transition. `bun run ngrace` executes this checkout's `src/grace.ts`, not a published package.

      **What B1/B2 already shipped, measured on this base.** The supersede transaction is integrated: `supersedeChangeBundle` (`src/gates/ledger.ts:1537`) validates replacement existence/location/identity under the sorted candidate locks before `discardAndFoldEpoch` (`:1680`), composes rollback and exactly-once cleanup on post-fold failure, and revalidates the explicit replacement under the lock. The command path (`src/grace-supersede.ts`) resolves the implicit mint id once before any write and delegates the transaction to `supersedeChangeBundle`. The cursor module carries the per-bundle lock, uniqueness scan, and own-file renumber, and the deterministic discard preflight refuses before writing residue. The record writers `--flush` and `--retire` (`scripts/validate-record-retirement.ts:1577`, `:1069`) serialize the four live and seven total XML files respectively, and no writer touches `decisions.md`.

      **Measured shipped behavior at spec stage (disposable projects; real CLI and shipped writers, direct exits) — attribution: "disposable projects", NOT "real-`.git`".** Twelve concurrent real-CLI `cursor advance` writers against one opened epoch produced thirteen loose events with thirteen unique ids `1..13`, every writer exited 0, and an explicit-`--replacement` supersede of that bundle then exited 0 (one ledger epoch, one `discarded`, no `terminal`) — the `AC-LOCK-AND-ALLOCATOR-KEPT` integrated obligation is satisfiable with no production source change. The empty-`Pays` close was rehearsed on a disposable clone of the current record: `--flush --change C-SUPERSEDE-INTEGRATION-CLOSE-1-ABC123 --pays ""` exited 0 and byte-changed only `registry.xml`, minting a row with an empty `&lt;Pays&gt;`; after an archive directory appeared, `--retire` exited 0 (`moved 1`) and byte-changed only `registry.xml` and `registry-retired.xml`. **These two drives ran on disposable projects that were not Git repositories**, and are reported as shipped behavior on disposable projects, not as real-`.git` drives. Current tests already cover `AC-SUCCESS-PATH-PRESERVED` (two rows, implicit mint) and the `AC-REFUSED-NO-SUCCESSOR` five-step positive control (`src/grace-supersede.test.ts:714`, `:767`, `:780`) and the twelve-writer unique-id exercises (`src/grace-cursor.test.ts:4153`, `:4169`); the remaining integrated obligation is to assemble the eight criteria under this bundle's id and base, add the explicit-replacement and post-fold rows, pin the lock structurally, re-author the guard and the empty-`Pays` ratchet, and run the governed close.

      **Dormancy versus activated scope — the one defect measured at spec stage, and its owner.** With this draft active, `bun run validate:ci` is red: the committed predecessor guard's `has no predecessor supersede transition and recognizes only this bundle's lifecycle identities` assertion (`src/artifact/scope.test.ts:500-514`) runs **unconditionally, with no exact switch**, and requires the git-derived population to hold no foreign `C-` bundle path; this bundle's untracked `spec.xml` is such a path, so the narrow test exits 1 at `:512`, receiving only `.ngrace/changes/active/C-SUPERSEDE-INTEGRATION-CLOSE-1-82073AAC/spec.xml` (`bun run validate:ci` = 2341 pass / 1 fail over 2345 tests). The approved B5 spec's `AC-NO-UNFORCED-WRITES` requires dormancy without its exact switch; it does not make the mint-time absence of every future sibling bundle a permanent invariant. The correction is narrow and belongs to this bundle's `AC-SCOPE-GUARD-FINITE-FILES`/`AC-SCOPE-GUARD-GIT-STATUS` re-authoring: **dormant**, the guard collects no evidence, computes no offenders, and does not inspect or fail on a future sibling bundle; **activated** against the recorded base, it recognizes only this bundle's own lifecycle files and still rejects a newly changed foreign sibling path as outside scope. No blanket sibling or `C-*` exemption is granted, the activated finite guard is not weakened, no production source change is implied, and no scope is widened.

      **Spec-stage discrimination, four evidence classes, on HEAD — not a suite rerun and not a hand-written claim.** (A) **Product-hook prototypes in actual disposable Git repositories** (`/tmp/b3-real-git-drives.ts`: real `git init` + commit, production `supersedeChangeBundle` `io` hooks because the injected failures are not CLI-reachable; every row asserts on mismatch): a real-CLI refusal (existing implicit successor) exited non-zero with a byte-identical tree delta; the cleaned same-id refusal fired at write 1, recorded exactly one cleanup callback with `removed===true`, left the successor absent and the predecessor active, and left the exact active directory names unchanged across **two** repeats; the retained-residue refusal recorded exactly one cleanup callback with `removed===false` and exactly one `Residual state preserved` diagnostic, preserved the foreign bytes, and a same-id repeat grew the residue set by none; the rollback arm failed at write **2** (after a successful predecessor status write) and restored the spec bytes, removed the successor, left the predecessor active and unarchived, and left no predecessor-tree differences outside the allowed `run-ledger.xml`, `run.xml`, and `run/` paths; the probe did not assert a complete folded-ledger projection or each directional run-file change; a first-write-failure control recorded exactly one removing cleanup. (B) **A disposable version of the shipped ratchet plus the shipped writers** (`/tmp/b3-payer-drive/b3-payer.test.ts`, `6 pass / 0 fail`, exit 0): the factored `C-HASHED-BUNDLE-IDS-2` relation (`scripts/validate-record-retirement.test.ts:5225`) for this id is green on the exactly-once retired chartered row with an empty `Pays`, and reddens on a duplicate row, on a `StatusText` naming an `F` token outside `Pays`, and on a status disagreeing with the holding layer; a staged-buffer-only edit does not green it; and the shipped `--flush` (empty `--pays`) then `--retire` transition shows the relation red pre-flush, green post-flush (live), and green post-retire (retired). (C) **A synthetic predicate prototype** (`/tmp/b3-payer-row.ts`) reproduces the same green/reds over hand-written XML; it is a prototype only, superseded by (B), and carries no product or ratchet weight. (D) **A disposable clone prototype of the re-authored guard** (`/tmp/b3-guard-clone`: the real `bun test` on `src/artifact/scope.test.ts` re-authored to this id and base): the unmodified B5 test **reds** with this draft present (`24 pass / 1 fail` at `:512`, receiving `.ngrace/changes/active/C-SUPERSEDE-INTEGRATION-CLOSE-1-82073AAC/spec.xml`); after the re-authoring, the **dormant** run with a foreign sibling draft present is green (`26 pass / 1 skip / 0 fail`), and the **activated** run is green (`26 pass / 1 skip / 0 fail`) with the foreign sibling non-allowed write, the forbidden ordinary path, the non-record file in the record directory, and the non-lifecycle own-bundle file each reddening, own lifecycle admitted, unavailable git evidence refusing, and the one-checked-read pinned. The re-authored guard grants no blanket sibling exemption. The earlier `/tmp/b3-guard-drive.ts` emulation is retained only as a semantic sketch and is **not** the evidence. The four behavior suites named in the final command — including the unchanged `src/artifact/run-membership.test.ts` — currently run `480 pass / 0 fail` across `480 tests`; `src/artifact/scope.test.ts` is separately red only because of the dormancy defect above, and turns green under the re-authored guard. These discriminate representative B3 directions at spec stage; the repository tests and complete product matrix remain plan-stage obligations.

      **The chosen shape, and why the alternatives lose.** This bundle is **test-only plus the close**: it forces the five test files/ratchet that carry the integrated matrix and the two re-authored guards, and it forces the seven record XML files the close writers touch. It does **not** edit a production source module, because the integrated behavior is already implemented and a source edit would be an invented change that fills a work set rather than a measured gap. The runner-up — folding B3's integrated rows back into B1/B2 — was rejected by the split: the broad criteria assert behavior built by both predecessors and must be assigned once, to the later closer, over the B2-closed baseline. A second rejected shape kept the old monolith's conditional `decisions.md` allowance and its exact predecessor archive arrival: a current-writer inspection shows no writer touches `decisions.md`, and this fresh bundle has no predecessor transition, so both allowances would be an entitlement the product does not have. A third rejected shape re-implemented behavior to make the bundle look substantial; the split forbids it and it would re-pay closed findings.

      **Boundary that is not this bundle's.** The retained bounded competing-writer stage-(ii-a) residue and the distinct post-ledger `C-RUN-LEDGER` write-verify-delete boundary are B2's and stand; no criterion here relabels them. The already-paid findings `F292`, `F292.1`, `F158`, `F313`, `F314`, `F315`, `F295`, and `F295.1` are not re-paid.

      **The paid close shape.** B3 pays nothing: its successor-named charter row carries an empty `&lt;Pays&gt;`, and the close proves the row exists exactly once across the registry layers with a holding-consistent status, `kind="chartered"`, an empty `Pays`, and no finding token in `StatusText`. The four close criteria are re-derived for this bundle's id and base. The faithful full lifecycle is rehearsed on a clone before the production close; the close must prove every writer-touched path lies inside the twelve declared literals and that `decisions.md` is never touched.
    </Problem>
    <Assumptions>
      <Assumption>Base commit `73044f83c3f4eaaaa5c5b7648a8a31c115337c39` is the committed applied/archived close of `C-DISCARD-PREFLIGHT-1-5087B21A`; B1 and B2 are archived, `.ngrace/changes/active/` held no bundle before this mint, and this fresh slug has no predecessor transition. All were confirmed by reading the commit, listing the active directory, and checking the worktree before this draft. The measured integration and close figures are the dated snapshot above and expire when a bundle archives, so the close re-measures.</Assumption>
      <Assumption>Projected relations, not pinned figures: a deterministic refusal leaves the whole recursive tree byte-identical; a successful supersede mints exactly one successor and archives exactly the predecessor; an empty-`Pays` flush byte-changes only the registry while active and the registry pair after archive. The criteria assert the relations; the concrete ids and byte-differing subsets are plan/close measurements.</Assumption>
      <Assumption>The retirement ratchet will need one empty-`Pays` row relation for `C-SUPERSEDE-INTEGRATION-CLOSE-1-82073AAC`, modeled on the shipped `C-HASHED-BUNDLE-IDS-2` relation (`scripts/validate-record-retirement.test.ts:5225`). Whether the fixture lands in one edit or several is a plan measurement.</Assumption>
      <Assumption>The twelve-path allowlist is correct as declared: the five forced test files/ratchet plus the seven record XML files, with `decisions.md` and the ignored scratch buffer excluded. The guard's base is the full hash above. The plan re-measures the exact byte-differing record subset at the close.</Assumption>
      <Assumption>The disposable instruments under `/private/tmp` are the reproduction basis for this draft; their saved receipts match fresh runs on this base. The normative verification is repository tests derived from them; no close criterion depends on `/private/tmp` surviving.</Assumption>
    </Assumptions>
  </C-SUPERSEDE-INTEGRATION-CLOSE-1-82073AAC>
</NgraceChangeSpec>
