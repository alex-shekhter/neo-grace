<NgraceChangeSpec graceVersion="1.0" status="approved">
  <C-TEST-TIMEOUT-CEILING-1-4985C820>
    <Summary>
      T0 hotfix on `RM-GOVERNED-PATH`: **no per-test timeout; one job ceiling.** Every `bun test`
      invocation in `package.json` carries `--timeout=0`; all **82** per-test timeout pins across six
      test files are removed; `.github/workflows/validate.yml` (all three jobs) and
      `.github/workflows/publish.yml` (`verify`) gain `timeout-minutes`; the taught
      `ngrace-spec` rule (both trees) and the taught-rules token in
      `scripts/check-teaching-surface.ts` and its fixture table are rewritten to the new mechanism;
      `README.md`'s development snippet changes `bun test` to `bun run test`. The bundle flushes
      `F266` through the shipped canonical serializer and mints its chartered row; the close's
      `--retire` moves **2**. No `src/` production file changes, no version surface, no new lint
      code, verb, or lint-ignore. This spec is written at `status="draft"` and stops there: the
      approval write, the gates, the applied close and the `--retire` are the authority's.
    </Summary>
    <Problem>
      **The CI failure, measured.** GitHub run `35138369045` (the push of branch `c-record-schema`
      at `844acd3`, the close of `C-RECORD-SCHEMA-2-0785BD5E`) failed the `validate` step:
      `src/review/core.test.ts`'s "archive corpus: zero live and retired findings on every
      dynamically enumerated C-*" (30 640 ms) and "archive ratchet: exact product multiset of three
      pairs" (31 353 ms) timed out under `bun test --timeout=30000`
      (`gh run view 35138369045 --log-failed`; `Ran 1996 tests across 64 files. [268.43s]`). The
      third archive walker passed at 32 154 ms only because `C-STATUS-TEST-TIMEOUT-1-E7DA46E8` gave
      it `}, 60_000)` after the same failure shape (run `35017776037`). Locally the two take about
      19 s together and the four slowest tests are 10 025 / 9 487 / 9 342 / 8 545 ms
      (`grep -aE '^\s*\((pass|fail)\)'` over `bun test --timeout=0`, 2026-09-16 at `844acd3`). Both
      tests enumerate every directory under `.ngrace/changes/archive/` (111), so their duration is
      linear in the corpus on a runner about three times slower than this machine.

      **The population, measured 2026-09-16 at `844acd3`.** **82** per-test pins in six files: 29
      one-line (`}, N);`) across `scripts/audit-regressions.test.ts` (1),
      `scripts/validate-record-retirement.test.ts` (24), `src/grace-status.test.ts` (1),
      `src/artifact/xml.test.ts` (2), `src/grace-context.test.ts` (1); and 53 multi-line (numeric
      last argument on its own line, closing `);`) across
      `scripts/validate-record-retirement.test.ts` (49) and
      `scripts/check-teaching-surface.test.ts` (4). No `test.timeout`, `setDefaultTimeout` or
      `setConfig` call exists. Three `package.json` scripts carry `--timeout` (`test` 30 s,
      `validate:cli` and `validate:determinism` 120 s), chained by `validate:ci` (line 78) and
      `validate:release` (line 80); the other real invocations are the three `validate.yml` jobs and
      `publish.yml`'s `verify` job, all via `bun run test …`, plus `README.md:382` (`bun test`,
      bare), `RELEASING.md:95` and one comment in `src/test-support/c-apply-verb-close-evidence.ts`.
      `validate.yml` sets no `timeout-minutes`; the only real ceiling today is GitHub's six-hour
      default.

      **The probes, driven 2026-09-16.** A seven-second test passes under `bun test … --timeout=0`
      and fails under the 5 s default (exit 0 / exit 1, read directly). A per-test pin overrides
      even `--timeout=0`: a three-second sleep with `}, 1000);` fails at 1 000 ms under
      `--timeout=0` (exit 1), so removing the pins is required and not merely cosmetic. Bare
      `bun test` on this tree already fails: the two unpinned walkers time out at 5 000 ms
      (`2 tests failed`, `Ran 1996 tests … [119.87s]`, exit 1) — `README.md:382`'s documented
      command is broken today.

      **The remedy — the maintainer's ruling, 2026-09-16 (variant B), carried not re-derived.** No
      per-test timeout: `--timeout=0` on every `bun test` invocation in `package.json`, every
      per-test pin removed, and one job-level `timeout-minutes` as the sole ceiling. The rule it
      answers is already this repository's: a per-test number written to outrun the runner on a walk
      whose length is the archive is a dated measurement wearing a guard's clothes, and the archive
      grows with every close (F266, `[verified]`).

      **The base commit.** Branch `c-record-schema` at **`844acd3`** (six commits above `main`
      `aaae414`), tree clean, `.ngrace/changes/active/` holding `.gitkeep` only, 111 archive
      directories. The record's live roots read
      `&lt;Findings base="7343" headroom="372" ceiling="7715"&gt;`,
      `&lt;RecordIndex base="265" headroom="31" ceiling="296"&gt;`,
      `&lt;Registry base="12" headroom="14" ceiling="26"&gt;` and
      `&lt;Rulings base="1715" headroom="168" ceiling="1883"&gt;`.
    </Problem>
    <Goals>
      <Goal>
        **The timeout.** `package.json`'s `test`, `validate:cli` and `validate:determinism` scripts
        run `bun test --timeout=0 …` (no numeric `--timeout`); every one of the **82** per-test pins
        across the six named test files is removed; the full suite is green under `bun test
        --timeout=0`, including the two walkers that failed in the run above. A guard the plan lands
        in `scripts/audit-regressions.test.ts` asserts that no `*.test.ts` carries a numeric last
        argument to `it(`/`test(`, so a re-introduced pin reddens rather than passing under the
        ceiling it was written to outrun.
      </Goal>
      <Goal>
        **The ceiling.** Every job that runs the suite declares `timeout-minutes: 30`: the three
        jobs in `.github/workflows/validate.yml` (`validate`, `windows-compatibility`,
        `dart-adapter`) and `verify` in `.github/workflows/publish.yml`. The same guard asserts each
        suite-running job carries the key, by parsing the workflow bytes, so the only ceiling a hang
        cannot escape. The number and its derivation are stated in `Assumptions`.
      </Goal>
      <Goal>
        **The taught rule and the README.** The rule in `skills/ngrace/ngrace-spec/SKILL.md`
        `acceptance_criteria_anchors` and its packaged mirror is rewritten from "any test driven
        against a real repository carries an explicit generous timeout" to the variant-B mechanism:
        the test runs under `bun run test` (equivalently `bun test --timeout=0`), no per-test number
        is pinned, and the CI job's `timeout-minutes` is the **only ceiling**. The load-bearing
        token in `scripts/check-teaching-surface.ts`'s `TAUGHT_RULES` and the fixture copy in
        `scripts/check-teaching-surface.test.ts` change from `explicit generous timeout` to the new
        token (`only ceiling`), so the real-repository taught-rules test stays green.
        `README.md:382`'s bare `bun test` becomes `bun run test`, because bare `bun test` now (and
        already) dies at the 5 s default. The canonical skill and its mirror stay byte-identical.
      </Goal>
      <Goal>
        **The payment.** The plan flushes `F266` (cut at its trailing `---` separator, rendered
        through the shipped `xmlEscape` and the canonical serializer's form) into
        `docs/plans/active/RM-GOVERNED-PATH/findings.xml` with one index
        `&lt;Entry id="f266" token="F266" genre="finding" layer="live" /&gt;`, and mints the
        chartered row `C-TEST-TIMEOUT-CEILING-1-4985C820` into `registry.xml` with
        `&lt;Pays&gt;F266&lt;/Pays&gt;`, then runs `--rewrite-roots`. This is a hand flush through
        the shipped serializer, **overturning** `C-RECORD-SCHEMA-2-0785BD5E`'s sentence that its own
        flush "is the last sanctioned" hand flush and "the next flush goes through the verb"; the
        decision and its reason are in `Constraints`. At the close the authority archives the bundle
        and runs `--retire`, which moves **two** — `F266` stamped
        `&lt;PaidBy&gt;C-TEST-TIMEOUT-CEILING-1-4985C820&lt;/PaidBy&gt;` and the row — and flips
        its index layer in place; a second `--retire` reports `moved 0`. Projected roots are in
        `Assumptions` and expire.
      </Goal>
    </Goals>
    <Constraints>
      <Constraint>
        Ceremony tier **T0** (hotfix: CI timeouts, one taught sentence, and the record flush for
        `F266`). Roadmap `RM-GOVERNED-PATH`. Issue: CI run `35138369045`
        (`src/review/core.test.ts`, `this test timed out after 30000ms`), staged as `F266` in
        `.ngrace/scratch/staged-findings.md` (read whole; `F252`, `D40`, `F265` there are already in
        the record — untouched). Base commit `844acd3`. The bundle touches no `src/` non-test file;
        `scripts/` is linted and type-checked (`.ngrace-lint.json` `ignoredDirs` is `["examples"]`).
        Binding live rulings: `D28`/`D28.1` (the flush lands in the findings window and the close's
        move re-opens it), `D30` (the record freezes for the lifetime of a bundle that measures it;
        no hard `Constraint` here transcribes a scalar count of a file it changes — every occupancy,
        delta and root figure lives in `Assumptions` and expires), `D31` (the index has no retired
        sibling; its `Entry` layer flips in place), `D11` (nothing deferred; no new debt), `D20`
        (the authority owns the close), `F43` (escape markup as character data).
      </Constraint>
      <Constraint>
        **Overturn 1 — `C-STATUS-TEST-TIMEOUT-1-E7DA46E8`.** Its remedy: *"give
        `src/grace-status.test.ts`'s live-repo `reRatificationCount` test an explicit generous
        timeout"* — the spec's Goal 1, and F263's rule *"A test driven against this repository's real
        archive carries an explicit generous timeout … The house in-code form is `}, 60_000);`"*.
        Answered, not ignored: that number was chosen to outrun a walk over a 106-directory archive;
        the archive is 111 today and grows with every close, so the number expires at the next
        threshold (this is the fourth timeout incident in a week). A hung test is still bounded — by
        the job's `timeout-minutes`, at the only scale CI bills — while a per-test number is a dated
        measurement wearing a guard's clothes. The remedy therefore moves from the test to the job.
      </Constraint>
      <Constraint>
        **Overturn 2 — the flush, decided (a).** `C-RECORD-SCHEMA-2-0785BD5E`'s spec says its own
        flush is *"the **last sanctioned** hand flush; the next flush goes through the verb"*, and
        `D40` rules a governed XML document is written by a verb or not at all. Decision: **(a) a
        hand flush through the shipped canonical serializer**, explicitly overturning that sentence.
        Reason: the flush/mint verb is not built, `F266` must be recorded by the bundle that found
        it rather than carried in a one-copy `.gitignore`d buffer, and step (1)'s serializer makes
        the write canonical — `serialize(parse(file))` equal to `file` is a shipped, passing test
        (canonical-at-rest) — so the bytes are the serializer's and the schema refuses a malformed
        write. The alternative **(b)** — leave `F266` staged, mint the row with `Pays F266`, let the
        verb bundle flush it already paid — is **available**: probed 2026-09-16, a live row whose
        `Pays` names a token absent from both findings layers validates `EXIT=0` (the shipped
        validator checks row name against the archive, never the token's presence), the close's
        `--retire` moves the row by name, and a retired row pays a later-flushed finding (its
        `PaidBy` is stamped on the next `--retire`, then `EXIT=0`). It is not chosen: it leaves a
        verified finding outside the record and makes the bundle's `--retire` move 1, not 2.
      </Constraint>
      <Constraint>
        **Overturn 3 — the taught rule.** The sentence "any test driven against a real repository
        carries an explicit generous timeout" is in `skills/ngrace/ngrace-spec/SKILL.md`
        `acceptance_criteria_anchors` and its packaged mirror, added by `C-TAUGHT-RULES` as one of
        the paid lessons, and asserted by `checkTaughtRules`'s real-repository test
        (`scripts/check-teaching-surface.test.ts:723`). Variant B overturns the mechanism that
        sentence teaches, so the sentence and both token tables change with it. Leaving it would
        re-teach the per-test remedy the ruling removes, on the next authoring pass.
      </Constraint>
      <Constraint>
        **Write scope, stated once.** Files this bundle forces into write scope: `package.json` (the
        three `--timeout` flags), `.github/workflows/validate.yml` and
        `.github/workflows/publish.yml` (`timeout-minutes`),
        `scripts/audit-regressions.test.ts` (the pin and workflow guards),
        `scripts/validate-record-retirement.test.ts` (73 pins removed),
        `scripts/check-teaching-surface.test.ts` (4 pins and the fixture token),
        `scripts/check-teaching-surface.ts` (the production token),
        `src/grace-status.test.ts` (1 pin), `src/artifact/xml.test.ts` (2 pins),
        `src/grace-context.test.ts` (1 pin), `README.md` (the development snippet),
        `skills/ngrace/ngrace-spec/SKILL.md` and
        `plugins/ngrace/skills/ngrace/ngrace-spec/SKILL.md` (the taught rule, byte-identical),
        `docs/plans/active/RM-GOVERNED-PATH/findings.xml`, `decisions.xml`, `registry.xml` (the
        flush, the row, `--rewrite-roots`), `findings-retired.xml` and `registry-retired.xml` (the
        close's `--retire`), and this bundle's own artifacts under
        `.ngrace/changes/active/C-TEST-TIMEOUT-CEILING-1-4985C820/`. Nothing else: no `src/`
        non-test file, no `examples/`, no version surface, no `bun.lock`, no lint catalog, no skill
        other than `ngrace-spec`. All test files are `*.test.ts` and `scripts/` paths are outside
        `src/` (`isModuleOwnableWritePath` is false for both), so
        `change.graph-anchors-miss-write-scope` cannot fire.
      </Constraint>
      <Constraint>
        Row and payment law. The row is `C-TEST-TIMEOUT-CEILING-1-4985C820` (the minted id, an
        archive directory only at the close), `kind="chartered"`; its `&lt;Pays&gt;` names exactly
        `F266` and no other `F` token; its `&lt;StatusText&gt;` names no `F` token and carries no
        `Closed with` sentence. `F266` is flushed live with one index `Entry` whose `genre` and
        `layer` agree with the holding file; the close's `--retire` moves both and stamps the
        `PaidBy`. Structural counts come from the shipped parser, never `grep -c`. Probes mutate
        copies, never the production record. The flush is performed at the start of execution, under
        the `D30` window this bundle itself opens.
      </Constraint>
      <Constraint>
        **The archived specs are not re-run.** Twelve archived specs' `CloseEvidence` commands carry
        `bun test --timeout=…` — 74 occurrences of `bun test --timeout=30000` and 9 of
        `bun test --timeout=120000` across those specs; archives are immutable and their commands are
        not re-executed. The only site that executes a `CloseEvidence` command is
        `src/gates/ledger.ts:490` `evaluateCloseEvidenceChildren`, called from `recordVerdict`
        (`gate verdict`) for the one bundle resolved by `changeId`, and gated by
        `isAppliedArchiveBundle` (`:487` — in `changes/archive/` and `specStatus === "applied"`).
        Nothing in `ngrace lint`, `validate:ci`, or any live walk re-executes an old archive's
        commands.
      </Constraint>
      <Constraint>
        **What the T0 tier drops, and what it keeps.** The recent increments carried teaching-sentence
        Goals, instrument Goals, footprint Goals, a criterion-mapping Constraint, a roots-held
        criterion, a mirror-sync criterion and a no-current-mode criterion; T0 makes those optional.
        Kept: the flush of `F266` and its row (`AC-FLUSH-LANDED`, `AC-ROW-PAYS`), one base-pinned
        byte-invariance guard naming `844acd3` with the bundle's own archive arrival excluded
        (`AC-NO-OTHER-SRC`), and the post-archive close-lint criterion (`AC-CLOSE-LINT`). Every
        `CloseEvidence` `Command` matches a shipped discriminating shape (`bun test` for the suites,
        the `git-diff-exit-code` shape with the named base commit, `grace-ts-lint-fail-on` for
        close-time lint, and the configured `bun run validate:teaching-surface` prefix). Exit codes
        are read directly, never through a pipe.
      </Constraint>
    </Constraints>
    <NonGoals>
      <NonGoal>
        `examples/` unchanged: no polyglot surface, no evidence rebalance, no `.ngrace` fixture. The
        change is a test-runner ceiling and a taught sentence; nothing under `examples/` states a
        timeout rule. Owner: unchanged; no user-visible example surface.
      </NonGoal>
      <NonGoal>
        The 111 archived bundles, every skill other than `ngrace-spec`, the teaching templates, the
        lint catalog, `.ngrace-lint.json`, `.ngrace/graph/`, `.ngrace/verification/`, `bun.lock`
        and every version surface — unchanged; their pins stay green by non-edit.
      </NonGoal>
      <NonGoal>
        A new lint issue code, a new CLI verb, or a new lint-ignore: the guard is a TypeScript test in
        `scripts/audit-regressions.test.ts`, not a lint rule; the remedy is the invocation, the job
        ceiling and the taught rule. Owner: unchanged; no product grammar moves.
      </NonGoal>
      <NonGoal>
        The flush/mint **verb** and the `D40` later steps (`spec edit`, `plan edit`, the graph-module
        verb). Owner: the chartered successor (`D40` step (2)); the dependency is the canonical
        serializer, which exists, so only sequencing keeps it out of this T0.
      </NonGoal>
      <NonGoal>
        Running a gate, a cursor or supersede verb, `ngrace review` beyond the pre-report command, or
        committing against the production bundle. Owner: the authority; this spec is authored at
        `status="draft"`.
      </NonGoal>
    </NonGoals>
    <AcceptanceCriteria>
      <AC-TIMEOUT-ZERO>
        `package.json`'s `test`, `validate:cli` and `validate:determinism` scripts carry
        `--timeout=0` and no numeric `--timeout`; no `*.test.ts` carries a numeric last argument to
        `it(`/`test(`; the full suite is green under `--timeout=0`, the two walkers of
        `src/review/core.test.ts` included. The plan lands the pin guard in
        `scripts/audit-regressions.test.ts`.
        <CloseEvidence>
          <Command>bun run test</Command>
          <Command>bun test --timeout=0 scripts/audit-regressions.test.ts</Command>
        </CloseEvidence>
      </AC-TIMEOUT-ZERO>
      <AC-CEILING>
        Every job that runs the suite declares `timeout-minutes: 30`: `validate`,
        `windows-compatibility` and `dart-adapter` in `.github/workflows/validate.yml`, and `verify`
        in `.github/workflows/publish.yml`. The guard parses the workflow bytes and asserts the key
        on each such job.
        <CloseEvidence>
          <Command>bun test --timeout=0 scripts/audit-regressions.test.ts</Command>
        </CloseEvidence>
      </AC-CEILING>
      <AC-TAUGHT-RULE>
        The `ngrace-spec` rule at `acceptance_criteria_anchors` in both trees teaches the variant-B
        mechanism and carries the token `only ceiling`; `TAUGHT_RULES` in
        `scripts/check-teaching-surface.ts` and the fixture copy in its test carry the same token;
        the canonical and packaged skill bodies are byte-identical; no skill carries the stale
        sentence.
        <CloseEvidence>
          <Command>bun run validate:teaching-surface</Command>
        </CloseEvidence>
      </AC-TAUGHT-RULE>
      <AC-FLUSH-LANDED>
        After the flush, `f266` exists exactly once across `findings.xml` and
        `findings-retired.xml` with no fixed expectation of which file holds it: its `status` agrees
        with the holding file, its `token` and `genre` agree, and its single index `&lt;Entry&gt;`
        has `genre` and `layer` agreeing with that holding file; no landed `Body` ends with a `---`
        separator line; the findings root `base` equals the file's `newlineCount` and the index
        `base` equals its live `Entry` count.
        <CloseEvidence>
          <Command>bun test --timeout=0 scripts/validate-record-retirement.test.ts</Command>
        </CloseEvidence>
      </AC-FLUSH-LANDED>
      <AC-ROW-PAYS>
        This bundle's registry row exists exactly once across `registry.xml` and
        `registry-retired.xml` with no fixed expectation of which file holds it: `status` agrees with
        the holding file, `kind="chartered"`, its `&lt;Pays&gt;` contains `F266` (containment, never
        an exact set), and its `&lt;StatusText&gt;` names no `F` token outside `Pays` and carries no
        `Closed with` sentence. The close's `--retire` reports `moved 2`; a second `--retire` reports
        `moved 0`; the applied-archive record validates clean.
        <CloseEvidence>
          <Command>bun test --timeout=0 scripts/validate-record-retirement.test.ts</Command>
        </CloseEvidence>
      </AC-ROW-PAYS>
      <AC-NO-OTHER-SRC>
        No file changes relative to this spec's base commit `844acd3` except the write-scope files
        named here and in the command's excludes, and this bundle's own archive arrival under
        `.ngrace/changes/` (the close renames the bundle's directory, so the close's own move is
        excluded by path).
        <CloseEvidence>
          <Command>git diff --exit-code 844acd3 -- . ':(exclude)package.json' ':(exclude).github/workflows/validate.yml' ':(exclude).github/workflows/publish.yml' ':(exclude)src/grace-status.test.ts' ':(exclude)src/artifact/xml.test.ts' ':(exclude)src/grace-context.test.ts' ':(exclude)scripts/audit-regressions.test.ts' ':(exclude)scripts/validate-record-retirement.test.ts' ':(exclude)scripts/check-teaching-surface.test.ts' ':(exclude)scripts/check-teaching-surface.ts' ':(exclude)skills/ngrace/ngrace-spec/SKILL.md' ':(exclude)plugins/ngrace/skills/ngrace/ngrace-spec/SKILL.md' ':(exclude)README.md' ':(exclude)docs/plans/active/RM-GOVERNED-PATH' ':(exclude).ngrace/changes'</Command>
        </CloseEvidence>
      </AC-NO-OTHER-SRC>
      <AC-CLOSE-LINT>
        Post-archive lint 0/0. This is the only place current-mode lint of this root appears.
        <CloseEvidence>
          <Command>bun ./src/grace.ts lint --path . --fail-on warnings</Command>
        </CloseEvidence>
      </AC-CLOSE-LINT>
    </AcceptanceCriteria>
    <Assumptions>
      <Assumption>
        **The pin population (measured 2026-09-16 at `844acd3`, expires).** 82 pins / 6 files. One-line
        `}, N);`: 29 — `scripts/audit-regressions.test.ts` 1, `scripts/validate-record-retirement.test.ts`
        24, `src/grace-status.test.ts` 1, `src/artifact/xml.test.ts` 2, `src/grace-context.test.ts` 1.
        Multi-line numeric last argument: 53 — `scripts/validate-record-retirement.test.ts` 49,
        `scripts/check-teaching-surface.test.ts` 4. The plain `grep -rnE '^\s+[0-9_]+,\s*$'` also
        matches four non-timeout `2,` arguments (`scripts/prove-record-preservation.test.ts:55`,
        `scripts/release-check.test.ts:19`, `scripts/validate-record-retirement.test.ts:140`,
        `scripts/validate-citation-anchors.test.ts:69`), only one of which also closes `);`
        (`release-check`:19, a `JSON.stringify` indent argument); the count above requires the numeric
        line to close `);` and the value to be a timeout, so none is counted.
      </Assumption>
      <Assumption>
        **The `bun test` invocation population (measured, expires).** `package.json` scripts `test`
        (`--timeout=30000`), `validate:cli` and `validate:determinism` (`--timeout=120000`), reached
        by `validate:ci` and `validate:release`; the three `validate.yml` jobs
        (`validate` via `validate:ci` + `validate:determinism`, `windows-compatibility` via
        `bun run test …` + `validate:cli`, `dart-adapter` via `bun run test …`) and `publish.yml`'s
        `verify` (`bun run test` + `validate:cli`); `README.md:382` (`bun test`, bare);
        `RELEASING.md:95` (`bun run test`); a comment in
        `src/test-support/c-apply-verb-close-evidence.ts:18`. `lefthook.yml` runs no test.
      </Assumption>
      <Assumption>
        **The `timeout-minutes` derivation (measured 2026-09-16, expires).** The green `validate` job
        is 477 s (run `35057498687`, 111 archives): "Run CI validation" 409 s, "Run determinism…"
        61 s. The failed run's `validate` step aborted at 274 s
        (`Ran 1996 tests across 64 files. [268.43s]`). The archive walkers are ~3× slower on the
        runner than locally (30.6 / 31.4 s vs 9.5 / 9.3 s) and duration is linear in the corpus, so
        **30 minutes** is ~3.8× the measured worst green job — the maintainer's pick over 20 (2.5×):
        a ceiling exists only to bound a hang, and nothing should red on runner speed — and 12×
        below GitHub's six-hour default. A single value covers every job (the
        largest is `validate`; `windows-compatibility` was 141 s and `dart-adapter` 19 s).
      </Assumption>
      <Assumption>
        **The flush projection (measured by driving the shipped parser, renderer and
        `--rewrite-roots`/`--retire` on a throwaway copy, 2026-09-16; expires).** The `F266` finding
        element renders to 26 newlines and adds 27 to `findings.xml`. After flush +
        `--rewrite-roots`: findings `7370 / 345 / 7715`, index `266 / 30 / 296`,
        registry `13 / 13 / 26`, rulings held `1715 / 168 / 1883`. After the close's `--retire`
        (`moved 2`): findings `7343 / 265 / 7608`, index `265 / 31 / 296`,
        registry `12 / 14 / 26`, rulings held; a second `--retire` reports `moved 0` and the record
        validates clean. These are projections, never criteria.
      </Assumption>
      <Assumption>
        **The probes (2026-09-16).** `--timeout=0`: a 7 s test passes (exit 0) and fails under the 5 s
        default (exit 1). A per-test pin overrides `--timeout=0`: `}, 1000);` on a 3 s sleep fails at
        1 000 ms (exit 1). Bare `bun test` on this tree fails 2 tests at 5 000 ms (exit 1) —
        `README.md:382`'s command is already broken. A live registry row whose `Pays` names `F266`
        (absent from both findings layers) validates exit 0; `--retire` moves it by name; a retired
        row pays a later-flushed finding whose `--retire` stamps `PaidBy` and validates exit 0.
        Probes ran on copies; the production record was never mutated.
      </Assumption>
      <Assumption>
        **The taught-rule guard (measured, expires).** Only four sites carry the token
        `explicit generous timeout`: the two `ngrace-spec/SKILL.md` files and the two `TAUGHT_RULES`
        tables (`scripts/check-teaching-surface.ts:379`,
        `scripts/check-teaching-surface.test.ts:496`). The test's table builds the synthetic skills
        `checkTaughtRules` is checked against, so production and fixture tables change together.
        `RELEASING.md`'s `timeout` is `GRACE_RELEASE_SUMMARY_TIMEOUT_MS`, unrelated.
      </Assumption>
    </Assumptions>
    <AffectedAreas>
      <M-SKILLS />
      The only module anchor this spec honestly holds is `M-SKILLS` (canonical skill sources), because
      `skills/ngrace/ngrace-spec/SKILL.md` is rewritten; its packaged mirror under `plugins/` is
      owned by no module. No `src/` non-test file changes, and `scripts/` files
      (`check-teaching-surface.ts`) are not module-ownable, so no module-ownable write scope exists
      and the plan's `DurableScope` carries no anchor for them; do not mint a synthetic module and do
      not add a `LINKS:` header to a validation script. The write scope is enumerated once, in the
      write-scope `Constraint` (`F136.1`'s rule — no second module list anywhere).
    </AffectedAreas>
    <VerificationIntent>
      <ExpectedCommand>bun run test</ExpectedCommand>
      <ExpectedCommand>bun test --timeout=0 scripts/audit-regressions.test.ts</ExpectedCommand>
      <ExpectedCommand>bun run validate:teaching-surface</ExpectedCommand>
      <ExpectedCommand>bun test --timeout=0 scripts/validate-record-retirement.test.ts</ExpectedCommand>
      <ExpectedCommand>bun ./scripts/validate-record-retirement.ts docs/plans/active/RM-GOVERNED-PATH</ExpectedCommand>
      <ExpectedCommand>bun run validate:ci</ExpectedCommand>
      <ExpectedEvidence>
        T0 integration: every `bun test` invocation in `package.json` carries `--timeout=0` and every
        per-test pin is gone, proven by a guard that reddens on a planted pin; every suite-running
        job in both workflows declares `timeout-minutes: 30`, proven by a guard that reddens on a job
        without it; the `ngrace-spec` rule and both token tables carry the new rule and the two trees
        stay byte-identical; `F266` is flushed with one index `Entry` and its row is minted live
        before the close; a mid-bundle `--retire` reports `moved 0`; the close's move reports
        `moved 2` and flips the one index layer, and a second `--retire` reports `moved 0`; the
        applied-archive record validates clean; every `CloseEvidence` command and the full
        `validate:ci` run green inside a faithful copy of the post-close state, with both directions
        driven for each criterion (clean pass and planted red). Structural counts come from the
        shipped parser; exit codes read directly, never through a pipe; probes mutate copies, never
        the production record.
      </ExpectedEvidence>
    </VerificationIntent>
  </C-TEST-TIMEOUT-CEILING-1-4985C820>
</NgraceChangeSpec>
