<NgraceChangeSpec graceVersion="1.0" status="applied">
  <C-FOLD-RETRY-CORE-1-000C564C>
    <Summary>
      **Bundle 3 of five ordered small successors of the superseded `C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1`, minted fresh from the reserved semantic slug `C-FOLD-RETRY-CORE`.** Bundles 1 (`C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5`, vanished-entry membership) and 2 (`C-LOOSE-MALFORMED-FOLD-1-4C18E876`, unparseable loose input at both write boundaries) are applied and committed at this bundle's base. This bundle — **interrupted-fold retry idempotence, conflict refusal, and the post-ledger boundary** — owns exactly the three ordinary criteria `AC-FOLD-RETRY-IDEMPOTENT`, `AC-FOLD-CONFLICT-REFUSAL`, and `AC-POST-LEDGER-BOUNDARY-PRESERVED`, and pays exactly `F313`. Ceremony tier **T1**. The affected module is exactly `M-CURSOR`; the later plan's `DurableScope` carries `V-M-CURSOR`. The sole forced production file is `src/grace-cursor.ts` (`LINKS: M-CURSOR`); the forced behavior test is `src/grace-cursor.test.ts`. The two later small bundles are reserved as fresh semantic slugs — `C-FOLD-MIXED-RECOVERY` (bundle 4) and `C-DISCARD-PREFLIGHT` (bundle 5) — followed by the already-reserved B3 slug `C-SUPERSEDE-INTEGRATION-CLOSE`; each is authored only after its predecessor is closed and committed. There is **no predecessor supersede transition** for this fresh slug. Base commit `0ad7cb4e49b1bb5d930b27732f2978fa66221371`. The `mint-search` line printed at mint is carried verbatim as governance data: `mint-search: 0 active, 0 archive prior bundle(s) share slug FOLD-RETRY-CORE`.
    </Summary>
    <Goals>
      <Goal>
        **An interrupted fold resumes against its recorded epoch instead of appending a duplicate.** After either shipped interruption hook (`injectFailureBeforeVerify`, `injectFailureAfterWrite`) and after a partial verified deletion in the shipped sequential delete-loop order, a real `ngrace cursor fold` retry exits 0, reuses the interrupted epoch, adds no new epoch and no synthesized `opened`, retains the recorded epoch exactly once, leaves no loose files, and preserves the recorded ledger bytes and payloads byte-for-byte (the complete `run-ledger.xml` bytes captured after the injected post-write failure and before the first loose event is removed are asserted exactly unchanged after the retry; byte identity is claimed for the ledger bytes, and the payload is separately re-checked semantically). Repeated interruption is reachable on **successive fresh epochs**, and each of the two shipped hooks is driven independently — for `injectFailureBeforeVerify` and for `injectFailureAfterWrite`: interrupt a logical epoch, recover it once, allocate a fresh epoch, interrupt that at the same hook, recover it once — and each logical epoch occurs exactly once (never a same-residue second hook fire, because the pure-resume branch returns before either write hook can fire again). A third, empty fold remains the existing no-op. A prior completed epoch stays byte-identical, and a fresh-only allocated set (ids matching no recorded event) folds normally into the next epoch.
      </Goal>
      <Goal>
        **A genuine conflict or an older-epoch id reuse refuses before any write.** A loose event whose id matches a recorded event but whose canonical payload differs under `expectedLedgerEventAttributes` plus children makes the fold exit non-zero naming the id and the mismatch, writes no new epoch, and leaves the recorded bytes byte-identical. A loose event whose id matches an event in an older recorded epoch is refused; untouched older epochs are never rewritten and no synthesized `opened` is fabricated for an already-recorded epoch. A payload-matching subset of the **last** recorded epoch is residue and resumes, never a conflict.
      </Goal>
      <Goal>
        **The post-ledger write-verify-delete boundary is preserved, not relabelled.** A fold interrupted after its ledger write may leave the written ledger and engine-authored loose events; the refuse path removes, renumbers, and rewrites no `run/` event and no recorded epoch it did not itself write, and a subsequent successful fold recovers that evidence idempotently rather than accumulating it.
      </Goal>
      <Goal>
        **The payment and the write guard are executable.** The bundle pays exactly `F313`; its retirement-payer fixture is forced scope and the close includes the narrow single-token payer assertion. An executable close-time discriminator covers both tracked and untracked changes against the named base commit, with a closed allowed-write set, is bound to this bundle's exact id, is dormant without its exact activation switch, recognizes only exact lifecycle identities and XML under its own `run/`, and reddens on a planted forbidden file outside and inside this bundle's own directory. The ignored `.ngrace/scratch/staged-findings.md` input is a transient pre-flush mutation declared by its exact one owner line, outside the guard's observable population.
      </Goal>
    </Goals>
    <Constraints>
      <Constraint>
        **Tier and base.** Ceremony tier **T1** (module change in one package). Roadmap `RM-GOVERNED-PATH`. Base commit `0ad7cb4e49b1bb5d930b27732f2978fa66221371` — the committed application of bundle 2 `C-LOOSE-MALFORMED-FOLD-1-4C18E876`. The full hash is the baseline for every byte-invariance relation and the scope-guard base. Every measurement in this artifact was re-driven against that base on disposable real-`.git` clones; counts expire when a bundle archives, so the close re-measures.
      </Constraint>
      <Constraint>
        **Modules, kept in agreement.** `AffectedAreas` names exactly `M-CURSOR`. The `Ownership of forced files` constraint pairs each forced path with its owner from that same set; the two are kept in agreement (F136.1), and the later plan's `DurableScope` carries the exact verification anchor `V-M-CURSOR`. While this artifact is **draft**, either listing may be corrected in place (`RM-GOVERNED-PATH D27.1`). Once approved the artifact is immutable: a change that requires a new contract is made by `ngrace supersede` and a replacement bundle, never by editing the approved bytes (`RM-GOVERNED-PATH D27`).
      </Constraint>
      <Constraint>
        **Ownership of forced files.** The one forced module-ownable source file is `src/grace-cursor.ts` (`LINKS: M-CURSOR`; this bundle owns the pre-write reconciliation of the loose set against the durable ledger, the interrupted-epoch resume, and the conflict/older-reuse refusals). The forced behavior test `src/grace-cursor.test.ts` and the close-time guard/payer files (`src/artifact/scope.test.ts`, `scripts/validate-record-retirement.test.ts`) are not module-ownable and carry no module anchor; they are in the write scope because the deliverable forces them. `src/artifact/run-membership.ts`, `src/artifact/xml.ts`, and `src/gates/ledger.ts` are **not** forced and must not be edited. Do not require a file while excluding its owner, and do not invent a module or a `LINKS:` header to place one.
      </Constraint>
      <Constraint>
        **The reconciliation is pre-write and independent of the writer's transform.** The loose set is compared against the durable ledger **before** loose-only closure/hole checks or `maybeAutoOpenCoveringAllocation` can run, using independent payload comparison (`expectedLedgerEventAttributes` plus complete children), never the writer's own transform. A resume writes no new epoch and synthesizes no `opened` for an already-recorded epoch; a genuine canonical-payload conflict or an older-epoch id reuse refuses before any write.
      </Constraint>
      <Constraint>
        **Payment set, staged handoff, and close writes, enumerated — no blanket permission.** This bundle pays exactly `F313`, and no other finding. The plan grammar has no `&lt;Pays&gt;` element, so the payment contract is stated exactly three ways: the plan `IntentSummary` semantically declares exactly `F313`; the successor-named registry charter row is the structural `&lt;Pays&gt;` carrier; and the retirement payer fixture carries the one successor `bundleMinted` pin after the close. **The clean base `0ad7cb4e49b1bb5d930b27732f2978fa66221371` carries no `F313` `bundleMinted` entry** in `scripts/validate-record-retirement.test.ts`, so this bundle introduces exactly **one** entry naming `C-FOLD-RETRY-CORE-1-000C564C`, not a re-home of an existing pin. **Staged-buffer owner handoff:** `.ngrace/scratch/staged-findings.md` is ignored by `.gitignore`, read verbatim by `flushRecord`, and invisible to the git-based scope guard. The paid entry is the F313 finding whose `Owner:` line is exactly `:184`; this bundle rewrites exactly that one owner line and no other staged owner line. This is a **transient pre-flush input mutation**, outside the guard's observable population; it does not broaden to `.ngrace/scratch/**`. **`PaidBy` is never hand-marked in the staged markdown:** the `--retire` writer derives and stamps XML `&lt;PaidBy&gt;` from the registry payer; the staged correction is the owner-line rewrite only. **The finite non-lifecycle write contract** consists of the one forced production file, the forced behavior test, the two close-time guard/payer files, the exact `.ngrace/scratch/staged-findings.md` path, and the seven record XML files under `docs/plans/active/RM-GOVERNED-PATH/` (`decisions.xml`, `findings.xml`, `findings-retired.xml`, `rulings.xml`, `rulings-retired.xml`, `registry.xml`, `registry-retired.xml`), with `decisions.md` **excluded** unconditionally. There is no predecessor supersede transition for this fresh slug. **Writer mechanics, accurate and re-measured at close:** while this bundle is **active**, `--flush` serializes the four live XML roots (`decisions.xml`, `findings.xml`, `rulings.xml`, `registry.xml`); after **archive**, `--retire` moves the paid finding plus this bundle's charter row and, when anything moves, serializes **all seven** XML files. The exact byte-differing subset is re-measured by a drive at the close; every writer-touched path is declared, and no catch-all clause licenses repairing whatever a later check flags.
      </Constraint>
      <Constraint>
        **Ordered remainder, no invented ids.** The two later small bundles are reserved as fresh semantic slugs (`C-FOLD-MIXED-RECOVERY`, `C-DISCARD-PREFLIGHT`) and B3 as `C-SUPERSEDE-INTEGRATION-CLOSE`; each is authored only after its predecessor is closed and committed, and each hash suffix is minted when it is authored. The collision/name search is re-run at each actual mint; the search recorded here is evidence for this draft only.
      </Constraint>
      <Constraint>
        **No new dependency, production file family, or permanent artifact.** No new production `src/*.ts` file, no new package dependency, and no permanent file family under `run/`. `listLooseEvents`, `listRunOrphans`, `listLedgerEvents`, and `listAccountingEvents` keep their current public contracts and ordering; only the pre-write ledger reconciliation and the resume/refusal branch are added. The close-time write guard lives in the existing forced test file `src/artifact/scope.test.ts`.
      </Constraint>
      <Constraint>
        **Discriminating evidence for every guard, from an isolated baseline where needed.** Every guard is driven in both directions: a passing control proves it can pass, a planted or injected violation proves it can redden. The interrupted-fold, partial-deletion, conflict, and older-epoch-reuse rows run against an **isolated baseline copy** of the unmodified base `0ad7cb4e49b1bb5d930b27732f2978fa66221371`, which cannot both duplicate and resume the same epoch. Refusal byte-invariance is asserted with a recursive snapshot that records directory entries, symlinks, relative paths, and per-file bytes. Exit codes are read directly, never through a pipe. No failed import, compilation error, timeout, or failed instrument counts as the intended red. Any `bun test` close command carries `--timeout=0`; no per-test number is pinned.
      </Constraint>
      <Constraint>
        **Escape markup as character data.** Where this artifact must name a tag, attribute form, or angle-bracketed token, the entity form is used (`&lt;`, `&gt;`, `&amp;`); brackets are never paraphrased away, and markup-byte assertions belong in a TypeScript test (F43).
      </Constraint>
      <Constraint>
        **The full CI-equivalent verification is close-time only.** `bun run validate:ci` is the later plan's single final `MustPassCommand`, and the plan must rehearse the applied/archive close on a faithful clone before the production close: the payer test must red while the one `bundleMinted` pin and the durable route are absent/stale and green only after they name `C-FOLD-RETRY-CORE-1-000C564C`. The narrow `CloseEvidence` commands stay tied to their own subjects; a whole-suite command is not attached to a criterion that is not about the whole suite.
      </Constraint>
    </Constraints>
    <NonGoals>
      <NonGoal>**Mixed residue plus a fresh set and its validate-first/F/F∪A state table are bundle 4's contract.** The mixed-recovery ordering, the `injectFailureAfterResidueDelete` / `injectFailureAfterAutoOpen` hooks, and the `F`/`F ∪ A` post-states are owned by the reserved bundle `C-FOLD-MIXED-RECOVERY` (`AC-FOLD-MIXED-RECOVERY`, `AC-MIXED-RECOVERY-VALIDATE-FIRST`, `AC-RECOVERY-HOOKS-CORRECT`) and do not appear as `AcceptanceCriteria` children in this spec. This bundle's reconciliation handles the pure resume (loose ⊆ last epoch) and the fresh-only set; the mixed case is deliberately left to bundle 4.</NonGoal>
      <NonGoal>**The deterministic discarded preflight and F158 are bundle 5's contract.** The allocation/duplicate-id/range-hole refusal before the engine-authored `discarded` write, the bounded competing-writer analogue, and the F158 attribution are owned by the reserved bundle `C-DISCARD-PREFLIGHT` and do not appear as `AcceptanceCriteria` children in this spec. The post-ledger `C-RUN-LEDGER` boundary asserted here is **never** labelled F158.</NonGoal>
      <NonGoal>**B3's integrated supersede matrix and governed close are not carried here.** The integrated end-to-end supersede success/refusal/post-fold matrices, the allocator control, and B3's finite close-time scope-guard fixtures are owned by the reserved `C-SUPERSEDE-INTEGRATION-CLOSE`, authored only after the five small bundles close and commit.</NonGoal>
      <NonGoal>**Loose membership and malformed-input behavior closed by bundles 1–2 are unchanged.** The vanished-entry tolerance (`C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5`) and the unparseable loose-input refusal at both write boundaries (`C-LOOSE-MALFORMED-FOLD-1-4C18E876`) are preserved exactly; this bundle adds no membership or malformed-input behavior.</NonGoal>
      <NonGoal>**`src/artifact/run-membership.ts`, `src/artifact/xml.ts`, and `src/gates/ledger.ts` are not changed.** Owner: unchanged; the reconciliation lives in the fold/discard module that owns the ledger write, the shared reader already parses, and the supersede ledger writer already calls the discard path.</NonGoal>
      <NonGoal>**`README.md` is unchanged.** Owner: unchanged; no user-visible surface. The retry idempotence and conflict refusal are operator-visible only through an existing refuse/recover outcome, and no documented outcome changes; no stale sentence was identified.</NonGoal>
      <NonGoal>**`examples/` is unchanged.** Owner: unchanged. Examples invoke `status`, `cursor fold`, and `gate archive`, but none exercises an interrupted-fold retry, a payload conflict, or the post-ledger boundary, so no expected output changes.</NonGoal>
      <NonGoal>**`CHANGELOG.md` is not hand-edited; no version bump or release act.** Owner: release automation (`scripts/release-*.ts`); this dispatch authorizes neither.</NonGoal>
      <NonGoal>**No new production file family, dependency, or permanent artifact.** No new production `src/*.ts` file and no new package dependency are introduced. Owner: this bundle's contract.</NonGoal>
    </NonGoals>
    <AcceptanceCriteria>
      <AC-FOLD-RETRY-IDEMPOTENT>For each of `injectFailureBeforeVerify` and `injectFailureAfterWrite`: after the throw, the ledger contains exactly the one epoch that fold wrote; a real `bun run ngrace cursor fold` retry then exits 0, reuses that epoch, and leaves each event exactly once. **Repeated interruption is reachable on successive fresh epochs, not on one residue set** — a pure-resume retry returns before either write hook can fire again. The measured reachable form is driven independently for **each** shipped hook: interrupt a logical epoch and recover it once; allocate a fresh epoch (ids above the recorded max), interrupt it at the same hook, and recover it once; the ledger then holds exactly one copy of each logical epoch and the first epoch's bytes are unchanged. A third, empty fold is the existing no-op that returns the same last epoch with `eventCount 0`. **Partial verified deletion is resumed, not treated as conflict:** after a successful ledger write, delete the first event of the verified set in the actual delete-loop order (the shipped sequential loop at `src/grace-cursor.ts:1522-1530`, the `unlinkSync` at `:1529`), then run a real CLI retry; it exits 0, reuses the interrupted epoch, adds no new epoch and no synthesized `opened`, retains the recorded epoch exactly once, leaves no loose files, the complete `run-ledger.xml` bytes captured immediately after the injected post-write failure and before the first loose event is removed are exactly unchanged after the retry, and the recorded payloads are independently byte-identical. **Byte identity and semantic payload equivalence are distinct claims and both are asserted:** the ledger-bytes equality proves the file was not rewritten, and the `expectedLedgerEventAttributes`-plus-children comparison proves the payload is unchanged; neither is called the other. **Initial-state fixtures:** a prior completed epoch is unchanged; the interrupted epoch is retained exactly once; a conflicting payload refuses without writes; newly allocated events (ids matching no recorded event) fold normally into the next epoch. Payload preservation is asserted with independent ledger/event inspection using `expectedLedgerEventAttributes` plus complete children, never the writer's own transform. This criterion is red on the current base and green only after the repair: measured against `0ad7cb4e49b1bb5d930b27732f2978fa66221371` with the final repository tests applied, `bun test --timeout=0 src/grace-cursor.test.ts` is 218 pass / 10 fail on the base and 228 pass / 0 fail on the candidate. Both hooks leave `Epoch-1` and the real-CLI retry appends `Epoch-2`; the successive-epoch form, driven independently for **each** hook, leaves `[1,2,3,4]`; the partial-deletion retry appends `Epoch-2` = `2:terminal, 3:opened` — the duplicate of the already-recorded terminal plus a synthesized covering `opened` — and rewrites the recorded `run-ledger.xml` bytes. The measured candidate (one production file) leaves exactly one copy of each logical epoch, the partial-deletion ledger bytes exactly unchanged, and empty `run/` in every row.</AC-FOLD-RETRY-IDEMPOTENT>
      <AC-FOLD-CONFLICT-REFUSAL>A **genuine payload conflict** — a loose event whose id matches a recorded event but whose canonical payload differs under `expectedLedgerEventAttributes` plus children — makes the fold exit non-zero with a message naming the id and the mismatch, writes no new epoch, and leaves the recorded bytes byte-identical. A **payload-matching subset** of the last recorded epoch (the partial-deletion state) is **not** a conflict and must resume per `AC-FOLD-RETRY-IDEMPOTENT`. A loose event whose id matches an event in an **older** recorded epoch is refused (the allocator never reuses a recorded id). Untouched older epochs are never rewritten, and no synthesized `opened` is fabricated for an already-recorded epoch. The criterion does not infer that all partial overlaps are recoverable, nor that all are corruption. Measured against `0ad7cb4e49b1bb5d930b27732f2978fa66221371`: the base does not produce a payload-conflict refusal naming the id (a conflicting loose `opened` is consumed by the allocation check and refuses with `unterminated range for w0`), and an older-epoch id reuse folds at exit 0 into a new `Epoch-3`; the measured candidate refuses both, naming the id, with a byte-complete recursive project-tree snapshot unchanged (`treeDiff` `[]`) and the ledger and every untouched epoch byte-identical.</AC-FOLD-CONFLICT-REFUSAL>
      <AC-POST-LEDGER-BOUNDARY-PRESERVED>A fold interrupted **after** its ledger write (the `C-RUN-LEDGER` write-verify-delete window, **not** F158) may leave the written ledger and any engine-authored loose events; the refuse path removes, renumbers, and rewrites no `run/` event and no recorded epoch it did not itself write, and a subsequent successful fold recovers that evidence idempotently rather than accumulating it. The snapshot exception for post-ledger evidence is explicit in the expected post-state, so the criterion is satisfiable in the state the ceremony produces. This boundary is the RM-GOVERNED-PATH D3/`C-RUN-LEDGER` contract and is never labelled F158. Measured against `0ad7cb4e49b1bb5d930b27732f2978fa66221371`: after `injectFailureAfterWrite` the written ledger holds `Epoch-1`, and the real-CLI retry rewrites the ledger by appending `Epoch-2` (ledger bytes not preserved); the measured candidate leaves the written ledger byte-identical after the retry and after a second retry.</AC-POST-LEDGER-BOUNDARY-PRESERVED>
      <AC-NO-UNFORCED-WRITES>Activated only by the explicit switch `NGRACE_SCOPE_GUARD_CHANGE=C-FOLD-RETRY-CORE-1-000C564C`, used for this bundle's target and final verification and present in this criterion's CloseEvidence. Measured against base `0ad7cb4e49b1bb5d930b27732f2978fa66221371` in the applied-archive state, this bundle's complete tracked-and-untracked change set is within the closed allowed set: the forced source file `src/grace-cursor.ts`; the forced tests {`src/grace-cursor.test.ts`, `src/artifact/scope.test.ts`, `scripts/validate-record-retirement.test.ts`}; this bundle's active directory and its archive arrival (`.ngrace/changes/active/C-FOLD-RETRY-CORE-1-000C564C/`, `.ngrace/changes/archive/C-FOLD-RETRY-CORE-1-000C564C/`), never an archive glob; and the close's enumerated record/charter writes: **exactly the seven named XML files** under `docs/plans/active/RM-GOVERNED-PATH/`, with `decisions.md` **excluded** — neither `--flush` nor an eligible `--retire` writes the stub, and the guard admits no conditional path. There is **no predecessor supersede transition** for this fresh slug, so the allowed set contains no predecessor path. The bundle-lifecycle recognition is **guard-only, finite, and identity-exact**: the guard may recognize only this bundle's own lifecycle files under its exact id (`spec.xml`, `plan.xml`, `run-ledger.xml`, `run.xml`) and XML under its own `run/`, and that recognition is **not** permission for an arbitrary extra file inside the bundle directory — a planted non-lifecycle file under it still reddens. The later plan must **not** add this successor's own `spec.xml`, `plan.xml`, `design-context.xml`, `run/`, `run.xml`, or `run-ledger.xml` to `ObservedWriteScope`, because those are governed by identity/lifecycle rules; the non-lifecycle plan scope remains exactly the one forced source, the three forced tests, the `.ngrace/scratch/staged-findings.md` input, and the seven record XML files, with zero globs. Declared **separately, outside the guard's allowlist**, is one transient ignored input mutation: the exact `.ngrace/scratch/staged-findings.md` owner-line substitution at `:184`. The guard's git-derived observable population cannot include the ignored staged buffer, so the staged substitution is audited as a pre-flush input and is **never** a git-observable allowed write. The guard covers untracked files as well as tracked diffs, so `git diff --stat HEAD` alone is insufficient; it is dormant without its exact activation switch, and unavailable git evidence refuses rather than reporting clean. Fixtures prove (a) a clean applied archive passes; (b) a forbidden ordinary path reddens; (c) a planted file inside the governed record directory that is not one of the seven named XML files reddens; (d) a planted non-lifecycle file inside this bundle's own directory reddens; (e) unavailable git evidence refuses rather than reporting clean; and (f) without activation a later unrelated change of the same kind does not fail. Deactivation is by the switch, never by editing the archive later. No catch-all path and no glob may be added later. The guard's activation is finite: identity plus `baseCommit` never deactivates it on its own.<CloseEvidence><Command>NGRACE_SCOPE_GUARD_CHANGE=C-FOLD-RETRY-CORE-1-000C564C bun test --timeout=0 src/artifact/scope.test.ts</Command></CloseEvidence></AC-NO-UNFORCED-WRITES>
      <AC-CLOSE-PAYER>This bundle pays exactly `F313`, and no other finding. The clean base `0ad7cb4e49b1bb5d930b27732f2978fa66221371` carries **no** `F313` `bundleMinted` pin in `scripts/validate-record-retirement.test.ts`, so this bundle introduces exactly **one** entry naming `C-FOLD-RETRY-CORE-1-000C564C`; it is not described as a re-home of a clean-base pin. The successor-named charter row's `&lt;Pays&gt;` child in `registry.xml` (and `registry-retired.xml` once the row retires) carries exactly `F313`; the derived `&lt;PaidBy&gt;` stamped on the retired Finding carrier in `findings-retired.xml` names this bundle; and the flushed durable Finding body carries the successor ownership wording. The staged handoff is exactly the **one** `Owner:` line in `.ngrace/scratch/staged-findings.md` at `:184`; this bundle changes exactly that line and no other staged owner line. The payer ratchet proves absent/stale/current and staged-only directions: an absent or stale pin reds the derivation walk, only the current pin greens it, and a staged-buffer-only owner-line edit cannot green it because the test ratchets the durable XML result the flush produces, never the ignored buffer. No other finding token or pin is written by this bundle. The test never reads the ignored staged buffer. The CloseEvidence command is red in the applied-archive fixture until the durable route names this bundle and green only then.<CloseEvidence><Command>bun test --timeout=0 scripts/validate-record-retirement.test.ts</Command></CloseEvidence></AC-CLOSE-PAYER>
      <AC-CLOSE-LINT>Post-close lint of the repository root reports zero errors and zero warnings, evaluated in the applied-archive state.<CloseEvidence><Command>bun run ngrace lint --path . --fail-on warnings</Command></CloseEvidence></AC-CLOSE-LINT>
      <AC-CLOSE-TESTS>The behavior-pinning suite for the changed surface passes with no wall-clock timeout, evaluated in the applied-archive state.<CloseEvidence><Command>bun test --timeout=0 src/grace-cursor.test.ts</Command></CloseEvidence></AC-CLOSE-TESTS>
    </AcceptanceCriteria>
    <AffectedAreas>
      <M-CURSOR />
    </AffectedAreas>
    <VerificationIntent>
      <ExpectedCommand>bun test --timeout=0 src/grace-cursor.test.ts</ExpectedCommand>
      <ExpectedEvidence>Bundle-3 repository tests (`src/grace-cursor.test.ts`) plus corroborating disposable real-`.git` drives, with direct exits and recursive snapshots that record directory entries, symlinks, relative paths, and per-file bytes: (1) the two shipped interruption hooks — after the throw the ledger holds exactly the one written epoch and the loose files remain, and a real `ngrace cursor fold` retry reuses that epoch, adds no epoch and no synthesized `opened`, and empties `run/`; (2) repeated same-hook interruption/recovery on two successive fresh epochs, driven independently for **each** of the two hooks — one copy of each logical epoch and the first epoch byte-identical for each; (3) the partial verified deletion in the shipped delete-loop order, resumed through the real CLI with one retained epoch, no duplicate event, no synthesized `opened`, the complete `run-ledger.xml` bytes captured after the injected write and before the loose delete exactly unchanged, and the complete recorded payload separately preserved; (4) the initial-state controls — a prior completed epoch byte-identical, a fresh-only allocated set folding normally into the next epoch, and the third empty fold's no-op (`eventCount 0`); (5) the genuine payload conflict — refused non-zero naming the id and the mismatch, with a byte-complete recursive project tree unchanged — and the payload-matching subset that resumes instead; (6) the older-epoch id reuse refusal, non-zero naming the id and the older epoch, with the byte-complete recursive project tree unchanged; (7) the post-ledger boundary — the written ledger byte-identical after the retry and after a second retry, with no accumulated evidence. The same final test file on an untouched `0ad7cb4e49b1bb5d930b27732f2978fa66221371` base clone is red: `bun test --timeout=0 src/grace-cursor.test.ts` is 218 pass / 10 fail (the nine F313 behavioral guards plus the write-surface inventory pin), while the candidate is 228 pass / 0 fail. The base directions: both hooks leave `Epoch-1` and the retry appends `Epoch-2`; successive interruption, for each hook independently, leaves `[1,2,3,4]`; partial deletion appends `Epoch-2` with a duplicate terminal, a synthesized `opened`, and changed ledger bytes; older-epoch reuse folds into `Epoch-3` with a non-empty tree diff; the conflict refusal does not name the id (`unterminated range for w0`); the post-ledger retry rewrites the written ledger. Payload preservation is inspected independently with `expectedLedgerEventAttributes` plus complete children. The full CI-equivalent verification (`bun run validate:ci`) is the later plan's single final `MustPassCommand`, not a per-task command, and no close criterion depends on `/private/tmp` surviving.</ExpectedEvidence>
    </VerificationIntent>
    <Problem>
      **Bundle 3 of the maintainer-directed split, minted fresh from the reserved slug `C-FOLD-RETRY-CORE`.** Bundles 1 and 2 are applied and committed at this base; this bundle owns the interrupted-fold retry, the genuine conflict refusal, and the post-ledger boundary. The approved predecessor contract (`C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1`) carried the criterion bodies preserved here; the bundle is not a lineage supersede and there is no predecessor transition.

      **The measured defect (F313).** For both shipped hooks (`injectFailureBeforeVerify`, `injectFailureAfterWrite`), an interrupt leaves the ledger holding `Epoch-1` = `[1:opened, 2:terminal]` with the loose files still present. A real `ngrace cursor fold` retry reads the loose set, `nextEpochNumber` (`src/grace-cursor.ts:4026`) returns last+1, and `appendEpochToLedger` (`src/grace-cursor.ts:3451`) appends rather than replaces, so the ledger ends with the same two events in both `Epoch-1` and `Epoch-2` — a duplicate epoch violating `C-RUN-LEDGER` `AC-FOLD-ORDERING`. The existing pin (`src/grace-cursor.test.ts:212-227`) never asserts the ledger holds one epoch or that events are not duplicated, so it is green over the defect. A **partial verified deletion** in the shipped delete-loop order (`src/grace-cursor.ts:1522-1530`) is a second recovery boundary: after `injectFailureAfterWrite`, deleting the first verified event (the `opened`) and retrying makes the base append `Epoch-2` = `2:terminal, 3:opened`, duplicating the already-recorded terminal **and** synthesizing a covering `opened`. An **older-epoch id reuse** is a third: re-presenting `1:opened` and `2:terminal` after `Epoch-2` exists folds at exit 0 into `Epoch-3`. These were re-driven through the real local CLI against base `0ad7cb4e49b1bb5d930b27732f2978fa66221371` on disposable clones. `bun run ngrace` executes this checkout's `src/grace.ts`, not a published package.

      **The chosen shape, and the rejected alternatives.** A naive "not an exact match → conflict" recovery rule misclassifies a legitimate prefix-deleted subset, so the repair must compare the loose set against the durable ledger **before** loose-only closure/hole checks or `maybeAutoOpenCoveringAllocation` can run. The chosen shape adds one pre-write reconciliation in `src/grace-cursor.ts` that classifies each loose event against the recorded epochs using independent payload comparison (`expectedLedgerEventAttributes` plus complete children): a payload-matching match in the last epoch is residue and the fold resumes that epoch (deleting only the residue it re-verified, writing no new epoch, synthesizing no `opened`); a match in an older epoch or a payload mismatch refuses before any write; a match in no epoch is fresh and folds normally. The mixed residue-plus-fresh set is deliberately left to bundle 4. The smallest production shape is the single forced file `src/grace-cursor.ts`; the measured candidate is `+138` lines there. The final `src/grace-cursor.test.ts` delta is 172 insertions / 4 deletions (`git diff --numstat`): the F313 behavior block (164 lines) plus the write-surface inventory pin move (`cursorContained` 2→3, `callSites` 9→10), because the resume adds one `unlinkSync(contained.absolutePath)` delete site (the pin at `src/grace-cursor.test.ts` counts contained-path deletes). `src/artifact/xml.ts`, `src/artifact/run-membership.ts`, and `src/gates/ledger.ts` are not widened.

      **Measured red/green directions, re-driven against `0ad7cb4` on disposable clones.** The final B3 behavior block lives in the repository test file `src/grace-cursor.test.ts` (164 lines) and is applied unchanged to both clones. Base: `bun test --timeout=0 src/grace-cursor.test.ts` = 218 pass / 10 fail (the nine F313 behavioral guards plus the write-surface inventory pin); candidate: 228 pass / 0 fail; `bunx tsc --noEmit` exits 0 on both; `bun run validate:ci` exits 0 on the candidate. Direct drives: both hooks leave `Epoch-1` and the retry appends `Epoch-2` on base → `[1]` on candidate; successive same-hook interruption, driven independently for each hook, yields `[1,2,3,4]` on base → `[1,2]` with `Epoch-1` byte-identical on candidate; partial deletion leaves `[1,2]` with `opened` count 2 and rewritten ledger bytes on base → `[1]` with count 1 and the captured ledger bytes exactly unchanged on candidate; fresh-only folds into `[1,2]` on both; the genuine conflict is refused by the base with `unterminated range for w0` (no id named) and by the candidate naming the id and the payload mismatch, both with a byte-complete recursive `treeDiff` of `[]`; older-epoch reuse folds on base at exit 0 into `Epoch-3` with a non-empty `treeDiff` and is refused by the candidate naming the id and the older epoch with `treeDiff` `[]`; the post-ledger retry rewrites the written ledger on base and leaves it byte-identical through the retry and a second retry on the candidate. Byte-complete recursive snapshots (directory entries, symlinks, relative paths, types, per-file sha256) back every refusal claim. Correction to the prior report: the earlier `218 pass / 0 fail` figure was the pre-existing suite plus structural maintenance, not evidence that the repository command can fail on F313, and the earlier drive did not implement the recursive refusal snapshots; both are corrected here.
    </Problem>
    <Assumptions>
      <Assumption>Base commit `0ad7cb4e49b1bb5d930b27732f2978fa66221371` is the committed application of bundle 2 `C-LOOSE-MALFORMED-FOLD-1-4C18E876`; bundles 1 and 2 are archived, and this fresh slug has no predecessor transition. All were confirmed by listing the commit's tree and the worktree before this draft was written.</Assumption>
      <Assumption>The two later small bundles are reserved as semantic slugs only: `C-FOLD-MIXED-RECOVERY` (bundle 4) and `C-DISCARD-PREFLIGHT` (bundle 5), followed by `C-SUPERSEDE-INTEGRATION-CLOSE` (B3). Their hash suffixes are minted when authored, each after its predecessor closes and commits.</Assumption>
      <Assumption>Projected relations, not pinned figures: a resumed fold leaves exactly one epoch and an emptied `run/`; a refusal leaves the ledger and every untouched epoch byte-identical; the exact record write set for the single-token payment state is re-measured by a `--flush`/`--retire` drive at the close. The criteria assert the relations; the figures are the executor's to measure.</Assumption>
      <Assumption>The `bundleMinted` map will need exactly one entry (`F313`) naming `C-FOLD-RETRY-CORE-1-000C564C`, because the real payer invariant loop walks every derived payer token against `baseline[token] ?? bundleMinted[token]`. Measured 2026-09-21: the map holds no `F313` at this base and `F313` is not a durable record entry. Whether the fixture's exact extension lands in one edit or several is a plan measurement.</Assumption>
      <Assumption>The write-surface inventory pin at `src/grace-cursor.test.ts` counts contained-path `unlinkSync` call sites and must move by exactly one for the resume delete; the measured candidate proves the pin's new count. Whether the final bundle's exact criterion tests extend that pin in one edit is a plan measurement.</Assumption>
    </Assumptions>
  </C-FOLD-RETRY-CORE-1-000C564C>
</NgraceChangeSpec>
