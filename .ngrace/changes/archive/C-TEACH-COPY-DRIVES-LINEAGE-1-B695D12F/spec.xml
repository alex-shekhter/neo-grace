<NgraceChangeSpec graceVersion="1.0" status="applied">
  <C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F>
    <Summary>
      The fifth teaching increment of the `RM-GOVERNED-PATH` record: flush the five findings staged in
      this dispatch (`F258`–`F262`), teach five sentences the two preceding execution dispatches had
      to learn by driving, and pin every sentence with a `scripts/check-teaching-surface.ts` needle in
      both trees. **The staged findings.** Act 1 of this dispatch appended five entries to
      `.ngrace/scratch/staged-findings.md` under `F258`–`F262`, each a measurement at `dda5548`:
      `F258` (a copy drive that opens its epoch with the approved `plan.xml` uncommitted puts that path
      into every attempt's `WriteEvidence`, and after the archive move the scope audit reddens on it),
      `F259` (`cursor attempt --outcome pass` accepts and silently discards `--signature-kind` /
      `--signature-key`; the event carries no `FailureSignature`), `F260` (after `ngrace supersede` the
      successor's plan is authored from the archived predecessor's draft with the id changed where it
      is data, and no skill says so), `F261` (`ngrace-spec` `shape_sources` names only `spec new
      &lt;SLUG&gt;`; the `--supersedes` lineage path and the `supersede` stamp are unnamed), and `F262`
      (no skill says `ngrace gate verdict` on the post-close copy is the evaluator of a spec's
      `CloseEvidence`; executors hand-rolled readers that route around the verb). **The flush.** All
      five land in `docs/plans/active/RM-GOVERNED-PATH/findings.xml`
      with five index `&lt;Entry&gt;` lines, then one
      `bun ./scripts/validate-record-retirement.ts --rewrite-roots`; one live chartered row lands in
      `registry.xml` with `&lt;Pays&gt;F258 F259 F260 F261 F262&lt;/Pays&gt;` and a second
      `--rewrite-roots`. **The teaching.** Five sentences: `F258` and `F262` in `ngrace-plan` `must_do`
      row 19, `F260` in `ngrace-plan` `approved_plan_immutability`, `F259` in `ngrace-execute`
      `cursor_kinds` kind `attempt`, `F261` in `ngrace-spec` `shape_sources`. Each lands in the
      canonical `skills/ngrace/*` skill and its packaged mirror `plugins/ngrace/skills/ngrace/*`.
      **The instruments.** `scripts/check-teaching-surface.ts` gains five `TAUGHT_RULES` needles
      (68 → 73) and its suite mirrors every one with a red direction. **The payment.** The plan extends the
      payer-derivation baseline `bundleMinted` with the five tokens and adds a payment-invariant
      carrier-and-row walk. At the close the authority archives the bundle and runs `--retire`, which
      moves **six** — the five findings to `findings-retired.xml` stamped
      `&lt;PaidBy&gt;C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F&lt;/PaidBy&gt;` and the row — and flips the
      five index layers in place. `F252` stays staged; no `src/` production file changes; no lint issue
      code; no version bump; `examples/` untouched; `README.md` is written only in the skill-footprint
      measurement cell.
    </Summary>
    <Problem>
      **Five candidates measured, five staged, two dropped.** `.ngrace/scratch/staged-findings.md` holds
      six entries as of 2026-09-15 — `f252`, `f258`, `f259`, `f260`, `f261`, `f262` (measured by the
      shipped `parseFrozenMarkdown`: 6 entries, `chartered` 0, `sweep` 0, `registryHeadingIndex` -1).
      Each staged finding is a measurement taken on this dispatch; the exact commands and outputs are in
      the buffer. **`F252`** (the `---` separator in rendered bodies, 54 existing bodies, the
      parser/render trim and the flush-invariant tail walk) was staged at the close of
      `C-PAIR-AUDIT-MINT-CWD-1-ED6B7D22` and belongs to the record-schema bundle `F207`/`F221`; it is
      **not** flushed and **not** paid here. **Two candidates were not staged.** The closing task's
      post-close copy drive (a named pass-only cycle) is already covered: `ngrace-execute` rule 13
      states the pass-only cycle generically, `ngrace-plan` row 19 states the post-close copy, and the
      two bundles that recorded it did so correctly (`C-TEACH-PLAN-DRIVES-CORRECTIONS-2-1E59AEAA`
      attempt 27 and `C-EXPLAIN-ANCHOR-COMMANDS-1-FC0ED3DA` attempt 44, both `T-00x` `pass`), so no
      silence is measurable. The `D13` module-private report-state pattern is repository convention
      recorded in a ruling, not product instruction a governed skill's `SKILL.md` should carry; no
      skill is the right home and it was not staged.
      **`F258`, driven both ways at `dda5548` on a whole-repository copy.** A probe bundle's plan was
      left untracked; `ngrace cursor attempt --change C-PROBE-COPY-DRIVE-1-3FD48F0F --task T-001
      --outcome pass --path .` exited 0 and its `&lt;WriteEvidence&gt;` carried
      `.ngrace/changes/active/C-PROBE-COPY-DRIVE-1-3FD48F0F/plan.xml`. After the bundle directory was
      committed, a second attempt's `&lt;WriteEvidence&gt;` carried no `plan.xml`; after the directory was
      moved under `.ngrace/changes/archive/`, `ngrace review --path . --change
      C-PROBE-COPY-DRIVE-1-3FD48F0F` returned `Findings: 3 (errors: 2, warnings: 1, infos: 0)` with two
      `review.write-evidence-outside-scope` errors naming `plan.xml` and `spec.xml`.
      `auditWriteEvidenceOutsideScope` (`src/review/core.ts:1135`) exempts tool-owned lifecycle only
      (`run.xml`, `run-ledger.xml`, `run/**`) and `docs/plans` — never `plan.xml` or `spec.xml`.
      **`F259`, measured at `dda5548`.** `cursor attempt --outcome pass --signature-kind test
      --signature-key probe-key` exited 0 and the written event carries no `FailureSignature`
      (`grep -c FailureSignature` = 0); `recordAttempt` (`src/grace-cursor.ts:1940`) pushes the
      signature node only under `outcome === "fail" &amp;&amp; signature` (`:2000`).
      **`F260`, measured at `dda5548` over the archive.** The predecessor
      `C-TEACH-PLAN-DRIVES-CORRECTIONS-1-14457871/plan.xml` and its successor
      `C-TEACH-PLAN-DRIVES-CORRECTIONS-2-1E59AEAA/plan.xml` each carry the id 19 times; normalizing each
      id to `&lt;ID&gt;` and diffing shows the successor is the predecessor's plan with the id changed
      where it is data. `ngrace-plan` `approved_plan_immutability` names `spec new --supersedes` and
      `supersede` but not authoring from the archived draft; `ngrace-spec` is silent.
      **`F261`, measured at `dda5548` on a copy.** `spec new --supersedes C-PROBE-PRED-1-3FC34AAB
      --timestamp … --path .` minted `C-PROBE-PRED-2-C6DF5A38` with `lineage=2`, inheriting the slug;
      `spec new PROBE-X --supersedes …` refused `spec new requires exactly one of a bare &lt;SLUG&gt; or
      --supersedes &lt;C-PRED&gt;.`; `supersede --change C-PROBE-PRED-1-3FC34AAB --replacement
      C-PROBE-PRED-2-C6DF5A38 --path .` moved the bundle and stamped `status="superseded"` and
      `&lt;Replacement&gt;C-PROBE-PRED-2-C6DF5A38&lt;/Replacement&gt;` on both `spec.xml` and
      `plan.xml`. `ngrace-spec` `shape_sources` names only `spec new &lt;SLUG&gt;`.
      **`F262`, measured at `dda5548` on a whole-repository copy.** The shipped verb set (`grep -n
      defineGraceCommand src/grace.ts` and its `subCommands` map) is `apply, context, cursor, doctor,
      file, gate, graph, lint, module, plan, review, scaffold, spec, status, supersede, verification`
      — no verb is *named* for extracting a spec's `&lt;Command&gt;` text, but `ngrace gate verdict` on
      an **applied-archive** bundle already evaluates every `CloseEvidence` `Command`.
      `evaluateCloseEvidenceChildren` (`src/gates/ledger.ts:490`) runs only when `isAppliedArchiveBundle`
      holds (`:485`), spawns each command with `spawnShellCommand` (`src/artifact/assertions.ts:720`,
      `sh -lc`), and `recordReviewVerdict` refuses `pass` when any `Result` is `fail` (`:423`–`:433`).
      Driven on a probe bundle archived with `ngrace apply --force --reason`: `ngrace review --change
      C-PROBE-GATE-VERDICT-1-AAAA1111` returned two `review.close-evidence-unevaluated` errors
      (`AC-PASS-ONE` id `82ecf1f1963effd2`, `AC-PASS-TWO` id `0c35f130ac617b28`); `gate verdict
      --outcome pass --ack-finding …` exited 0 and its `&lt;Verdict&gt;` carries
      `&lt;AC-PASS-ONE&gt;&lt;Exit&gt;0&lt;/Exit&gt;&lt;Result&gt;pass&lt;/Result&gt;&lt;/AC-PASS-ONE&gt;`
      and the `AC-PASS-TWO` twin; planting `exit 3` in the copy's archived `AC-PASS-TWO`
      `CloseEvidence` made the verb refuse `CloseEvidence Command exited non-zero; refuse to record
      outcome pass.` (exit 1). The silence is not the verb — no skill says the verb is the evaluator.
      **No decision is chartered.** A token search of the live `rulings.xml` decisions for `copy`,
      `epoch`, `signature`, `supersede`, `lineage` and `CloseEvidence` finds no decision that codifies
      any of the five rules; the nearest are the `D8`/`D27` supersede-and-record rules and the `F11`
      lifecycle exemption. The teaching states shipped behaviour, not a new ruling. The bundle adds no
      decision.
      **The base commit and the freeze.** Branch `c-teach-copy-drives-lineage` at `dda5548`, tree clean.
      The record's four live roots at this commit read `&lt;Findings base="7522" headroom="274"
      ceiling="7796"&gt;`, `&lt;RecordIndex base="268" headroom="28" ceiling="296"&gt;`,
      `&lt;Registry base="12" headroom="14" ceiling="26"&gt;` and `&lt;Rulings base="1697"
      headroom="186" ceiling="1883"&gt;`.
    </Problem>
    <Goals>
      <Goal>
        **The five taught rules, at their section homes.** Each sentence and token is pinned verbatim in
        the teaching Constraint below; the plan lands exactly those bytes in the canonical skill and its
        packaged mirror together. (1) `ngrace-plan` `must_do` row 19 gains the copy-commit clause.
        (2) `ngrace-execute` `cursor_kinds` kind `attempt`'s "How to emit" line gains the pass-discard
        clause. (3) `ngrace-plan` `approved_plan_immutability` gains the archived-draft clause.
        (4) `ngrace-spec` `shape_sources` gains the lineage-path clause. (5) `ngrace-plan` `must_do`
        row 19 gains the close-evidence verdict clause. Every taught sentence is generic product
        instruction: it names no bundle, no `F`-token, no `D`-token and no governed record file name —
        the three skills are in `scripts/check-teaching-surface.ts`'s governed set, whose
        `checkRecordTokens` refuses those tokens outright.
      </Goal>
      <Goal>
        **The instruments.** `scripts/check-teaching-surface.ts` gains the five `TAUGHT_RULES` needles
        pinned in the instruments Constraint inside its existing table, which scans `GOVERNED_SKILLS`
        (`ngrace-spec`, `ngrace-plan`, `ngrace-execute`, `ngrace-reviewer`) in both `GOVERNED_TREES`.
        `scripts/check-teaching-surface.test.ts` mirrors every added needle in its local fixture table
        (so the all-tokens-green test covers them through `taughtSkillBody`) and drives a red direction
        for every added needle: dropping the token from one tree turns `checkTaughtRules` non-zero with
        a refusal naming the file, the section and the missing token; restoring the bytes returns zero.
      </Goal>
      <Goal>
        **The flush, in one wave, cut at the separator.** Land exactly five `&lt;Finding&gt;` elements
        for the anchor ids `f258`, `f259`, `f260`, `f261` and `f262` — selected by the
        `&lt;a id="…" name="…"&gt;&lt;/a&gt;` forms in the buffer, never by position — into
        `docs/plans/active/RM-GOVERNED-PATH/findings.xml` in the house `renderFinding` shape, appended
        after the last live element and before the root close tag; five index
        `&lt;Entry id="…" token="…" genre="finding" layer="live" /&gt;` lines into
        `docs/plans/active/RM-GOVERNED-PATH/decisions.xml` after the last existing entry; then one
        `bun ./scripts/validate-record-retirement.ts --rewrite-roots`, the only sanctioned writer of the
        roots. Each body is `parseFrozenMarkdown`'s `bodyRaw` cut at its trailing `---` separator (the
        separator line and the blank lines around it removed) before `xmlEscape`; all five carry a
        trailing separator because the dispatch appended them each ending in one and `f262`, the
        buffer's last entry, ends in one too. `F252` is not flushed. The decode round trip returns the
        cut staged bytes for all five.
      </Goal>
      <Goal>
        **The payment.** The plan mints one live chartered row into
        `docs/plans/active/RM-GOVERNED-PATH/registry.xml`:
        `&lt;Row name="C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F" status="live" kind="chartered"&gt;` with
        an empty `&lt;Number&gt;`, a `&lt;Charter&gt;` carrying the searched-before-minting statement in
        its two halves, `&lt;Pays&gt;F258 F259 F260 F261 F262&lt;/Pays&gt;`, and a `&lt;StatusText&gt;`
        naming no `F` token outside `Pays` and carrying no `Closed with` sentence. The row's name is not
        an archive directory until the close, so the shipped `derivePayerMap` mints nothing from it while
        the bundle is active. At the close the authority archives the bundle and runs `--retire`; the
        row's name now equals an archive directory, the payer map mints the five tokens, and the shipped
        writer moves six elements — `f258`–`f262`, each stamped
        `&lt;PaidBy&gt;C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F&lt;/PaidBy&gt;` in `findings-retired.xml`,
        and the row — while flipping the five index layers in place. The plan extends the
        payer-derivation baseline in `scripts/validate-record-retirement.test.ts` (`bundleMinted`) with
        the five tokens and adds a payment-invariant carrier-and-row walk over the five ids.
      </Goal>
      <Goal>
        **The footprint the teaching moves.** The skill-footprint pin
        `src/test-support/token-accounting.test.ts` reports the true re-measured line total and UTF-8
        byte total after the five sentences land (never written toward), and the `README.md` measurement
        cell at `README.md:304` publishes the same two figures, which the shipped test cross-checks
        against the live measurement. Sentences (1), (2) and (5) append to existing lines (line-neutral,
        byte-moving); (3) and (4) add a line each (+1), so the line total rises by two and the byte
        total by the five sentences (measured 864 → 866 lines and 77198 → 78384 bytes); the plan
        re-measures both from the tree. The pin is a transcription reconciled by re-measure, not a
        ratchet.
      </Goal>
    </Goals>
    <Constraints>
      <Constraint>
        Ceremony tier **T2** (cross-cutting: three governed skills plus their packaged mirrors, three
        `validate:ci` scripts, one test-support pin, the `README.md` measurement cell, and the record's
        findings/index/registry). Roadmap: `RM-GOVERNED-PATH` (teaching surface and record integrity).
        Binding live rulings applied: `D28` and `D28.1` (every genre carries a retirement rule and a size
        ratchet — the flush lands inside the findings window and the close's move re-opens it; figures
        live in Constraints and Assumptions and expire); `D30` (scoped, not skirted — findings raised
        while this bundle is open go to the staged buffer under the next staged number; no hard
        Constraint transcribes a scalar count of a file this bundle writes); `D31` (the index has no
        retired sibling; its eligible `Entry` layers flip in place); `D27` (nothing is approved unread;
        ratification is the maintainer's); `D11` (nothing is deferred — every out-of-scope entry is a
        named NonGoal with an owner); `D20` (the authority owns the close); `F43` (escape markup as
        character data — `&lt;`, `&gt;`, `&amp;` — and never paraphrase brackets away). This bundle runs
        no `ngrace review`, no gate, no cursor or supersede verb, and no commit: the executor authors
        this spec at `status="draft"` and stops; the approval write and the close's move are the
        authority's.
      </Constraint>
      <Constraint>
        **The five taught rules, pinned — sentence, section and token, per rule.** Each sentence lands
        inside the named section, in the skill's own voice, and the plan's check asserts the listed
        tokens. **(1) the copy drive commits the plan — `ngrace-plan` `must_do` row 19, appended:** "On
        a copy, commit the approved plan before opening the epoch: an uncommitted `plan.xml` enters every
        attempt's `WriteEvidence`, and after the close's archive move the WriteEvidence scope audit
        reddens on it." Token: `commit the approved plan`. **(2) a pass discards a supplied signature —
        `ngrace-execute` `cursor_kinds` kind `attempt`, the "How to emit" line, appended:** "A signature
        passed on a pass is discarded: `--signature-kind` and `--signature-key` are accepted on `pass`
        but the event carries no `FailureSignature`, so supply them only on `fail`." Token: `the event
        carries no`. **(3) author the successor from the archived draft — `ngrace-plan`
        `approved_plan_immutability`, appended:** "Author the successor's artifacts from the archived
        predecessor's draft, changing the id where it is data — the archived `plan.xml` is the only
        copy, and an approved plan is never edited in place." Token: `archived predecessor's draft`.
        **(4) the lineage path — `ngrace-spec` `shape_sources`, appended:** "Lineage path: `ngrace spec
        new --supersedes &lt;C-PRED&gt;` mints the successor and requires exactly one of a bare slug or
        `--supersedes`; `ngrace supersede` stamps `superseded` and `&lt;Replacement&gt;` on the spec and
        the draft plan and moves the bundle." Token: `exactly one of a bare slug`. **(5) evaluate
        close evidence with the shipped verb — `ngrace-plan` `must_do` row 19, appended:** "On the
        post-close copy, `ngrace gate verdict --change C --outcome pass` (acking the post-archive
        unevaluated findings) evaluates every `CloseEvidence` command through the shell, refuses `pass`
        on a non-zero exit, and records `Result` and `Exit` per criterion on the verdict — read them
        from the ledger, never from a hand-rolled reader." Token: `never from a hand-rolled reader`.
        What reddens a
        criterion checking each sentence: deleting the sentence (or a listed token) from one tree turns
        the new `checkTeachingSurface` needle non-zero with a refusal naming the file, the section and
        the missing token; restoring the bytes returns zero. Every token is a byte-substring of its
        sentence (`checkTaughtRules` matches with `body.includes`, no case folding). Sentences (1), (2)
        and (5) append to existing lines (line-neutral, byte-moving); (3) and (4) add a line each (+1),
        so the SKILL.md line total rises by two.
      </Constraint>
      <Constraint>
        **The instruments, pinned.** `scripts/check-teaching-surface.ts` gains exactly these five entries
        in its existing `TAUGHT_RULES` table (68 entries at this commit, 73 after this change): `{ skill:
        "ngrace-plan", section: "must_do", token: "commit the approved plan" }`, `{ skill:
        "ngrace-execute", section: "cursor_kinds", kind: "attempt", token: "the event carries no" }`,
        `{ skill: "ngrace-plan", section: "approved_plan_immutability", token: "archived predecessor's
        draft" }`, `{ skill: "ngrace-spec", section: "shape_sources", token: "exactly one of a bare
        slug" }`, and `{ skill: "ngrace-plan", section: "must_do", token: "never from a hand-rolled
        reader" }`. **The needle form handles a bullet-list home:** `section` extracts the whole
        enclosing block, so a token anywhere in `approved_plan_immutability` or `must_do` (including its
        numbered table) is found; `kind` selects a `&lt;kind id="…"&gt;` sub-block inside `cursor_kinds`
        and is used only there. `scripts/check-teaching-surface.test.ts` mirrors the same five entries in
        its local `TAUGHT_RULES` table and drives a red direction for each new needle by dropping the
        token from one tree and asserting `checkTaughtRules` returns non-zero naming the file, section
        and token. `ngrace-spec` `shape_sources`, `ngrace-plan` `approved_plan_immutability` and
        `ngrace-execute` `cursor_kinds` already carry needles; none of the five new tokens collides with
        an existing one. Every refusal path names the file, the rule and the missing token before
        exiting non-zero; check mode writes nothing.
      </Constraint>
      <Constraint>
        **Windows, measured, not assumed.** At this base commit the findings live root reads
        `base="7522" headroom="274" ceiling="7796"` (324 bodies across the findings pair); the index root
        reads `&lt;RecordIndex base="268" headroom="28" ceiling="296"&gt;`; the registry root reads
        `&lt;Registry base="12" headroom="14" ceiling="26"&gt;` (12 live rows); the rulings root reads
        `&lt;Rulings base="1697" headroom="186" ceiling="1883"&gt;` and is untouched. The five cut bodies
        render **8** (`f258`), **7** (`f259`), **7** (`f260`), **7** (`f261`) and **7** (`f262`) element
        lines by the shipped rule (`newlineCount(elementSpan) + 1` over the `renderFinding` shape),
        **36** total; uncut they render 11, 10, 10, 10 and 9 (50 total) and all five end with a `---`
        line. **Driven on a whole-repository copy** (a faithful copy of the working tree with a real
        `node_modules`, `.git` included, `.ngrace/scratch` present; the record mutated only in the copy):
        post-flush-and-`--rewrite-roots` findings `&lt;Findings base="7558" headroom="238"
        ceiling="7796"&gt;`, index `&lt;RecordIndex base="273" headroom="23" ceiling="296"&gt;`; after
        the row mint's `--rewrite-roots`, registry `&lt;Registry base="13" headroom="13"
        ceiling="26"&gt;` (13 live rows), rulings unchanged. The counterfactual, driven: without the
        separator cut the five bodies render 50 element lines and all five end with a `---` line — the
        shape the buffer header forbids. A read-back above a held ceiling stops the task and reports; it
        does not raise the ceiling and does not trim a staged body.
      </Constraint>
      <Constraint>
        **The flush's order, the mid-bundle state, and the close, driven.** The committed sequence is:
        land the five findings, land the five index `&lt;Entry&gt;` lines, mint the row live,
        `--rewrite-roots`, then validation exits 0; then the teaching and its instruments; then the close
        (statuses `applied`, the bundle's directory renamed into
        `.ngrace/changes/archive/C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F/`, the authority's `--retire`,
        validation exits 0). `--rewrite-roots` follows every hand write of a live genre root. **Driven on
        the copy:** a mid-bundle `--retire` reports `moved 0` with every root unchanged; with the
        bundle's directory in the archive, `--retire` reports **`moved 6`** (five findings and the row),
        findings `&lt;Findings base="7522" headroom="274" ceiling="7796"&gt;` (the ceiling holds, never
        rises), the index `&lt;RecordIndex base="268" headroom="28" ceiling="296"&gt;` (five layers
        flipped in place), registry back to `&lt;Registry base="12" headroom="14" ceiling="26"&gt;`;
        `findings-retired.xml` carries `&lt;PaidBy&gt;C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F&lt;/PaidBy&gt;`
        on all five moved findings; a second `--retire` reports `moved 0`; the shipped
        `validate-record-retirement.ts`, `validate-citation-anchors.ts` and
        `prove-record-preservation.ts` each return 0.
      </Constraint>
      <Constraint>
        **The row's prose law, and the searched-before-minting statement.** The shipped `derivePayerMap`
        mints payments from an archived row's `&lt;Pays&gt;` and from `Closed with` sentences in its
        `&lt;StatusText&gt;`, gated on the row's name being a directory under
        `.ngrace/changes/archive/`, first-row-wins. The row's `&lt;StatusText&gt;` therefore names no `F`
        token outside `&lt;Pays&gt;` and contains no `Closed with` sentence; `&lt;Pays&gt;` contains
        `F258`, `F259`, `F260`, `F261` and `F262` (containment, never an exact set). The row's
        `&lt;Charter&gt;` carries the searched-before-minting statement in two halves. **Tool half
        (verbatim):** `mint-search: 0 active, 0 archive prior bundle(s) share slug
        TEACH-COPY-DRIVES-LINEAGE`. **Author half (the scope search), measured at this commit:** the
        last bundle to open `ngrace-plan/SKILL.md` and `ngrace-execute/SKILL.md` is
        `C-TEACH-PLAN-DRIVES-CORRECTIONS-2-1E59AEAA` (24 archived bundles name `ngrace-plan/SKILL.md`,
        35 name `ngrace-execute/SKILL.md`), and the last to open `ngrace-spec/SKILL.md` is
        `C-EXPLAIN-ANCHOR-COMMANDS-1-FC0ED3DA` (15 name it); over the 105 archive directories and both
        registry layers no name collides with or better fits a bundle that teaches the copy-commit
        order, the pass-signature discard, the archived-draft re-homing, the supersede lineage path and
        the close-evidence extraction. The `&lt;Charter&gt;` names no `F` token.
      </Constraint>
      <Constraint>
        **Write scope, anchors, and enumeration — stated once (`F136.1`'s rule), derived from the Goals
        (`F227`'s rule).** Files the Goals force into write scope:
        `skills/ngrace/ngrace-plan/SKILL.md`, `skills/ngrace/ngrace-execute/SKILL.md`,
        `skills/ngrace/ngrace-spec/SKILL.md`, and each packaged mirror under
        `plugins/ngrace/skills/ngrace/`; `scripts/check-teaching-surface.ts`;
        `scripts/check-teaching-surface.test.ts`; `scripts/validate-record-retirement.test.ts`;
        `src/test-support/token-accounting.test.ts` (the footprint pin, re-measured); `README.md` (the
        measurement cell at `:304`, the only `README.md` write); this bundle's own artifacts under
        `.ngrace/changes/active/C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F/`; and the record files the flush
        and the close write — `findings.xml`, `decisions.xml`, `registry.xml` (flush, hand write and
        `--rewrite-roots`), `findings-retired.xml` and `registry-retired.xml` (the close's `--retire`,
        the only sanctioned writer of the retired siblings and the `PaidBy` stamp). No `src/` production
        file changes; the one `src/` write is a `.test.ts` pin, for which `isModuleOwnableWritePath` is
        false, so `change.graph-anchors-miss-write-scope` cannot fire. `scripts/` is lint-ignored
        (`.ngrace-lint.json` `ignoredDirs`), and the three skill `SKILL.md` files carry no file-local
        `LINKS:` header (skills carry none), so no module anchor is forced. The two real anchors are
        named once here and once in `AffectedAreas`, the only module list this spec carries: `M-SKILLS`
        owns the canonical skill tree and `M-TEST-SUPPORT` owns `src/test-support`, whose pin this
        deliverable re-measures. Do not mint a synthetic module, do not repeat this list as a second
        module list, do not add a lint issue code, and do not register the record files as
        `CONTEXT_ARTIFACTS`.
      </Constraint>
      <Constraint>
        **Criterion mapping, stated on its face.** Every `AC-*` below except `AC-NO-CURRENT-MODE` is
        close-bound by its own `CloseEvidence` child; a task `Satisfies` naming any of them is the
        shipped error (`change.close-evidence-and-satisfied`). Exactly one criterion —
        `AC-NO-CURRENT-MODE` — carries no `CloseEvidence` and is mapped by exactly one task `Satisfies`
        in the later plan; unmapped it is a warning, and `AC-CLOSE-LINT`'s `--fail-on warnings` turns
        that warning into an uncloseable bundle. Every `CloseEvidence` is evaluated only in the
        applied-archive state, after the close's own move has run (`F196`'s rule), so every criterion is
        written to hold there.
      </Constraint>
      <Constraint>
        **Evidence hygiene, the rules this repository teaches applied to its own criteria.** Every
        byte-invariance baseline is this spec's base commit `dda5548` — the base-less `git diff` form
        cannot fail, and a criterion whose baseline cannot fail was never a guard (`F210`'s rule). The
        window is this bundle's own diff, over tracked paths only (`F174`), probed in the post-close
        state (`F215`'s rule). The archive pathspec excludes this bundle's own archive arrival with its
        reason — the close itself renames the bundle's directory into the archive (`F188`) — and the
        record files the flush and the close do touch are covered by the parse-based relations in
        `AC-FLUSH-LANDED`, `AC-ROW-PAYS` and `AC-RECORD-VALID`, never by byte-invariance. Every
        `CloseEvidence` `Command` matches a shipped discriminating shape — `bun test` for the suites, the
        `grace-ts-lint-fail-on` shape for close-time lint, the `git-diff-exit-code` shape with the named
        base commit for every byte-invariance claim, and the configured
        `bun run ./scripts/validate-marketplace.ts` prefix for mirror sync. Structural counts come from
        the shipped parser (`parseFrozenMarkdown`, `parseGraceXmlArtifact`, `newlineCount`), never
        `grep -c`; exit codes are read directly, never through a pipe; any test the later plan adds that
        measures the real repository carries an explicit generous timeout (`F185`). Probes mutate copies,
        never the production record. A criterion is producible: it asserts no state the shipped engine
        will not produce, requires no change another criterion forbids, and names no probe that cannot be
        run.
      </Constraint>
      <Constraint>
        **Stop and re-spec rather than touching:** any `src/` production file (the only `src/` write is
        the named `src/test-support/token-accounting.test.ts` pin); the parser and render step of
        `scripts/validate-record-retirement.ts` (`parseFrozenMarkdown`, `renderFinding`,
        `renderDecision`, `derivePayerMap`, `persistSnapshot`, `--retire`, `--rewrite-roots`) and the
        existing separator-laden bodies — the cut this bundle applies is to its own five bodies' bytes
        only, and `F252`'s remedy belongs to the record-schema bundle; `src/artifact/grammar.ts`,
        `src/lint/catalog.ts`, `src/lint/core.ts`, `src/review/**`, `src/gates/**`; the other thirteen
        skills and their mirrors except the three named skills' pinned sentences; the spec and plan
        teaching templates and every other `references/` file; `examples/**`; `docs/plans/**` outside
        the record files this bundle and its close touch; `docs/plans/archive/`; tracked paths under
        `.ngrace/changes/archive/` except this bundle's own later arrival there; `CHANGELOG.md`; any
        version surface; `package.json` and `bun.lock`; `.ngrace-lint.json`; `CONTRIBUTING.md`;
        `CLAUDE.md`; `AGENTS.md`; and the staged buffer `.ngrace/scratch/staged-findings.md` (read,
        never written — in particular `F252` is neither flushed nor pruned).
      </Constraint>
    </Constraints>
    <NonGoals>
      <NonGoal>
        `examples/` unchanged; no user-visible surface there, and no polyglot evidence rebalance. Owner:
        unchanged; no user-visible surface.
      </NonGoal>
      <NonGoal>
        `F252` — the `---` separator in rendered bodies, the `parseFrozenMarkdown`/render-step trim, the
        flush-invariant tail walk, and the sweep of the existing separator-laden bodies. It is left
        staged and unpaid here; the cut this bundle applies is to its own five bodies only. Owner: the
        record-schema bundle (`F207`/`F221`).
      </NonGoal>
      <NonGoal>
        The closing task's post-close copy drive as a named rule. `ngrace-execute` rule 13 already states
        the pass-only cycle generically and `ngrace-plan` row 19 already states the post-close copy; the
        two bundles that recorded it applied rule 13 correctly, so no silence is measurable and no new
        sentence is authored. Owner: unchanged; the shipped rules suffice.
      </NonGoal>
      <NonGoal>
        The `D13` module-private report-state pattern. It is repository convention recorded in a ruling,
        not product instruction for a governed skill's `SKILL.md`; no skill is its home. Owner:
        unchanged; the ruling stands.
      </NonGoal>
      <NonGoal>
        A new CLI verb that extracts a spec's `CloseEvidence` commands. The verb exists: on the
        post-close copy `ngrace gate verdict --change C --outcome pass` evaluates every `CloseEvidence`
        command through the shell and records `Result` and `Exit` per criterion, and `F262` teaches
        that verb. Owner: unchanged; the shipped verb is the remedy and no `src/` bundle is wanted.
      </NonGoal>
      <NonGoal>
        A product version bump, a `package.json`/`bun.lock` change, a new CLI verb, a new lint issue
        code, or a grammar/schema-reference section. A version is a release act, not this bundle's;
        packaged version at authoring `6.2.0`; `validate:marketplace` enforces version sync across
        `README.md`, `openpackage.yml`, `.claude-plugin/marketplace.json` and `package.json`, and
        packaged-versus-canonical byte identity — both trees change together, so it stays green.
        `CHANGELOG.md` is generated and untouched. `README.md` is written only in the footprint
        measurement cell at `:304` (a Goal), never in a Skills Overview row — the three edited skills'
        frontmatter descriptions do not change. Owner: unchanged; release surfaces.
      </NonGoal>
      <NonGoal>
        Editing `docs/plans/**` outside the record files this bundle and its close write,
        `docs/plans/archive/`, `CLAUDE.md`, `CONTRIBUTING.md`, `AGENTS.md`, `docs/plans/README.md`,
        `package.json`, `bun.lock`, `.ngrace-lint.json`, the other thirteen skills, the teaching
        templates, or any archived bundle. Owner: unchanged surfaces; their pins stay green by
        non-edit.
      </NonGoal>
      <NonGoal>
        Writing or pruning the staged buffer, re-flushing any entry already in the record, or rendering
        any entry this spec does not pin. `F252` stays staged; pruning the five flushed entries is
        post-merge hygiene owned by the authority, after the flush PR merges. Owner: the authority.
      </NonGoal>
      <NonGoal>
        Running `ngrace review`, any gate or cursor or supersede verb, or committing: authoring this
        bundle's artifacts is the executor's act; ratification is the maintainer's; the approval write
        and the close's move are the authority's. This spec is written at `status="draft"` and the
        bundle stops there. Owner: the authority.
      </NonGoal>
    </NonGoals>
    <AcceptanceCriteria>
      <AC-TAUGHT-SENTENCES>
        The five taught rules (the sentences pinned in the teaching Constraint) sit at their named
        section homes in the three named skills, in both trees, byte-identical between canonical and
        packaged, and the extended teaching-surface check asserts the listed load-bearing tokens per
        section per tree through the five new `TAUGHT_RULES` needles. The instrument is the production
        assertions the plan adds to `scripts/check-teaching-surface.test.ts` over isolated roots plus
        this repository measured with an explicit generous timeout, and
        `bun run ./scripts/validate-marketplace.ts` for the mirror. Red direction, driven on a copy at
        this commit: deleting any one sentence (or a listed token) from one tree turns
        `bun ./scripts/check-teaching-surface.ts check` non-zero, refusing with the file, the section
        and the missing token; restoring the bytes returns zero. This criterion is invariant under the
        close: the close's ceremony edits no skill.
        <CloseEvidence>
          <Command>bun test --timeout=30000 scripts/check-teaching-surface.test.ts</Command>
        </CloseEvidence>
      </AC-TAUGHT-SENTENCES>
      <AC-FLUSH-LANDED>
        After the wave, each of `f258`, `f259`, `f260`, `f261` and `f262` exists exactly once across the
        findings pair (`findings.xml` and `findings-retired.xml`), with no fixed expectation of which
        file holds it: the element's `status` agrees with the holding file, its token and genre agree,
        and the single index `&lt;Entry&gt;` for the id has `genre` and `layer` agreeing with that
        holding file — the relation survives the close's own move, which retires the five, flips their
        index layers, and leaves every clause true. No landed body ends with a `---` separator line (a
        criterion property; the criterion does not compare a landed body to the buffer's uncut text,
        because the house-form suite never reads the buffer and the buffer moves under a live spec,
        `D30`). The wave's written findings root base equals the file's `newlineCount` after the write
        and the index base equals its live-`Entry` count, both via `--rewrite-roots`; the post-flush root
        is `&lt;Findings base="7558" headroom="238" ceiling="7796"&gt;` and the index base 273 (measured
        on a driven whole-repository copy). `F252` is not among the flushed ids. All five bodies' fragment
        citations resolve on the post-flush record; the plan's suite asserts this read-only through the
        shipped `validateStubAndIndex` entry point with a generous timeout. The instrument is the
        production assertion the plan adds to `scripts/validate-record-retirement.test.ts` in the
        flush-invariant house form. Red direction, driven on a copy: duplicating one flushed
        `&lt;Finding&gt;` reddens the at-most-once clause; flipping a flushed index layer against its
        holding file reddens the layer clause and the shipped roots tripwire.
        <CloseEvidence>
          <Command>bun test --timeout=30000 scripts/validate-record-retirement.test.ts</Command>
        </CloseEvidence>
      </AC-FLUSH-LANDED>
      <AC-ROW-PAYS>
        This bundle's registry row exists exactly once across `registry.xml` and
        `registry-retired.xml`, with no fixed expectation of which file holds it: the `Row`'s `status`
        agrees with the holding file, `kind="chartered"`, its `&lt;Pays&gt;` contains `F258`, `F259`,
        `F260`, `F261` and `F262`, and its `&lt;StatusText&gt;` names no `F` token outside
        `&lt;Pays&gt;` and carries no `Closed with` sentence. The `Pays` assertion is containment, never
        an exact set, so a later lawful retired-row append cannot expire it. The relation survives the
        row's own retirement at the close. The instrument is the production assertion the plan adds to
        `scripts/validate-record-retirement.test.ts`, parsed with the shipped parser over both registry
        layers, payment-invariant by construction. Red direction, driven on mutated copies: a second row
        with the same name, or a `StatusText` naming a token outside `Pays`, each turn the assertion
        non-zero.
        <CloseEvidence>
          <Command>bun test --timeout=30000 scripts/validate-record-retirement.test.ts</Command>
        </CloseEvidence>
      </AC-ROW-PAYS>
      <AC-RECORD-VALID>
        The record is valid at every committed point of this bundle's life and in the applied-archive
        state. Before the close: the shipped `validateRecordRetirement` returns zero findings with the
        five findings flushed and the row minted live (the row's name is not yet an archive directory, so
        the payer map mints nothing and no element is eligible); a mid-bundle `--retire` reports
        `moved 0`. At the close: the move reports six (the five findings and the row), stamps
        `&lt;PaidBy&gt;C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F&lt;/PaidBy&gt;` on each moved finding in
        the retired sibling, flips the five index `&lt;Entry&gt;` layers in place, and a second
        `--retire` reports zero. After the close: the shipped `validateRecordRetirement` returns zero
        findings; the shipped `validateStubAndIndex` returns zero findings (the flushed bodies' fragment
        citations resolve); and the shipped `proveRecordPreservation` returns zero. Both validators
        export their entry points and the plan's suite imports them and asserts zero findings read-only
        on the production record with a generous timeout. The instrument is the production assertions in
        `scripts/validate-record-retirement.test.ts` — the read-only `validateRecordRetirement` run, the
        imported `validateStubAndIndex` and `proveRecordPreservation` runs, the carrier/row walks — plus
        the payer-derivation baseline extension (`bundleMinted`) with the five tokens. Red direction,
        driven on mutated copies: a planted eligibility, a duplicated carrier, or a minted token missing
        from the extended baseline each turn the suite non-zero; the driven full close reports `moved 6`,
        validator 0, citation 0, preservation 0, second `--retire` `moved 0`.
        <CloseEvidence>
          <Command>bun test --timeout=30000 scripts/validate-record-retirement.test.ts</Command>
        </CloseEvidence>
      </AC-RECORD-VALID>
      <AC-ROOTS-HELD>
        The four live roots satisfy the shipped relations and no ceiling rose across the bundle: for
        `findings.xml`, `rulings.xml`, `registry.xml` and `decisions.xml` — `base + headroom = ceiling`
        and each `base` equals its shipped metric read back (`newlineCount` for findings and rulings, the
        live `&lt;Entry&gt;` rule for the index, the live `Row` count for registry). The relations are
        payment-invariant by construction: the only shipped writers of the roots, `persistSnapshot` and
        `--rewrite-roots`, rewrite them in exactly this form, and a ceiling holds or falls, never rises.
        Measured on the driven copy: pre-flush findings `7522 / 274 / 7796`, post-flush `7558 / 238 /
        7796`, post-close `7522 / 274 / 7796` (the ceiling held, it never rose); index `268 / 28 / 296`
        pre-flush, `273 / 23 / 296` post-flush, `268 / 28 / 296` post-close; registry `12 / 14 / 26`
        pre-mint and post-close, `13 / 13 / 26` after the row mint's `--rewrite-roots` (13 live rows);
        rulings unchanged at `1697 / 186 / 1883`. The instrument is the shipped roots-invariant tripwire
        in `scripts/validate-record-retirement.test.ts`. Red direction, driven on a copy: a root whose
        `headroom` no longer sums against its ceiling reddens the tripwire.
        <CloseEvidence>
          <Command>bun test --timeout=30000 scripts/validate-record-retirement.test.ts</Command>
        </CloseEvidence>
      </AC-ROOTS-HELD>
      <AC-MIRROR-SYNC>
        Every taught sentence is present in the canonical skill and its packaged mirror, byte-identical,
        and the version surfaces stay synchronized. The instrument is
        `bun run ./scripts/validate-marketplace.ts`, which compares each packaged path to its canonical
        source and enforces version sync; both trees change together, so it stays green.
        <CloseEvidence>
          <Command>bun run ./scripts/validate-marketplace.ts</Command>
        </CloseEvidence>
      </AC-MIRROR-SYNC>
      <AC-FOOTPRINT-PIN>
        The skill-footprint pin reports the true re-measured values after the teaching lands — the line
        total and the UTF-8 byte total from the tree, never written toward — and the `README.md`
        measurement cell at `README.md:304` publishes the same two figures, which the shipped test
        cross-checks against the live measurement. At this commit the pin reads 864 lines / 77198 bytes;
        sentences (1), (2) and (5) append to existing lines (line-neutral, byte-moving); (3) and (4)
        add a line each (+1), so the prototype measured 866 lines / 78384 bytes (the tree wins) and the plan
        re-measures both from the tree. The pin is a transcription reconciled by re-measure, not a
        ratchet: it moves to whatever the true value is and never by deletion or loosening. Its red
        direction is the shipped test's own cross-check failing when the cell and the tree disagree.
        This criterion is invariant
        under the close.
        <CloseEvidence>
          <Command>bun test --timeout=30000 src/test-support/token-accounting.test.ts</Command>
        </CloseEvidence>
      </AC-FOOTPRINT-PIN>
      <AC-NO-SRC>
        No file under `src/` or `scripts/` changes relative to this spec's base commit `dda5548` except
        the files the Goals force or conditionally permit, named here and in the command's excludes:
        `src/test-support/token-accounting.test.ts`, `scripts/check-teaching-surface.ts`,
        `scripts/check-teaching-surface.test.ts` and `scripts/validate-record-retirement.test.ts`.
        Prose and command name the same pathset. Failure: any other `src/` or `scripts/` path differs at
        close, and in particular no `src/` production file changes.
        <CloseEvidence>
          <Command>git diff --exit-code dda5548 -- src/ scripts/ ':(exclude)src/test-support/token-accounting.test.ts' ':(exclude)scripts/check-teaching-surface.ts' ':(exclude)scripts/check-teaching-surface.test.ts' ':(exclude)scripts/validate-record-retirement.test.ts'</Command>
        </CloseEvidence>
      </AC-NO-SRC>
      <AC-RECORD-PRESERVED>
        The record files the flush and the close provably do not change are byte-identical to this spec's
        base commit: `docs/plans/active/RM-GOVERNED-PATH/rulings.xml`, `rulings-retired.xml`,
        `decisions.md`, `record-inventory.json`, `plan.md`, `review.md`, `brownfield-findings.md`,
        `brownfield-run4-findings.md`, `p0-calibration-derivation.md`, `p0-cursor-derivation.md`,
        `p0-derivation.md`, `p0-report-honesty-derivation.md`, and the `sources/` subtree — driven, not
        assumed: the flush writes `findings.xml`, `decisions.xml` and the row into `registry.xml`, and
        the close writes only the findings pair, the registry pair and the index. The files the flush and
        the close do change are deliberately absent from this criterion and are covered by the
        parse-based relations in `AC-FLUSH-LANDED`, `AC-ROW-PAYS` and `AC-RECORD-VALID`. The window is
        this bundle's own diff, over tracked paths only. Probed in the post-close state.
        <CloseEvidence>
          <Command>git diff --exit-code dda5548 -- docs/plans/active/RM-GOVERNED-PATH/rulings.xml docs/plans/active/RM-GOVERNED-PATH/rulings-retired.xml docs/plans/active/RM-GOVERNED-PATH/decisions.md docs/plans/active/RM-GOVERNED-PATH/record-inventory.json docs/plans/active/RM-GOVERNED-PATH/plan.md docs/plans/active/RM-GOVERNED-PATH/review.md docs/plans/active/RM-GOVERNED-PATH/brownfield-findings.md docs/plans/active/RM-GOVERNED-PATH/brownfield-run4-findings.md docs/plans/active/RM-GOVERNED-PATH/p0-calibration-derivation.md docs/plans/active/RM-GOVERNED-PATH/p0-cursor-derivation.md docs/plans/active/RM-GOVERNED-PATH/p0-derivation.md docs/plans/active/RM-GOVERNED-PATH/p0-report-honesty-derivation.md docs/plans/active/RM-GOVERNED-PATH/sources/</Command>
        </CloseEvidence>
      </AC-RECORD-PRESERVED>
      <AC-NO-ARCHIVE-EDIT>
        Tracked paths under `docs/plans/archive/` and `.ngrace/changes/archive/` are not modified; this
        bundle's own later arrival under
        `.ngrace/changes/archive/C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F/` is excluded with its reason:
        the close itself renames this bundle's directory into the archive, and the pathspec excludes that
        arrival so the close's own rename cannot redden the criterion. The record's sanctioned writes in
        this bundle's window touch no path under either archive root. The prose claims tracked paths
        only. Probed in the post-close state.
        <CloseEvidence>
          <Command>git diff --exit-code dda5548 -- docs/plans/archive/ .ngrace/changes/archive/ ':(exclude).ngrace/changes/archive/C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F'</Command>
        </CloseEvidence>
      </AC-NO-ARCHIVE-EDIT>
      <AC-DOCS-EXAMPLES>
        Every surface this bundle does not write is byte-identical to this spec's base commit:
        `examples/`, `CLAUDE.md`, `CONTRIBUTING.md`, `AGENTS.md`, `docs/plans/README.md`,
        `docs/plans/archive/`, `package.json`, `bun.lock`, `CHANGELOG.md`, `.ngrace-lint.json`, and every
        published skill except the three `SKILL.md` files the deliverable rewrites — those are excluded
        with their reason (the teaching) and their mirror identity is asserted by `AC-MIRROR-SYNC`, while
        every `references/` file stays byte-identical and the teaching templates are not regenerated.
        `README.md` sits outside this pathset because the measurement cell is a forced write; it is
        covered by `AC-FOOTPRINT-PIN`'s cross-check. Prose and command name the same pathset.
        <CloseEvidence>
          <Command>git diff --exit-code dda5548 -- examples/ CLAUDE.md CONTRIBUTING.md AGENTS.md docs/plans/README.md docs/plans/archive/ package.json bun.lock CHANGELOG.md .ngrace-lint.json skills/ngrace/ plugins/ngrace/skills/ngrace/ ':(exclude)skills/ngrace/ngrace-plan/SKILL.md' ':(exclude)skills/ngrace/ngrace-execute/SKILL.md' ':(exclude)skills/ngrace/ngrace-spec/SKILL.md' ':(exclude)plugins/ngrace/skills/ngrace/ngrace-plan/SKILL.md' ':(exclude)plugins/ngrace/skills/ngrace/ngrace-execute/SKILL.md' ':(exclude)plugins/ngrace/skills/ngrace/ngrace-spec/SKILL.md'</Command>
        </CloseEvidence>
      </AC-DOCS-EXAMPLES>
      <AC-NO-CURRENT-MODE>
        The later plan's task `Verification` commands and `TargetAssertions` contain no current-mode lint
        of this project root, checked by hand (the shipped detector misses the house
        `bun ./src/grace.ts lint` form). Failure: a `Command` whose text contains `--assertions current`,
        or that invokes lint of this root and omits `--assertions`. This is the spec's single
        `Satisfies`-mapped criterion; it carries no `CloseEvidence` and must be mapped by exactly one
        task `Satisfies` in the later plan.
      </AC-NO-CURRENT-MODE>
      <AC-CLOSE-LINT>
        Post-archive lint 0/0. This is the only place current-mode lint of this root appears.
        <CloseEvidence>
          <Command>bun ./src/grace.ts lint --path . --fail-on warnings</Command>
        </CloseEvidence>
      </AC-CLOSE-LINT>
    </AcceptanceCriteria>
    <AffectedAreas>
      <M-SKILLS />
      <M-TEST-SUPPORT />
      No module-ownable path is written. The one `src/` write is
      `src/test-support/token-accounting.test.ts`, a `.test.ts` file that `isModuleOwnableWritePath`
      does not own; `scripts/` is lint-ignored; the three skill `SKILL.md` files carry no file-local
      `LINKS:` header; the record files and `README.md` are owned by no module in the graph. The two
      real anchors the deliverable touches are named once here and once in the write-scope `Constraint`:
      `M-SKILLS` owns the canonical skill tree the three skills live in, and `M-TEST-SUPPORT` owns
      `src/test-support`, whose pin this deliverable re-measures. The module enumeration is these two
      anchors and no more; do not mint a synthetic module.
    </AffectedAreas>
    <VerificationIntent>
      <ExpectedCommand>bun test --timeout=30000 scripts/check-teaching-surface.test.ts</ExpectedCommand>
      <ExpectedCommand>bun test --timeout=30000 scripts/validate-record-retirement.test.ts</ExpectedCommand>
      <ExpectedCommand>bun test --timeout=30000 src/test-support/token-accounting.test.ts</ExpectedCommand>
      <ExpectedCommand>bun ./scripts/check-teaching-surface.ts check</ExpectedCommand>
      <ExpectedCommand>bun ./scripts/validate-record-retirement.ts docs/plans/active/RM-GOVERNED-PATH</ExpectedCommand>
      <ExpectedCommand>bun ./scripts/validate-citation-anchors.ts</ExpectedCommand>
      <ExpectedCommand>bun ./scripts/prove-record-preservation.ts</ExpectedCommand>
      <ExpectedCommand>bun run ./scripts/validate-marketplace.ts</ExpectedCommand>
      <ExpectedCommand>bun run validate:ci</ExpectedCommand>
      <ExpectedCommand>bun ./src/grace.ts lint --path .</ExpectedCommand>
      <ExpectedEvidence>
        Integration (T2): the five staged findings are in the record with their index entries, the three
        skills carry the five rules at their named section homes in both trees, every added
        teaching-surface needle reddens on a planted deletion and returns zero on restore, the row is
        minted live and validates clean before the close, the mid-bundle `--retire` reports `moved 0`,
        the close's move reports six and flips the five index layers, the applied-archive record
        validates clean with the citation validator and the preservation proof, the payer-derivation
        baseline carries the five minted tokens, the footprint pin and README cell agree with the live
        measurement, and every `CloseEvidence` command and the full `validate:ci` run green inside a
        faithful copy of the post-close state with both directions driven for every criterion and every
        exit code read directly. Structural counts come from the shipped parser, never `grep -c`; the
        record's copy-drive runs from inside a whole-repository copy. The reds are ledger-recorded
        fail/pass pairs, not claims.
      </ExpectedEvidence>
    </VerificationIntent>
    <Assumptions>
      <Assumption>
        **Base commit `dda5548` (inherited by this bundle's plan); authored at `dda5548`, branch
        `c-teach-copy-drives-lineage`, tree clean, 2026-09-15.** `bun ./src/grace.ts lint --path .` exits
        0 at 0/0 with this spec in the tree; `bun run validate:ci` exits 0. Every count here expires; the
        implementation re-measures (`D30`).
      </Assumption>
      <Assumption>
        **The mint, verbatim.** The verb printed
        `mint: C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F slug=TEACH-COPY-DRIVES-LINEAGE lineage=1
        hash=B695D12F branch=c-teach-copy-drives-lineage timestamp=2026-09-15T20:23:56.000Z` and
        `mint-search: 0 active, 0 archive prior bundle(s) share slug TEACH-COPY-DRIVES-LINEAGE`. The path
        printed is `.ngrace/changes/active/C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F/spec.xml`. The id was
        never hand-typed and the clock was handed in with `--timestamp`. The tool's collision half is the
        `mint-search:` line; the author's scope half is the row-Constraint search.
      </Assumption>
      <Assumption>
        **The buffer, parsed not read, after Act 1's staging.** `.ngrace/scratch/staged-findings.md`
        holds exactly six entries as of 2026-09-15 — `f252`, `f258`, `f259`, `f260`, `f261`, `f262`, all
        findings genre on `###` headings, anchors adjacent to their headings, no staged charter row and
        no registry heading (measured by the shipped `parseFrozenMarkdown`: 6 entries, `chartered` 0,
        `sweep` 0, `registryHeadingIndex` -1). `F258`–`F262` are this dispatch's Act-1 staging; `F252` is
        the record-schema bundle's. The buffer is untracked and `.gitignore`d, one copy; nothing here
        writes it.
      </Assumption>
      <Assumption>
        **The cut bodies, measured both ways.** `parseFrozenMarkdown` returns all five staged bodies
        ending with a blank line, a `---` line and a blank line (the section separator) — `f262` too,
        because Act 1 appended a trailing `---`. Cut at the separator: `f258` renders an 8-element body,
        `f259`–`f262` 7 each, **36** element lines total. Uncut: 11, 10, 10, 10 and 9 — **50** total —
        and all five end with `---`. The buffer's `f252` body ends with `---` as before.
      </Assumption>
      <Assumption>
        **Windows, by the shipped parser over the real record.** findings `7522 / 274 / 7796` with 324
        bodies across the pair; rulings `1697 / 186 / 1883`; index `268 / 28 / 296` with 268 live `Entry`
        lines; registry `12 / 14 / 26` with 12 live rows. Every live root satisfies the tripwire
        relations at this commit.
      </Assumption>
      <Assumption>
        **The driven flush and close, on a whole-repository copy.** A faithful copy (`rsync -a` of the
        working tree, `.git` included, a real `node_modules` directory, `.ngrace/scratch` present; the
        record mutated only in the copy). The five cut `&lt;Finding&gt;` elements and five index
        `&lt;Entry&gt;` lines landed, then `--rewrite-roots`: findings `7558 / 238 / 7796`, index
        `273 / 23 / 296`, registry `13 / 13 / 26` after the row mint, rulings unchanged; the shipped
        validator returns zero findings and a mid-bundle `--retire` reports `moved 0` with every root
        unchanged. The bundle directory moved into the archive and `--retire` run from inside the copy:
        **`moved 6`**; findings `7522 / 274 / 7796`, index `268 / 28 / 296`, registry back to
        `12 / 14 / 26`; validator 0; citation 0; preservation 0; a second `--retire` `moved 0`. The five
        retired carriers carry `&lt;PaidBy&gt;C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F&lt;/PaidBy&gt;`.
      </Assumption>
      <Assumption>
        **The five staging measurements, re-stated.** `F258`: uncommitted `plan.xml` in a copy attempt's
        `WriteEvidence`, committed plan absent, and two `review.write-evidence-outside-scope` errors
        after the archive move. `F259`: a pass attempt with `--signature-kind`/`--signature-key` exits 0
        and carries no `FailureSignature`. `F260`: the predecessor and successor plans each carry the id
        19 times and the successor is the predecessor's draft with the id changed where it is data.
        `F261`: `spec new --supersedes` mints the lineage successor, `spec new &lt;SLUG&gt; --supersedes`
        refuses, and `supersede` stamps `superseded` + `&lt;Replacement&gt;` on the spec and the draft
        plan. `F262`: the shipped verb set has no *named* extraction verb, but `ngrace gate verdict` on
        an applied-archive bundle evaluates every `CloseEvidence` command through the shell, refuses
        `pass` on a non-zero exit, and records `Result` and `Exit` per criterion — the silence is that
        no skill says so, and `F262` now teaches the verb. Each is recorded in the buffer with its
        command and output.
      </Assumption>
      <Assumption>
        **Two candidates dropped, with reasons, and no decision chartered.** The closing task's post-close
        copy drive is covered by rule 13 + row 19 (the two recording bundles applied rule 13 correctly,
        attempts 27 and 44), and the `D13` module-private report-state pattern is repository convention,
        not a skill's product instruction — neither staged. A token search of the live `rulings.xml` for
        `copy`, `epoch`, `signature`, `supersede`, `lineage` and `CloseEvidence` finds no decision that
        codifies any of the five rules. This bundle adds no decision.
      </Assumption>
      <Assumption>
        **Name search, measured 2026-09-15 at `dda5548`.** Over the 105 archive directories and both
        registry layers: the last bundle to open `ngrace-plan/SKILL.md` and `ngrace-execute/SKILL.md` is
        `C-TEACH-PLAN-DRIVES-CORRECTIONS-2-1E59AEAA` (24 and 35 archived bundles name them), and the last
        to open `ngrace-spec/SKILL.md` is `C-EXPLAIN-ANCHOR-COMMANDS-1-FC0ED3DA` (15 name it). Command:
        `grep -l 'ngrace-plan/SKILL\.md' .ngrace/changes/archive/*/spec.xml
        .ngrace/changes/archive/*/plan.xml | xargs -n1 dirname | sort -u | wc -l` → 24 (the
        `ngrace-execute` twin → 35, the `ngrace-spec` twin → 15). No colliding or better-fitting prior
        name.
      </Assumption>
      <Assumption>
        **Payer surface, measured.** The shipped `derivePayerMap` over the real archive and both registry
        layers names none of `F258`–`F262` at this commit. Mid-bundle `--retire` on the driven copy
        reports `moved 0`. The `bundleMinted` extension in
        `scripts/validate-record-retirement.test.ts` is the house form for a new payer, consulted only
        for tokens the derivation actually mints, so the walk is green with the row live and green in the
        applied-archive state.
      </Assumption>
      <Assumption>
        **Instrument state and footprint.** `scripts/check-teaching-surface.ts` and
        `scripts/check-teaching-surface.test.ts` each hold a 68-entry `TAUGHT_RULES` table (73 after this
        change); the three named skills are `GOVERNED_SKILLS` members and their target sections already
        carry needles that none of the five new tokens collides with (`shape_sources` carries `rewritten
        wholesale`, `relative to this skill's directory`, `codes are namespaced`; `approved_plan_immutability`
        carries `--supersedes`; `cursor_kinds` kind `attempt` carries `report evidence`, `interleaves
        them`, `task that owns the surface`, `attributes to it`, `the intended task`). At this commit the
        footprint pin reads 864 lines / 77198 bytes; sentences (1), (2) and (5) append to existing
        lines (line-neutral, byte-moving); (3) and (4) add a line each (+1), so the measured totals
        are 866 lines / 78384 bytes and the plan re-measures them from the tree.
      </Assumption>
      <Assumption>
        **Versions and packaging, decided the way the teaching-surface precedents decided them.** No
        product version bump: a version is a release act, not this bundle's. Packaged version at
        authoring `6.2.0`; `validate:marketplace` enforces version sync across `README.md`,
        `openpackage.yml`, `.claude-plugin/marketplace.json` and `package.json`, and
        packaged-versus-canonical byte identity — both trees change together, so it stays green.
        `CHANGELOG.md` is generated and untouched; `README.md` is written only in the footprint cell;
        `examples/` is untouched.
      </Assumption>
    </Assumptions>
  </C-TEACH-COPY-DRIVES-LINEAGE-1-B695D12F>
</NgraceChangeSpec>
