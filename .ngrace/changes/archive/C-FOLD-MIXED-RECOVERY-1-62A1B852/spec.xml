<NgraceChangeSpec graceVersion="1.0" status="applied">
  <C-FOLD-MIXED-RECOVERY-1-62A1B852>
    <Summary>
      **Bundle 4 of five ordered small successors of the superseded `C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1`, minted fresh from the reserved semantic slug `C-FOLD-MIXED-RECOVERY`.** Bundles 1 (`C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5`, vanished-entry membership), 2 (`C-LOOSE-MALFORMED-FOLD-1-4C18E876`, unparseable loose input at both write boundaries), and 3 (`C-FOLD-RETRY-CORE-1-000C564C`, interrupted-fold retry idempotence and the post-ledger boundary) are applied and committed at this bundle's base `8d08255775b036b63ef1c74c2ec47ea1fc593d0c`. This bundle — **mixed residue-plus-fresh recovery ordered validate-first/destroy-last, with exact F and F union A stage post-states** — owns exactly the three ordinary criteria `AC-FOLD-MIXED-RECOVERY`, `AC-MIXED-RECOVERY-VALIDATE-FIRST`, and `AC-RECOVERY-HOOKS-CORRECT`. Ceremony tier **T1**. The affected module is exactly `M-CURSOR`; the later plan's `DurableScope` carries `V-M-CURSOR` and its non-lifecycle `ObservedWriteScope` is exactly `src/grace-cursor.ts`, `src/grace-cursor.test.ts`, and `src/artifact/scope.test.ts`, zero globs. This bundle **pays no finding**: the payment set is empty, there is no `bundleMinted` pin, no staged-finding owner handoff, no `--flush`/`--retire` ceremony, and no RM-GOVERNED-PATH record XML or `.ngrace/scratch/staged-findings.md` write. The later small bundle `C-DISCARD-PREFLIGHT` (bundle 5) and the already-reserved `C-SUPERSEDE-INTEGRATION-CLOSE` (B3) each follow only after its predecessor is closed and committed; there is **no predecessor supersede transition** for this fresh slug. The `mint-search` line printed at mint is carried verbatim as governance data: `mint-search: 0 active, 0 archive prior bundle(s) share slug FOLD-MIXED-RECOVERY`.
    </Summary>
    <Goals>
      <Goal>
        **A mixed residue-plus-fresh set recovers validate-first and destroys residue last.** The reachable mixed set is the interrupted epoch's matching residue (loose events whose id and canonical payload match an event already recorded in the last epoch) plus a normally allocated fresh set **F** whose ids are above the durable maximum. Every residue/conflict check and every fresh foldability check (allocation, density, closer) is completed **before** any destructive action: an invalid fresh set refuses with the ledger and every loose byte unchanged and no synthesized `opened` materialized, and only after all pre-mutation validation succeeds is the already-recorded residue deleted, the legitimate covering `opened` singleton **A** materialized when **F** requires it, the new epoch written containing exactly **F union A**, and the fresh loose files deleted.
      </Goal>
      <Goal>
        **The two interrupted mixed-recovery stages ship as test-only hooks with exact post-states.** `injectFailureAfterResidueDelete` fires after the already-recorded residue is deleted and before any auto-open, leaving the ledger holding only `Epoch-1` and loose state exactly **F**. `injectFailureAfterAutoOpen` fires after the covering `opened` is materialized and before the ledger write, leaving the ledger holding only `Epoch-1` and loose state exactly **F union A**. A real-CLI retry from either post-state writes `Epoch-2` once, synthesizes no second **A**, and empties `run/`. Both original bundle-3 hooks are re-driven against the resulting epoch so this bundle does not regress bundle 3's one-epoch idempotence.
      </Goal>
      <Goal>
        **The empty-payment close is executable and its guard forbids record and scratch writes.** The bundle's payment set is empty and stated as such; the close runs no `--flush` and no `--retire` and writes no RM-GOVERNED-PATH record XML and no `.ngrace/scratch/staged-findings.md`. An executable close-time discriminator in the forced test `src/artifact/scope.test.ts` covers both tracked and untracked changes against the named base commit with a closed allowed set of exactly the one forced source and the two forced tests, recognizes only this bundle's own lifecycle identities, and reddens on a planted ordinary file, a planted record-directory file, and a planted non-lifecycle file inside this bundle's own directory, and the close rehearsal separately records the pre-close digest of the ignored `.ngrace/scratch/staged-findings.md` buffer and requires it byte-identical after the close, because `git ls-files --others --exclude-standard` cannot report that ignored path.
      </Goal>
    </Goals>
    <Constraints>
      <Constraint>
        **Tier and base.** Ceremony tier **T1** (module change in one package). Roadmap `RM-GOVERNED-PATH`. Base commit `8d08255775b036b63ef1c74c2ec47ea1fc593d0c` — the committed application of bundle 3 `C-FOLD-RETRY-CORE-1-000C564C`. The full hash is the baseline for every byte-invariance relation and the scope-guard base. Every measurement in this artifact is dated 2026-09-22 and was reproduced against this base in disposal clones; counts expire when a bundle archives, so the close re-measures. There is **no predecessor supersede transition** for this fresh bundle.
      </Constraint>
      <Constraint>
        **Modules, kept in agreement.** `AffectedAreas` names exactly `M-CURSOR`. The `Ownership of forced files` constraint pairs each forced path with its owner from that same set; the two are kept in agreement (F136.1), and the later plan's `DurableScope` carries the exact verification anchor `V-M-CURSOR`. While this artifact is **draft**, either listing may be corrected in place (`RM-GOVERNED-PATH D27.1`); once approved the artifact is immutable and a change that requires a new contract is made by `ngrace supersede` and a replacement bundle, never by editing the approved bytes (`RM-GOVERNED-PATH D27`).
      </Constraint>
      <Constraint>
        **Ownership of forced files.** The one forced module-ownable source file is `src/grace-cursor.ts` (`LINKS: M-CURSOR`; this bundle owns the mixed-residue reconciliation, the pre-mutation validation of the fresh set, the residue delete and auto-open stages, and the two new test-only stage hooks). The forced behavior test `src/grace-cursor.test.ts` and the close-time guard `src/artifact/scope.test.ts` are not module-ownable and carry no module anchor; they are in the write scope because the deliverable forces them. `src/artifact/run-membership.ts`, `src/artifact/xml.ts`, and `src/gates/ledger.ts` are **not** forced and must not be edited. Do not require a file while excluding its owner, and do not invent a module or a `LINKS:` header to place one.
      </Constraint>
      <Constraint>
        **Validate first, destroy last.** The mixed path validates **F** in full before any destructive action. It must not delete the residue, materialize **A**, or write the ledger until the fresh set's prospective allocation, density, and closer requirements have all passed. The prospective allocation for a fresh set carrying no explicit allocation is the covering allocation a single-controller auto-open **would** synthesize; it is computed in memory for validation and is materialized only after the residue delete. Multi-worker fresh sets refuse before any mutation, exactly as the shipped auto-open refuses them.
      </Constraint>
      <Constraint>
        **The measured fixture, named concretely, and reached through the product path.** The durable ledger initially holds `Epoch-1` = `{1:opened w0[1,2], 2:terminal}`. The fixture is not manufactured: the initial loose epoch `{1:opened, 2:terminal}` is folded and interrupted after the ledger write (`Epoch-1` written, both loose files remain), the first verified loose event in the shipped delete-loop order (`1:opened`) is deleted to leave the matching residue `{2:terminal}`, and **F** is written by `ngrace cursor advance` (the real allocator), so its ids are above the durable maximum by construction; no direct fixture write stands in for the residue or **F**. The fresh set is **F** = `{3:progress, 4:terminal}`; the invalid fresh set is **F_bad** = `{3:progress}`; the synthesized covering singleton is **A** = `{5:opened w0[3,103]}` (id 5 is the allocator's next id after the durable maximum and the fresh maximum; the bounds run from the fresh minimum 3 to the allocator headroom). Measured on the candidate against this base: the success path writes `Epoch-2` = `{3:progress, 4:terminal, 5:opened}` with allocation `w0[3,103]`; the `injectFailureAfterResidueDelete` post-state is loose `{3:progress, 4:terminal}`; the `injectFailureAfterAutoOpen` post-state is loose `{3:progress, 4:terminal, 5:opened}`; the **F_bad** refusal is `unterminated range for w0`. These figures are the plan's measured evidence; a criterion states the relation and not the projected figure.
      </Constraint>
      <Constraint>
        **Payment set is empty; no record or scratch write.** This bundle pays no finding, and no other. It introduces no `bundleMinted` pin in `scripts/validate-record-retirement.test.ts`, rewrites no `Owner:` line in `.ngrace/scratch/staged-findings.md`, adds no successor-named `registry.xml` charter row, stamps no derived `PaidBy`, and runs neither `--flush` nor `--retire`. The record files under `docs/plans/active/RM-GOVERNED-PATH/` are **outside** the bundle's write scope and **outside** the scope guard's allowed set, so a planted record write is a tracked diff the guard reddens on. The ignored staged buffer `.ngrace/scratch/staged-findings.md` is untracked and invisible to `git ls-files --others --exclude-standard`, so it is excluded from the allowed set and the close rehearsal records its pre-close bytes and requires them byte-identical after the close, rather than silently admitting a write. There is no predecessor supersede transition for this fresh slug.
      </Constraint>
      <Constraint>
        **Ordered remainder, no invented ids.** The later small bundle is reserved as the fresh semantic slug `C-DISCARD-PREFLIGHT` (bundle 5) and the already-reserved B3 slug `C-SUPERSEDE-INTEGRATION-CLOSE` follows all five; each hash suffix is minted when it is authored, only after its predecessor is closed and committed. The collision/name search is re-run at each actual mint; the search recorded here was run at this mint and is evidence for this draft only.
      </Constraint>
      <Constraint>
        **No new dependency, production file family, or permanent artifact.** No new production `src/*.ts` file, no new package dependency, and no permanent file family under `run/`. `listLooseEvents`, `listRunOrphans`, `listLedgerEvents`, and `listAccountingEvents` keep their current public contracts and ordering; the two new hook options are test-only and unreachable from the CLI, matching the shipped `injectFailure*` hooks. The close-time write guard lives in the existing forced test file `src/artifact/scope.test.ts`.
      </Constraint>
      <Constraint>
        **Discriminating evidence for every guard, from an isolated baseline where needed.** Every guard is driven in both directions: a passing control proves it can pass, a planted or injected violation proves it can redden. Refusal byte-invariance is asserted with a recursive snapshot that records directory entries, symlinks, relative paths, and per-file bytes. Exit codes are read directly, never through a pipe. No failed import, compilation error, timeout, or failed instrument counts as the intended red. Any `bun test` close command carries `--timeout=0`; no per-test number is pinned.
      </Constraint>
      <Constraint>
        **Escape markup as character data.** Where this artifact must name a tag, attribute form, or angle-bracketed token, the entity form is used (`&lt;`, `&gt;`, `&amp;`); brackets are never paraphrased away, and markup-byte assertions belong in a TypeScript test (F43).
      </Constraint>
      <Constraint>
        **The full CI-equivalent verification is close-time only.** `bun run validate:ci` is the later plan's single final `MustPassCommand`, and the plan must rehearse the applied/archive close on a faithful clone before the production close. The narrow `CloseEvidence` commands stay tied to their own subjects; a whole-suite command is not attached to a criterion that is not about the whole suite. Because this bundle pays no finding, the close rehearsal proves the three listed close commands can pass without any `--flush`/`--retire` writer run and that the record XML and the staged buffer are byte-identical across the close.
      </Constraint>
    </Constraints>
    <NonGoals>
      <NonGoal>**The deterministic discarded preflight and F158 are bundle 5's contract.** The allocation/duplicate-id/range-hole refusal before the engine-authored `discarded` write, the bounded competing-writer analogue, and the F158 attribution are owned by the reserved bundle `C-DISCARD-PREFLIGHT` (`AC-DISCARD-NOT-WRITTEN-ON-PREWRITE-REFUSE`, `AC-F158-DEFECT-CLOSED`, `AC-DISCARD-PREFLIGHT-NO-ALLOC`) and do not appear as `AcceptanceCriteria` children in this spec.</NonGoal>
      <NonGoal>**B3's integrated supersede matrix and governed close are not carried here.** The integrated end-to-end supersede success/refusal/post-fold matrices, the allocator control, and B3's own finite close-time scope-guard fixtures are owned by the reserved `C-SUPERSEDE-INTEGRATION-CLOSE`, authored only after the five small bundles close and commit.</NonGoal>
      <NonGoal>**Bundle 3's contract is not re-paid.** Interrupted-fold retry idempotence, genuine payload-conflict refusal, older-epoch id reuse refusal, partial verified deletion, and the post-ledger `C-RUN-LEDGER` write-verify-delete boundary are preserved exactly and keep their tests green. This bundle adds the mixed residue-plus-fresh ordering and does not duplicate `AC-FOLD-RETRY-IDEMPOTENT`, `AC-FOLD-CONFLICT-REFUSAL`, or `AC-POST-LEDGER-BOUNDARY-PRESERVED`. The post-ledger boundary is never relabelled F158.</NonGoal>
      <NonGoal>**Loose membership and malformed-input behavior closed by bundles 1 and 2 are unchanged.** The vanished-entry tolerance (`C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5`) and the unparseable loose-input refusal at both write boundaries (`C-LOOSE-MALFORMED-FOLD-1-4C18E876`) are preserved exactly; this bundle adds no membership or malformed-input behavior.</NonGoal>
      <NonGoal>**`src/artifact/run-membership.ts`, `src/artifact/xml.ts`, and `src/gates/ledger.ts` are not changed.** Owner: unchanged; the mixed-recovery ordering lives in the fold module that owns the ledger write, the shared reader already parses, and the supersede ledger writer already calls the discard path.</NonGoal>
      <NonGoal>**`README.md` is unchanged.** Owner: unchanged; no user-visible surface. The mixed-recovery ordering and the validate-first refusal are visible only through an existing fold recovery/refuse outcome, and no documented outcome changes; no stale sentence was identified.</NonGoal>
      <NonGoal>**`examples/` is unchanged.** Owner: unchanged. Examples invoke `status`, `cursor fold`, and `gate archive`, but none exercises a mixed residue-plus-fresh recovery, a validate-first refusal, or either new hook post-state, so no expected output changes.</NonGoal>
      <NonGoal>**`CHANGELOG.md` is not hand-edited; no version bump or release act.** Owner: release automation (`scripts/release-*.ts`); this dispatch authorizes neither.</NonGoal>
      <NonGoal>**No new production file family, dependency, permanent artifact, or roadmap edit.** No new production `src/*.ts` file and no new package dependency are introduced, and no `docs/plans/README.md` or roadmap file is touched. Owner: this bundle's contract.</NonGoal>
    </NonGoals>
    <AcceptanceCriteria>
      <AC-FOLD-MIXED-RECOVERY>The reachable **mixed set** — the interrupted epoch's matching residue (loose events whose id and canonical payload match an event already recorded in the last epoch) **plus** a normally allocated fresh set **F** whose ids are above the durable maximum — is driven end to end, and its ordering is normative: **validate first, destroy last**. (1) Every residue/conflict check and every fresh foldability check (allocation, density, closer) completes **before any destructive action**. (2) On a **pre-mutation validation refusal** the invocation writes nothing, materializes no auto-open, deletes nothing, and preserves every loose byte including the residue and the invalid fresh set; an **injected interruption after an authorized mutation** preserves the exact mutation already made — **F** after the residue delete, **F union A** after auto-open materialization, and the written ledger plus remaining loose state after the ledger write. (3) On success: delete the matched residue (already durable), materialize the legitimate synthesized `opened` singleton **A** only when **F** requires it, write the new epoch containing exactly **F union A**, then delete the fresh loose files. The measured fixture is named concretely in `Constraints`: ledger `Epoch-1` = `{1:opened w0[1,2], 2:terminal}`, residue `{2:terminal}`, **F** = `{3:progress, 4:terminal}`, **A** = `{5:opened w0[3,103]}`. The fixture is reached through the product path — the initial loose epoch is interrupted after its ledger write, the residue is the survivor of the shipped delete-loop's first delete, and **F** is allocated by `ngrace cursor advance` — so the reachable mixed set is driven end to end rather than manufactured. Fixture state table (concrete ids/kinds are the plan's measured evidence):
        | stage | ledger | loose bytes | outcome |
        | initial after interrupt | Epoch-1 {1,2} | residue {2} | — |
        | plus fresh set | Epoch-1 (prior events) | residue union F | — |
        | invalid fresh (**F_bad**) | Epoch-1 unchanged | residue union F_bad preserved | refuse, no writes, no auto-open |
        | successful recovery | Epoch-1 unchanged, Epoch-2 (**F union A**) | empty | residue deleted, **A** materialized then folded, **F** folded |
        | interrupt after residue delete, before auto-open | Epoch-1 unchanged | **F** | retry validates **F**, then materializes **A** and writes Epoch-2 (**F union A**) |
        | interrupt after auto-open, before new ledger write | Epoch-1 unchanged | **F union A** | retry recognizes the generated **A** and folds Epoch-2 (**F union A**) exactly once |
        | interrupt after new ledger write | Epoch-1 unchanged, Epoch-2 (**F union A**) | **F union A** | retry sees **F union A** as matching Epoch-2 residue, deletes it, writes no new epoch |
        | retry | Epoch-1 unchanged, Epoch-2 (**F union A**) | empty | idempotent |
        Assert across the table: no already-recorded event is duplicated; no `opened` is synthesized for the already-recorded epoch (only for the fresh set); every older epoch is byte-identical; next-epoch numbering is correct; and the residue is never left behind after `Epoch-2` is written. A genuine conflicting-payload control is separately refused per bundle 3's `AC-FOLD-CONFLICT-REFUSAL`. **Measured red on the base `8d08255775b036b63ef1c74c2ec47ea1fc593d0c`:** the real CLI `cursor fold` on the mixed fixture exits **0** and writes `Epoch-2` = `{2:terminal, 3:progress, 4:terminal, 5:opened}` with allocation `w0[2,103]`, so the residue event id 2 is recorded a second time; the same drive on the measured candidate exits 0 and writes `Epoch-2` = `{3:progress, 4:terminal, 5:opened}` with allocation `w0[3,103]`, leaving `Epoch-1` byte-identical and `run/` empty.</AC-FOLD-MIXED-RECOVERY>
      <AC-MIXED-RECOVERY-VALIDATE-FIRST>A **progress-only fresh set** beside a matching residue refuses `unterminated range for w0` while the ledger bytes and every loose byte are byte-identical and **no** synthesized `opened` file exists, proving no delete and no auto-open occurred before validation. The measured fixture is residue `{2:terminal}` plus **F_bad** = `{3:progress}`: the refuse path must name `unterminated range for w0`, leave `run-ledger.xml` holding only `Epoch-1`, and leave `run/` holding exactly `2-T-001-terminal.xml` and `3-T-001-progress.xml`. **Measured on the base:** the real CLI exits **0** and writes `Epoch-2` = `{2:terminal, 3:progress, 4:opened}` with allocation `w0[2,102]`, because the base folds residue and fresh together and the residue's terminal closes the synthesized covering range; the same drive on the measured candidate exits **1** with `unterminated range for w0`, the byte-identical ledger, and both loose files preserved. A refusal is distinguished from a mutation by a recursive byte-complete project snapshot (directory entries, symlinks, relative paths, per-file bytes) that is empty-diff after the refusal.</AC-MIXED-RECOVERY-VALIDATE-FIRST>
      <AC-RECOVERY-HOOKS-CORRECT>`injectFailureAfterResidueDelete` leaves exactly **F**; `injectFailureAfterAutoOpen` leaves exactly **F union A**; retries from both post-states through the **real CLI** satisfy the normative state table. **Both original hooks are driven on the mixed F union A write path**, not only on bundle 3's pure-residue path: `injectFailureBeforeVerify` and `injectFailureAfterWrite` each interrupt the mixed fold after the single new `Epoch-2` is written, leaving `Epoch-1` byte-identical and loose state exactly `F union A`; a real-CLI retry removes the matching residue, writes no `Epoch-3`, and a second retry is idempotent. This bundle does not regress bundle 3's one-epoch idempotence. Measured on the candidate for the concrete fixture: after `injectFailureAfterResidueDelete` the ledger holds only `Epoch-1` and `run/` holds exactly `3-T-001-progress.xml` and `4-T-001-terminal.xml`; after `injectFailureAfterAutoOpen` the ledger holds only `Epoch-1` and `run/` holds exactly `3-T-001-progress.xml`, `4-T-001-terminal.xml`, and `5-T-001-opened.xml`; a real `ngrace cursor fold` retry from each post-state exits **0**, writes `Epoch-2` = `{3:progress, 4:terminal, 5:opened}` once — synthesizing no second `opened` — and empties `run/`. **All four hook rows — both new hooks and both original hooks on the mixed path — then perform the table's final empty retry**: a second real-CLI fold exits **0**, leaves the same `Epoch-2`, writes no `Epoch-3`, leaves both epoch byte-slices unchanged, and empties `run/`. `Epoch-1` is captured **before** the injected fold and asserted byte-identical immediately after the injection and after each retry, so older-epoch byte identity is not sampled late. The post-states are asserted by enumerating and hashing `run/` (`snapshotTree`: type, relative path, symlink target, file sha256) as well as by filename, never by substring checks.</AC-RECOVERY-HOOKS-CORRECT>
      <AC-NO-UNFORCED-WRITES>Activated only by the explicit switch `NGRACE_SCOPE_GUARD_CHANGE=C-FOLD-MIXED-RECOVERY-1-62A1B852`, used for this bundle's target and final verification and present in this criterion's CloseEvidence. Measured against base `8d08255775b036b63ef1c74c2ec47ea1fc593d0c` in the applied-archive state, this bundle's complete tracked-and-untracked change set is within the closed allowed set: the one forced source `src/grace-cursor.ts`; the two forced tests {`src/grace-cursor.test.ts`, `src/artifact/scope.test.ts`}; and this bundle's active directory and its archive arrival (`.ngrace/changes/active/C-FOLD-MIXED-RECOVERY-1-62A1B852/`, `.ngrace/changes/archive/C-FOLD-MIXED-RECOVERY-1-62A1B852/`), never an archive glob and never a blanket bundle-directory prefix. **The allowed set contains no record path and no scratch path:** this bundle pays no finding, so `docs/plans/active/RM-GOVERNED-PATH/` (all seven XML files and `decisions.md`) and `scripts/validate-record-retirement.test.ts` are all outside it, and a planted write to any of them is a tracked diff the guard reddens on. The ignored `.ngrace/scratch/staged-findings.md` buffer is untracked and untrackable here (`git ls-files --others --exclude-standard` omits it and no base blob exists), so it is excluded from the allowed set and the close rehearsal records its pre-close bytes and requires them byte-identical after the close; a scratch write therefore cannot pass silently. The bundle-lifecycle recognition is **guard-only and identity-exact**: the guard may recognize only this bundle's own lifecycle files under its exact id (`spec.xml`, `plan.xml`, `run-ledger.xml`, `run.xml`) and XML under its own `run/`, and a planted non-lifecycle file under the bundle directory still reddens. The later plan's non-lifecycle `ObservedWriteScope` is exactly the three literal paths above with zero globs, and must not add this bundle's own `spec.xml`, `plan.xml`, `design-context.xml`, `run/`, `run.xml`, `run-ledger.xml`, the record XML, the scratch buffer, or any other path. The guard covers untracked files as well as tracked diffs, so `git diff --stat HEAD` alone is insufficient; it is dormant without its exact activation switch, and unavailable git evidence refuses rather than reporting clean. Fixtures prove (a) a clean applied archive passes; (b) a planted ordinary path reddens; (c) a planted record-directory file reddens; (d) a planted non-lifecycle file inside this bundle's own directory reddens; (e) unavailable git evidence refuses; and (f) without activation a later unrelated change of the same kind does not fail. Deactivation is by the switch, never by editing the archive later. The close rehearsal additionally proves all RM-GOVERNED-PATH record XML and `.ngrace/scratch/staged-findings.md` are byte-identical across this bundle's close, with no `--flush` and no `--retire` run.<CloseEvidence><Command>NGRACE_SCOPE_GUARD_CHANGE=C-FOLD-MIXED-RECOVERY-1-62A1B852 bun test --timeout=0 src/artifact/scope.test.ts</Command></CloseEvidence></AC-NO-UNFORCED-WRITES>
      <AC-CLOSE-LINT>Post-close lint of the repository root reports zero errors and zero warnings, evaluated in the applied-archive state.<CloseEvidence><Command>bun run ngrace lint --path . --fail-on warnings</Command></CloseEvidence></AC-CLOSE-LINT>
      <AC-CLOSE-TESTS>The behavior-pinning suite for the changed surface passes with no wall-clock timeout, evaluated in the applied-archive state.<CloseEvidence><Command>bun test --timeout=0 src/grace-cursor.test.ts</Command></CloseEvidence></AC-CLOSE-TESTS>
    </AcceptanceCriteria>
    <AffectedAreas>
      <M-CURSOR />
    </AffectedAreas>
    <VerificationIntent>
      <ExpectedCommand>bun test --timeout=0 src/grace-cursor.test.ts</ExpectedCommand>
      <ExpectedEvidence>Bundle-4 repository tests in `src/grace-cursor.test.ts` plus corroborating disposable real-`.git` drives, with direct exits and recursive snapshots that record directory entries, symlinks, relative paths, and per-file sha256. The mixed fixture is reached through the product path, never manufactured: the initial loose epoch is folded and interrupted after the ledger write, the first verified loose event is deleted in the shipped delete-loop order to leave the matching residue, and **F** is written by `ngrace cursor advance` (the real allocator), with the intermediate state asserted exact before the target fold. The population: (1) the mixed success path through the real CLI — `Epoch-1` captured before the fold and byte-identical after, one correctly numbered `Epoch-2` containing `F union A` exactly once, `run/` empty, and no duplicate of the residue event id; (2) the validate-first discriminator — the progress-only **F_bad** refuses `unterminated range for w0` with a byte-complete empty tree diff and no synthesized `opened`; (3) `injectFailureAfterResidueDelete` — `Epoch-1` unchanged, the post-state hash-exact against the captured **F** bytes with the residue entry absent, then a real-CLI retry that writes `Epoch-2` once and a second retry that is idempotent; (4) `injectFailureAfterAutoOpen` — `Epoch-1` unchanged, the post-state hash-exact `F union A` with both **F** hashes preserved and the generated **A** captured and payload-compared after the retry through `expectedLedgerEventAttributes` plus children, then a real-CLI retry that reuses **A**, synthesizes no second `opened`, writes `Epoch-2` once, and a second retry that is idempotent; (5) `injectFailureBeforeVerify` on the mixed path — `Epoch-1` captured before the fold and byte-identical immediately after the injection and after both the first and second real-CLI retries, the `F union A` post-state hash-exact against the captured **F** entries with the generated **A** payload-compared to the written `Epoch-2` event before recovery, `Epoch-2` unchanged, and no `Epoch-3`; (6) `injectFailureAfterWrite` on the mixed path with the same pre-capture, hash-exact post-state, **A** payload comparison, and both retries; (7) the existing bundle-1/2/3 controls (pure residue, fresh-only, genuine conflict, older-epoch reuse, both original hooks, the third empty fold) re-driven and directionally unchanged. Payload preservation is inspected independently with `expectedLedgerEventAttributes` plus complete children, never the writer's own transform. Measured final repository-test population: the six-test bundle-4 block appended to `src/grace-cursor.test.ts` and applied **byte-identically** to the untouched base and the candidate — mixed real-CLI success, progress-only F_bad validate-first, both new-hook post-states, and both original hooks on the mixed path. `src/grace-cursor.ts` is 67 insertions / 0 deletions against the base, and the write-surface inventory pin moves from `cursorContained` 3 to 4 and `callSites` 10 to 11 (the mixed residue delete adds one contained-path `unlinkSync` site). With that identical final test file: the **untouched base** is `bunx tsc --noEmit` exit 0 and `bun test --timeout=0 src/grace-cursor.test.ts` = **227 pass / 7 fail** — six behavioral reds (mixed `Epoch-2` duplicates residue id 2; **F_bad** exits 0 instead of refusing; each new-hook test sees no throw; each mixed original-hook test sees a four-file loose post-state) plus the expected structural pin red; the **candidate** is `bunx tsc --noEmit` exit 0 and **234 pass / 0 fail**, and `bun run validate:ci` exits **0** with `2322 pass / 0 fail` over `2325 tests across 68 files`. The full CI-equivalent verification (`bun run validate:ci`) is the later plan's single final `MustPassCommand`, not a per-task command, and no close criterion depends on `/private/tmp` surviving.</ExpectedEvidence>
    </VerificationIntent>
    <Problem>
      **Bundle 4 of the maintainer-directed split, minted fresh from the reserved slug `C-FOLD-MIXED-RECOVERY`.** Bundles 1, 2, and 3 are applied and committed at this base; this bundle owns only the mixed residue-plus-fresh recovery ordering. The approved predecessor contract carried the criterion bodies preserved here; the bundle is not a lineage supersede and there is no predecessor transition. `bun run ngrace` executes this checkout's `src/grace.ts`, not a published package.

      **The measured defect, reproduced against base `8d08255775b036b63ef1c74c2ec47ea1fc593d0c` on disposable real-`.git` clones.** `reconcileLooseWithLedger` (`src/grace-cursor.ts:1399`) already classifies each loose event as residue, fresh, conflict, or older-epoch reuse, but it sets `resumeEpoch` only for the **pure-residue** case (`:1463`, the condition `residue.length &gt; 0 &amp;&amp; !hasFresh &amp;&amp; !conflict &amp;&amp; !olderReuse`). When the loose set mixes residue with a fresh set, `hasFresh` is true, `resumeEpoch` stays undefined, and control falls through to the ordinary fresh path at `:1558`: `collectEffectiveAllocations` over **all** loose events (residue included), `maybeAutoOpenCoveringAllocation` (`:936`) if that set carries no allocation, membership validation at `:1575`, and the write at `:1596`. The residue is therefore folded a second time. Measured with the concrete fixture (ledger `Epoch-1` = `{1:opened w0[1,2], 2:terminal}`; loose `{2:terminal, 3:progress, 4:terminal}`), the real CLI `cursor fold` exits **0** and writes `Epoch-2` = `{2:terminal, 3:progress, 4:terminal, 5:opened}` with allocation `w0[2,103]` — event id 2 is recorded in both `Epoch-1` and `Epoch-2`, and the synthesized `opened` covers from the residue's id 2 rather than from the fresh minimum 3. With the progress-only fresh set (`{2:terminal, 3:progress}`), the real CLI exits **0** and writes `Epoch-2` = `{2:terminal, 3:progress, 4:opened}` with allocation `w0[2,102]` — the base does not refuse, because it validates the residue's terminal as the closer for the covering range. The two shipped interruption hooks are at `src/grace-cursor.ts:1605` (`injectFailureBeforeVerify`) and `:1656` (`injectFailureAfterWrite`), and the shipped contained-path delete sites are `:1527` (bundle 3's resume delete), `:1667` (fold delete loop), and `:4100` (writer renumber).

      **The chosen shape, and why the runner-up loses.** The smallest coherent shape keeps the shipped `reconcileLooseWithLedger` classification and adds the missing mixed branch in `foldEpochImpl`: when the reconciliation holds both residue and fresh events, compute the fresh set's **prospective** allocation (the explicit allocations, or the covering allocation a single-controller auto-open would synthesize) and validate it in memory **before** any mutation; refuse on any issue while the ledger and every loose byte remain untouched; only then delete the residue, materialize the covering `opened` if the fresh set needs one, and let the ordinary write/verify/delete path fold exactly `F union A`. The rejected runner-up was to keep folding the whole loose union and rely on the ledger's append/replace to reconcile — that is precisely the measured defect, and it duplicates a recorded event rather than recovering it. A second rejected shape deleted the residue first and validated afterward; that is not validate-first and would leave `F_bad` with the residue already destroyed. The measured prototype is `+67` lines in the single forced production file, no deletions.

      **Measured red/green directions.** The fixture is reached through the product path — the initial loose epoch is interrupted after its ledger write, the residue is the survivor of the shipped delete-loop's first delete, and **F** is allocated by `ngrace cursor advance` — so the mixed set is driven end to end, not manufactured. Real CLI, concrete fixture, base versus candidate: mixed success base exit 0 with `Epoch-2` `{2,3,4,5}` allocation `w0[2,103]` and the duplicate residue id 2, candidate exit 0 with `Epoch-2` `{3,4,5}` allocation `w0[3,103]`, `Epoch-1` byte-identical and `run/` empty. `F_bad` base exit 0 with `Epoch-2` `{2,3,4}` allocation `w0[2,102]`, candidate exit 1 with `unterminated range for w0`, the byte-identical ledger, and both loose files preserved. `injectFailureAfterResidueDelete` post-state `Epoch-1` only with loose `{3:progress, 4:terminal}`, then a real-CLI retry writing `Epoch-2` `{3,4,5}` once. `injectFailureAfterAutoOpen` post-state `Epoch-1` only with loose `{3:progress, 4:terminal, 5:opened}`, then a real-CLI retry writing `Epoch-2` `{3,4,5}` once and synthesizing no second `opened`. Interruption after the new ledger write leaves `Epoch-1` unchanged and `Epoch-2` written with loose `{3,4,5}`; the real-CLI retry deletes the matching residue and writes no `Epoch-3`. On the mixed path, **both** original hooks are evidenced: `injectFailureBeforeVerify` and `injectFailureAfterWrite` each leave the one written `Epoch-2` with loose `{3,4,5}`, and the real-CLI retry deletes the matching residue and writes no `Epoch-3`. With the identical final test file applied to both clones, the untouched base is `bunx tsc --noEmit` exit 0 and `bun test --timeout=0 src/grace-cursor.test.ts` = **227 pass / 7 fail** (six behavioral reds plus the structural pin red), the candidate is `bunx tsc --noEmit` exit 0 and **234 pass / 0 fail**, and `bun run validate:ci` on the candidate exits **0** (`2322 pass / 0 fail` over `2325 tests across 68 files`). The measured candidate keeps every bundle-3 F313 test green, and the write-surface inventory pin moves because the mixed residue delete adds one contained-path `unlinkSync` call site.

      **The empty-payment close shape, and what the plan must rehearse.** This bundle pays no finding. Code and search evidence, not a close run, establishes that the shape needs no record writer: a search over `src/gates/*.ts` finds **0** `registry` references and `gate archive` only evaluates and records the archive permit, so the close runs neither `--flush` nor `--retire` and writes no RM-GOVERNED-PATH record XML and no `.ngrace/scratch/staged-findings.md`. The faithful full lifecycle has **not** been run and remains a plan-stage rehearsal on a clone. The complete future authority sequence is: final assertions with terminal and fold; pre-close review and `gate verdict`; `gate apply`; `gate archive`; `ngrace apply`; then the post-archive review and verdict with the three `CloseEvidence` acknowledgements. The rehearsal must prove all RM-GOVERNED-PATH record XML and the ignored scratch buffer byte-identical across that full sequence. The scope guard's allowed set excludes every record path and the scratch path; because the scratch buffer is ignored and untracked, its byte-identity is asserted by the rehearsal's before/after record rather than by the git-derived guard.
    </Problem>
    <Assumptions>
      <Assumption>Base commit `8d08255775b036b63ef1c74c2ec47ea1fc593d0c` is the committed application of bundle 3 `C-FOLD-RETRY-CORE-1-000C564C`; bundles 1, 2, and 3 are archived, `.ngrace/changes/active/` holds no other bundle, and this fresh slug has no predecessor transition. All were confirmed by listing the commit, the active directory, and the worktree before this draft was written.</Assumption>
      <Assumption>The ordered remainder is reserved as semantic slugs only: `C-DISCARD-PREFLIGHT` (bundle 5) and `C-SUPERSEDE-INTEGRATION-CLOSE` (B3). Their hash suffixes are minted when authored, each after its predecessor closes and commits.</Assumption>
      <Assumption>Projected relations, not pinned figures: a successful mixed recovery leaves `Epoch-1` byte-identical and exactly one `Epoch-2` containing `F union A` exactly once with `run/` empty; a validate-first refusal leaves the ledger and every loose byte byte-identical; a retry from either new-hook post-state writes `Epoch-2` once. The criteria assert the relations; the concrete ids and bounds named above are the prototype's measured evidence and the plan re-measures them.</Assumption>
      <Assumption>The write-surface inventory pin in `src/grace-cursor.test.ts` counts contained-path `unlinkSync` call sites and counts `cursorContained` 3 and `callSites` 10 at this base; the mixed residue delete adds one contained-path site, so the pin must move to 4 and 11. The measured prototype proves the new counts; whether the final bundle's exact criterion tests extend that pin in one edit is a plan measurement.</Assumption>
      <Assumption>The disposable real-`.git` clones built for this draft under `/private/tmp/ngrace-base-b4` (untouched base plus the final test file) and `/private/tmp/ngrace-cand-b4` (base plus the 67-line production prototype plus the identical final test file) are the reproduction basis; their receipts match the runs recorded in `Problem` and `VerificationIntent`. The normative verification is repository tests derived from them; no close criterion depends on `/private/tmp` surviving. All counts expire when a bundle archives, so the close re-measures.</Assumption>
    </Assumptions>
  </C-FOLD-MIXED-RECOVERY-1-62A1B852>
</NgraceChangeSpec>
