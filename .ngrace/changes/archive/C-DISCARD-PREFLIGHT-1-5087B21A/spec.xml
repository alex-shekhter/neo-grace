<NgraceChangeSpec graceVersion="1.0" status="applied">
  <C-DISCARD-PREFLIGHT-1-5087B21A>
    <Summary>
      **Bundle 5 of five ordered small successors of the superseded `C-FOLD-MEMBERSHIP-RECOVERY-1-DE8479F1`, minted fresh from the reserved semantic slug `C-DISCARD-PREFLIGHT`.** Bundles 1 (`C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5`, vanished-entry membership), 2 (`C-LOOSE-MALFORMED-FOLD-1-4C18E876`, unparseable loose input at both write boundaries), 3 (`C-FOLD-RETRY-CORE-1-000C564C`, interrupted-fold retry idempotence and the post-ledger boundary), and 4 (`C-FOLD-MIXED-RECOVERY-1-62A1B852`, mixed residue-plus-fresh recovery) are applied, archived, and committed at this bundle's base `98e009a8a4713616c1223733d61416958c3053a3`. This bundle — **the deterministic discard preflight** — owns exactly the three ordinary criteria `AC-DISCARD-NOT-WRITTEN-ON-PREWRITE-REFUSE`, `AC-F158-DEFECT-CLOSED`, and `AC-DISCARD-PREFLIGHT-NO-ALLOC`. Ceremony tier **T1**. The affected module is exactly `M-CURSOR`; the later plan's `DurableScope` carries `V-M-CURSOR`. This bundle **pays exactly `F292 F292.1 F158`** and no other token: the preflight-before-`discarded` half of `F292`, its staged correction `F292.1`, and the durable live Finding `F158`. Its non-lifecycle write population is exactly twelve literal paths, zero globs (see `Constraints`). The already-reserved `C-SUPERSEDE-INTEGRATION-CLOSE` (B3) follows only after this bundle is closed and committed; there is **no predecessor supersede transition** for this fresh slug. The `mint-search` line printed at mint is carried verbatim as governance data: `mint-search: 0 active, 0 archive prior bundle(s) share slug DISCARD-PREFLIGHT`.
    </Summary>
    <Goals>
      <Goal>
        **A stream that cannot fold refuses before any engine-authored write.** The fold/discard operation in `src/grace-cursor.ts` validates the exact snapshot it reads — the current effective allocation set; the prospective `discarded` event it is about to write when no effective allocation carries a closer; when that prospective stream has no explicit allocation, the same single-controller covering `opened` that the ordinary fold would synthesize, computed in memory with the correct future ids and headroom; and the duplicate-id, allocation-membership, density/range-hole, and closer requirements over that prospective stream. On any issue the operation refuses before `writeEventFile`, before auto-open materialization, before any `run-ledger.xml` write, and before any status or successor/governance mutation, preserving the complete recursive project tree byte-for-byte. The measured too-narrow-allocation row (`F158`), the duplicate-event-id row (`F292.1`), and the pre-existing-range-hole and no-allocation rows all refuse with no `*-discarded.xml`, no synthesized `opened`, and no ledger change.
      </Goal>
      <Goal>
        **The preflight mirrors fold's reconciliation and never refuses a stream fold would accept.** The preflight reuses the shipped effective-allocation, durable-ledger reconciliation, and single-controller auto-open semantics rather than inventing a second policy: the prospective stream is reconciled against the durable ledger first, so already-recorded residue resumes and is never re-validated as a fresh set, a mixed residue-plus-fresh set validates only the fresh subset, a genuine conflict or older-epoch reuse refuses, explicit allocations win, a single worker may be covered by an in-memory retrospective `opened`, and a multi-worker stream with no allocation refuses exactly as the shipped auto-open refuses. It preserves the no-loose-events no-op and does not change ordinary `foldEpoch` semantics. A repaired dense, uniquely identified, validly allocated stream without a closer still succeeds through the real product path, writes exactly one `discarded`, folds it exactly once, and leaves `run/` empty; a partial-deletion residue hole and a mixed residue-plus-fresh set each resume through the changed caller and the real CLI `ngrace supersede`, as the base does.
      </Goal>
      <Goal>
        **The bounded competing-writer seam is preserved and its three boundaries stay distinct.** The whole operation runs under the existing candidate lock, so cooperating writers are serialized; a non-cooperating filesystem writer that ignores the lock and changes the stream after the preflight snapshot and before the prospective `discarded` write can still produce a stage-(ii-a) pre-ledger residue: exactly one `discarded` written, a refusal before any ledger write, and no stage-(i) preflight outcome. After that same actor reverses only its own mutation, a real `bun run ngrace cursor fold` retry consumes the preserved `discarded` exactly once, creates no second `discarded`, produces the correct ledger projection, and empties `run/`. This bounded race and the distinct `C-RUN-LEDGER` post-ledger write-verify-delete boundary are never used to deny payment of `F158`.
      </Goal>
      <Goal>
        **The payment and the close-time write guard are executable.** The bundle's payment set is declared exactly (`F292 F292.1 F158`); the retirement payer fixture is forced scope; the close includes the narrow payer assertion; and the close-time discriminator covers both tracked and untracked changes against the named base commit with a closed allowed set that admits the forced non-lifecycle population and this bundle's enumerated record writes, and reddens on a planted forbidden ordinary path, a non-record file in the record directory, and a non-lifecycle file inside the bundle's own directory.
      </Goal>
    </Goals>
    <Constraints>
      <Constraint>
        **Tier and base.** Ceremony tier **T1** (module change in one package). Roadmap `RM-GOVERNED-PATH`. Base commit `98e009a8a4713616c1223733d61416958c3053a3` — the committed close of bundle 4 `C-FOLD-MIXED-RECOVERY-1-62A1B852`, whose spec and plan are `status="applied"`, no bundle is active, and `.ngrace/changes/active/` is empty. The full hash is the baseline for every byte-invariance relation and the scope-guard base. Every measurement in this artifact is dated 2026-09-22 and was reproduced against this base in disposable real-`.git` clones; counts expire when a bundle archives, so the close re-measures. There is **no predecessor supersede transition** for this fresh bundle.
      </Constraint>
      <Constraint>
        **Modules, kept in agreement.** `AffectedAreas` names exactly `M-CURSOR`. The `Ownership of forced files` constraint pairs each forced path with its owner from that same set; the two are kept in agreement (F136.1), and the later plan's `DurableScope` carries the exact verification anchor `V-M-CURSOR`. While this artifact is **draft**, either listing may be corrected in place (`RM-GOVERNED-PATH D27.1`); once approved the artifact is immutable and a change that requires a new contract is made by `ngrace supersede` and a replacement bundle, never by editing the approved bytes (`RM-GOVERNED-PATH D27`).
      </Constraint>
      <Constraint>
        **Ownership of forced files.** The one forced module-ownable source file is `src/grace-cursor.ts` (`LINKS: M-CURSOR`; this bundle owns the deterministic discard preflight, the prospective in-memory covering-allocation simulation, and the test-only pre-write seam). The behavior test `src/grace-cursor.test.ts`, the close-time guard `src/artifact/scope.test.ts`, and the retirement payer ratchet `scripts/validate-record-retirement.test.ts` are not module-ownable and carry no module anchor; they are in the write scope because the deliverable forces them. `src/gates/ledger.ts` is the production supersede caller (`discardAndFoldEpoch(projectRoot, changeId)` at `src/gates/ledger.ts:1680`) and is **not** edited: the preflight is called by the fold/discard operation it already invokes, and no preflight is duplicated into the CLI or the gate module. `src/grace-supersede.ts`, `src/artifact/run-membership.ts`, and `src/artifact/xml.ts` are **not** forced and must not be edited. Do not require a file while excluding its owner, and do not invent a module or a `LINKS:` header to place one.
      </Constraint>
      <Constraint>
        **The deterministic preflight contract, enumerated.** The preflight runs inside the fold/discard operation under the existing candidate lock and validates the exact snapshot it reads before any engine-authored write. It mirrors what the subsequent ordinary fold would validate and reconcile: (1) the prospective stream against the durable ledger — already-recorded residue resumes and is never re-validated as a fresh set, a genuine conflict or older-epoch reuse refuses before the `discarded` write, and a mixed residue-plus-fresh set validates only the subset that is actually fresh; (2) the current effective allocation set; (3) the prospective `discarded` event this operation is about to write when no effective allocation carries a closer; (4) when that prospective stream has no explicit allocation, the same single-controller covering `opened` ordinary fold would synthesize, computed in memory with the correct future ids and headroom; (5) duplicate-id, allocation membership, density/range-hole, and closer requirements over that prospective stream. If the prospective stream cannot fold, the operation refuses before `writeEventFile`, auto-open materialization, ledger creation or change, status change, or successor/governance mutation. The preflight must not reject a stream the ordinary fold would accept — in particular a supported partial-deletion residue hole and a mixed residue-plus-fresh set, each driven through this changed caller and the real CLI `ngrace supersede` — must preserve the no-loose-events no-op, and must not change ordinary `foldEpoch` semantics. Reusing the shipped mixed-path helper is permitted only if its semantics fit the discard stream; simulating both a prospective `discarded` id and the later prospective `opened` id may require a distinct in-memory shape.
      </Constraint>
      <Constraint>
        **Failure taxonomy by stage, and the three boundaries kept distinct.** The verb's stages and their required post-states:
        **(i) preflight snapshot**: a refusal here writes nothing — a byte-complete recursive tree, no `run/` change, no `discarded`, no `opened`, no ledger write.
        **(ii-a) discarded write (pre-ledger)**: once the engine-authored `discarded` event is written, the stage is pre-ledger. The measured deterministic `F158` reproduction (too-narrow allocation / an event outside every allocation, `unterminated range`), the `F292.1` duplicate-event-id row, and the pre-existing-range-hole and no-allocation rows **no longer reach this stage**: the preflight refuses first (`AC-F158-DEFECT-CLOSED`, `AC-DISCARD-NOT-WRITTEN-ON-PREWRITE-REFUSE`, `AC-DISCARD-PREFLIGHT-NO-ALLOC`). A **controlled competing writer** — a non-cooperating filesystem actor that changes the stream after the preflight snapshot and before `foldEpoch` — can still write exactly one `discarded` and then fail pre-ledger; that bounded **competing-writer stage-(ii-a) analogue** is retained as an explicit non-cooperating robustness case, has the same residue shape, and is **not** the measured `F158` defect.
        **(iii) ledger write and beyond**: a fold interrupted after its ledger write may leave the written ledger and engine-authored loose events; that distinct post-ledger write-verify-delete boundary is the `C-RUN-LEDGER`/RM-GOVERNED-PATH D3 contract, is preserved, and is **never relabelled `F158`**.
        No refuse path removes, renumbers, or rewrites evidence it does not own; a later successful fold may verify and delete recoverable residue under the shipped recovery protocol. [RM-GOVERNED-PATH D41](docs/plans/active/RM-GOVERNED-PATH/rulings.xml) forbids an agent hand-editing a `run/` event or `run-ledger.xml` to make a refused fold succeed; the sanctioned fold protocol and the actor's reversal of its own mutation are not such a repair.
      </Constraint>
      <Constraint>
        **Preflight bound.** The preflight validates the stream it reads and mirrors fold's allocation and single-controller auto-open semantics. The whole fold/discard operation already runs under the existing candidate lock, so a **cooperating** writer is serialized; the guarantee is nonetheless bounded to the snapshot the preflight reads, because a **non-cooperating** filesystem actor that ignores the lock can change the stream after that snapshot and before `foldEpoch` and still produce a refused fold carrying a `discarded` residue. That competing-writer outcome occurs **after** `discarded` is written and **before** any ledger write: it is a **competing-writer stage-(ii-a) pre-ledger** race, **not** a stage-(i) preflight-snapshot outcome and **not** the distinct post-ledger `C-RUN-LEDGER` boundary. It never promises what the later fold refuses, and it never uses either residue to deny `F158`'s payment. The bounded seam is retained: the operation exposes a test-only seam after the preflight snapshot succeeds and before the prospective `discarded` write, so the plan can drive a controlled non-cooperating actor and its consume-once retry. No new lock behavior is proposed; adding cooperating-lock coverage cannot serialize an actor that ignores it.
      </Constraint>
      <Constraint>
        **Payment set, enumeration, and the close writes.** The bundle pays exactly `F292`, its staged correction `F292.1`, and `F158` — three tokens, no other. It does **not** re-pay `F295`, `F295.1`, `F313`, or `F315`; bundles 1–4 already own their split portions. `F158` is a durable live Finding at `docs/plans/active/RM-GOVERNED-PATH/findings.xml:5848` and has **no** staged entry. The plan grammar has no `&lt;Pays&gt;` element, so the payment contract is stated exactly three ways: the plan `IntentSummary` **semantically declares** exactly `F292 F292.1 F158`; the successor-named registry charter row is the **structural `&lt;Pays&gt;` carrier**; and the retirement payer fixture carries the three successor `bundleMinted` pins after the close. The current clean base carries **none** of the three `bundleMinted` pins in `scripts/validate-record-retirement.test.ts`, so the close **adds three** entries naming `C-DISCARD-PREFLIGHT-1-5087B21A`. **Staged-buffer owner handoff:** `.ngrace/scratch/staged-findings.md` is ignored by `.gitignore`, read verbatim by `flushRecord`, and invisible to the git-based scope guard; no `git diff` or `git ls-files --others --exclude-standard` can observe it. Its paid-finding `Owner:` lines for this bundle are exactly **two** — `F292` at `:79` and `F292.1` at `:143` — both of which must be rewritten to name this bundle before flush. `F158` has no staged line and this bundle must not add one. **The scratch close is a phase-bounded surgical edit, not a byte-invariant one:** capture the original scratch bytes, allow exactly those two `Owner:` substitutions, prove every other byte unchanged, then capture the post-handoff bytes and require those unchanged across flush, apply, archive, and retire; restoring the old owners to satisfy an equality check is forbidden. **`F292` ownership.** `F292` names the successor residue a refused `ngrace supersede` left under `active/`; the closed `C-SUPERSEDE-MEMBERSHIP-4-20941257` (B1) paid the candidate acquisition/cleanup and validate-before-fold half, and this bundle pays only the remaining engine-authored `discarded` residue half. `F292` did not originate as a cursor-only finding. **`PaidBy` is never hand-marked in the staged markdown:** the `--retire` writer derives and stamps XML `&lt;PaidBy&gt;` from the registry payer; the staged correction is the owner-line rewrite only. **The finite non-lifecycle write contract consists of exactly twelve literal paths, zero globs:** the one forced source `src/grace-cursor.ts`; the forced tests `src/grace-cursor.test.ts` and `src/artifact/scope.test.ts`; the retirement ratchet `scripts/validate-record-retirement.test.ts`; the exact `.ngrace/scratch/staged-findings.md` path; and the seven record XML files under `docs/plans/active/RM-GOVERNED-PATH/` (`decisions.xml`, `findings.xml`, `findings-retired.xml`, `rulings.xml`, `rulings-retired.xml`, `registry.xml`, `registry-retired.xml`). There is no predecessor supersede transition for this fresh bundle. **Writer mechanics, re-measured at close:** while this bundle is **active**, `--flush` serializes the four live XML roots (`decisions.xml`, `findings.xml`, `rulings.xml`, `registry.xml`); after **archive**, an eligible `--retire` moves the paid findings plus this bundle's charter row and, when anything moves, serializes all seven XML files. The exact byte-differing subset is re-measured by a drive at the close; every writer-touched path is declared, and no catch-all clause licenses repairing whatever a later check flags. `decisions.md` is never a record-writer target.
      </Constraint>
      <Constraint>
        **The close-time write guard is exact-id and finite.** The guard lives in the forced file `src/artifact/scope.test.ts`, activated only by `NGRACE_SCOPE_GUARD_CHANGE=C-DISCARD-PREFLIGHT-1-5087B21A` and measured against base `98e009a8a4713616c1223733d61416958c3053a3`. Its allowed non-lifecycle set is exactly the eleven observable literal paths above (the twelve-path declared population minus the ignored scratch), with zero globs. It recognizes only this bundle's own lifecycle identities under its exact id (`spec.xml`, `plan.xml`, `run-ledger.xml`, `run.xml`, and XML under its own `run/`) in active and archive locations, and that recognition is not permission for an arbitrary extra file inside the bundle directory. It covers tracked diffs and untracked files, is dormant without its exact switch, and refuses when git evidence is unavailable. The ignored `.ngrace/scratch/staged-findings.md` buffer cannot appear in the git-derived population, so it is declared as a transient pre-flush input mutation audited by the surgical relation in the payment `Constraint`: the close rehearsal captures the original scratch bytes, allows exactly the two specified `Owner:` substitutions and proves every other byte unchanged, then captures the post-handoff bytes and requires those byte-identical across flush, apply, archive, and retire. The old owners are never restored to satisfy equality, and no other scratch byte may move. `decisions.md` is outside the allowed set. Fixtures prove (a) a clean applied-archive pass; (b) a planted forbidden ordinary path reddens; (c) a non-record file inside the record directory reddens; (d) a planted non-lifecycle file inside this bundle's own directory reddens; (e) unavailable git evidence refuses rather than reporting clean; and (f) without activation a later unrelated change of the same kind does not fail.
      </Constraint>
      <Constraint>
        **Ordered remainder, no invented ids.** The already-reserved `C-SUPERSEDE-INTEGRATION-CLOSE` (B3) is the remaining ordered successor; its hash suffix is minted only when it is authored, after this bundle is closed and committed. The collision/name search is re-run at each actual mint; the search recorded here was run at this mint and is evidence for this draft only.
      </Constraint>
      <Constraint>
        **No new dependency, production file family, or permanent artifact.** No new production `src/*.ts` file, no new package dependency, and no permanent file family under `run/`. `listLooseEvents`, `listRunOrphans`, `listLedgerEvents`, and `listAccountingEvents` keep their current public contracts and ordering; the new seam option is test-only and unreachable from the CLI, matching the shipped `injectFailure*` hooks. The close-time write guard lives in the existing forced test file `src/artifact/scope.test.ts`.
      </Constraint>
      <Constraint>
        **Discriminating evidence for every guard, from an isolated baseline where needed.** Every guard is driven in both directions: a passing control proves it can pass, a planted or injected violation proves it can redden. Refusal byte-invariance is asserted with a recursive snapshot that records directory entries, symlinks, relative paths, and per-file bytes. Exit codes are read directly, never through a pipe. No failed import, compilation error, timeout, or failed instrument counts as the intended red. Any `bun test` close command carries `--timeout=0`; no per-test number is pinned.
      </Constraint>
      <Constraint>
        **Test-surface pins move only where the production delta forces them.** The measured prototype adds no `unlinkSync(contained.absolutePath)` site, so the write-surface inventory pin stays `cursorContained` 4 and `callSites` 11. It does add a second `kind: "discarded"` literal — the prospective event the preflight constructs in memory — so the `AC-DISCARDED-CALLER` count moves 1 to 2, both occurrences inside the discard operation's source region; the plan re-measures and pins the final count rather than assuming this figure.
      </Constraint>
      <Constraint>
        **Escape markup as character data.** Where this artifact must name a tag, attribute form, or angle-bracketed token, the entity form is used (`&lt;`, `&gt;`, `&amp;`); brackets are never paraphrased away, and markup-byte assertions belong in a TypeScript test (F43).
      </Constraint>
      <Constraint>
        **The full CI-equivalent verification is close-time only.** `bun run validate:ci` is the later plan's single final `MustPassCommand`, and the plan must rehearse the applied/archive close on a faithful clone before the production close. The narrow `CloseEvidence` commands stay tied to their own subjects; a whole-suite command is not attached to a criterion that is not about the whole suite.
      </Constraint>
    </Constraints>
    <NonGoals>
      <NonGoal>**Bundles 1–4 are closed and their contract is not carried or re-paid here.** Vanished-entry membership tolerance (`C-FOLD-MEMBERSHIP-RECOVERY-2-2E6A79D5`), unparseable loose-input refusal at both write boundaries (`C-LOOSE-MALFORMED-FOLD-1-4C18E876`), interrupted-fold retry idempotence and the post-ledger boundary (`C-FOLD-RETRY-CORE-1-000C564C`), and mixed residue-plus-fresh recovery (`C-FOLD-MIXED-RECOVERY-1-62A1B852`) are preserved exactly and keep their tests green. Owner: bundles 1–4 (closed). Their criteria do not appear as `AcceptanceCriteria` children in this spec; `F295`, `F295.1`, `F313`, and `F315` are not paid here.</NonGoal>
      <NonGoal>**B1's supersede acquisition, candidate cleanup, and mint exclusivity are not reopened.** Candidate acquisition and bounded CL-2 cleanup, the candidate lock and the cooperating-writer population, and the validate-before-fold ordering are owned by the closed `C-SUPERSEDE-MEMBERSHIP-4-20941257`. Owner: B1 (closed). This bundle fixes only the engine-authored `discarded` residue the deterministic preflight can prevent.</NonGoal>
      <NonGoal>**B3's integrated supersede matrix and governed close are not carried here.** The integrated end-to-end supersede success/refusal/post-fold matrices, the allocator control, and `C-SUPERSEDE-INTEGRATION-CLOSE`'s own finite close-time scope-guard fixtures are owned by the ordered successor `C-SUPERSEDE-INTEGRATION-CLOSE`, authored only after this bundle closes and is committed. Owner: B3.</NonGoal>
      <NonGoal>**The post-ledger `C-RUN-LEDGER` boundary is not re-implemented.** The write-verify-delete interruption and its preserve-and-recover behavior are bundle 3's, stand unchanged, and no criterion here relabels them `F158`. Owner: bundle 3 (closed).</NonGoal>
      <NonGoal>**`src/gates/ledger.ts`, `src/grace-supersede.ts`, `src/artifact/run-membership.ts`, and `src/artifact/xml.ts` are not changed.** Owner: unchanged; the preflight lives in the fold/discard operation that owns the ledger write, the supersede caller already invokes it, and the shared reader and membership module already have their contracts. If the cursor-only preflight cannot satisfy the measured contract, stop and report the contradiction rather than widening silently.</NonGoal>
      <NonGoal>**`README.md` is unchanged.** Owner: unchanged; no user-visible surface. The deterministic refusal and the competing-writer bound are visible only through an existing fold/refuse outcome; no documented refuse/recovery outcome changes and no stale sentence was identified.</NonGoal>
      <NonGoal>**`examples/` is unchanged.** Owner: unchanged. Examples invoke `status`, `cursor fold`, and `gate archive`, but none exercises a too-narrow allocation, a duplicate-id refusal, a no-allocation discard, or a competing-writer pre-ledger residue, so no expected output changes.</NonGoal>
      <NonGoal>**`CHANGELOG.md` is not hand-edited; no version bump or release act.** Owner: release automation (`scripts/release-*.ts`); this dispatch authorizes neither.</NonGoal>
      <NonGoal>**`F312` (`--retire --help` / `--flush --help` run the verb), `F280`, `F287`, and `F294` are out of scope with their real owners.** Owner: `F312` awaits the next actual flush-verb touch (and invoking the flush/retire writers at this bundle's close is not itself such a code touch); `F280` is knowledge/history with no bundle owed; `F287` awaits a review-surface bundle; `F294` awaits a roadmap-format bundle. None is folded into this contract.</NonGoal>
      <NonGoal>**No new production file family, dependency, permanent artifact, or roadmap edit.** No new production `src/*.ts` file and no new package dependency are introduced, no permanent artifact family is created under `run/`, and no `docs/plans/README.md` or roadmap file is touched. Owner: this bundle's contract.</NonGoal>
    </NonGoals>
    <AcceptanceCriteria>
      <AC-DISCARD-NOT-WRITTEN-ON-PREWRITE-REFUSE>The deterministic refusals are driven to refuse **before** the `discarded` write. The **too-narrow-allocation** row (the `F158` reproduction: an event outside every allocation, `unterminated range`), the **duplicate-event-id** row (the `F292.1` measurement), and a **pre-existing range hole** all refuse with the invocation adding no file under `run/` (in particular no `*-discarded.xml`, no synthesized `opened`) and leaving the ledger byte-identical when it exists; a byte-complete recursive snapshot (directory entries, symlinks, relative paths, types, per-file sha256) is unchanged after the refusal. Discriminating controls: (a) the positive control repairs uniqueness **and** preserves density and allocation validity, so the stream folds successfully and the success path still writes exactly one `discarded` event and folds it once, with the full ledger projection (every event's complete expected attributes plus its complete children, never a regex count) equal to the expected projection; (b) a competing writer that changes the stream between the preflight snapshot and the fold is driven to establish the bound — the guarantee is stated for the snapshot, and the competing-writer result is recorded as a **competing-writer stage-(ii-a) pre-ledger** outcome (after `discarded` is written, before any ledger write), explicitly **not** a stage-(i) preflight-snapshot outcome and explicitly **not** the distinct post-ledger `C-RUN-LEDGER` boundary; (c) **the competing-writer analogue retry is bound:** after the controlled competing writer reverses only its own mutation — a fixture action by the writer that introduced it, **not** an engine repair and **not** an unauthorized RM-GOVERNED-PATH D41 edit — and the stream is valid again, a real subsequent `ngrace cursor fold` consumes the preserved `discarded` evidence exactly once unchanged — its captured pre-retry payload equals the recorded payload — creates no duplicate `discarded` evidence, and leaves the ledger/event projection correct. The current base is measured red: the too-narrow row exits 1 with `event 2 outside every allocation event 3 outside every allocation unterminated range for w0` and leaves `run/3-T-001-discarded.xml`; the duplicate-id row exits 1 with `duplicate event id 2 appears 2 times; the stream cannot fold until every event id is unique` and leaves `run/3-T-001-discarded.xml`. On the base the test-only seam does not exist: the extra callback is ignored, the competing actor's mutation is never injected, and the drive performs an ordinary successful fold rather than the competing-writer refusal — the red is the missing-seam assertion, distinct from the deterministic-residue reds. The criterion's preservation arm re-drives the supported recovery states through this changed caller and the real CLI `ngrace supersede`: a partial-deletion residue hole and a mixed residue-plus-fresh set resume exactly as before, the prior epoch stays byte-identical, and the predecessor is archived.</AC-DISCARD-NOT-WRITTEN-ON-PREWRITE-REFUSE>
      <AC-F158-DEFECT-CLOSED>The **original deterministic `F158` reproduction** — a fold/`discardAndFoldEpoch` against a **too-narrow allocation** (an event outside every allocation, `unterminated range`) that previously appended an engine-authored `discarded` before the ledger error — now **refuses before the `discarded` write**: the invocation adds no file under `run/` (in particular no `*-discarded.xml`), leaves `run-ledger.xml` byte-identical when it exists, and leaves a byte-identical `run/` set. The **duplicate-event-id** control (the `F292.1` measurement) and the **pre-existing-range-hole** control are asserted to refuse the same way, but only the measured too-narrow-allocation row is attributed to `F158`. Red on the unmodified base, where the reproduction writes the `discarded` residue; green only after the pre-flight repair. The separately bounded competing-writer stage-(ii-a) analogue is asserted by the analogue fixture, never by this criterion, and the post-ledger `C-RUN-LEDGER` boundary is never labelled `F158`. Measured on the base: the reproduction leaves `run/3-T-001-discarded.xml` with no `run-ledger.xml`; measured on the candidate: the whole tree is unchanged.</AC-F158-DEFECT-CLOSED>
      <AC-DISCARD-PREFLIGHT-NO-ALLOC>The measured no-allocation stream `{1:progress, 3:progress}` refuses `range hole at 2 for w0` with **no** `*-discarded.xml`, no synthesized `opened`, and a byte-identical ledger — the preflight mirrors fold's single-controller auto-open **in memory** and refuses before any write. The repaired positive control (uniqueness, density, and allocation validity repaired) folds with exactly one `discarded`. Measured on the base: the stream leaves `run/4-T-001-discarded.xml` and `run/5-T-001-opened.xml`; measured on the candidate: the whole tree is unchanged. The no-loose-events state remains a no-op that writes nothing.</AC-DISCARD-PREFLIGHT-NO-ALLOC>
      <AC-NO-UNFORCED-WRITES>Activated only by the explicit switch `NGRACE_SCOPE_GUARD_CHANGE=C-DISCARD-PREFLIGHT-1-5087B21A`, used for this bundle's target and final verification and present in this criterion's CloseEvidence. Measured against base `98e009a8a4713616c1223733d61416958c3053a3` in the applied-archive state, this bundle's complete tracked-and-untracked change set is within the closed allowed set of exactly eleven observable literal paths: the one forced source `src/grace-cursor.ts`; the forced tests {`src/grace-cursor.test.ts`, `src/artifact/scope.test.ts`}; the retirement ratchet `scripts/validate-record-retirement.test.ts`; and the seven named record XML files under `docs/plans/active/RM-GOVERNED-PATH/` (`decisions.xml`, `findings.xml`, `findings-retired.xml`, `rulings.xml`, `rulings-retired.xml`, `registry.xml`, `registry-retired.xml`), with `decisions.md` **excluded**. The twelfth declared non-lifecycle path is the ignored transient input `.ngrace/scratch/staged-findings.md`; because git cannot report it, it is declared **outside the guard's git-derived allowlist** as a pre-flush input mutation audited separately by the close rehearsal's before/after bytes. There are zero globs and no archive glob, no blanket bundle-directory prefix, and no catch-all path. The bundle-lifecycle recognition is **guard-only and identity-exact**: the guard may recognize only this bundle's own lifecycle/archive identity under its exact id, and a planted non-lifecycle file under that directory still reddens. The later plan must **not** add this bundle's own `spec.xml`, `plan.xml`, `design-context.xml`, `run/`, `run.xml`, or `run-ledger.xml` to `ObservedWriteScope`. The ignored `.ngrace/scratch/staged-findings.md` is invisible to the git-derived population, so it is declared as a transient pre-flush input mutation audited by the surgical relation in `Constraints`: exactly the two specified `Owner:` substitutions against the captured original bytes with every other byte proven unchanged, and the post-handoff bytes required byte-identical across flush, apply, archive, and retire; the old owners are never restored and no scratch write can pass silently. Fixtures prove (a) a clean applied-archive pass; (b) a planted forbidden ordinary path reddens; (c) a non-record file inside the record directory reddens; (d) a planted non-lifecycle file inside this bundle's own directory reddens; (e) unavailable git evidence refuses rather than reporting clean; and (f) without activation a later unrelated change of the same kind does not fail. Deactivation is by the switch, never by editing the archive later.<CloseEvidence><Command>NGRACE_SCOPE_GUARD_CHANGE=C-DISCARD-PREFLIGHT-1-5087B21A bun test --timeout=0 src/artifact/scope.test.ts</Command></CloseEvidence></AC-NO-UNFORCED-WRITES>
      <AC-CLOSE-PAYER>This bundle pays exactly `F292 F292.1 F158`, and no other finding. The clean base `98e009a8a4713616c1223733d61416958c3053a3` carries **none** of the three `bundleMinted` pins in `scripts/validate-record-retirement.test.ts`, so this bundle introduces **three** entries naming `C-DISCARD-PREFLIGHT-1-5087B21A`; it is not described as a re-home of existing clean-base pins. `F158` is a durable live Finding and gains its route only at this close; `F292` and `F292.1` are staged corrections whose two `Owner:` lines (`:79`, `:143`) are rewritten to name this bundle before `--flush` while the bundle is active. The successor-named charter row's `&lt;Pays&gt;` child in `registry.xml` (and `registry-retired.xml` once the row retires) carries exactly those three tokens; the derived `&lt;PaidBy&gt;` stamped on the retired Finding carriers names this bundle. The test never reads the ignored staged buffer; it ratchets the durable XML result the flush produces. The CloseEvidence command is red in the applied-archive fixture until all three durable routes name this bundle and green only then; a staged-buffer edit alone cannot green the test. While this bundle is active, the same durable assertion is exercised against a disposable fixture (green on valid routes; red on a missing/wrong `PaidBy`, a live status, a wrong charter `Pays`, and a missing row; staged-only insufficient; the shipped `--retire` writer proves the move that greens it), and the real-repository ratchet is the after-close check.<CloseEvidence><Command>bun test --timeout=0 scripts/validate-record-retirement.test.ts</Command></CloseEvidence></AC-CLOSE-PAYER>
      <AC-CLOSE-LINT>Post-close lint of the repository root reports zero errors and zero warnings, evaluated in the applied-archive state.<CloseEvidence><Command>bun run ngrace lint --path . --fail-on warnings</Command></CloseEvidence></AC-CLOSE-LINT>
      <AC-CLOSE-TESTS>The behavior-pinning suite for the changed surface passes with no wall-clock timeout, evaluated in the applied-archive state.<CloseEvidence><Command>bun test --timeout=0 src/grace-cursor.test.ts</Command></CloseEvidence></AC-CLOSE-TESTS>
    </AcceptanceCriteria>
    <AffectedAreas>
      <M-CURSOR />
    </AffectedAreas>
    <VerificationIntent>
      <ExpectedCommand>bun test --timeout=0 src/grace-cursor.test.ts</ExpectedCommand>
      <ExpectedEvidence>Bundle-5 repository tests in `src/grace-cursor.test.ts` plus corroborating disposable real-`.git` drives, with direct exits and recursive snapshots that record directory entries, symlinks, relative paths, and per-file sha256; the disposable probes compare the union of before/after snapshot keys (a deleted path is proven detected, not escaped by key iteration), and the repository tests' full-object `snapshotTree` equality is the primary byte-complete assertion. The population: (1) the four deterministic refusal rows — too-narrow allocation, duplicate event id, pre-existing range hole, and the no-allocation `{1:progress, 3:progress}` stream — each driven through the exported production boundary and, for the too-narrow row, through the real CLI `ngrace supersede` with a valid explicit lineage replacement, asserting the refusal names the relation and the byte-complete tree is unchanged; (2) the positive control — a repaired dense, uniquely identified, validly allocated stream without a closer folds through the real product path, writes exactly one `discarded`, folds it once, and empties `run/`; (3) the competing-writer seam — a controlled non-cooperating actor adds a deterministic conflicting event after the preflight snapshot, the operation writes exactly one `discarded` and refuses pre-ledger, the actor reverses only its own mutation, and a real `bun run ngrace cursor fold` retry consumes the preserved `discarded` exactly once unchanged (its captured pre-retry payload equals the recorded payload) and the recorded ledger's complete event population, attributes, and children equal the projection of the captured pre-retry loose population, built independently of the result, with `run/` empty; (4) the recovery preservation arm through this changed caller — a partial-deletion residue hole and a mixed residue-plus-fresh set through `discardAndFoldEpoch`, plus real CLI `ngrace supersede` archiving the predecessor from both; each row asserts the prior epoch byte-identical and the recorded ledger's complete event population, attributes, and children against an independently derived expected set (the captured epoch-1 and fresh events plus the engine's synthetic `discarded` and covering `opened`, with bounds derived from the captured ids and the shipped headroom); (5) the no-loose-events no-op; (6) the durable payer fixture — a disposable record fixture drives the same durable assertion the close ratchet uses: green on valid retired Findings and a retired charter row; red on a missing `PaidBy`, a wrong `PaidBy`, a live status, a wrong charter `Pays`, and a missing row; staged-only insufficient; live versus archived distinguished by `derivePayerMap`; and the shipped `--retire` writer moves a live PaidBy-less finding and the live charter row into the durable state, after which the assertion greens. This is a fixture state, not an approved lifecycle, and the real-repository after-close ratchet skips until this bundle is archived; (7) all bundle-1/2/3/4 controls re-driven: vanishing entries, malformed closer/progress, pure residue, partial deletion, genuine conflict, older-epoch reuse, mixed success and `F_bad`, both mixed hooks, both original hooks, and the third empty retry. Payload and ledger assertions parse complete event attributes and children via `expectedLedgerEventAttributes` plus children, never substring-only checks: the positive success compares the whole ledger projection, and the competing-writer residue is captured before the retry and proven identical after it is consumed exactly once. Measured final repository-test population: the bundle-5 block appended to `src/grace-cursor.test.ts` and applied **byte-identically** to the untouched base and the candidate, together with the `AC-DISCARDED-CALLER` pin moving from 1 to 2 occurrences. With that identical final test file: the **untouched base** is `bunx tsc --noEmit` exit 0 and `bun test --timeout=0 src/grace-cursor.test.ts` = **240 pass / 6 fail** — four deterministic-refusal reds (too-narrow, duplicate-id, no-allocation, and the real-CLI too-narrow `supersede` refusal) plus one missing-seam red (the competing-writer drive, whose extra callback the base ignores) plus the expected structural pin red; the range-hole row and every recovery-preservation row pass on both as controls. The **candidate** is `bunx tsc --noEmit` exit 0 and **246 pass / 0 fail**, and `bun run validate:ci` exits **0** with `2335 pass / 0 fail` over `2338 tests across 68 files`. The measured production delta is `src/grace-cursor.ts` +119/−3; the write-surface `unlinkSync` pin is unchanged (`cursorContained` 4, `callSites` 11). The full CI-equivalent verification (`bun run validate:ci`) is the later plan's single final `MustPassCommand`, not a per-task command, and no close criterion depends on `/private/tmp` surviving. The four `CloseEvidence` criteria (`AC-NO-UNFORCED-WRITES`, `AC-CLOSE-PAYER`, `AC-CLOSE-LINT`, `AC-CLOSE-TESTS`) are evaluated by the close in the applied-archive state and are deliberately absent from every task's future `Satisfies`; the three ordinary criteria are the only task-mapped population.</ExpectedEvidence>
    </VerificationIntent>
    <Problem>
      **Bundle 5 of the maintainer-directed split, minted fresh from the reserved slug `C-DISCARD-PREFLIGHT`.** Bundles 1–4 are applied and committed at this base; this bundle owns only the deterministic discard preflight. `bun run ngrace` executes this checkout's `src/grace.ts`, not a published package.

      **The measured defect, reproduced against base `98e009a8a4713616c1223733d61416958c3053a3` on disposable real-`.git` clones.** `discardAndFoldEpoch` / `discardAndFoldEpochImpl` (`src/grace-cursor.ts:1794-1825` at the base) reads the loose set, refuses unparseable input, detects an in-allocation closer, writes a synthetic `discarded` at `:1818-1823` when none exists, and only then calls `foldEpoch` at `:1824`. Ordinary fold validation is `validateEventsAgainstAllocations` (`:3333-3375`: duplicate ids, membership, density, closer), and single-controller materialization is `maybeAutoOpenCoveringAllocation` (`:936-958`). None of that validation runs before the `discarded` write. Measured through the real product path for the no-allocation stream `{1:progress, 3:progress}`, `discardAndFoldEpoch` refused `range hole at 2 for w0` but changed the recursive tree by adding both `4-T-001-discarded.xml` and `5-T-001-opened.xml`; loose state became `{1:progress, 3:progress, 4:discarded, 5:opened}`. Measured for the too-narrow row (an `opened` with allocation `w0[1,1]` plus a `progress` id 2), the invocation refused `event 2 outside every allocation event 3 outside every allocation unterminated range for w0` and left `run/3-T-001-discarded.xml` with no `run-ledger.xml`. Measured for the duplicate-id row (`1:opened w0[1,99]`, two events with id 2), it refused `duplicate event id 2 appears 2 times; the stream cannot fold until every event id is unique` and left `run/3-T-001-discarded.xml`. The positive control `{1:opened w0[1,99], 2:progress}` applied successfully, wrote exactly one `discarded`, and emptied `run/`. The real CLI `ngrace supersede --change C-OLD --replacement C-OLD-2` reproduced the same too-narrow residue while correctly refusing with exit 1; the same CLI with a valid explicit replacement exits 0 and archives the predecessor. `F158` records exactly this engine-authored `discarded` residue written before the ledger error; `F292.1` records the same residue shape triggered by the deterministic duplicate-id refusal, as a correction to `F292`. The authority's independent driver (`/private/tmp/b5-authority-review.ts`) measured a blocking regression in the first prototype: real CLI `ngrace supersede` on the supported partial-deletion state `{1:opened, 3:terminal}` exited 1 with `range hole at 2 for w0` and did not archive, where the base exits 0 and archives. The corrected preflight reconciles first and matches the base on that row.

      **The chosen shape, and why the runner-up loses.** The smallest coherent shape adds an in-memory preflight inside `discardAndFoldEpochImpl`, under the existing candidate lock, after unparseable-input refusal and the empty-set no-op, and before `writeEventFile`. It reconciles the prospective stream against the durable ledger first — already-recorded residue resumes and is never re-validated as a fresh set, a genuine conflict or older-epoch reuse refuses, and a mixed residue-plus-fresh set validates only the fresh subset — then computes the current effective allocations, appends the prospective `discarded` event it would write when no effective allocation carries a closer, and — when that prospective stream carries no explicit allocation — computes the same single-controller covering `opened` the ordinary fold would synthesize, with the correct future ids and headroom, and validates duplicate ids, membership, density, and closer over the prospective stream. A refusal throws before any write. A test-only seam after the successful snapshot and before the write lets the plan drive the bounded competing writer. The rejected runner-up was to validate only the events already on disk and let `foldEpoch` catch the rest: that is the measured defect, because `foldEpoch` is reached only after `discarded` (and, on the no-allocation path, its auto-open) has been written. A second rejected shape moved the preflight into the CLI or the gate module; the fold is owned by the ledger operation, and duplicating the policy is the second-fold-path the closed `C-SUPERSEDE-RECORD-2` contract forbids. A third rejected shape — the first draft of this prototype — validated the whole loose population as a fresh allocation with no reconciliation; the authority measured that it refuses the supported partial-deletion recovery state `{1:opened, 3:terminal}` (which exactly matches the durable epoch and which the base accepts and archives), so it is rejected. The whole operation already runs under the existing candidate lock; no lock change is proposed, and a cooperating lock cannot serialize a non-cooperating filesystem actor.

      **Measured red/green directions, and the boundary that is not `F158`.** With the identical final test file applied to both clones: the untouched base is `bunx tsc --noEmit` exit 0 and `bun test --timeout=0 src/grace-cursor.test.ts` = 240 pass / 6 fail (the four deterministic-refusal reds plus the competing-writer missing-seam red plus the structural pin red); the candidate is `bunx tsc --noEmit` exit 0 and 246 pass / 0 fail; and `bun run validate:ci` on the candidate exits 0 (`2335 pass / 0 fail` over `2338 tests across 68 files`). The competing-writer probe measured on the candidate: after the preflight snapshot the actor added a duplicate-id event, the operation wrote exactly one `3-T-001-discarded.xml` and refused with `duplicate event id 2 appears 2 times` with no ledger; the actor removed only its own file; the real CLI retry exited 0, left one epoch with exactly the events `{1:opened, 2:progress, 3:discarded}` and `run/` empty. That post-`discarded`, pre-ledger outcome is a competing-writer stage-(ii-a) residue and is explicitly not `F158`; the fold interrupted after its ledger write remains the distinct `C-RUN-LEDGER` write-verify-delete boundary.

      **The paid close shape, and what the plan must rehearse.** This bundle pays `F292 F292.1 F158`. The plan adds exactly three `bundleMinted` pins naming `C-DISCARD-PREFLIGHT-1-5087B21A` and makes the payer ratchet discriminate absent / stale / staged-only / current / archive states; rewrites exactly the two staged `Owner:` lines (`F292` at `:79`, `F292.1` at `:143`) before `--flush` while the bundle is active; flushes the charter row and the two staged findings; applies and archives the bundle; then runs an eligible `--retire` so the durable carriers (the charter row's `&lt;Pays&gt;` and the retired Findings' derived `&lt;PaidBy&gt;`) name this bundle. `F158` is already durable and needs no staged line. The scope guard in `src/artifact/scope.test.ts` is re-authored to this bundle's exact id and its eleven observable allowed literal paths (the twelve-path declared population minus the ignored scratch). The complete future authority sequence is: final assertions with terminal and fold; pre-close review and `gate verdict`; `gate apply`; `gate archive`; `ngrace apply`; post-archive review and verdict with all four `CloseEvidence` acknowledgements. The faithful full lifecycle has **not** been run at spec stage; it remains a plan-stage rehearsal on a clone, and the close must prove every writer-touched path lies inside the twelve declared literals and that `decisions.md` is never touched by a record writer.
    </Problem>
    <Assumptions>
      <Assumption>Base commit `98e009a8a4713616c1223733d61416958c3053a3` is the committed close of bundle 4 `C-FOLD-MIXED-RECOVERY-1-62A1B852`; bundles 1–4 are archived, `.ngrace/changes/active/` holds no other bundle, and this fresh slug has no predecessor transition. All were confirmed by reading the commit, listing the active directory, and checking the worktree before this draft was written. The measured `validate:ci`, `bunx tsc --noEmit`, and `bun test` figures are the dated snapshot above and expire when a bundle archives, so the close re-measures.</Assumption>
      <Assumption>Projected relations, not pinned figures: a deterministic refusal leaves the whole recursive tree byte-identical; a successful discard writes exactly one `discarded` and folds it once leaving `run/` empty; a handled competing-writer analogue leaves exactly one `discarded` residue consumed once by a later real fold. The criteria assert the relations; the concrete ids and the candidate's tree-unchanged result are the prototype's measured evidence and the plan re-measures them.</Assumption>
      <Assumption>The retirement payer map will need exactly three entries (`F292`, `F292.1`, `F158`) naming `C-DISCARD-PREFLIGHT-1-5087B21A`, because the payer invariant loop walks every derived payer token against `baseline[token] ?? bundleMinted[token]`. **Measured 2026-09-22:** the map holds none of the three at this base, `F158` is already a live durable Finding, and the two staged `Owner:` lines for `F292`/`F292.1` are at `:79`/`:143`. Whether the fixture's exact extension lands in one edit or several is a plan measurement.</Assumption>
      <Assumption>The `AC-DISCARDED-CALLER` pin counts literal `kind: "discarded"` occurrences and moves from 1 to 2 when the preflight constructs the prospective event; the plan re-measures whether the final implementation produces that count. The write-surface `unlinkSync` inventory pin does not move, because the preflight performs no delete.</Assumption>
      <Assumption>The disposable real-`.git` clones built for this draft under `/private/tmp/ngrace-base-b5` (untouched base plus the final test file) and `/private/tmp/ngrace-cand-b5` (base plus the production prototype plus the identical final test file) are the reproduction basis; their receipts match the runs recorded in `Problem` and `VerificationIntent`. The normative verification is repository tests derived from them; no close criterion depends on `/private/tmp` surviving.</Assumption>
    </Assumptions>
  </C-DISCARD-PREFLIGHT-1-5087B21A>
</NgraceChangeSpec>
