<NgraceChangePlan graceVersion="1.0" status="applied">
  <C-EXPLAIN-COVERAGE>
    <IntentSummary>
      Deliver RM-GOVERNED-PATH P1 steps 1–3 only: write a coverage test whose
      universe is collectEmittedIssueCodes and whose predicate binds the
      resolution path (exact / prefix / review-catalog / gate-catalog pass;
      emittable-fallback and unknown fail); record that red; then wire review
      and gate catalog objects into getLintIssueGuide; then author only the
      constructed codes the test still reports red, plus five
      change.task-*dependency* exact guides each bound to its own emitted
      message from a distinct plant; then append (ngrace lint --explain
      &lt;code&gt;) once per distinct error code on text lint, suppressed
      under --format json.
    </IntentSummary>
    <BaselineAssertions>
      <MustExist><Value>src/lint/catalog.ts</Value></MustExist>
      <MustExist><Value>src/lint/catalog.test.ts</Value></MustExist>
      <MustExist><Value>src/lint/core.ts</Value></MustExist>
      <MustExist><Value>src/lint/core.test.ts</Value></MustExist>
      <MustExist><Value>src/grace-lint.test.ts</Value></MustExist>
      <MustExist><Value>.ngrace/changes/active/C-EXPLAIN-COVERAGE/spec.xml</Value></MustExist>
      <MustExist><Value>M-LINT-CATALOG</Value></MustExist>
      <MustExist><Value>M-LINT-CORE</Value></MustExist>
      <!-- Pre-change world (F28: each load-bearing MustNotContain / MustContain below is true at plan-authoring HEAD 12d897a). -->
      <MustContain>
        <File>src/lint/catalog.ts</File>
        <Text>function getLintIssueGuide</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/catalog.test.ts</File>
        <Text>function collectEmittedIssueCodes</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/core.ts</File>
        <Text>function formatTextReport</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/catalog.ts</File>
        <Text>See the review catalog (src/review/catalog.ts)</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/catalog.ts</File>
        <Text>See the gate catalog (src/gates/catalog.ts)</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/catalog.ts</File>
        <Text>does not yet have a dedicated explanation entry</Text>
      </MustContain>
      <MustContain>
        <File>src/grace-lint.test.ts</File>
        <Text>/ngrace review/i</Text>
      </MustContain>
      <MustNotContain>
        <File>src/lint/catalog.test.ts</File>
        <Text>AC-COVERAGE-NO-BOILERPLATE</Text>
      </MustNotContain>
      <MustNotContain>
        <File>src/lint/catalog.test.ts</File>
        <Text>AC-FIX-SHAPE</Text>
      </MustNotContain>
      <MustNotContain>
        <File>src/lint/catalog.ts</File>
        <Text>"change.task-invalid-dependency"</Text>
      </MustNotContain>
      <MustNotContain>
        <File>src/lint/catalog.ts</File>
        <Text>"change.task-unknown-dependency"</Text>
      </MustNotContain>
      <MustNotContain>
        <File>src/lint/catalog.ts</File>
        <Text>"change.task-self-dependency"</Text>
      </MustNotContain>
      <MustNotContain>
        <File>src/lint/catalog.ts</File>
        <Text>"change.task-dependency-cycle"</Text>
      </MustNotContain>
      <MustNotContain>
        <File>src/lint/catalog.ts</File>
        <Text>"change.task-duplicate-dependency"</Text>
      </MustNotContain>
      <MustNotContain>
        <File>src/lint/core.ts</File>
        <Text>ngrace lint --explain</Text>
      </MustNotContain>
      <MustNotContain>
        <File>src/lint/core.test.ts</File>
        <Text>ngrace lint --explain</Text>
      </MustNotContain>
      <MustVerify><Module>M-LINT-CATALOG</Module></MustVerify>
      <MustVerify><Module>M-LINT-CORE</Module></MustVerify>
    </BaselineAssertions>
    <TargetAssertions>
      <MustExist><Value>src/lint/catalog.ts</Value></MustExist>
      <MustExist><Value>src/lint/catalog.test.ts</Value></MustExist>
      <MustExist><Value>src/lint/core.ts</Value></MustExist>
      <MustExist><Value>src/lint/core.test.ts</Value></MustExist>
      <MustExist><Value>src/grace-lint.test.ts</Value></MustExist>
      <MustExist><Value>M-LINT-CATALOG</Value></MustExist>
      <MustExist><Value>M-LINT-CORE</Value></MustExist>
      <MustContain>
        <File>src/lint/catalog.ts</File>
        <Text>function getLintIssueGuide</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/catalog.test.ts</File>
        <Text>function collectEmittedIssueCodes</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/core.ts</File>
        <Text>function formatTextReport</Text>
      </MustContain>
      <MustNotContain>
        <File>src/lint/catalog.ts</File>
        <Text>See the review catalog (src/review/catalog.ts)</Text>
      </MustNotContain>
      <MustNotContain>
        <File>src/lint/catalog.ts</File>
        <Text>See the gate catalog (src/gates/catalog.ts)</Text>
      </MustNotContain>
      <MustNotContain>
        <File>src/lint/catalog.ts</File>
        <Text>does not yet have a dedicated explanation entry</Text>
      </MustNotContain>
      <MustNotContain>
        <File>src/grace-lint.test.ts</File>
        <Text>/ngrace review/i</Text>
      </MustNotContain>
      <MustContain>
        <File>src/lint/catalog.test.ts</File>
        <Text>AC-COVERAGE-NO-BOILERPLATE</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/catalog.test.ts</File>
        <Text>AC-FIX-SHAPE</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/catalog.ts</File>
        <Text>"change.task-invalid-dependency"</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/catalog.ts</File>
        <Text>"change.task-unknown-dependency"</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/catalog.ts</File>
        <Text>"change.task-self-dependency"</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/catalog.ts</File>
        <Text>"change.task-dependency-cycle"</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/catalog.ts</File>
        <Text>"change.task-duplicate-dependency"</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/core.ts</File>
        <Text>ngrace lint --explain</Text>
      </MustContain>
      <MustContain>
        <File>src/lint/core.test.ts</File>
        <Text>ngrace lint --explain</Text>
      </MustContain>
      <MustVerify><Module>M-LINT-CATALOG</Module></MustVerify>
      <MustVerify><Module>M-LINT-CORE</Module></MustVerify>
      <MustPassCommand>
        <Command>bun test src/lint/catalog.test.ts</Command>
      </MustPassCommand>
      <MustPassCommand>
        <Command>bun test src/lint/core.test.ts</Command>
      </MustPassCommand>
      <MustPassCommand>
        <Command>bun test src/grace-lint.test.ts</Command>
      </MustPassCommand>
      <MustPassCommand>
        <Command>bun run validate:ci</Command>
      </MustPassCommand>
    </TargetAssertions>
    <DurableScope>
      <GraphAnchors>
        <M-LINT-CATALOG />
        <M-LINT-CORE />
      </GraphAnchors>
      <VerificationAnchors>
        <V-M-LINT-CATALOG />
        <V-M-LINT-CORE />
      </VerificationAnchors>
    </DurableScope>
    <ObservedWriteScope>
      <File>src/lint/catalog.ts</File>
      <File>src/lint/catalog.test.ts</File>
      <File>src/lint/core.ts</File>
      <File>src/lint/core.test.ts</File>
      <File>src/grace-lint.test.ts</File>
      <File>.ngrace/changes/active/C-EXPLAIN-COVERAGE/plan.xml</File>
      <File>.ngrace/changes/active/C-EXPLAIN-COVERAGE/spec.xml</File>
    </ObservedWriteScope>
    <!--
      DESIGN (binding for implementers; authority reviews the fixture hardest)

      Plan-authoring HEAD: 12d897a (branch feat/explain-coverage).
      Spec is approved. Amendment of AC-COVERAGE-NO-BOILERPLATE and the
      F35 citation is authorized so those sentences describe the
      resolution-path predicate; do not otherwise rewrite claims.
      Re-measure every count at execute (F33).
      Do not pin EXACT_GUIDES size, allowlist length, or catalog cardinalities.
      Do not touch grammar.ts, xml.ts, query/health.ts,
      skills, packaged mirrors, docs/plans, CHANGELOG, or version surfaces.

      Why four tasks, not six. The coverage predicate
      (expect(fallbackHits).toEqual([]), hits = universe members whose
      resolution is emittable-fallback or unknown) is red at HEAD and stays red
      after the wire. bun test src/lint/catalog.test.ts therefore cannot
      go green until the constructed-code delta lands. A red-only T-001
      whose Verification is that command can never close. The product's
      task-close rule and the spec's three-phase sequencing are reconciled
      by putting write-test / wire / still-red / delta in T-001 as four
      separate tool round trips. Fix-shape and the pointer each have their
      own red-then-green close, so they stay separate tasks.

      ─────────────────────────────────────────────────────────────────
      D0 — Task decomposition
      ─────────────────────────────────────────────────────────────────
      | Task | Surface | Satisfies |
      | T-001 | catalog.ts, catalog.test.ts, grace-lint.test.ts
        | AC-COVERAGE-UNIVERSE, AC-COVERAGE-NO-BOILERPLATE,
          AC-WIRE-CATALOG-GUIDES, AC-FALLBACK-HONEST |
      | T-002 | catalog.ts, catalog.test.ts
        | AC-FIX-SHAPE |
      | T-003 | core.ts, core.test.ts
        | AC-EXPLAIN-POINTER, AC-POINTER-JSON |
      | T-004 | dogfood close
        | AC-SUITE-AND-LINT, AC-HEAD-RED (full) |

      T-003 has empty DependsOn: core.ts is file-disjoint from T-001/T-002.
      T-002 DependsOn T-001 because both write EXACT_GUIDES and because
      T-001 must not author the five dependency guides (they pass the
      resolution-path predicate at HEAD via the change. family prefix).

      ─────────────────────────────────────────────────────────────────
      D1 — Sequencing (hard rule 3; T-001 internal)
      ─────────────────────────────────────────────────────────────────
      Round trip 1 (test only). Write the coverage test in
      src/lint/catalog.test.ts. Universe = collectEmittedIssueCodes
      (reuse the existing function; do not extract unless a compile
      constraint forces it). Predicate: for every universe code,
      getLintIssueGuideResolution is exact, prefix, review-catalog,
      or gate-catalog; emittable-fallback and unknown fail. Observe
      the path via that dedicated accessor, not by matching lint-explain
      prose and not by grepping guideFor / GATE_CATALOG /
      hasExactSurfaceGuide. Family prefix guides pass this predicate.
      Record cursor fail. Do not land the P1.1 wire in this round trip.

      HEAD today: the function exists and only feeds allowlist
      completeness, which passes. No test uses it as an explain
      universe. The new predicate fails on every REVIEW_CATALOG and
      GATE_CATALOG code, xml.parse, xml.missing-file, every
      design-context.* literal, and every health.* literal except the
      two exact UI-state guides. It PASSES all five
      change.task-*dependency* codes (family prefix, not fallback).

      Round trip 2 (test only). Write AC-WIRE field-equality tests:
      for every key of REVIEW_CATALOG and every key of GATE_CATALOG,
      getLintIssueGuide(code) title / explanation / remediation
      equal the catalog object fields. Classification remains
      emittable-uncatalogued. None of those codes is an EXACT_GUIDES
      key. Sample at least review.scope-outside-write-scope and
      gate.apply.no-verdict; iterating both catalogs is the live
      assertion, not a one-code sample. Record fail. Still no
      catalog.ts production edit.

      HEAD today: both sampled codes return dedicated-entry
      boilerplate; CLI lint explain review.scope-outside-write-scope
      prints Classification: emittable-uncatalogued and matches
      /ngrace review/i because of that boilerplate.

      Round trip 3 (wire). Consult guideFor / REVIEW_CATALOG and
      GATE_CATALOG[code] after PREFIX_GUIDES miss. Copy title,
      explanation, remediation from the object. Do not add review.*
      or gate.* keys to EXACT_GUIDES. Do not change classifyIssueCode
      to call them exact. Delete the review and gate boilerplate
      branches. Catalog miss on those prefixes must not claim a
      coverage gap (unknown path, or an honest "not a registered
      code on this surface" message). Move the
      src/grace-lint.test.ts /ngrace review/i assertion onto the
      catalog title "Changed File Outside ObservedWriteScope"; keep
      Classification: emittable-uncatalogued. Run the coverage test:
      it is still red on remaining constructed codes. Record that
      still-red. Do not author remaining exact guides in this
      round trip.

      Round trip 4 (delta). Author exact guides only for constructed
      universe members the coverage test still reports red. Do not
      author the five change.task-*dependency* codes here (they
      already pass the predicate). Do not mass-author
      PREFIX_COVERED_LEGACY_CODES. Rewrite or delete the
      isEmittableIssueCode fallback: keeping "does not yet have a
      dedicated explanation entry" is F10. Invented xml.foo stays
      classified emittable and must not claim this binary emits a
      constructed code we forgot to document. Prefix rule 6 stays.
      Coverage test goes green. Discrimination (probe, not a
      fail-then-pass attempt pair; all 23 guides already exist):
      remove the exact guide for xml.parse and the test reddens
      with [xml.parse] (no xml. family prefix matches xml.parse;
      xml.generic- does not); restore. Leave the five
      change.task-*dependency* codes on family text (resolution
      prefix) and the test stays green. Record probes verbatim;
      revert.

      Never fail and pass in one tool call. Never a red backed only
      by a grep. Never a retrospective red via stash.

      ─────────────────────────────────────────────────────────────────
      D2 — AC-FIX-SHAPE fixture (T-002; review this hardest)
      ─────────────────────────────────────────────────────────────────
      Home: src/lint/catalog.test.ts. Use lintGraceProject on a temp
      project (writeMinimalNgraceProject plus a custom approved
      plan), not validateChangeArtifact alone and not a
      catalog-to-catalog comparison. Do not write grammar.ts.

      Five plants, five temp projects, five expected codes. A helper
      that takes a kind and returns { code, issue, guide }. Assert
      the plant fired that code. Assert it did not fire the other
      four dependency codes (one shared plant does not satisfy the
      set).

      Plants (spec :639):
        invalid   T-001 DependsOn text not-a-task
                  (not a comma list of T-NNN; P0.3 accepts those)
        unknown   T-001 DependsOn T-999; no such task exists
        self      T-001 DependsOn T-001
        cycle     T-001 DependsOn T-002 and T-002 DependsOn T-001
        duplicate T-001 exists; T-002 DependsOn T-001 T-001
                  (repeated valid id, not an invalid token)

      Binding, not a substring copy. Per code, a closed expected
      shape list lives in the test (authored, not read from
      issue.message at runtime):
        invalid    multi-value text list of T-NNN ids (comma,
                   semicolon, or whitespace); Task-element children
                   holding a T-NNN id; self-closing T-NNN anchor
                   children
        unknown    depends on unknown task
        self       cannot depend on itself
        cycle      dependency cycle involving
        duplicate  repeats dependency

      Assert issue.message names every expected shape for that code.
      Assert getLintIssueGuide(code).remediation names every
      expected shape for that code. Do not
      expect(guide).toContain(issue.message). Do not grep isolated
      words such as "canonical" as the only check.

      Additional negatives, required:
        all five   remediation does not contain Keep draft and
                   approved bundles (the family archive-placement)
        four siblings  remediation does not name invalid's three
                   DependsOn authoring shapes (copying them onto a
                   token that already is valid T-NNN fails)

      markup.unparsed-link-token plant: a governed file whose LINKS
      contains TYPO-BAD (same token project-utils.test.ts uses).
      Assert message and guide still agree on accepted families
      (LINKS: M-*, DF-*, V-M-*) and the [,;\s]+ separator set.
      This assertion is green at HEAD and is the discrimination
      anchor, not a red. It must not be the sole member asserted.
      It must not carry AC-FIX-SHAPE's red.

      HEAD-RED: write all six plants and the agreement asserts
      before any EXACT_GUIDES edit. The five dependency asserts
      fail at HEAD (family title "neo-grace Change Lifecycle
      Issue"; remediation is keep-drafts-active / move-applied to
      archive). markup's assert passes at HEAD. Record fail for
      all five. Then add five exact guides, each answering its
      own message. AC-FIX-SHAPE is not satisfiable by
      change.task-invalid-dependency alone.

      PREFIX_COVERED_LEGACY_CODES remains; do not pin its length.
      Removing the five from the allowlist once they are exact
      keys is allowed and is not a criterion.

      ─────────────────────────────────────────────────────────────────
      D3 — Pointer (T-003)
      ─────────────────────────────────────────────────────────────────
      Reuse the existing plural-baseline fixture in
      src/lint/core.test.ts (two assertion.MustNotExist errors).
      Write tests first: formatTextReport contains the exact
      pointer (ngrace lint explain assertion.MustNotExist) once,
      not twice; warning-only codes do not receive a pointer;
      JSON.stringify of the same LintResult does not contain
      ngrace lint explain. Both directions together. HEAD today:
      formatTextReport never contains that pointer; JSON already
      lacks it (JSON-absence alone is not evidence). Record fail.
      Then edit formatTextReport only. Pointer lives in the text
      renderer, not as a new LintResult field or string. Do not
      confuse lint format json with the explain JSON envelope
      (which must still carry guide).

      ─────────────────────────────────────────────────────────────────
      D4 — HEAD-RED ownership
      ─────────────────────────────────────────────────────────────────
      | Spec AC | Owner | Pre-edit measurement |
      | AC-COVERAGE-UNIVERSE
        + AC-COVERAGE-NO-BOILERPLATE first fail | T-001 RT1
        | new test fails; universe is collectEmittedIssueCodes |
      | AC-WIRE-CATALOG-GUIDES | T-001 RT2 then RT3
        | field equality fails on catalog objects |
      | AC-COVERAGE-NO-BOILERPLATE still-red + green
        + AC-FALLBACK-HONEST | T-001 RT3 then RT4
        | still red after wire; green after delta |
      | AC-FIX-SHAPE | T-002
        | five plants fail on family archive-placement;
          markup already green |
      | AC-EXPLAIN-POINTER + AC-POINTER-JSON | T-003
        | text report lacks the pointer; pair fails |
      | AC-HEAD-RED (full) + AC-SUITE-AND-LINT | T-004 Satisfies;
        reds recorded on T-001..T-003 |

      ─────────────────────────────────────────────────────────────────
      D5 — ObservedWriteScope (measured at 12d897a)
      ─────────────────────────────────────────────────────────────────
      Forced by the spec:
        src/lint/catalog.ts
        src/lint/catalog.test.ts
        src/lint/core.ts
        src/lint/core.test.ts
        src/grace-lint.test.ts   (ungoverned; OWS as a path.
          isModuleOwnableWritePath excludes *.test.ts, so this
          does not raise change.graph-anchors-miss-write-scope)
        .ngrace/changes/active/C-EXPLAIN-COVERAGE/plan.xml

      spec.xml is not in ObservedWriteScope (omitted at plan
      authoring because the spec was then treated as immutable).
      This sweep amends the coverage-predicate restatement in
      spec.xml without expanding OWS.

      Deliberately excluded: grammar.ts, xml.ts, query/health.ts,
      skills, plugins, docs/plans, archive, CHANGELOG, version
      surfaces, run-ledger.xml / run.xml (execution companions,
      not this deliverable; expand OWS at execute if the cursor
      writes them).

      ─────────────────────────────────────────────────────────────────
      D6 — Counts
      ─────────────────────────────────────────────────────────────────
      Determinism ratchet: re-measure; do not drop keys. If
      validate:ci cardinality moves only because a catch is
      intentionally added, re-measure and record. Otherwise stop
      and report.

      Guide / allowlist / catalog cardinalities are not pins.

      ─────────────────────────────────────────────────────────────────
      D7 — Baseline assertion.* count predictions
      ─────────────────────────────────────────────────────────────────
      Load-bearing flips:
        A  MustNotContain catalog.test.ts  AC-COVERAGE-NO-BOILERPLATE
        B  MustContain    catalog.ts       See the review catalog (src/review/catalog.ts)
        C  MustContain    catalog.ts       See the gate catalog (src/gates/catalog.ts)
        D  MustContain    catalog.ts       does not yet have a dedicated explanation entry
        E  MustContain    grace-lint.test.ts /ngrace review/i
        F  MustNotContain catalog.test.ts  AC-FIX-SHAPE
        G–K MustNotContain catalog.ts      the five quoted change.task-*dependency* keys
        L  MustNotContain core.ts          ngrace lint explain
        M  MustNotContain core.test.ts     ngrace lint explain

      Stay-true (not in N): getLintIssueGuide, collectEmittedIssueCodes,
      formatTextReport MustContain; all MustExist / MustVerify.

        After plan approval, before any task: N = 0. Flag if N ≠ 0.
        After T-001 (A–E): N = 5. Flag if N ≠ 5.
        After T-001 + T-002 (F, G–K): N = 11. Flag if N ≠ 11.
        After T-001 + T-003 only (A–E, L, M): N = 7. Flag if N ≠ 7.
        After T-001 + T-002 + T-003: N = 13. Flag if N ≠ 13.
        After T-004 / apply-archive: lint 0/0 at close only (F26).

      Never weaken a BaselineAssertion to reduce N.

      ─────────────────────────────────────────────────────────────────
      D8 — Discrimination probes (must see red once)
      ─────────────────────────────────────────────────────────────────
      1. Remove the exact guide for xml.parse → coverage test
         reddens with [xml.parse] (no xml. family prefix matches
         xml.parse; xml.generic- does not). Restore.
      2. Leave the five change.task-*dependency* codes on family
         text (resolution prefix) → coverage test stays green.
      3. Universe = Object.keys of any guide table → must not ship.
      4. One fixture asserting a shared string, or five guides that
         all name invalid's three DependsOn shapes → AC-FIX-SHAPE
         fails; do not ship.
      5. Only markup or only change.task-invalid-dependency asserted
         for AC-FIX-SHAPE → fails.
      6. Pointer stuffed into LintResult so JSON grows the string →
         AC-POINTER-JSON fails.
      7. JSON-absence asserted without a text report that has the
         pointer → not evidence.

      Record verbatim; do not leave a break committed.

      ─────────────────────────────────────────────────────────────────
      D9 — Execution discipline
      ─────────────────────────────────────────────────────────────────
      One open epoch. T-001 four round trips. T-002 and T-003 each
      fail-then-pass in their own trips. T-004: no manufactured red
      if green. Terminal / fold / gate / commit are operator steps
      after T-004 unless authority says otherwise. Packaged version
      stays 6.2.0. No Clarifications section (artifact.semantic-anchor-attribute
      on a typed Clarification targeting AC-*).
    -->
    <ImplementationPlan>
      <T-001>
        <Title>Coverage test, wire catalogs, author the leftover constructed-code guides</Title>
        <DependsOn></DependsOn>
        <Satisfies>
          <AC-COVERAGE-UNIVERSE />
          <AC-COVERAGE-NO-BOILERPLATE />
          <AC-WIRE-CATALOG-GUIDES />
          <AC-FALLBACK-HONEST />
        </Satisfies>
        <AcceptanceCriteria>
          <Criterion>
            AC-HEAD-RED (coverage half): before the P1.1 wire, in its
            own tool round trip, write a coverage test in
            src/lint/catalog.test.ts whose universe is
            collectEmittedIssueCodes (reused or extracted) and whose
            predicate asserts each universe member's
            getLintIssueGuideResolution is exact, prefix,
            review-catalog, or gate-catalog — not emittable-fallback
            and not unknown. Observe the path via that dedicated
            accessor, not by matching --explain prose. HEAD today: no
            test uses collectEmittedIssueCodes as an explain universe;
            the predicate fails on review, gate, xml.parse,
            xml.missing-file, every design-context.* literal, and every
            health.* literal except the two exact UI-state guides; it
            PASSES all five change.task-*dependency* codes (family
            prefix). Record cursor fail. Never a red backed only by a
            grep; never fail and pass in one tool call; never a
            retrospective red via stash. Failure conditions: universe =
            Object.keys of any guide table; universe =
            isEmittableIssueCode over a handmade prefix expansion;
            completeness test renamed and reused; green at pre-change
            HEAD; P1.1 wire landed in this round trip.
          </Criterion>
          <Criterion>
            AC-HEAD-RED (wire half): before the wire, in its own tool
            round trip, write field-equality tests: for every
            REVIEW_CATALOG code and every GATE_CATALOG code,
            getLintIssueGuide returns that object's title, explanation,
            and remediation; classification remains
            emittable-uncatalogued; none of those codes is an
            EXACT_GUIDES key. HEAD today:
            getLintIssueGuide("review.scope-outside-write-scope") and
            getLintIssueGuide("gate.apply.no-verdict") return the
            dedicated-entry boilerplate. Record fail. Failure
            conditions: only one code sampled; assertion greps guideFor
            / GATE_CATALOG identifiers instead of comparing returned
            fields to the catalog object; this round trip also lands
            the wire.
          </Criterion>
          <Criterion>
            Wire: getLintIssueGuide returns the review catalog guide
            object and the gate catalog guide object for registered
            codes (field copy, not synthesized toTitleFromCode).
            classifyIssueCode stays emittable-uncatalogued for those
            codes. They do not become EXACT_GUIDES keys. Delete the
            review and gate boilerplate branches; a catalog miss must
            not claim a dedicated-entry coverage gap. Move
            src/grace-lint.test.ts /ngrace review/i onto the catalog
            title Changed File Outside ObservedWriteScope; keep
            Classification: emittable-uncatalogued. After this edit,
            in its own tool round trip, the coverage test is still red
            on remaining constructed codes. Record that still-red.
            Do not author the leftover exact guides in the same round
            trip as the wire. Failure conditions: synthesized title
            kept while explanation is swapped; review/gate added to
            EXACT_GUIDES; classification changed to exact; coverage
            test green before the constructed-code delta.
          </Criterion>
          <Criterion>
            Author exact guides only for constructed universe members
            the coverage test still reports red after the wire. Do not
            author the five change.task-*dependency* codes (they
            already pass via the change. PREFIX_GUIDES family;
            remediation quality is AC-FIX-SHAPE / T-002). Do not
            mass-author PREFIX_COVERED_LEGACY_CODES. Rewrite or delete
            the isEmittableIssueCode fallback so no reachable
            getLintIssueGuide branch claims a dedicated-entry coverage
            gap; invented prefix matches, if still classified
            emittable, must not say the entry is merely unwritten.
            Prefix rule 6 stays. The coverage predicate binds to the
            resolution path, not to --explain prose: every
            collectEmittedIssueCodes member must resolve via exact
            guide, review catalog, gate catalog, or a matching
            PREFIX_GUIDES family; a member that falls through to the
            generic emittable fallback or to unknown fails. Observe
            the path via a dedicated exported accessor, not a new
            field on the guide object (withLintIssueGuide spreads
            guide fields onto lint issues and --format json). The
            three dedicated-entry sentences are gone after
            AC-FALLBACK-HONEST and cannot be the predicate.
            Discrimination is substantiated by probe, not by a
            fail→pass attempt pair (all 23 constructed guides already
            exist; do not delete one to manufacture a red): (1)
            remove the exact guide for xml.parse and the coverage
            test reddens (no xml. family prefix matches xml.parse;
            xml.generic- does not); restore. (2) leave the five
            change.task-*dependency* codes on family text and the
            test stays green. Failure conditions: count pins; this
            predicate treated as proof of AC-FIX-SHAPE; fallback left
            in place because "it is unreachable for registered
            codes"; five dependency codes given invalidMessage's
            three DependsOn shapes here; predicate that matches
            deleted boilerplate sentences and is green
            unconditionally.
          </Criterion>
        </AcceptanceCriteria>
        <Verification>
          <Command>bun test src/lint/catalog.test.ts</Command>
          <Command>bun test src/grace-lint.test.ts</Command>
        </Verification>
      </T-001>
      <T-002>
        <Title>Five dependency plants, each bound to its own emitted message</Title>
        <DependsOn>
          <Task>T-001</Task>
        </DependsOn>
        <Satisfies>
          <AC-FIX-SHAPE />
        </Satisfies>
        <AcceptanceCriteria>
          <Criterion>
            AC-HEAD-RED (fix-shape half): before any of the five
            EXACT_GUIDES edits, in its own tool round trip, write the
            review-replay fixture in src/lint/catalog.test.ts. Six
            plants via lintGraceProject on temp projects: not-a-task
            (change.task-invalid-dependency); T-999 with no such task
            (change.task-unknown-dependency); T-001 DependsOn T-001
            (change.task-self-dependency); T-001↔T-002
            (change.task-dependency-cycle); T-001 T-001 repeated
            (change.task-duplicate-dependency); LINKS token TYPO-BAD
            (markup.unparsed-link-token). Each dependency plant must
            fire its own code and must not fire the other four. HEAD
            today: all five dependency --explain bodies are the
            change. family guide (archive-placement); markup's exact
            guide already agrees with unparsedLinkTokenMessage and is
            green (not a red). The five agreement asserts fail; markup
            does not. Record fail for all five. Never a red backed
            only by a grep; never fail and pass in one tool call;
            markup must not carry this red. Failure conditions: one
            shared plant; only markup asserted; only
            change.task-invalid-dependency asserted; fixture omitted
            so the bind is catalog-to-catalog.
          </Criterion>
          <Criterion>
            For each fixture-bound code, --explain remediation and the
            emitted issue.message agree on the accepted repair shapes
            that message names. Binding is a closed expected-shape
            list per code, authored in the test, asserted against both
            sides — not expect(guide).toContain(issue.message) and not
            a word-hunt for tokens such as "canonical". invalid:
            multi-value text list of T-NNN ids (comma, semicolon, or
            whitespace); Task-element children holding a T-NNN id;
            self-closing T-NNN anchor children. unknown: depends on
            unknown task. self: cannot depend on itself. cycle:
            dependency cycle involving. duplicate: repeats dependency.
            markup (already green): accepted LINKS families M-*, DF-*,
            V-M-* and the [,;\s]+ separator set. A topical "this is a
            dependency problem" guide fails. A guide that says the
            token must be canonical when the token already is fails.
            Copying invalid's three DependsOn shapes onto a sibling
            whose token is already valid T-NNN fails. Family
            archive-placement left in place fails. All five dependency
            codes are required red-to-green; this criterion is not
            satisfiable by change.task-invalid-dependency alone.
          </Criterion>
          <Criterion>
            Probe: restore the family archive-placement remediation
            for one sibling, confirm that sibling's agreement assert
            reddens and the others stay green, revert, record
            verbatim. Probe: copy invalid's three-shape text onto
            change.task-dependency-cycle, confirm that sibling fails,
            revert.
          </Criterion>
        </AcceptanceCriteria>
        <Verification>
          <Command>bun test src/lint/catalog.test.ts</Command>
        </Verification>
      </T-002>
      <T-003>
        <Title>Text lint points at ngrace lint --explain once per distinct error code</Title>
        <DependsOn></DependsOn>
        <Satisfies>
          <AC-EXPLAIN-POINTER />
          <AC-POINTER-JSON />
        </Satisfies>
        <AcceptanceCriteria>
          <Criterion>
            AC-HEAD-RED (pointer half): before editing formatTextReport,
            in its own tool round trip, write tests in
            src/lint/core.test.ts on the existing plural-baseline
            fixture (two assertion.MustNotExist errors): the text
            report contains the exact pointer (ngrace lint --explain
            assertion.MustNotExist) once, not twice; warning-only
            codes do not receive a pointer; JSON.stringify of the same
            LintResult does not contain ngrace lint --explain. Both
            directions are asserted together. HEAD today:
            formatTextReport never contains ngrace lint --explain;
            JSON already lacks it (not evidence by itself). The pair
            fails. Record fail. Never a red backed only by a grep;
            never fail and pass in one tool call.
          </Criterion>
          <Criterion>
            formatTextReport on a LintResult that contains
            error-severity issues includes (ngrace lint --explain
            &lt;code&gt;) once per distinct error code. Warning-only
            codes do not receive a pointer. JSON.stringify of the same
            LintResult and ngrace lint --format json stdout do not
            contain ngrace lint --explain. The pointer lives only in
            the text renderer, not as a new LintResult field or
            string. The explain JSON envelope must still carry guide.
            Failure conditions: pointer on warnings; pointer once per
            issue rather than once per distinct code; pointer only
            documented in a comment; identifier grep for a helper name
            instead of asserting the rendered string; JSON-absence
            asserted alone; pointer stuffed into LintResult; explain
            JSON envelope confused with lint --format json.
          </Criterion>
        </AcceptanceCriteria>
        <Verification>
          <Command>bun test src/lint/core.test.ts</Command>
        </Verification>
      </T-003>
      <T-004>
        <Title>Dogfood close: suite, lint at close, full HEAD-RED proof</Title>
        <DependsOn>
          <Task>T-002</Task>
          <Task>T-003</Task>
        </DependsOn>
        <Satisfies>
          <AC-SUITE-AND-LINT />
          <AC-HEAD-RED />
        </Satisfies>
        <AcceptanceCriteria>
          <Criterion>
            AC-SUITE-AND-LINT at close (F26 qualifier): after apply
            and archive, bun run ngrace lint --path . is 0 errors and
            0 warnings. Do not claim 0/0 mid-change while Baseline
            falsifiers are expected red (predicted N = 13 after
            T-001..T-003). Never weaken a BaselineAssertion to move a
            lint count.
          </Criterion>
          <Criterion>
            bun test green for touched packages; bun run validate:ci
            green (determinism ratchet cardinality only moves if a
            catch is intentionally added — re-measure, do not drop
            keys). ObservedWriteScope covers every path the
            deliverable forces, including src/grace-lint.test.ts as a
            path. No edits under docs/plans/ or
            .ngrace/changes/archive/; no CHANGELOG; no version
            surfaces (stays 6.2.0). Failure conditions: suite red;
            lint 0/0 claimed mid-window; ratchet keys dropped; writes
            outside declared scope; HEAD-green lint cited as proof
            the pointer or the wire landed.
          </Criterion>
          <Criterion>
            AC-HEAD-RED post-change: coverage universe is
            collectEmittedIssueCodes and the predicate is green;
            review/gate field-equal to catalog objects;
            five dependency guides each agree with their own emitted
            message; markup remains the already-green anchor; text
            pointer present and JSON pair holds; no reachable
            dedicated-entry coverage-gap claim. Pre-edit reds were
            recorded on T-001 (coverage, wire, still-red-after-wire),
            T-002 (all five plants, not markup, not invalid alone),
            and T-003 (pointer pair) — not manufactured at close.
            Failure conditions: a check that passed on pre-change
            HEAD while claiming to prove the new property; fail and
            pass in one tool call; AC-SUITE-AND-LINT required to be
            red at HEAD; only change.task-invalid-dependency recorded
            red for AC-FIX-SHAPE.
          </Criterion>
        </AcceptanceCriteria>
        <Verification>
          <Command>bun run validate:ci</Command>
          <Command>bun test src/lint/catalog.test.ts src/lint/core.test.ts src/grace-lint.test.ts</Command>
        </Verification>
      </T-004>
    </ImplementationPlan>
  </C-EXPLAIN-COVERAGE>
</NgraceChangePlan>
