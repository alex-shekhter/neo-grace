# Auth keyboard-navigation interviews (fixture)

Synthetic research notes for the golden-path example. Used by
`C-ADD-KEYBOARD-NAV` as a `<UserResearch>` design reference.

- Users expect arrow keys to move focus between ledger rows.
- Home/End should jump to first/last visible row.
