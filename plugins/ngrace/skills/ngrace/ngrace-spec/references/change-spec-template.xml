<NgraceChangeSpec graceVersion="1.0" status="draft">
  <C-CHANGE-ID>
    <Summary>$SUMMARY</Summary>
    <Problem>$PROBLEM</Problem>
    <Goals>
      <Goal>$GOAL</Goal>
    </Goals>
    <Constraints>
      <Constraint>$CONSTRAINT</Constraint>
    </Constraints>
    <NonGoals>
      <NonGoal>$NON_GOAL</NonGoal>
    </NonGoals>
    <AcceptanceCriteria>
      <!-- Prefer addressable AC-* anchors so ngrace-plan can map them via <Satisfies>. Legacy free-text <Criterion> remains valid. -->
      <AC-EXAMPLE-CRITERION>$ACCEPTANCE_CRITERION</AC-EXAMPLE-CRITERION>
    </AcceptanceCriteria>
    <AffectedAreas>
      <M-AFFECTED-MODULE />
    </AffectedAreas>
    <VerificationIntent>
      <ExpectedCommand>$EXPECTED_COMMAND</ExpectedCommand>
      <ExpectedEvidence>$EXPECTED_EVIDENCE</ExpectedEvidence>
    </VerificationIntent>
    <!-- Optional design-artifact links (G-18). Figma url must be http(s); UserResearch is project-relative. -->
    <DesignReferences>
      <Figma url="https://www.figma.com/design/EXAMPLE/Frame">$FIGMA_LABEL</Figma>
      <UserResearch>docs/research/EXAMPLE.md</UserResearch>
    </DesignReferences>
    <Assumptions>
      <Assumption>$ASSUMPTION</Assumption>
    </Assumptions>
    <!-- Optional typed holes (D12). target must be IC-*, INV-*, or AC-*. Never use prose [NEEDS CLARIFICATION]. -->
    <!--
    <Clarifications>
      <Clarification target="IC-EXAMPLE">$CLARIFICATION</Clarification>
    </Clarifications>
    -->
  </C-CHANGE-ID>
</NgraceChangeSpec>
